# vLLM Merged PR Report

**Report Date:** 2026-03-04 PST

**Total Merged PRs:** 59

---

## 1. [[Docs] Update `CacheConfig` block_size docstring to remove inaccurate limit when using CUDA](https://github.com/vllm-project/vllm/pull/35632)


### Base Information

- **PR Number:** #35632
- **Author:** [eicherseiji](https://github.com/eicherseiji)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 22:24:09
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35632/files) (1):**
  - `vllm/config/cache.py`

### Summary

**What changed and why**  
The PR updates the docstring for `CacheConfig.block_size` to remove an outdated statement about CUDA devices only supporting block sizes up to 32 tokens. This correction reflects that modern implementations, such as those using CUTLASS MLA, now support larger block sizes (e.g., 128).

**Technical impact**  
This change has no functional impact on the code's execution; it only updates documentation. However, it ensures that users and developers have accurate information about the system's capabilities, preventing confusion and potential misconfiguration based on outdated limits.

**Potential risks**  
The primary risk is that the documentation might still be incomplete or could become outdated again if other platforms have different constraints that aren't documented. Additionally, if there are any hidden platform-specific limitations not covered elsewhere, users might assume unrestricted block sizes.

**Key insights**  
Always verify documentation against the actual implementation, especially for performance-critical parameters like `block_size`. Developers should consider adding platform-specific notes or links to the relevant code (like the CUDA platform check) in docstrings to keep them maintainable and accurate.

---

## 2. [[Misc] Remove deprecated items that are due for removal](https://github.com/vllm-project/vllm/pull/36006)


### Base Information

- **PR Number:** #36006
- **Author:** [hickeyma](https://github.com/hickeyma)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 22:14:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36006/files) (3):**
  - `vllm/config/cache.py`
  - `vllm/config/compilation.py`
  - `vllm/multimodal/processing/processor.py`

### Summary

**What changed and why**  
This maintenance PR removes three deprecated features that were scheduled for removal across different vLLM versions. The changes eliminate deprecated configuration fields (`cpu_offload_gb` and `cpu_offload_params` from `CacheConfig`), a deprecated `level` parameter from `CompilationConfig`, and support for multiple prompt updates per multi-modal item in the processor logic.

**Technical impact**  
These deletions simplify the codebase by removing legacy pathways and reducing maintenance overhead. Users relying on the deprecated features must migrate to the newer alternatives (e.g., `OffloadConfig.uva` for CPU offloading, `mode` for compilation settings). The removal enforces cleaner APIs and eliminates deprecated warnings, aligning with the project's versioning policy.

**Potential risks**  
If any users or downstream integrations still depend on the removed deprecated features, their code may break without clear migration guidance. The removal of the multi-modal prompt updates warning could hide issues if existing code still generates multiple updates per item, though the functionality itself appears unchanged.

**Key insights**  
This cleanup is a positive step toward maintaining a lean codebase, but ensure release notes clearly document these removals and provide migration examples. Consider verifying that no internal tests or examples inadvertently depend on the deprecated fields. The PR follows semantic versioning appropriately by removing items after their deprecation cycles.

---

## 3. [[Bugfix] Fix block_size for hybrid model MTP](https://github.com/vllm-project/vllm/pull/36036)


### Base Information

- **PR Number:** #36036
- **Author:** [benchislett](https://github.com/benchislett)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-03-04 22:10:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36036/files) (2):**
  - `tests/v1/spec_decode/test_eagle.py`
  - `vllm/v1/spec_decode/eagle.py`

### Summary

**What changed and why**  
The changes fix a bug where `block_size` was inconsistently accessed in the hybrid model MTP (EAGLE) implementation. Previously, `block_size` was retrieved from `self.draft_attn_groups[0].kv_cache_spec.block_size` in multiple methods, but this could fail if the attention groups weren't fully initialized. Now, `block_size` is stored as an instance variable during initialization and set properly in `initialize_attn_backend`.

**Technical impact**  
This ensures consistent access to `block_size` across the EAGLE proposer's methods, particularly in `propose()` and `set_inputs_first_pass()`. The initialization sequence is now safer, as `block_size` is validated (`assert block_size > 0`) before use, preventing runtime errors. The test updates reflect this by using a centralized `BLOCK_SIZE` constant.

**Potential risks**  
If `initialize_attn_backend` is called late or not at all, `block_size` may remain uninitialized (`-1`), causing assertion failures. Additionally, the change assumes all draft attention groups share the same `block_size`, which should hold but may break if heterogeneous cache configurations are introduced.

**Key insights**  
Centralizing `block_size` management improves code maintainability and reduces duplication. Developers should ensure `initialize_attn_backend` is invoked before any drafting operations. The added debug log (`logger.debug`) aids in monitoring the configured block size during initialization.

---

## 4. [[Doc] Add Parallel Draft Models](https://github.com/vllm-project/vllm/pull/35973)


### Base Information

- **PR Number:** #35973
- **Author:** [zihaoanllm](https://github.com/zihaoanllm)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 21:44:07
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35973/files) (3):**
  - `docs/features/speculative_decoding/README.md`
  - `docs/features/speculative_decoding/parallel_draft_model.md`
  - `pyproject.toml`

### Summary

**What changed and why**  
This PR adds documentation for Parallel Draft Models (PARD), a new speculative decoding method. It updates the main speculative decoding README to include PARD in the list of supported methods and adds a new dedicated documentation file with usage examples for both offline and online modes.

**Technical impact**  
These changes are purely documentation updates that introduce PARD as a supported speculative decoding technique within vLLM. They provide users with configuration examples and clarify that PARD offers "low draft model latency" compared to other methods. The pyproject.toml changes add "PARD" and "pard" to the typos dictionary to prevent false positives in spell checking.

**Potential risks**  
The documentation states PARD provides "high gain" for both single-user and multi-user scenarios, but no performance benchmarks or validation data are included to support these claims. The examples use specific model names ("amd/PARD-Qwen3-0.6B") which might become outdated if model repositories change. There's a minor inconsistency where the method is set to "draft_model" rather than a dedicated "parallel_draft_model" identifier.

**Key insights**  
Developers should note that PARD appears to be implemented as a configuration option (`parallel_drafting: true`) within the existing draft model infrastructure rather than as a completely separate method. The documentation successfully follows the established pattern for other speculative decoding methods. When implementing PARD support, ensure the actual code implementation matches the documented behavior and performance characteristics.

---

## 5. [[Kernel] [Helion] [11/N] Retune configs for silu_mul_fp8](https://github.com/vllm-project/vllm/pull/36062)


### Base Information

- **PR Number:** #36062
- **Author:** [gmagogsfm](https://github.com/gmagogsfm)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-04 21:25:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36062/files) (1):**
  - `vllm/kernels/helion/configs/silu_mul_fp8.json`

### Summary

**What changed and why**  
The PR updates the autotuned kernel configurations for the `silu_mul_fp8` operation on H200 hardware. The changes replace previously generated "quick" effort configurations with new ones derived from a "full" autotuning effort, with additional manual adjustments for small intermediate sizes and token counts to improve effectiveness.

**Technical impact**  
This significantly enhances kernel performance across various intermediate sizes, as evidenced by the provided geomean speedups ranging from 1.29x to 2.31x. The update directly affects low-level computation efficiency for FP8 operations, which are critical for model inference throughput, especially in large language models.

**Potential risks**  
The extensive changes (+4034/-4026 lines) in a single JSON configuration file indicate a complete replacement, which could introduce regressions if the new configs have not been validated across all expected hardware/software environments or edge cases. There is also a risk that the manual tweaks for small sizes might not generalize well to all model architectures or input patterns.

**Key insights**  
Developers should verify performance gains align with expectations in their specific deployment scenarios and monitor for any anomalies, particularly with smaller intermediate sizes. The improvements are substantial, but the complete config overhaul warrants careful integration testing to ensure stability and consistent performance uplift.

---

## 6. [[Bugfix] Fix score layer quantization for sequence classification models  - Qwen3 (VL) Reranker](https://github.com/vllm-project/vllm/pull/35849)


### Base Information

- **PR Number:** #35849
- **Author:** [gkswns0531](https://github.com/gkswns0531)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-04 20:57:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35849/files) (1):**
  - `vllm/model_executor/models/adapters.py`

### Summary

**What changed and why**  
The fix addresses quantization bugs for sequence classification models like Qwen3 Reranker by ensuring the dynamically created `score` layer (output dimension 1) does not receive a `quant_config`. This prevents FP8 tile alignment violations and NVFP4 attribute errors, as these quantization methods are incompatible with such small output dimensions. The changes also remove `quant_config` from temporary `ParallelLMHead` instances used only for weight extraction.

**Technical impact**  
Sequence classification models now correctly bypass quantization for the `score` layer when weights are derived online (e.g., from token embeddings), while preserving quantization for other layers. This maintains performance benefits for quantizable components and ensures model loading and inference work without crashes or zero-score outputs.

**Potential risks**  
If future models introduce checkpoint-based weights for the `score` layer, the current logic (which disables quantization based on `classifier_from_token` or `method` attributes) might incorrectly skip quantization. Additionally, any new quantization methods with similar tile alignment constraints could face analogous issues if not handled.

**Key insights**  
Always validate quantization compatibility with layer dimensions, especially for dynamic heads. Consider adding a centralized check for quantization support based on output dimensions or layer type. For similar models, ensure weight derivation methods are clearly documented to avoid misalignment between loading logic and quantization decisions.

---

## 7. [[Perf] Use dummy M for weight prepacking on x86](https://github.com/vllm-project/vllm/pull/35890)


### Base Information

- **PR Number:** #35890
- **Author:** [tianmu-li](https://github.com/tianmu-li)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-03-04 20:56:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35890/files) (1):**
  - `csrc/cpu/dnnl_helper.cpp`

### Summary

**What changed and why**  
The PR extends the use of a dummy M dimension (kProbeM=128) for weight prepacking from ARM platforms to x86 systems without AMX support. Previously, x86 used runtime dimensions (DNNL_RUNTIME_DIM_VAL), which could lead to suboptimal weight layouts (ab layout) instead of blocked layouts. This change ensures consistent weight prepacking behavior across architectures to improve performance.

**Technical impact**  
This modifies oneDNN's kernel selection by providing a concrete M dimension during weight prepacking, enabling optimized blocked weight layouts. The change affects both `W8A8MatMulPrimitiveHandler` and `MatMulPrimitiveHandler`, removing architecture-specific conditional logic and standardizing the approach. Performance improvements are demonstrated in benchmarks with increased token throughput and reduced latency.

**Potential risks**  
Using a fixed dummy M value (128) may not be optimal for all model configurations or batch sizes, potentially leading to suboptimal layouts in edge cases. The change assumes the dummy M approach is universally beneficial for x86 without AMX, but there may be scenarios where runtime dimensions are preferable. Additionally, removing the ACL-specific logic could affect ARM platforms if not thoroughly validated.

**Key insights**  
The PR successfully improves performance on x86 non-AMX systems by aligning weight prepacking with ARM's proven approach. Developers should verify that the dummy M value (128) is appropriate for typical workloads and consider making it configurable if needed. Ensure cross-architecture testing, especially for ARM platforms, to confirm no regressions from the consolidated logic.

---

## 8. [Order `config.py` in Lexicographical order](https://github.com/vllm-project/vllm/pull/35866)


### Base Information

- **PR Number:** #35866
- **Author:** [askliar](https://github.com/askliar)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-04 20:56:47
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35866/files) (1):**
  - `vllm/model_executor/models/config.py`

### Summary

**What changed and why**  
The changes reorder all model configuration classes in `config.py` into lexicographical (alphabetical) order. This is a pure refactoring change with no functional modifications—only the sequence of class definitions has been rearranged.

**Technical impact**  
This improves code maintainability by making it easier to locate specific model configs and ensuring consistency. Since the ordering is based on class names, future additions will naturally fit into the sorted structure, reducing merge conflicts and aiding readability.

**Potential risks**  
The risk is minimal as no logic was altered. However, any code that relies on the file’s original ordering (e.g., through reflection or dynamic imports) could be affected, though such dependencies are unlikely in this context.

**Key insights**  
Alphabetical ordering is a best practice for large configuration files. Developers should continue to place new model configs in the correct lexicographical position to maintain consistency. This change also highlights the value of automated tooling (e.g., `isort` or custom scripts) to enforce ordering in the future.

---

## 9. [[compile] Fix extra cache save on warm start.](https://github.com/vllm-project/vllm/pull/35921)


### Base Information

- **PR Number:** #35921
- **Author:** [zhxchen17](https://github.com/zhxchen17)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 20:56:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35921/files) (2):**
  - `tests/compile/test_startup.py`
  - `vllm/compilation/backends.py`

### Summary

**What changed and why**  
The fix addresses an issue where warm starts incorrectly saved an extra cache file to disk. This occurred because the in-memory autograd cache (layer 2) wasn't being populated when loading from the vllm cache (layer 1), causing cache misses and unnecessary disk writes. The solution stores the autograd cache key alongside the graph handle in the vllm cache, allowing layer 2 to be restored during warm starts.

**Technical impact**  
Changes ensure the three-layer cache hierarchy (vllm in-memory, autograd in-memory, autograd disk) remains consistent. By persisting the autograd cache key, warm starts now correctly hit all cache layers, eliminating redundant disk saves and improving startup performance. The update also adds robustness by handling outdated cache formats gracefully.

**Potential risks**  
If the cache format changes incompatibly, the new parsing logic could fail, though the try-except block mitigates this by forcing a recompile. There's a slight risk of increased memory usage from storing additional metadata in the vllm cache, but it's negligible. The fix assumes the autograd cache key is stable and reproducible across runs.

**Key insights**  
Developers should note that cache keys are now part of the persisted state, which may affect cache invalidation strategies. The fix highlights the importance of maintaining consistency across layered caches. Ensure any future cache layer modifications consider this inter-dependency to avoid regression.

---

## 10. [[CI] Don't leave docs preview comment on closed PRs](https://github.com/vllm-project/vllm/pull/36087)


### Base Information

- **PR Number:** #36087
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 20:51:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36087/files) (1):**
  - `.github/mergify.yml`

### Summary

**What changed and why**  
A condition `-closed` was added to the Mergify configuration to prevent the bot from leaving documentation preview comments on pull requests that have already been closed. This change addresses the issue where the automated cleanup process triggered unwanted notifications on PRs that were no longer active.

**Technical impact**  
This change modifies the rule's trigger logic in the CI/CD workflow. The documentation label automation will now only run on open pull requests, reducing unnecessary API calls and comment noise in the repository's closed PR section. The system behavior becomes more precise by respecting the PR's lifecycle state.

**Potential risks**  
If the syntax `-closed` is not a valid Mergify condition for checking open PRs, the rule may fail to execute entirely. There's also a minor risk that PRs closed during the rule evaluation window could still trigger the action, though this is unlikely. No other parts of the automation are affected.

**Key insights**  
Always validate Mergify condition syntax in a test repository before deploying to production. Consider adding similar state checks to other automated rules that should only apply to open PRs. This change demonstrates good practice in making automation lifecycle-aware to improve user experience.

---

## 11. [[Core] Move ray-specific WorkerWrapperBase methods to RayWorkerWrapper](https://github.com/vllm-project/vllm/pull/35328)


### Base Information

- **PR Number:** #35328
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 20:11:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35328/files) (2):**
  - `vllm/v1/executor/ray_utils.py`
  - `vllm/v1/worker/worker_base.py`

### Summary

**What changed and why**  
Two methods (`adjust_rank` and `execute_method`) were moved from the base class `WorkerWrapperBase` in `worker_base.py` to the Ray-specific implementation `RayWorkerWrapper` in `ray_utils.py`. This refactoring aims to better separate concerns by keeping Ray-distributed execution logic isolated to the Ray-specific wrapper.

**Technical impact**  
The change reduces the scope of the base class, making it more generic and focused on core worker functionality. The Ray-specific methods are now only available where they are actually needed—in distributed Ray execution contexts. This improves code organization and maintainability by adhering to the principle of separating abstraction from implementation details.

**Potential risks**  
If other non-Ray worker implementations (e.g., for other distributed frameworks) rely on these methods, they will break unless they are explicitly added to those implementations. Additionally, any existing tests or code that directly calls these methods on `WorkerWrapperBase` will need to be updated to use the Ray-specific wrapper instead.

**Key insights**  
This refactoring enhances modularity and clarity in the codebase. Developers should ensure that any new distributed worker implementations provide their own versions of these methods if needed. Reviewers should verify that all references to the moved methods have been updated to use the correct wrapper class.

---

## 12. [[Model] Add LoRA support for Whisper models](https://github.com/vllm-project/vllm/pull/29856)


### Base Information

- **PR Number:** #29856
- **Author:** [daje0601](https://github.com/daje0601)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-03-04 18:38:25
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/29856/files) (4):**
  - `tests/lora/conftest.py`
  - `tests/lora/test_whisper.py`
  - `vllm/lora/worker_manager.py`
  - `vllm/model_executor/models/whisper.py`

### Summary

**What changed and why**  
This PR adds LoRA support for Whisper speech-to-text models by implementing the `SupportsLoRA` interface. The changes enable multi-LoRA serving from a single base model, eliminating the need for separate model instances per fine-tuned adapter. Key modifications include updating the Whisper model to define LoRA-compatible attributes and extending the LoRA layer logic to handle Whisper's KV-only cross-attention projections.

**Technical impact**  
The implementation integrates Whisper into vLLM's existing LoRA framework, allowing dynamic loading of multiple adapters. Architectural changes include support for 2-slice merged linear layers (KV-only) in cross-attention and a fallback for Whisper's `max_target_positions` configuration. This maintains backward compatibility while extending LoRA functionality to encoder-decoder models.

**Potential risks**  
The KV-only slice handling in `MergedQKVParallelLinearWithLoRA` may introduce edge cases with other models using similar patterns. The fallback to `max_target_positions` assumes encoder-decoder models follow Whisper's config convention, which could break with custom implementations. Additionally, the spawn method for multiprocessing in tests may affect performance or compatibility on some systems.

**Key insights**  
Developers should verify that custom encoder-decoder models align with the new `max_target_positions` fallback logic. The updated `packed_modules_mapping` uses simplified keys (`qkv_proj`, `kv_proj`), ensuring consistency across LoRA-enabled models. When extending LoRA to other architectures, ensure merged linear layers correctly handle variable slice counts and test with the provided Whisper integration as a reference.

---

## 13. [[CI] Fix pre-commit mypy issue in main](https://github.com/vllm-project/vllm/pull/36049)


### Base Information

- **PR Number:** #36049
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-04 18:13:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36049/files) (1):**
  - `vllm/tool_parsers/hermes_tool_parser.py`

### Summary

**What changed and why**  
The fix addresses mypy type checking errors in the Hermes tool parser by moving the `None` check for `current_tool_call` earlier in the logic. Previously, the code attempted to access `.get("name")` on `current_tool_call` before verifying it wasn't `None`, causing union-attr errors. The adjustment ensures safe access to the dictionary.

**Technical impact**  
This change resolves static type checking failures in CI without altering runtime behavior. It maintains the same functional flow but reorders conditionals to satisfy mypy's type safety requirements, ensuring the codebase passes pre-commit checks consistently.

**Potential risks**  
If `current_tool_call` could be a non-`None` but non-dictionary type (e.g., due to upstream issues), the `.get("name")` call might still fail at runtime. However, this risk existed before the change and is mitigated by the earlier JSON parsing logic.

**Key insights**  
Always validate nullable variables before attribute access. This fix highlights the importance of aligning runtime logic with static type expectations to prevent CI disruptions. Developers should ensure similar patterns are reviewed across the codebase to preempt analogous mypy errors.

---

## 14. [Add platform method to enable custom collective ops registration](https://github.com/vllm-project/vllm/pull/34760)


### Base Information

- **PR Number:** #34760
- **Author:** [nkm-meta](https://github.com/nkm-meta)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-04 16:50:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34760/files) (4):**
  - `vllm/distributed/parallel_state.py`
  - `vllm/platforms/cuda.py`
  - `vllm/platforms/interface.py`
  - `vllm/platforms/rocm.py`

### Summary

**What changed and why**  
Added a new `use_custom_op_collectives()` method to the Platform interface (defaulting to False) to allow hardware platforms to opt-in to using custom `torch.ops.vllm.*` collective operations. CUDA and ROCm platforms now override this method to return True, enabling proper graph tracing for `torch.compile` and `torch.export`. This provides a clean extension point for out-of-tree platforms to register custom collective backends.

**Technical impact**  
The change decouples the decision to use custom collective ops from the `is_cuda_alike()` check, moving it to a dedicated platform method. This improves extensibility for custom hardware platforms while maintaining backward compatibility for existing CUDA, ROCm, and TPU platforms. The `use_custom_op_call` logic in `parallel_state.py` now relies on this new method, though a temporary TPU-specific check remains pending future cleanup.

**Potential risks**  
The temporary `is_tpu()` check alongside the new method creates a slight inconsistency until TPU platforms are updated to override `use_custom_op_collectives()`. If other platforms (e.g., CPU) override the method incorrectly, it could lead to runtime errors or performance degradation in collective operations. There is also a risk that out-of-tree platforms may not be aware of this new extension point.

**Key insights**  
This change introduces a more modular and explicit mechanism for platforms to enable custom collective ops, aligning with good abstraction practices. Developers should ensure that any new hardware platform overrides `use_custom_op_collectives()` appropriately. The TODO comment indicates that the TPU-specific check will be removed once TPU platforms are updated, so future cleanup should be prioritized.

---

## 15. [[compile] Reduce log spam from compile.](https://github.com/vllm-project/vllm/pull/36044)


### Base Information

- **PR Number:** #36044
- **Author:** [zhxchen17](https://github.com/zhxchen17)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-04 16:48:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36044/files) (1):**
  - `vllm/compilation/decorators.py`

### Summary

**What changed and why**  
The change replaces a standard `logger.info` call with `logger.info_once` to reduce redundant log output during AOT compilation. Previously, each worker process logged both "saving" and "saved" messages, creating spammy logs in distributed setups. Now, only a single "saved" message is logged per unique compilation path.

**Technical impact**  
This reduces log volume significantly in multi-process environments (e.g., tensor parallelism), improving log readability and reducing I/O overhead. The `logger.info_once` with `scope="local"` ensures the message is logged only once per process, preventing duplicate entries while preserving per-process visibility.

**Potential risks**  
If `logger.info_once` is not properly configured or its scope behavior is misunderstood, critical debugging information could be lost when diagnosing compilation failures. The removal of the "saving" log eliminates visibility into the start of the save operation, which may hinder tracking of hung processes during compilation.

**Key insights**  
Use `logger.info_once` judiciously for high-frequency, repetitive logs in distributed contexts. Ensure the logging scope aligns with the intended audience—here, `scope="local"` is appropriate for per-process logging. Consider retaining a debug-level "saving" log if troubleshooting compilation latency is a common need.

---

## 16. [[Model Runner V2] Misc code simplification](https://github.com/vllm-project/vllm/pull/35941)


### Base Information

- **PR Number:** #35941
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-04 15:26:36
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35941/files) (3):**
  - `vllm/v1/worker/gpu/buffer_utils.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/spec_decode/rejection_sample.py`

### Summary

**What changed and why**  
The changes remove redundant code and simplify tensor creation patterns. Specifically, an unused `max_concurrency` parameter is removed from a buffer class, constructor formatting is cleaned up, and several tensor allocations are replaced with more efficient methods like `np.fromiter` and `new_empty`/`new_ones`.

**Technical impact**  
These simplifications reduce code verbosity and improve memory efficiency. Using `np.fromiter` avoids intermediate list creation, while `new_empty`/`new_ones` leverages existing tensor metadata (device, dtype) for safer, more maintainable tensor allocations. The changes are mostly internal optimizations with no functional impact.

**Potential risks**  
The removal of `max_concurrency` could affect future extensions if the parameter was intended for unused concurrency control. Additionally, replacing explicit tensor constructors with `new_empty` assumes the source tensor’s device and dtype are always appropriate, which could introduce subtle bugs if the source tensor properties change unexpectedly.

**Key insights**  
These are clean, low-risk improvements that enhance code clarity and performance. Developers should adopt `new_empty`/`new_ones` for consistency when creating tensors matching existing tensor properties. Ensure any removed parameters like `max_concurrency` are truly obsolete and not needed for future features.

---

## 17. [[ROCm][CI] Added MI325 mirrors (stage C)](https://github.com/vllm-project/vllm/pull/35239)


### Base Information

- **PR Number:** #35239
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-04 14:48:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35239/files) (4):**
  - `.buildkite/test_areas/entrypoints.yaml`
  - `.buildkite/test_areas/misc.yaml`
  - `.buildkite/test_areas/models_multimodal.yaml`
  - `.buildkite/test_areas/plugins.yaml`

### Summary

**What changed and why**  
This PR adds AMD MI325 GPU mirror configurations to four CI test areas (Entrypoints, Misc, Multi-Modal Models, and Plugins) to enable gating tests on AMD hardware. The changes are part of a staged rollout to evaluate test compatibility and prepare for broader AMD support.

**Technical impact**  
Each test job now includes a mirror configuration specifying `device: mi325_1` (or `mi325_2` for Plugins) with a dependency on the `image-build-amd` step. This enables parallel execution of these test suites on AMD infrastructure alongside existing GPU platforms.

**Potential risks**  
The dependency chain is complex, requiring multiple external PRs to merge first. Some test groups (Language Models Tests and Multi-Modal Models Extended 2) are explicitly excluded due to unresolved infrastructure issues, which could delay full coverage. Mirror configurations must align with available hardware resources to avoid queue bottlenecks.

**Key insights**  
This is an infrastructure-focused change that introduces no functional code modifications. Developers should verify that all dependent PRs are merged before this one. The staged approach allows incremental validation of AMD compatibility while isolating problematic test groups for future resolution.

---

## 18. [[Misc] Support OOT linear method registering](https://github.com/vllm-project/vllm/pull/35981)


### Base Information

- **PR Number:** #35981
- **Author:** [shen-shanshan](https://github.com/shen-shanshan)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-03-04 14:25:56
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35981/files) (1):**
  - `vllm/model_executor/layers/linear.py`

### Summary

**What changed and why**  
Added a decorator function `register_weight_loader_v2_supported_method` that allows out-of-tree (OOT) linear methods to register themselves in the `WEIGHT_LOADER_V2_SUPPORTED` list. This enables external projects (like vllm-ascend) to integrate custom linear methods without modifying vllm's core codebase.

**Technical impact**  
The change extends the framework's extensibility by providing a registration mechanism for OOT linear methods. It maintains backward compatibility since existing code remains unaffected, and the decorator simply appends class names to an existing list used for weight loading.

**Potential risks**  
If multiple OOT modules register the same class name, it could lead to duplicates in the list, though this likely wouldn't cause functional issues. There's also a risk of improper usage if decorators are applied to non-linear method classes, potentially breaking weight loading logic.

**Key insights**  
This is a clean, minimal change that enhances modularity. Developers should ensure that any OOT linear method using this decorator properly implements the required interface. Consider adding validation or deduplication in the decorator if registration conflicts become a concern.

---

## 19. [[Bugfix] Fix passing of activation_type to trtllm fused MoE NVFP4 and FP8](https://github.com/vllm-project/vllm/pull/36017)


### Base Information

- **PR Number:** #36017
- **Author:** [amitz-nv](https://github.com/amitz-nv)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-03-04 14:23:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36017/files) (2):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_nvfp4_moe.py`

### Summary

**What changed and why**  
The fix addresses an issue where the `activation_type` parameter was not being properly passed to two TRT-LLM fused MoE implementations (NVFP4 and FP8). Specifically, the NVFP4 flow was missing the parameter entirely, causing it to always default to `Swiglu` activation regardless of the model's actual configuration.

**Technical impact**  
These changes ensure that the correct activation function (SILU or RELU2_NO_MUL) is properly communicated to the underlying flashinfer kernels. This maintains consistency with other MoE implementations in the codebase and ensures models using different activation functions behave correctly when these optimized TRT-LLM paths are utilized.

**Potential risks**  
The main risk is type mismatch since `activation_to_flashinfer_int(activation)` returns an integer, while the flashinfer API might expect an `ActivationType` enum. However, removing the enum wrapper in the FP8 file suggests the underlying function accepts integers directly. There's also a risk if other activation types beyond SILU and RELU2_NO_MUL are introduced without updating the assertion.

**Key insights**  
The fix standardizes parameter passing across both TRT-LLM MoE implementations. Developers should verify that `activation_to_flashinfer_int` correctly maps all supported activation types to the expected integer values used by flashinfer. Future changes to activation support should update both the assertion and the mapping function.

---

## 20. [[RL] [Weight Sync] Guard IPC update-info pickle deserialization behind insecure serialization flag](https://github.com/vllm-project/vllm/pull/35928)


### Base Information

- **PR Number:** #35928
- **Author:** [simon-mo](https://github.com/simon-mo)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-04 14:05:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35928/files) (2):**
  - `tests/distributed/test_weight_transfer.py`
  - `vllm/distributed/weight_transfer/ipc_engine.py`

### Summary

**What changed and why**  
Added a security gate in `IPCWeightTransferUpdateInfo.__post_init__` that prevents unsafe unpickling of `ipc_handles_pickled` unless the environment variable `VLLM_ALLOW_INSECURE_SERIALIZATION=1` is set. This mitigates remote code execution risks when the IPC transport and dev-mode are enabled, ensuring pickle deserialization only occurs in explicitly opted-in environments.

**Technical impact**  
The change restricts the IPC weight-transfer HTTP path by requiring an explicit opt-in for insecure serialization. This affects any workflow relying on pickled IPC handles over HTTP, as they will now fail unless the flag is enabled. The test suite was updated to set the flag for existing tests and added a negative test to validate the guard.

**Potential risks**  
If existing deployments rely on pickled IPC handles without the flag, they will encounter `ValueError` failures. There is also a risk of developers bypassing security by enabling the flag in production without understanding the implications. The change does not address other potential insecure deserialization paths outside this specific class.

**Key insights**  
This is a critical security hardening measure that should be communicated to all users of the IPC weight-transfer feature. Developers must ensure that any environment using pickled IPC handles explicitly sets `VLLM_ALLOW_INSECURE_SERIALIZATION=1` and understands the associated risks. Consider documenting this requirement in deployment guides and adding runtime warnings when the flag is enabled.

---

## 21. [[Bugfix] Fix race in non-blocking num_accepted_tokens GPU->CPU copy](https://github.com/vllm-project/vllm/pull/36013)


### Base Information

- **PR Number:** #36013
- **Author:** [tdoublep](https://github.com/tdoublep)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-03-04 13:52:45
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36013/files) (1):**
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Added CUDA event synchronization to fix a data race where `num_accepted_tokens_cpu` (pinned memory) could be read by the CPU before a non-blocking GPU→CPU DMA transfer completes. This affects hybrid (Mamba) models with speculative decoding under async scheduling, where no implicit CUDA synchronization occurs between the copy and subsequent read.

**Technical impact**  
The fix introduces a `num_accepted_tokens_event` CUDA event that records after the non-blocking copy and synchronizes before reading the CPU tensor in the next model execution step. This ensures memory consistency without adding global synchronization that would break async scheduling benefits.

**Potential risks**  
If the event is not properly initialized (e.g., when `num_spec_tokens` is zero), the `assert` could fail. Additionally, the event synchronization adds a small overhead, though benchmarks show it's within noise. There's also a risk if the event is used incorrectly elsewhere, as it's only intended for this specific synchronization point.

**Key insights**  
The fix is minimal and targeted, addressing only the specific race condition without impacting overall performance. Developers should ensure similar patterns with non-blocking copies and subsequent CPU reads are reviewed for potential races. The use of CUDA events rather than stream or device synchronization preserves async scheduling efficiency.

---

## 22. [[CI] Bump `mypy` version to 1.15.0](https://github.com/vllm-project/vllm/pull/34950)


### Base Information

- **PR Number:** #34950
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 12:55:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34950/files) (13):**
  - `.pre-commit-config.yaml`
  - `tests/kernels/core/test_pos_encoding.py`
  - `tests/kernels/core/test_rotary_embedding.py`
  - `tests/kernels/mamba/test_mamba_ssm.py`
  - `tests/kernels/quantization/test_fp8_quant.py`
  - `vllm/config/parallel.py`
  - `vllm/distributed/elastic_ep/elastic_state.py`
  - `vllm/distributed/kv_transfer/kv_connector/utils.py`
  - `vllm/v1/attention/backends/flashinfer.py`
  - `vllm/v1/attention/backends/gdn_attn.py`
  - `vllm/v1/attention/backends/mamba_attn.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/sample/logits_processor/__init__.py`

### Summary

**What changed and why**  
This PR updates the mypy version from 1.11.1 to 1.15.0 to leverage improved type checking and performance enhancements. It also adds the `faster-cache` optional extra for better caching performance. The changes include incremental version bumps to manage PR size and address new type errors introduced by stricter mypy checks, primarily fixing `None` handling and type annotations.

**Technical impact**  
The update enhances static type analysis across the codebase, catching previously missed type inconsistencies. Changes in test files and core modules resolve mypy errors by improving `None` checks and adding explicit type annotations (e.g., `TypeAlias`, `@overload`). This improves code reliability and maintainability without altering runtime behavior.

**Potential risks**  
Some fixes involve adding assertions (e.g., `assert x is not None`) which could mask underlying logic errors if assumptions are incorrect. The incremental version bump strategy may leave some type issues unresolved until future updates. Additionally, the `faster-cache` extra may introduce dependency conflicts in certain environments.

**Key insights**  
Developers should review `None` handling patterns in their code, as mypy 1.15.0 enforces stricter checks. Use explicit type annotations and `@overload` for complex return types to avoid future errors. Consider running mypy locally with the new version to preemptively identify similar issues in other modules.

---

## 23. [Add PyTorch profiler schedule support with warmup/active iterations](https://github.com/vllm-project/vllm/pull/35240)


### Base Information

- **PR Number:** #35240
- **Author:** [fenypatel99](https://github.com/fenypatel99)
- **Merged By:** [houseroad](https://github.com/houseroad)
- **Merged time:** 2026-03-04 12:53:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35240/files) (2):**
  - `vllm/config/profiler.py`
  - `vllm/profiler/wrapper.py`

### Summary

**What changed and why**  
Added support for PyTorch profiler schedule-based profiling with configurable warmup, wait, and active iterations. This allows users to skip initial iterations (to avoid JIT compilation noise) and only collect profiling data during specific active iterations, improving trace quality and reducing overhead.

**Technical impact**  
The changes introduce a new schedule-based profiling mode that integrates with PyTorch's profiler schedule system. When warmup or wait iterations are configured, the profiler uses a schedule to control when data is collected, and the `_profiler_step()` method manages the transition between warmup (data discarded) and active (data recorded) phases.

**Potential risks**  
The `_warmup_steps_remaining` calculation subtracts 1 to account for profiler.start() consuming step 0, which could cause off-by-one errors if the schedule logic changes. There's also a risk that the default change of `torch_profiler_with_stack` from `True` to `False` might break existing workflows that rely on stack traces, though this is documented as a performance optimization.

**Key insights**  
The implementation correctly separates warmup (profiler runs but discards data) from wait (profiler off) phases. Developers should note that schedule-based profiling requires explicit `profiler.step()` calls, which are now handled automatically. The default disabling of stack tracing reduces overhead but may need to be re-enabled for detailed debugging.

---

## 24. [[Docs] Document security risks of GPT-OSS Python tool](https://github.com/vllm-project/vllm/pull/35139)


### Base Information

- **PR Number:** #35139
- **Author:** [russellb](https://github.com/russellb)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 12:27:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35139/files) (2):**
  - `docs/usage/security.md`
  - `vllm/entrypoints/openai/cli_args.py`

### Summary

**What changed and why**  
Added comprehensive security documentation for the built-in GPT-OSS demo tools, specifically highlighting that the Python code interpreter executes model-generated code in Docker containers without network isolation by default. Updated the CLI help text for `--tool-server` to include a warning about this risk.

**Technical impact**  
These changes improve security transparency by documenting a critical default behavior: containers created by the GPT-OSS Python tool inherit the host's Docker networking configuration, potentially exposing internal networks and services. The documentation also clarifies how to control tool availability via environment variables and suggests custom implementations for production.

**Potential risks**  
The lack of default network isolation could lead to SSRF attacks, access to cloud metadata services, or exploitation of vulnerable internal services if adversarial prompt injection influences the model's code generation. Users might enable the demo tools without understanding these risks, especially in multi-tenant or cloud environments.

**Key insights**  
Always treat the GPT-OSS Python tool as a reference implementation unsuitable for production without additional hardening. For production use, implement a custom sandbox with strict network isolation or avoid enabling the demo tools entirely. Monitor and restrict tool access via `VLLM_GPT_OSS_SYSTEM_TOOL_MCP_LABELS` to minimize attack surface.

---

## 25. [[UX] Remove NoOpOffloader log](https://github.com/vllm-project/vllm/pull/35678)


### Base Information

- **PR Number:** #35678
- **Author:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-04 12:13:41
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35678/files) (1):**
  - `vllm/model_executor/offloader/base.py`

### Summary

**What changed and why**  
The change modifies the logging behavior when setting the global offloader instance. Instead of always logging at INFO level, it now logs NoopOffloader specifically at DEBUG level with a custom message, while other offloaders continue to be logged at INFO level. Both cases now use once-per-session logging methods (`debug_once`/`info_once`) with local scope.

**Technical impact**  
This reduces log noise for the common NoopOffloader case while maintaining visibility for actual offloader configurations. The switch to `*_once()` methods prevents duplicate log messages during repeated initialization, and the `scope="local"` parameter ensures this behavior is consistent within the current execution context.

**Potential risks**  
The debug-level logging for NoopOffloader might be missed in production troubleshooting if debug logging is disabled. There's also a risk that developers expecting the previous INFO-level message for NoopOffloader might need to adjust their log monitoring. The change assumes NoopOffloader is the expected/common case worth demoting in verbosity.

**Key insights**  
This is a sensible UX improvement that prioritizes cleaner logs for typical use cases. Developers should ensure their logging configuration captures DEBUG messages when troubleshooting offloading issues. The consistent use of `*_once()` methods is a good pattern to adopt elsewhere to avoid log duplication.

---

## 26. [[torch.compile] Stop lazily compiling](https://github.com/vllm-project/vllm/pull/35472)


### Base Information

- **PR Number:** #35472
- **Author:** [zou3519](https://github.com/zou3519)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-04 12:13:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35472/files) (7):**
  - `tests/compile/test_compile_ranges.py`
  - `tests/compile/test_structured_logging.py`
  - `vllm/compilation/backends.py`
  - `vllm/compilation/caching.py`
  - `vllm/compilation/decorators.py`
  - `vllm/compilation/monitor.py`
  - `vllm/compilation/piecewise_backend.py`

### Summary

**What changed and why**  
The PR changes torch.compile from lazy to eager compilation in VllmBackend. Previously, PiecewiseBackend compiled each dynamic range on-demand during the first warmup execution. Now, `VllmBackend.__call__` compiles all ranges upfront via `PiecewiseBackend.compile_all_ranges()` before returning the callable, ensuring compilation completes before any warmup run with real tensors.

**Technical impact**  
This simplifies the compilation lifecycle by removing lazy compilation logic and associated callback mechanisms. Compilation now happens eagerly during backend invocation, which eliminates the need for `check_for_ending_compilation` and the `_on_compilation_complete_callback` context variable. Cache saving and monitoring are now performed directly after compilation finishes, making the flow more predictable and easier to reason about.

**Potential risks**  
The eager compilation approach may increase initial latency before the first execution, as all ranges are compiled upfront. There is also a risk if a range is not compiled upfront but encountered later—the code now raises a RuntimeError instead of lazily compiling. Additionally, changes to fake tensor handling and argument concretization could introduce subtle bugs if symbolic shape substitution is incorrect.

**Key insights**  
Developers should note that compilation is now deterministic and completes before any real tensor execution, simplifying debugging and monitoring. The removal of lazy compilation reduces runtime complexity but requires that all necessary ranges are defined upfront. Ensure that range definitions in configuration are comprehensive to avoid RuntimeErrors during inference.

---

## 27. [[Docs] Clarify structured outputs configuration for Qwen3 reasoning mode](https://github.com/vllm-project/vllm/pull/32441)


### Base Information

- **PR Number:** #32441
- **Author:** [davzaman](https://github.com/davzaman)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 11:44:39
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/32441/files) (1):**
  - `docs/features/structured_outputs.md`

### Summary

**What changed and why**  
Added a documentation note clarifying that Qwen3 Coder models with reasoning enabled require an explicit configuration flag (`--structured-outputs-config.enable_in_reasoning=True`) to produce structured outputs. This addresses an undocumented requirement where structured outputs are silently disabled when reasoning content isn't parsed separately.

**Technical impact**  
This change does not affect runtime behavior but improves documentation accuracy. It informs users that the default configuration may not work as expected for Qwen3 Coder models with reasoning, guiding them to set the necessary server flag to enable both features simultaneously.

**Potential risks**  
The note is version-specific (v0.11.2+), which could become outdated if the underlying issue is fixed in future releases. Additionally, users might overlook this documentation note if it's not prominently featured in troubleshooting guides or error messages.

**Key insights**  
Always verify feature compatibility when combining advanced model capabilities like reasoning and structured outputs. For Qwen3 Coder models, explicit configuration is required—a critical detail for deployment. Consider adding runtime warnings or validation checks in the codebase to surface this requirement more directly.

---

## 28. [[CI] Add Blackwell AsyncTP correctness test](https://github.com/vllm-project/vllm/pull/35871)


### Base Information

- **PR Number:** #35871
- **Author:** [stecasta](https://github.com/stecasta)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-04 11:41:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35871/files) (2):**
  - `.buildkite/test_areas/compile.yaml`
  - `tests/compile/correctness_e2e/test_async_tp.py`

### Summary

**What changed and why**  
This PR adds a B200 GPU CI test for AsyncTP correctness testing and disables the `FlashInferFP8ScaledMMLinearKernel` on Blackwell architectures. The change is necessary because this kernel, used on B200 (CC >= 100), generates operations that the AsyncTP pattern matcher cannot fuse, which would cause the correctness test to fail. The existing H100 test remains unaffected as the kernel is disabled via an environment variable that is a no-op on CC < 100.

**Technical impact**  
The modification extends test coverage to Blackwell GPUs and ensures the AsyncTP correctness test passes by aligning it with the existing fusion count tests. It introduces a conditional environment variable (`VLLM_DISABLED_KERNELS`) that selectively disables a specific kernel only on relevant hardware, maintaining backward compatibility and preventing test failures due to architectural differences in FP8 code paths.

**Potential risks**  
If the `VLLM_DISABLED_KERNELS` environment variable is not properly scoped or leaks into other tests, it could inadvertently disable the kernel in unrelated CI jobs or production scenarios. There is also a risk that future kernel changes or new Blackwell variants might require updates to this conditional logic. The test's optional/nightly status means regressions might not be caught immediately.

**Key insights**  
Developers should ensure environment-based kernel disabling is used consistently across all AsyncTP-related tests to maintain parity. The pattern of conditionally disabling incompatible kernels based on compute capability is a useful template for handling architectural differences. It's important to verify that the monkeypatch is correctly applied only to the intended test scope and does not persist.

---

## 29. [[Model Runner V2] Fix pooling](https://github.com/vllm-project/vllm/pull/36019)


### Base Information

- **PR Number:** #36019
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-04 10:53:17
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36019/files) (2):**
  - `vllm/v1/worker/gpu/async_utils.py`
  - `vllm/v1/worker/gpu/model_runner.py`

### Summary

**What changed and why**  
Two minor but important fixes were made to address issues with model output pooling. The first change converts an unbind operation's output tuple to a mutable list for proper iteration and modification, while the second corrects tuple unpacking to match the updated structure of `execute_model_state`.

**Technical impact**  
These changes ensure the pooling logic correctly processes and returns model outputs. The list conversion allows for in-place modifications of pooled outputs, and the updated unpacking prevents runtime errors by aligning with the current state tuple format.

**Potential risks**  
If `execute_model_state` contains more elements than expected in other code paths, similar unpacking mismatches could occur. The list conversion assumes the unbind operation always returns a tuple that needs mutability, which should be verified across different tensor configurations.

**Key insights**  
Always verify tuple unpacking matches the actual data structure after refactoring. When output structures require modification, ensure they are converted to mutable types early in the processing pipeline. Consider adding type hints or assertions to catch unpacking mismatches during development.

---

## 30. [[Kernel][Mamba] Optimize Mamba2 SSD prefill Triton kernels](https://github.com/vllm-project/vllm/pull/35397)


### Base Information

- **PR Number:** #35397
- **Author:** [tomeras91](https://github.com/tomeras91)
- **Merged By:** [tdoublep](https://github.com/tdoublep)
- **Merged time:** 2026-03-04 10:47:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35397/files) (6):**
  - `vllm/model_executor/layers/mamba/ops/mamba_ssm.py`
  - `vllm/model_executor/layers/mamba/ops/ssd_chunk_scan.py`
  - `vllm/model_executor/layers/mamba/ops/ssd_chunk_state.py`
  - `vllm/model_executor/layers/mamba/ops/ssd_combined.py`
  - `vllm/model_executor/layers/mamba/ops/ssd_state_passing.py`
  - `vllm/model_executor/layers/mamba/ops/triton_helpers.py`

### Summary

**What changed and why**  
This PR optimizes Mamba2 SSD prefill Triton kernels through four main changes: extended autotune search space with new configurations for better memory latency hiding and low register pressure, introduced a `fast_exp` helper using hardware `exp2` instead of libdevice `exp`, rewrote the state passing kernel to batch-parallelize across sequences, and removed dead `_chunk_state_varlen_kernel` code.

**Technical impact**  
The optimizations improve kernel performance by 0.9-34.4% across different kernels, with the most significant gains in `_chunk_cumsum_fwd_kernel` (-34.4%) and `_chunk_scan_fwd_kernel` (-8.7%). The architectural changes simplify the state passing kernel by eliminating per-chunk branching and leveraging existing `last_chunk_indices` tensor for batch parallelism.

**Potential risks**  
The `fast_exp` implementation using hardware `exp2` may have different numerical precision characteristics compared to libdevice `exp`, though quality tests show no regression. The batch-parallel state passing kernel, while beneficial for BS>1, has minimal impact on typical BS=1 prefill scenarios. Removal of dead code is safe but should be verified against all model configurations.

**Key insights**  
The most impactful changes are the expanded autotune configurations and hardware-accelerated exponential computation. Developers should note that the state passing optimization provides future-proofing for batch-parallel scenarios while maintaining single-batch performance. The clean removal of ~340 lines of dead code reduces maintenance burden without functional impact.

---

## 31. [[Frontend] Add vllm launch command for GPU-less preprocessing serving](https://github.com/vllm-project/vllm/pull/34551)


### Base Information

- **PR Number:** #34551
- **Author:** [hyeongyun0916](https://github.com/hyeongyun0916)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 10:41:53
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34551/files) (10):**
  - `tests/entrypoints/openai/cpu/test_render.py`
  - `tests/entrypoints/openai/test_launch_render.py`
  - `tests/entrypoints/test_launch_cli.py`
  - `tests/utils.py`
  - `vllm/entrypoints/cli/launch.py`
  - `vllm/entrypoints/cli/main.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/openai/generate/api_router.py`
  - `vllm/tasks.py`
  - `vllm/v1/engine/launch.py`

### Summary

**What changed and why**  
This PR introduces a new `vllm launch` command for GPU‑less preprocessing serving, implementing Phase 1 of RFC #34407. It adds a FastAPI server that exposes tokenization and chat‑template rendering endpoints (`/render`) without requiring GPU inference, enabling disaggregated tokenization for systems like llm‑d. The core change is a `LaunchEngineClient` that implements the `EngineClient` ABC as a null object—providing preprocessing components while raising `NotImplementedError` for inference methods.

**Technical impact**  
The architecture reuses the existing serving infrastructure (`build_app`, `init_app_state`, `serve_http`) via a new `build_and_serve()` helper, ensuring minimal divergence from `vllm serve`. The `LaunchEngineClient` initializes renderer, I/O processor, and input processor for tokenization but bypasses `AsyncLLM` entirely. The `tasks.py` is extended to include a `"render"` task, and the API router logic is updated to conditionally register generate endpoints for both `"generate"` and `"render"` tasks.

**Potential risks**  
1. **Error handling**: Inference endpoints (e.g., `/v1/chat/completions`) return errors; clients must be aware that only `/render` endpoints are functional.  
2. **Future compatibility**: The null‑object pattern may require updates if the `EngineClient` ABC evolves.  
3. **Testing gaps**: While unit and E2E tests are included, integration with external systems (like llm‑d) is not validated.  
4. **Resource leakage**: The `LaunchEngineClient` does not manage GPU memory, but any unintended GPU initialization in dependent components could cause resource leaks.

**Key insights**  
- The design cleanly separates preprocessing from inference, allowing GPU‑free tokenization as a standalone service.  
- Developers should note that `vllm launch` is strictly for preprocessing; generation requests will fail with a clear error.  
- The extracted `build_and_serve()` function improves code reuse and sets the stage for future gRPC support.  
- Ensure any new `EngineClient` methods added in the future are properly stubbed in `LaunchEngineClient` to maintain compatibility.

---

## 32. [docs: update CPU Docker images to reference Docker Hub instead of AWS ECR](https://github.com/vllm-project/vllm/pull/34882)


### Base Information

- **PR Number:** #34882
- **Author:** [cluster2600](https://github.com/cluster2600)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 10:31:36
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34882/files) (2):**
  - `docs/getting_started/installation/cpu.arm.inc.md`
  - `docs/getting_started/installation/cpu.x86.inc.md`

### Summary

**What changed and why**  
This PR updates CPU installation documentation to reference Docker Hub images (`vllm/vllm-openai-cpu`) instead of AWS ECR images. The change aligns documentation with a previous pipeline update (#32032) that started publishing CPU images to Docker Hub, addressing issue #33708.

**Technical impact**  
The changes are purely documentation updates—no code or build logic is altered. Users will now pull CPU Docker images from Docker Hub, which may improve accessibility and simplify image management by consolidating image repositories.

**Potential risks**  
If users have automation or scripts that hardcode the old ECR image URLs, those will break unless updated. Additionally, the new tagging scheme (e.g., `-x86_64`/`-arm64` suffixes) could cause confusion if users inadvertently use the wrong architecture-specific tag.

**Key insights**  
Ensure all downstream documentation and automation scripts are updated to use the new Docker Hub image names and tagging conventions. Consider adding a deprecation notice or migration guide for users who may still reference the old ECR images.

---

## 33. [docs: add version requirement note for --profiler-config flag](https://github.com/vllm-project/vllm/pull/32454)


### Base Information

- **PR Number:** #32454
- **Author:** [abhishkh](https://github.com/abhishkh)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 10:13:54
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/32454/files) (1):**
  - `docs/contributing/profiling.md`

### Summary

**What changed and why**  
Updated the profiling documentation to add a version requirement note for the `--profiler-config` flag, clarifying it is only available in vLLM v0.13.0 and later. This addresses user confusion and errors when attempting to use the flag on older versions, as reported in issue #31766.

**Technical impact**  
The change improves documentation accuracy and user experience by preventing misuse of an unsupported feature in earlier releases. It does not affect the codebase functionality, system behavior, or architecture, as it is purely a documentation update.

**Potential risks**  
The note's placement and formatting must be consistent with other admonition blocks in the documentation to maintain readability. There is a minor risk that users on older versions might still miss the note if they skim the documentation, but the immediate placement after the feature introduction mitigates this.

**Key insights**  
Always document version-specific features to prevent user errors and reduce support requests. For future features, consider adding version requirements at the time of introduction to avoid retrospective updates. Ensure documentation changes follow existing formatting patterns for consistency.

---

## 34. [[Docs] Add RunPod GPU deployment guide for vLLM](https://github.com/vllm-project/vllm/pull/34531)


### Base Information

- **PR Number:** #34531
- **Author:** [lisperz](https://github.com/lisperz)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 10:11:34
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34531/files) (1):**
  - `docs/deployment/frameworks/runpod.md`

### Summary

**What changed and why**  
This PR adds a new documentation file (`runpod.md`) that provides a deployment guide for running vLLM on RunPod GPU pods. The purpose is to help users quickly set up vLLM in a cloud GPU environment by addressing common onboarding issues like port exposure, service binding, and server verification.

**Technical impact**  
The change extends the vLLM documentation framework by adding a new deployment guide under `docs/deployment/frameworks/`. It does not affect the core codebase, but it enhances the user experience by providing clear, tested instructions for deploying vLLM on RunPod, which may reduce support requests and deployment errors.

**Potential risks**  
The guide assumes a specific RunPod setup (e.g., using the `runpod/pytorch` template), which might not cover all user environments or future RunPod changes. Additionally, the troubleshooting section addresses common errors but may not capture all edge cases, such as network configuration issues or model-specific loading problems.

**Key insights**  
This documentation is a valuable addition for users deploying vLLM on cloud GPU platforms, as it addresses practical deployment hurdles. Developers should ensure the guide stays updated with any changes to vLLM’s CLI arguments or RunPod’s service exposure mechanisms. Consider linking this guide from the main deployment documentation for better visibility.

---

## 35. [[Docs] Upgrade dynamic LoRA warning to admonition block](https://github.com/vllm-project/vllm/pull/35218)


### Base Information

- **PR Number:** #35218
- **Author:** [russellb](https://github.com/russellb)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 10:01:43
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35218/files) (1):**
  - `docs/features/lora.md`

### Summary

**What changed and why**  
The PR replaces a plain-text "Note:" about dynamic LoRA serving risks with a MkDocs warning admonition block. This change visually emphasizes the security warning and strengthens the language to clarify that the feature should not be used in production unless in a fully trusted, isolated environment.

**Technical impact**  
This is a documentation-only change that improves the visual prominence and clarity of a security warning. It does not affect code functionality but enhances user awareness of the risks associated with enabling dynamic LoRA configuration in production.

**Potential risks**  
There are no technical risks, as this only modifies documentation. However, if the warning is overlooked due to formatting issues in certain MkDocs themes or output formats, users might still miss the security guidance.

**Key insights**  
Always use appropriate admonition blocks for security warnings in documentation to ensure they stand out. Consider verifying that the warning renders correctly across all supported documentation themes and formats. This change aligns with best practices for security communication in technical documentation.

---

## 36. [[Feature][CI]: compare `func` & `no_func` outputs in test_functionalization.py](https://github.com/vllm-project/vllm/pull/35481)


### Base Information

- **PR Number:** #35481
- **Author:** [11happy](https://github.com/11happy)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-04 10:01:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35481/files) (1):**
  - `tests/compile/passes/test_functionalization.py`

### Summary

**What changed and why**  
The PR fixes a test flakiness issue where `model_func` and `model_no_func` were being instantiated as new instances of `model_class` after `model.example_inputs()` had consumed the random state, leading to different initial weights. The fix uses `deepcopy` on the original `model` to ensure both compiled models start with identical weights. Additionally, it now properly compares outputs using `assert_close` and removes the TODO comment since the comparison is now implemented.

**Technical impact**  
This change ensures test determinism by maintaining consistent model weights across both functionalized and non-functionalized compilation paths. The test now validates that functionalization doesn't alter model outputs, which is crucial for verifying compilation correctness. The removal of the TODO comment indicates the underlying issue (#34996) has been resolved.

**Potential risks**  
Using `deepcopy` on complex PyTorch models could potentially fail if the model contains non-pickleable objects or custom attributes. The additional `deepcopy` of inputs before each forward pass adds overhead, though minimal. There's a risk that the test might pass due to `deepcopy` masking other issues if model initialization has side effects.

**Key insights**  
Always ensure model state consistency when testing compilation variants—direct instantiation can lead to non-determinism from random seed consumption. The fix properly addresses the root cause rather than just suppressing symptoms. Consider whether `deepcopy` is appropriate for all model types in the test suite, and verify no model has initialization side effects that `deepcopy` would bypass.

---

## 37. [[Docs] Update design/multiprocessing.md](https://github.com/vllm-project/vllm/pull/30677)


### Base Information

- **PR Number:** #30677
- **Author:** [windsonsea](https://github.com/windsonsea)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 09:59:00
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/30677/files) (1):**
  - `docs/design/multiprocessing.md`

### Summary

**What changed and why**  
This PR updates the multiprocessing documentation with grammatical fixes, formatting improvements, and clearer phrasing. Changes include fixing punctuation, consolidating inline code and links onto single lines, and refining explanations for better readability.

**Technical impact**  
The changes are purely documentation-related and do not affect code functionality, system behavior, or architecture. They improve the clarity and maintainability of the documentation for developers working with vLLM's multiprocessing features.

**Potential risks**  
There are minimal risks since only documentation is modified. However, any inaccuracies introduced in technical descriptions could mislead developers, though the changes appear to be grammatical and formatting-focused.

**Key insights**  
The updates enhance documentation quality without altering code. Developers should review the revised content for technical accuracy, particularly around multiprocessing method descriptions and dependency compatibility notes. Maintaining clear documentation reduces user confusion and support overhead.

---

## 38. [fix minicpmo4.5: fix attn_mask in vit attn && fix resampler pos_emb i…](https://github.com/vllm-project/vllm/pull/34127)


### Base Information

- **PR Number:** #34127
- **Author:** [tc-mb](https://github.com/tc-mb)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 09:46:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34127/files) (2):**
  - `vllm/model_executor/models/idefics2_vision_model.py`
  - `vllm/model_executor/models/minicpmv.py`

### Summary

**What changed and why**  
Two accuracy fixes were implemented: 1) Added support for `attention_mask` in the ViT attention module to properly handle padded patches, aligning with HuggingFace NaViT SigLIP behavior. 2) Corrected positional embedding addition in the 3D resampler—previously added to `v` but now correctly added to `k`.

**Technical impact**  
The changes ensure attention masking is applied when padding exists, improving model accuracy for variable-sized inputs. The resampler fix corrects positional embedding usage, which is critical for spatial-temporal reasoning in vision models. Both modifications align the implementation with reference models and expected behavior.

**Potential risks**  
The conditional logic for attention masking could introduce performance overhead when checking `torch.any(~flat_patch_mask)`. Additionally, the switch to `F.scaled_dot_product_attention` when a mask is provided may cause subtle behavioral differences compared to the unified `MMEncoderAttention` implementation (e.g., different numerical precision or kernel selection).

**Key insights**  
Always validate attention masking and positional embedding placement against reference implementations. Consider profiling the mask-checking overhead for large batches. Ensure the `scaled_dot_product_attention` path is thoroughly tested for edge cases, as it now handles critical masking logic.

---

## 39. [docs(cpu): Clarify pre-built wheels requirement for CPU Python-only build](https://github.com/vllm-project/vllm/pull/35090)


### Base Information

- **PR Number:** #35090
- **Author:** [sagearc](https://github.com/sagearc)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 09:45:35
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35090/files) (1):**
  - `docs/getting_started/installation/cpu.md`

### Summary

**What changed and why**  
Added a clarification note in the CPU installation documentation stating that the Python-only build method requires pre-built wheels for the user's platform. This addresses potential confusion by explicitly linking the two sections.

**Technical impact**  
This is a documentation-only change that improves clarity for users attempting CPU installation. It explicitly connects the Python-only build instructions with the pre-built wheels requirement, which is especially relevant since macOS lacks these wheels.

**Potential risks**  
No technical risks exist as this only updates documentation. However, users on macOS may still encounter installation issues since pre-built wheels are unavailable for that platform, and the note doesn't explicitly warn about this limitation.

**Key insights**  
The change effectively prevents user confusion by cross-referencing requirements. For future improvements, consider adding a specific warning for macOS users or linking to alternative installation methods for unsupported platforms.

---

## 40. [[Doc] Add MTP docs and update speculative decoding guidance](https://github.com/vllm-project/vllm/pull/35197)


### Base Information

- **PR Number:** #35197
- **Author:** [XingLiu1](https://github.com/XingLiu1)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-04 09:23:34
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35197/files) (3):**
  - `docs/features/speculative_decoding/README.md`
  - `docs/features/speculative_decoding/mlp.md`
  - `docs/features/speculative_decoding/mtp.md`

### Summary

**What changed and why**  
This documentation-only PR adds comprehensive documentation for the Multi-Token Prediction (MTP) speculative decoding method and updates existing guidance. It creates a new MTP documentation page, adds MTP to the method overview, introduces a selection table for low vs high QPS scenarios, and updates MLP examples with a more practical 8B model pairing while adding a warning about a known 70B issue.

**Technical impact**  
These changes improve the discoverability and usability of vLLM's speculative decoding features by providing clearer guidance on method selection and practical configuration examples. The new MTP documentation fills a gap for models with native multi-token prediction support, while the updated MLP example (70B→8B) makes the documentation more accessible to users with limited GPU resources.

**Potential risks**  
The warning about the MLP 70B issue (#34106) indicates there may be unresolved technical limitations with larger models. Users might incorrectly assume MTP works with any model, though the documentation clearly states it only works with MTP-supported families. The qualitative selection table could lead to suboptimal choices if users don't validate with their specific environment as recommended.

**Key insights**  
The PR effectively organizes speculative decoding methods into a coherent framework with practical guidance. Developers should note the new MTP option for models with native multi-token prediction, and should always benchmark methods in their specific environment using the provided tools. The 8B MLP example is more practical for most users than the previous 70B example.

---

## 41. [fix(docs): use static rdzv backend in multi-node troubleshooting script](https://github.com/vllm-project/vllm/pull/34784)


### Base Information

- **PR Number:** #34784
- **Author:** [machov](https://github.com/machov)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 09:15:35
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34784/files) (1):**
  - `docs/usage/troubleshooting.md`

### Summary

**What changed and why**  
The documentation was updated to replace the `c10d` rendezvous backend with `static` in multi-node troubleshooting examples, addressing DNS resolution failures in distributed setups. The note about manual node rank assignment was consolidated into the main instructions, simplifying the guide.

**Technical impact**  
Using the `static` backend eliminates DNS dependency by requiring explicit node ranks (`--node-rank`), making multi-node initialization more reliable. The change also clarifies that `MASTER_ADDR` must include a port (e.g., `10.0.0.1:29400`), ensuring proper endpoint configuration.

**Potential risks**  
Users must now manually set `NODE_RANK` on each node (0 for master, incrementing for workers), which adds a configuration step. If ranks are misassigned (e.g., duplicates), distributed coordination will fail. The `static` backend may also lack advanced features of `c10d`, though it suffices for basic troubleshooting.

**Key insights**  
Always specify `--rdzv_backend=static` and `--node-rank` in multi-node environments to avoid DNS issues. Verify `MASTER_ADDR` includes a port, and ensure unique `NODE_RANK` values across nodes. This change prioritizes reliability over convenience for troubleshooting scenarios.

---

## 42. [docs: add README for logits_processor examples](https://github.com/vllm-project/vllm/pull/35933)


### Base Information

- **PR Number:** #35933
- **Author:** [mitre88](https://github.com/mitre88)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 09:09:20
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35933/files) (1):**
  - `examples/offline_inference/logits_processor/README.md`

### Summary

**What changed and why**  
A README.md file was added to the `examples/offline_inference/logits_processor/` directory to document three existing example scripts. This change fills a documentation gap, making it easier for users to discover and understand how to implement custom logits processors with vLLM's offline inference API.

**Technical impact**  
The addition improves the project's documentation consistency by aligning this directory with other example directories that already have READMEs. It provides clear guidance on batch-level versus request-level logits processors and how to use `SamplingParams.extra_args` for per-request customization.

**Potential risks**  
No technical risks are introduced, as this is purely a documentation change. However, if the underlying example scripts are updated in the future, the README may become outdated if not maintained accordingly.

**Key insights**  
This README effectively clarifies a nuanced aspect of vLLM's API—the distinction between batch-level and request-level logits processors. Developers should use this documentation as a reference when building custom processors and ensure any future changes to the examples are reflected here.

---

## 43. [Fix phi4-mm and remove cuda binding](https://github.com/vllm-project/vllm/pull/35964)


### Base Information

- **PR Number:** #35964
- **Author:** [yma11](https://github.com/yma11)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 09:08:05
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35964/files) (2):**
  - `vllm/model_executor/models/phi4mm_audio.py`
  - `vllm/model_executor/models/phi4mm_utils.py`

### Summary

**What changed and why**  
This PR fixes two issues in the phi4-mm model implementation: it removes hardcoded CUDA device checks and replaces them with generic device handling, and it replaces a tensor-based length calculation function with an integer-only version to prevent operations on meta tensors during model initialization. The changes address a RuntimeError that occurred when `.item()` was called on meta tensors.

**Technical impact**  
The device handling changes make the code more portable across different hardware (CPU/GPU) by using `.to(device)` instead of `.cuda()`. The replacement of `calc_length` with `calc_length_int` eliminates tensor operations during the model's `__init__`, which is critical for meta tensor initialization workflows and prevents the specific runtime error shown in the PR description.

**Potential risks**  
The integer-only calculation assumes the input `feat_in` is a static integer known at model definition time, which should be valid for this subsampling configuration. However, if `feat_in` were ever dynamic or a tensor, this would break. The device logic change assumes `xs_pad.device` is correctly set, which is standard but relies on proper tensor initialization upstream.

**Key insights**  
Always use `.to(device)` for device transfers instead of `.cuda()` to maintain hardware agnosticism. Avoid any tensor operations that require materialization (like `.item()`) during model `__init__` when meta tensors may be present. The fix is minimal and focused, directly addressing the root cause of the initialization error.

---

## 44. [[Doc] Fix GPU Worker count in Process Count Summary](https://github.com/vllm-project/vllm/pull/36000)


### Base Information

- **PR Number:** #36000
- **Author:** [simone-dotolo](https://github.com/simone-dotolo)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 09:03:14
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36000/files) (1):**
  - `docs/design/arch_overview.md`

### Summary

**What changed and why**  
Updated documentation to correct the GPU worker count formula in the Process Count Summary table. The change reflects that total GPU workers should equal `DP x PP x TP` (data parallel size × pipeline parallel size × tensor parallel size) rather than just `DP x TP`, aligning with the architectural documentation.

**Technical impact**  
This is a documentation-only change that fixes an inconsistency in the architectural overview. It ensures accuracy for users and developers referencing the table to understand vLLM's process distribution across GPUs, particularly when pipeline parallelism is involved.

**Potential risks**  
No technical risks since no code was modified. However, if the documentation still contains other inaccuracies or if users rely on outdated cached versions, it could lead to misunderstandings about process scaling.

**Key insights**  
Always verify documentation against authoritative sources like architecture guides. For developers, this highlights the importance of keeping documentation synchronized with implementation details, especially in distributed systems where parallel dimensions interact. Consider reviewing related documentation sections for similar inconsistencies.

---

## 45. [Use MMEncoderAttention (=use FlashAttention) instead of torch.sdpa in radio.py](https://github.com/vllm-project/vllm/pull/35653)


### Base Information

- **PR Number:** #35653
- **Author:** [netanel-haber](https://github.com/netanel-haber)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-03-04 08:43:14
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35653/files) (1):**
  - `vllm/model_executor/models/radio.py`

### Summary

**What changed and why**  
The PR replaces torch's scaled_dot_product_attention with MMEncoderAttention (which uses FlashAttention when available) in the radio.py model. This change introduces a new attention masking mechanism using `MaskMetadata` dataclass with `cu_seqlens` and `max_seqlen` instead of explicit boolean attention masks, enabling more efficient attention computation for variable-length sequences.

**Technical impact**  
The architecture shifts from using explicit boolean attention masks to a packed sequence representation via cumulative sequence lengths. This affects the attention implementation throughout the Radio model stack (ParallelAttention, EncoderLayer, Encoder) and changes how inter-image attention masking is handled for dynamic resolution images. The `imgs_sizes` parameter type changes from `torch.Tensor` to `list[tuple[int, int]]` to better represent image dimensions.

**Potential risks**  
The removal of explicit boolean mask validation could mask errors in sequence packing. The comment about keeping `max_seqlen` on CPU to avoid `.item()` sync suggests potential device synchronization issues. The assertion `assert len(imgs_sizes) > 0` could fail silently in production if `imgs_sizes` is empty but not None. The change assumes MMEncoderAttention's interface compatibility with the existing model architecture.

**Key insights**  
This optimization leverages FlashAttention's efficiency for packed sequences, which should improve performance for multi-image processing. Developers should verify that `MMEncoderAttention` properly handles the `cu_seqlens`/`max_seqlen` interface and that the sequence packing logic in `inter_image_mask_metadata` correctly matches the model's requirements. The device placement strategy for `max_seqlen` warrants attention to avoid unnecessary GPU-CPU synchronization.

---

## 46. [Split generic IO Processor plugins tests from Terratorch specific ones](https://github.com/vllm-project/vllm/pull/35756)


### Base Information

- **PR Number:** #35756
- **Author:** [christian-pinto](https://github.com/christian-pinto)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 08:01:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35756/files) (4):**
  - `.buildkite/test_areas/plugins.yaml`
  - `tests/conftest.py`
  - `tests/plugins_tests/test_io_processor_plugins.py`
  - `tests/plugins_tests/test_terratorch_io_processor_plugins.py`

### Summary

**What changed and why**  
This PR splits the generic IO Processor plugin tests from Terratorch-specific tests, creating two separate test files. The changes include adding new generic tests for plugin loading paths, moving Terratorch-specific tests to a dedicated file, and updating the CI pipeline to run both test suites independently.

**Technical impact**  
The refactoring improves test organization by separating generic plugin functionality tests from vendor-specific implementations. This enhances maintainability and scalability as more IO Processor plugins are added. The CI pipeline now runs generic tests first, followed by Terratorch and bge_m3 plugin tests, ensuring better isolation.

**Potential risks**  
The test split could introduce duplication if not carefully managed, especially for shared fixtures or helper functions. The added engine shutdown in `conftest.py` may mask underlying resource management issues if exceptions are silently ignored. Changes to the CI pipeline order could affect test dependencies or environment setup.

**Key insights**  
Developers should ensure new IO Processor plugins follow the same pattern: generic tests go in `test_io_processor_plugins.py`, while plugin-specific tests reside in separate files. The engine shutdown addition addresses GPU OOM in CI but should be monitored for unintended side effects. Future plugins must be added to the CI pipeline explicitly.

---

## 47. [[Misc] Add `--attention-backend auto` option](https://github.com/vllm-project/vllm/pull/35738)


### Base Information

- **PR Number:** #35738
- **Author:** [NickLucche](https://github.com/NickLucche)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-03-04 07:12:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35738/files) (3):**
  - `tests/kernels/attention/test_attention_selector.py`
  - `vllm/config/attention.py`
  - `vllm/engine/arg_utils.py`

### Summary

**What changed and why**  
Added support for `--attention-backend auto` as an alias for not specifying the flag, which triggers automatic backend selection. This change improves UX consistency with other vLLM flags like `--moe-backend` and provides flexibility for scripts where the backend value is templated.

**Technical impact**  
The `AttentionConfig` validator now maps the string "auto" (case-insensitive) to `None`, enabling automatic backend selection. This centralizes string-to-enum conversion logic, simplifying the engine config creation by reusing the validator. Test coverage ensures "auto" behaves identically to `None`.

**Potential risks**  
If existing code relies on the string "auto" being treated as an invalid backend, it may now silently default to automatic selection. The case-insensitive matching could inadvertently convert similar strings (e.g., "AUTOMATIC") to `None`, though this is mitigated by the exact `lower() == "auto"` check.

**Key insights**  
This is a clean, non-breaking enhancement that aligns with vLLM's flag conventions. Developers should update any scripts using `--attention-backend` to leverage "auto" for automatic selection. Ensure downstream logic expecting `None` for automatic selection remains compatible.

---

## 48. [[Core] Add All-to-All communication backend for DCP](https://github.com/vllm-project/vllm/pull/34883)


### Base Information

- **PR Number:** #34883
- **Author:** [sungsooha](https://github.com/sungsooha)
- **Merged By:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged time:** 2026-03-04 07:01:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34883/files) (8):**
  - `tests/distributed/test_dcp_a2a.py`
  - `vllm/config/parallel.py`
  - `vllm/config/vllm.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/model_executor/layers/attention/mla_attention.py`
  - `vllm/v1/attention/backends/flash_attn.py`
  - `vllm/v1/attention/backends/flashinfer.py`
  - `vllm/v1/attention/ops/dcp_alltoall.py`

### Summary

**What changed and why**  
This PR introduces an All-to-All (A2A) communication backend as an alternative to the existing AllGather+ReduceScatter (AG+RS) backend for Decode Context Parallel (DCP). The A2A backend exchanges partial attention outputs and their log-sum-exp (LSE) values across ranks, then combines them locally with a Triton kernel. This reduces NCCL calls from three to two per attention layer, lowering communication overhead for long-context decode.

**Technical impact**  
The changes add a new communication path for DCP that modifies how partial attention outputs are aggregated. A new Triton kernel (`dcp_lse_combine_triton`) performs LSE-weighted combination after A2A exchanges, replacing the previous AG+RS pattern. The backend is selectable via a new `--dcp-comm-backend` CLI option (`ag_rs` or `a2a`), with validation ensuring A2A is only used when DCP size > 1. Integration touches multiple attention backends (FlashAttention, FlashInfer, MLA) to route decode output through the selected backend.

**Potential risks**  
The A2A backend introduces a new Triton kernel and communication pattern, which may have subtle numerical differences compared to AG+RS, especially in edge cases with NaN/inf LSE values. The kernel’s handling of numerical stability (e.g., LSE max reduction) must align with the reference CPU implementation. Additionally, the backend adds conditional logic across attention implementations, increasing code complexity and potential for regression if not all code paths are thoroughly tested.

**Key insights**  
Developers should note that A2A is only beneficial when DCP size > 1 and communication latency is a bottleneck. The PR includes comprehensive unit tests for config validation and LSE combination correctness, which should be run to verify numerical equivalence. When enabling A2A, ensure CUDA graph compatibility is maintained (FULL for MLA, PIECEWISE for GQA). The reference CPU implementation (`_lse_weighted_combine`) serves as a critical validation tool for the Triton kernel.

---

## 49. [[Bugfix] Make `kaldi_native_fbank` optional](https://github.com/vllm-project/vllm/pull/35996)


### Base Information

- **PR Number:** #35996
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-04 06:47:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35996/files) (4):**
  - `requirements/common.txt`
  - `requirements/test.in`
  - `requirements/test.txt`
  - `vllm/transformers_utils/processors/fireredasr2_processor.py`

### Summary

**What changed and why**  
The PR makes `kaldi_native_fbank` an optional dependency by removing it from common requirements and moving it to test requirements. It also implements lazy loading in the FireRedASR2 processor module to prevent import failures when the package isn't installed. This addresses the issue where any import of `vllm.transformers_utils.processors` would fail without `kaldi_native_fbank`, even if FireRedASR2 isn't being used.

**Technical impact**  
These changes decouple the core vLLM codebase from the `kaldi_native_fbank` dependency, making it truly optional. The lazy loading pattern ensures the import only occurs when the FireRedASR2 processor is actually instantiated. This reduces installation overhead and prevents unnecessary dependencies for users who don't need FireRedASR2 functionality.

**Potential risks**  
The lazy loading implementation uses TYPE_CHECKING which may cause issues with static type checkers if not properly configured. There's also a risk that runtime imports could fail silently if `kaldi_native_fbank` is needed but not installed, though this is preferable to import-time failures. The PR author noted uncertainty about whether `AutoProcessor.register` needs to execute at import time, which could affect processor registration.

**Key insights**  
This is a pragmatic solution that balances dependency management with functionality. Developers should ensure type checking configurations handle the TYPE_CHECKING guard properly. Consider extending this lazy loading pattern to other optional dependencies in the codebase for consistency. The approach maintains backward compatibility while reducing installation friction.

---

## 50. [[BugFix] Fix implicit and incorrect assumption on ECConnector is_producer](https://github.com/vllm-project/vllm/pull/34783)


### Base Information

- **PR Number:** #34783
- **Author:** [furionw](https://github.com/furionw)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-03-04 06:01:30
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34783/files) (8):**
  - `examples/online_serving/ec_both_encoder/ec_both_encoder.sh`
  - `tests/v1/core/test_scheduler.py`
  - `tests/v1/ec_connector/unit/test_ec_example_connector.py`
  - `vllm/distributed/ec_transfer/ec_connector/example_connector.py`
  - `vllm/v1/core/sched/scheduler.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/executor/ray_executor.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
This PR fixes a critical bug where aggregated nodes with `ec_both` role would crash due to incorrect assumptions about producer/consumer roles. Previously, `is_producer` was equated with "encode-only" behavior, but `ec_both` nodes can both encode and decode, requiring KV cache allocation. The changes replace `is_producer` checks with `is_consumer` logic and ensure connector state is properly synchronized after encoder cache allocation.

**Technical impact**  
The modifications correct role-based logic across the scheduler, engine, and executor layers, enabling `ec_both` nodes to function as both producers and consumers. This allows aggregated nodes to allocate KV cache and perform forward passes, supporting mixed encoding/decoding workloads. The connector state update is now invoked during scheduling to maintain consistency between cache allocation and external storage.

**Potential risks**  
- The logic inversion (`is_producer` → `not is_consumer`) may introduce subtle bugs if other code paths still rely on the old assumption.  
- The `update_state_after_alloc` call is now unconditional in the scheduler, but the connector internally guards with `is_consumer` and cache existence; this could lead to unnecessary overhead if frequently called for non-consumer roles.  
- Test updates show expanded mocking and assertion changes, which may mask edge cases if the mocking does not fully replicate production behavior.

**Key insights**  
- Developers should verify that all role-dependent logic consistently uses `is_consumer` checks rather than `is_producer`.  
- The added example script provides a valuable reference for `ec_both` configuration and testing.  
- Ensure that any future extensions to EC roles (beyond producer/consumer/both) are evaluated against this updated role-handling paradigm.

---

## 51. [[Bugfix][Model] Fix FP8 k_scale/v_scale not loaded for Qwen3-MoE](https://github.com/vllm-project/vllm/pull/35656)


### Base Information

- **PR Number:** #35656
- **Author:** [oneraghavan](https://github.com/oneraghavan)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-03-04 05:15:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35656/files) (3):**
  - `tests/model_executor/test_weight_utils.py`
  - `vllm/model_executor/models/qwen3_moe.py`
  - `vllm/model_executor/models/qwen3_vl_moe.py`

### Summary

**What changed and why**  
The fix addresses a bug where FP8 KV cache scales (`qkv_proj.k_scale` and `qkv_proj.v_scale`) from llm-compressor checkpoints were being silently dropped during weight loading for Qwen3-MoE models. This caused the scales to default to 1.0, leading to accuracy degradation. The root cause was that these scale names were caught by `ignore_suffixes` before `maybe_remap_kv_scale_name` could remap them to the expected `attn.k_scale` format.

**Technical impact**  
The changes ensure FP8 KV cache scales are properly loaded by removing `k_scale` and `v_scale` from `ignore_suffixes` and adding early remapping for scale/zero_point names. This aligns Qwen3-MoE's weight loading behavior with Llama's approach, improving compatibility with llm-compressor and ModelOpt checkpoint formats. The refactoring also simplifies the logic by removing redundant KV-scale remapping code later in the function.

**Potential risks**  
If the remapping logic incorrectly matches non-KV scale names (e.g., other scale types like `weight_scale`), it could cause loading errors. The conditional `if "scale" in name or "zero_point" in name:` might be overly broad, though the existing tests validate edge cases. Additionally, the removal of the `kv_scale` remapping block could affect older checkpoint formats if not fully covered by `maybe_remap_kv_scale_name`.

**Key insights**  
Developers should verify that `maybe_remap_kv_scale_name` handles all historical KV scale naming conventions, as the function now centralizes this logic. The extensive test suite added is critical for preventing regressions across different checkpoint formats (llm-compressor, ModelOpt, NVFP4). When modifying scale handling in other models, follow this pattern of early remapping and ensure `ignore_suffixes` does not block required parameters.

---

## 52. [[Bugfix][CPUOffloadingManager] Prevent eviction of already-stored blocks in LRU/ARC `prepare_store()`](https://github.com/vllm-project/vllm/pull/35846)


### Base Information

- **PR Number:** #35846
- **Author:** [ronensc](https://github.com/ronensc)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-03-04 04:25:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35846/files) (3):**
  - `tests/v1/kv_offload/test_cpu_manager.py`
  - `vllm/v1/kv_offload/arc_manager.py`
  - `vllm/v1/kv_offload/lru_manager.py`

### Summary

**What changed and why**  
Fixed a critical bug in CPU offloading managers where blocks already present in the cache could be incorrectly evicted during `prepare_store()` operations. The issue occurred because eviction logic didn't exclude blocks that were part of the input list, even when those blocks were already stored and shouldn't be removed. Both LRU and ARC managers now protect input blocks from eviction.

**Technical impact**  
The fix ensures cache consistency by preventing data loss when storing new blocks alongside existing ones. Both managers now maintain a `protected` set of input block hashes during eviction selection, preserving the invariant that blocks specified in `prepare_store()` remain available after the operation completes. This affects cache replacement policies but maintains their core algorithms.

**Potential risks**  
The protection mechanism could potentially reduce available eviction candidates in edge cases where many protected blocks are unreferenced, though this aligns with the intended behavior. There's a minor performance overhead from creating the protected set and additional membership checks during eviction loops, but this is negligible given the small block counts involved.

**Key insights**  
Always validate eviction candidates against the full operation context, not just cache state. The fix demonstrates the importance of considering all inputs when making cache management decisions. The comprehensive test case covering both managers ensures this regression won't recur and provides clear documentation of the expected behavior through its scenario-based approach.

---

## 53. [[MISC] fixed tool_parser mypy errors](https://github.com/vllm-project/vllm/pull/35640)


### Base Information

- **PR Number:** #35640
- **Author:** [taneem-ibrahim](https://github.com/taneem-ibrahim)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 04:23:12
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35640/files) (4):**
  - `tools/pre_commit/mypy.py`
  - `vllm/tool_parsers/functiongemma_tool_parser.py`
  - `vllm/tool_parsers/glm4_moe_tool_parser.py`
  - `vllm/tool_parsers/step3p5_tool_parser.py`

### Summary

**What changed and why**  
This PR removes `vllm/tool_parser` from the mypy exclusion list, as the referenced directory doesn't exist, and fixes type annotation issues in three tool parser files to satisfy mypy checks. These changes are part of an ongoing effort to enable comprehensive mypy type checking across the codebase.

**Technical impact**  
Enabling mypy for the `tool_parsers` directory improves type safety and code quality for tool parsing functionality. The changes involve adding explicit type annotations, simplifying variable names for clarity, and removing an unused import and decorator, which collectively make the code more maintainable and less error-prone.

**Potential risks**  
The removal of the `@ToolParserManager.register_module("step3p5")` decorator could potentially affect dynamic registration of the Step3p5ToolParser if the registration system is actively used elsewhere. Additionally, the variable renaming from `frag` to `frag_or_none` is purely cosmetic but should be verified to not break any external dependencies or expectations.

**Key insights**  
The PR successfully resolves mypy errors with minimal, focused changes. Developers should ensure the Step3p5ToolParser is still properly registered via another mechanism if needed. This cleanup is a positive step toward stricter type checking, and similar efforts should continue for other excluded directories.

---

## 54. [[Feature] Add basic metrics for /realtime endpoint](https://github.com/vllm-project/vllm/pull/35500)


### Base Information

- **PR Number:** #35500
- **Author:** [pougetat](https://github.com/pougetat)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-04 03:56:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35500/files) (2):**
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/openai/realtime/metrics.py`

### Summary

**What changed and why**  
A new middleware (`WebSocketMetricsMiddleware`) was added to instrument the `/realtime` WebSocket endpoint with Prometheus metrics. The middleware tracks active connections, total connections, and connection duration, addressing the lack of metrics for realtime endpoints as outlined in issue #35004. It is designed as a standalone ASGI component with no internal vLLM dependencies to facilitate future extraction into a separate package.

**Technical impact**  
The changes introduce observable metrics for WebSocket connections without modifying existing business logic. The middleware integrates seamlessly into the FastAPI app when the `realtime` task is supported, enhancing monitoring capabilities. The architecture follows patterns similar to `prometheus-fastapi-instrumentator`, promoting consistency and reusability.

**Potential risks**  
If the middleware fails to correctly intercept `websocket.accept` messages, metrics may become inaccurate (e.g., missing increments/decrements). The `multiprocess_mode="livesum"` on the gauge assumes proper Prometheus client configuration in multiprocess environments, which could lead to incorrect active connection counts if misconfigured. Additionally, any exceptions in `send_wrapper` could disrupt WebSocket communication.

**Key insights**  
The middleware effectively decouples metrics collection from core logic, aligning with extensibility goals. Ensure Prometheus multiprocess mode is correctly set up in deployment to avoid gauge inaccuracies. Consider adding error handling in `send_wrapper` to prevent metric corruption while preserving WebSocket functionality. The design supports future extraction but requires maintaining the "zero vLLM imports" constraint.

---

## 55. [[Bugfix] Cap FULL decode cudagraph sizes for Mamba/hybrid models (#34094)](https://github.com/vllm-project/vllm/pull/34571)


### Base Information

- **PR Number:** #34571
- **Author:** [haosdent](https://github.com/haosdent)
- **Merged By:** [tdoublep](https://github.com/tdoublep)
- **Merged time:** 2026-03-04 02:56:23
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34571/files) (4):**
  - `tests/compile/test_config.py`
  - `tests/v1/worker/test_gpu_model_runner.py`
  - `vllm/config/compilation.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Added logic to cap CUDA graph capture sizes for hybrid Mamba/attention models when using FULL decode mode. The fix prevents an assertion failure in `causal_conv1d_update` by ensuring decode batch sizes do not exceed available Mamba cache blocks, since each decode request requires a dedicated cache line.

**Technical impact**  
The `CompilationConfig` now includes a new method `adjust_cudagraph_sizes_for_mamba_cache()` that filters capture sizes based on the number of Mamba cache blocks. This is invoked during KV cache initialization for models with Mamba layers, ensuring CUDA graph captures only use batch sizes that fit within cache limits. The change follows the existing pattern for speculative decoding adjustments.

**Potential risks**  
If all capture sizes exceed the available cache blocks, CUDA graph capture is disabled entirely, which could degrade performance for small-batch scenarios. The capping logic only applies to FULL decode captures; PIECEWISE captures remain unaffected, which may lead to inconsistent behavior if the mode changes dynamically. Edge cases with zero or negative cache blocks are handled via early return.

**Key insights**  
Developers should note that Mamba cache constraints now directly limit maximum CUDA graph batch sizes. To mitigate performance impacts, consider adjusting `max_num_seqs` or increasing GPU memory allocation. The fix is narrowly scoped to hybrid models and FULL decode mode, so integration with other CUDA graph optimizations (e.g., speculative decoding) should be validated.

---

## 56. [[XPU] bump vllm-xpu-kernels to v0.1.3](https://github.com/vllm-project/vllm/pull/35984)


### Base Information

- **PR Number:** #35984
- **Author:** [jikunshang](https://github.com/jikunshang)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-03-04 02:23:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35984/files) (2):**
  - `.buildkite/scripts/hardware_ci/run-xpu-test.sh`
  - `requirements/xpu.txt`

### Summary

**What changed and why**  
This PR updates the `vllm-xpu-kernels` dependency from version 0.1.2 to 0.1.3 to incorporate performance improvements in decode attention kernels. It also modifies the CI test script to skip `test_scheduler_e2e.py` due to a known limitation in the new kernel version that only supports block sizes of 64 or 128.

**Technical impact**  
Upgrading the kernel dependency may improve inference performance for XPU hardware, particularly in decode attention operations. The test exclusion ensures CI passes by avoiding unit tests that use unsupported block sizes (e.g., 16), maintaining CI stability while the kernel limitation exists.

**Potential risks**  
The kernel's restriction to block sizes 64/128 could affect models or configurations relying on smaller block sizes, potentially causing failures in production if not properly validated. Additionally, skipping tests reduces coverage for scheduler functionality, which might mask regressions.

**Key insights**  
Developers should verify that all production workloads use supported block sizes (64 or 128) and consider adding a runtime check for block size compatibility. The skipped test should be re-enabled once the kernel supports smaller block sizes, and performance gains from the update should be benchmarked in relevant scenarios.

---

## 57. [[Hardware] Replace `torch.cuda.empty_cache` with `torch.accelerator.empty_cache`](https://github.com/vllm-project/vllm/pull/30681)


### Base Information

- **PR Number:** #30681
- **Author:** [jikunshang](https://github.com/jikunshang)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-04 01:49:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/30681/files) (35):**
  - `.pre-commit-config.yaml`
  - `benchmarks/benchmark_topk_topp.py`
  - `benchmarks/kernels/benchmark_moe.py`
  - `benchmarks/kernels/benchmark_reshape_and_cache.py`
  - `benchmarks/kernels/benchmark_reshape_and_cache_flash.py`
  - `examples/offline_inference/lora_with_quantization_inference.py`
  - `examples/offline_inference/rlhf_colocate.py`
  - `examples/offline_inference/rlhf_utils.py`
  - `tests/compile/test_dynamic_shapes_compilation.py`
  - `tests/conftest.py`
  - `tests/entrypoints/openai/test_tensorizer_entrypoint.py`
  - `tests/kernels/mamba/test_causal_conv1d.py`
  - `tests/kernels/moe/test_moe.py`
  - `tests/model_executor/model_loader/tensorizer_loader/test_tensorizer.py`
  - `tests/test_regression.py`
  - `tests/v1/e2e/test_async_spec_decode.py`
  - `tests/v1/e2e/test_lora_with_spec_decode.py`
  - `tests/v1/e2e/test_mamba_prefix_cache.py`
  - `tests/v1/e2e/test_spec_decode.py`
  - `tests/v1/entrypoints/llm/test_struct_output_generate.py`
  - `tests/v1/sample/test_logprobs.py`
  - `tools/pre_commit/check_torch_cuda.py`
  - `vllm/compilation/cuda_graph.py`
  - `vllm/distributed/elastic_ep/elastic_execute.py`
  - `vllm/distributed/parallel_state.py`
  - `vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`
  - `vllm/model_executor/layers/quantization/utils/fp8_utils.py`
  - `vllm/model_executor/model_loader/bitsandbytes_loader.py`
  - `vllm/utils/mem_utils.py`
  - `vllm/v1/sample/ops/topk_topp_triton.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu_worker.py`
  - `vllm/v1/worker/xpu_model_runner.py`
  - `vllm/v1/worker/xpu_worker.py`

### Summary

**What changed and why**  
This PR replaces direct `torch.cuda.empty_cache()` calls with `torch.accelerator.empty_cache()` across the codebase to improve hardware backend compatibility. The change is part of a larger effort to remove CUDA-specific hardcoded calls, enabling vLLM to support non-CUDA devices (like XPU) more seamlessly. A new pre-commit hook is also added to prevent future `torch.cuda` API usage.

**Technical impact**  
The changes decouple memory management from CUDA, allowing the `torch.accelerator` API to dispatch `empty_cache` operations based on the active hardware platform. This improves code portability and aligns with vLLM's multi-backend design. The pre-commit hook enforces this pattern moving forward, reducing technical debt.

**Potential risks**  
If `torch.accelerator.empty_cache()` is not available in older PyTorch versions, it could cause runtime errors. The XPU-specific override removal (`torch.xpu.empty_cache`) might break existing XPU support if the accelerator API doesn't fully handle it. Additionally, the pre-commit hook may need updates as more `torch.cuda` APIs are phased out.

**Key insights**  
This is a foundational change for multi-hardware support; developers should adopt `torch.accelerator` APIs for new code. Ensure PyTorch version compatibility (likely >=2.5) when integrating these changes. The pre-commit hook is a proactive measure to maintain consistency, but its allowlist may need expansion for platform-specific files.

---

## 58. [[ROCm][Bugfix] Fall back from CK MXFP4 MoE when GEMM dimensions are unsupported](https://github.com/vllm-project/vllm/pull/35893)


### Base Information

- **PR Number:** #35893
- **Author:** [ChuanLi1101](https://github.com/ChuanLi1101)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-04 00:30:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35893/files) (3):**
  - `vllm/model_executor/layers/quantization/mxfp4.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`
  - `vllm/model_executor/layers/quantization/utils/mxfp4_utils.py`

### Summary

**What changed and why**  
Added validation to detect when CK's MXFP4 MoE GEMM kernels cannot support specific matrix dimensions due to alignment constraints. The fix adds fallback mechanisms in two code paths: `Mxfp4MoEMethod` falls back to the Triton backend, and `QuarkOCP_MX_MoEMethod` falls back to emulation mode (weight dequantization + bf16 GEMM). This prevents a `RuntimeError` when using certain tensor-parallel configurations with models like MiniMax-M2.1.

**Technical impact**  
The changes introduce runtime dimension checks during MoE layer initialization. When `intermediate_size_per_partition` is not a multiple of 256, the system gracefully degrades to a supported backend or emulation mode instead of crashing. This improves robustness for edge-case TP configurations while maintaining optimal performance for compatible dimensions.

**Potential risks**  
The fallback paths may have performance implications, as Triton or emulation modes are likely slower than native CK kernels. There's a risk of silent performance degradation if warnings are overlooked. The validation depends on correct calculation of `intermediate_size_per_partition`, which must account for tensor parallelism correctly.

**Key insights**  
Always validate kernel constraints at initialization rather than encountering runtime errors. The alignment requirement (256) is now centralized in `CK_MXFP4_MOE_DIM_ALIGNMENT` for consistency. Developers should monitor logs for fallback warnings and consider adjusting tensor-parallel size to maintain optimal performance when possible.

---

## 59. [[Bugfix] Add missing dynamic_arg_dims for Qwen3-ASR torch.compile](https://github.com/vllm-project/vllm/pull/35869)


### Base Information

- **PR Number:** #35869
- **Author:** [TheCodeWrangler](https://github.com/TheCodeWrangler)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-03-04 00:29:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35869/files) (1):**
  - `vllm/model_executor/models/qwen3_asr_realtime.py`

### Summary

**What changed and why**  
The fix removes the bare `@support_torch_compile` decorator from `Qwen3ASRRealtimeGeneration` because it was incorrectly overriding the inherited, properly configured decorator from its parent class `Qwen3ASRForConditionalGeneration`. The parent class now has the correct `dynamic_arg_dims` annotation, which this subclass inherits.

**Technical impact**  
This change ensures that `torch.compile` correctly identifies the dynamic batch dimension (-1) for the `positions` tensor used in MRoPE, aligning Qwen3-ASR's behavior with other MRoPE models in the codebase. The server can now start successfully without requiring `--enforce-eager` and completes CUDA graph capture.

**Potential risks**  
If the parent class's `@support_torch_compile` decorator is ever removed or modified incorrectly, the subclass would lose its compilation support. There is also a risk if future changes introduce new dynamic tensor arguments that aren't covered by the current `dynamic_arg_dims` specification in the parent.

**Key insights**  
Always verify that subclass decorators don't unintentionally override properly configured parent class decorators. The pattern of using explicit `dynamic_arg_dims` for MRoPE models is critical and must be consistently applied across all related model classes to avoid `ConstraintViolationError` during torch.compile tracing.

---

