# vLLM Merged PR Report

**Report Date:** 2026-03-20 PDT

**Total Merged PRs:** 45

---

## 1. [[Responses API] Add kv_transfer_params for PD disaggregation](https://github.com/vllm-project/vllm/pull/37424)


### Base Information

- **PR Number:** #37424
- **Author:** [bongwoobak](https://github.com/bongwoobak)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-03-20 22:48:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37424/files) (3):**
  - `vllm/entrypoints/openai/responses/context.py`
  - `vllm/entrypoints/openai/responses/protocol.py`
  - `vllm/entrypoints/openai/responses/serving.py`

### Summary

**What changed and why**  
Added `kv_transfer_params` support to the Responses API (`/v1/responses`) to enable PD (prefill-decode) disaggregation, mirroring existing Chat Completions API functionality. This involves propagating the field through request handling, sampling parameters, and response serialization across all four context types.

**Technical impact**  
The changes extend the Responses API to accept and return KV transfer parameters, allowing disaggregated serving workflows (like prefill and decode on separate hardware) to use this endpoint. The parameters are injected into `SamplingParams.extra_args` and tracked through `RequestOutput` to the final response.

**Potential risks**  
- If `kv_transfer_params` contains unsupported or malformed data, it could disrupt engine execution or KV cache management.  
- The field is optional and may be silently ignored if not properly validated, leading to subtle disaggregation failures.  
- Changes span multiple context classes; inconsistent handling (e.g., missing updates in one context) could cause data loss or incorrect behavior.

**Key insights**  
- Ensure `kv_transfer_params` validation aligns with the engine's expected format to prevent runtime errors.  
- Verify that all context types (`SimpleContext`, `ParsableContext`, etc.) handle the field uniformly, especially in edge cases like streaming or multi-turn interactions.  
- Consider adding integration tests for disaggregation scenarios to confirm end-to-end functionality across different model types and hardware configurations.

---

## 2. [[ROCm][CI] Mark gemma3 as large GPU test to avoid OOM on MI250](https://github.com/vllm-project/vllm/pull/37610)


### Base Information

- **PR Number:** #37610
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 21:57:25
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37610/files) (2):**
  - `.buildkite/test-amd.yaml`
  - `tests/models/multimodal/generation/test_common.py`

### Summary

**What changed and why**  
The changes address OOM (Out of Memory) errors on MI250 GPUs by marking the Gemma3 model test as a "large GPU" test when running on ROCm. This ensures the test only runs on GPUs with sufficient memory (≥80GB), preventing failures on smaller GPUs like MI250. Additionally, the CI configuration documentation was updated to reflect current hardware and improve test grouping clarity.

**Technical impact**  
This modifies test filtering logic in the CI pipeline to conditionally apply the `large_gpu_mark` to Gemma3 tests specifically on ROCm platforms. The test will now be skipped on GPUs with less than 80GB memory, preventing OOM crashes. The documentation updates provide more accurate hardware descriptions and better reflect the current test organization structure.

**Potential risks**  
The 80GB threshold may still be insufficient for future model variants or different batch sizes. There's a risk of creating platform-specific test behavior that could diverge from CUDA test coverage. If memory requirements change, the hardcoded threshold will need manual updates.

**Key insights**  
Always validate memory requirements against actual hardware constraints when marking tests. Consider making memory thresholds configurable based on model parameters rather than hardcoded values. The documentation improvements help maintain clarity as test suites evolve across different hardware platforms.

---

## 3. [Revert "[compile] Initialize passes at VllmBackend init"](https://github.com/vllm-project/vllm/pull/37733)


### Base Information

- **PR Number:** #37733
- **Author:** [simon-mo](https://github.com/simon-mo)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-20 21:35:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37733/files) (3):**
  - `tests/test_config.py`
  - `vllm/compilation/backends.py`
  - `vllm/compilation/decorators.py`

### Summary

**What changed and why**  
This PR reverts a previous change that initialized compilation passes during `VllmBackend` initialization. The revert moves the `configure_post_pass()` call back to the `__call__` method, aligning with the original behavior where pass configuration occurs at `torch.compile` time rather than backend setup.

**Technical impact**  
The change shifts the timing of post-grad pass configuration from backend initialization to the first invocation of the backend. This may increase perceived compilation latency during the first `torch.compile` call, as one-time setup costs (e.g., FlashInfer workspace allocation) are now attributed to compilation time instead of being amortized earlier.

**Potential risks**  
- Increased compilation latency for the first model compilation, which could affect user-facing metrics or benchmarks.
- The test update assumes `configure_post_pass()` is idempotent or safe to call multiple times; if not, repeated calls could cause issues.
- The removal of timing logging in backend init reduces visibility into pass configuration overhead.

**Key insights**  
- This revert simplifies the initialization flow but trades off early setup for deferred compilation costs.  
- Ensure `configure_post_pass()` is designed to be called exactly once per backend instance to avoid side effects.  
- Consider adding back timing metrics in `__call__` if monitoring pass configuration latency is important for performance analysis.

---

## 4. [[ROCm][CI] Guard CudaPlatform/RocmPlatform imports to fix test collection on cross-platform builds](https://github.com/vllm-project/vllm/pull/37617)


### Base Information

- **PR Number:** #37617
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 20:58:58
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37617/files) (1):**
  - `tests/kernels/attention/test_attention_selector.py`

### Summary

**What changed and why**  
The changes guard imports of `CudaPlatform` and `RocmPlatform` with try-except blocks to handle `ModuleNotFoundError` when these platforms are unavailable. This prevents test collection failures in cross-platform builds (e.g., CUDA tests on ROCm-only systems and vice versa) by conditionally skipping tests when the required platform is missing.

**Technical impact**  
This modification ensures test modules can be imported without raising exceptions during test discovery, allowing the test suite to run on heterogeneous CI environments. Tests now gracefully skip when the underlying hardware platform (CUDA/ROCm) is not available, improving cross-platform compatibility and CI reliability.

**Potential risks**  
If `CudaPlatform` or `RocmPlatform` are `None`, tests that rely on them will be skipped, potentially masking real failures on platforms where they should be available. The added conditional logic increases test complexity, and developers must ensure new tests continue to check for platform availability before use.

**Key insights**  
Always guard platform-specific imports in test files to support cross-platform CI pipelines. Use `pytest.skip()` with clear messages when dependencies are missing. Consider centralizing platform availability checks to reduce code duplication and maintain consistency across test modules.

---

## 5. [[MoE Refactor] Mxfp4 oracle rebased](https://github.com/vllm-project/vllm/pull/37128)


### Base Information

- **PR Number:** #37128
- **Author:** [zyongye](https://github.com/zyongye)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-20 20:37:05
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37128/files) (18):**
  - `docs/design/moe_kernel_features.md`
  - `tests/compile/fusions_e2e/conftest.py`
  - `tests/kernels/moe/test_gpt_oss_triton_kernels.py`
  - `tests/kernels/moe/test_ocp_mx_moe.py`
  - `tests/kernels/quantization/test_mxfp4_triton_ep.py`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_mxfp4_moe.py`
  - `vllm/model_executor/layers/fused_moe/fused_marlin_moe.py`
  - `vllm/model_executor/layers/fused_moe/gpt_oss_triton_kernels_moe.py`
  - `vllm/model_executor/layers/fused_moe/layer.py`
  - `vllm/model_executor/layers/fused_moe/oracle/mxfp4.py`
  - `vllm/model_executor/layers/fused_moe/oracle/nvfp4.py`
  - `vllm/model_executor/layers/fused_moe/rocm_aiter_fused_moe.py`
  - `vllm/model_executor/layers/fused_moe/trtllm_moe.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe.py`
  - `vllm/model_executor/layers/quantization/mxfp4.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`
  - `vllm/model_executor/layers/quantization/utils/marlin_utils_fp4.py`
  - `vllm/model_executor/layers/quantization/utils/mxfp4_utils.py`

### Summary

**What changed and why**  
This PR refactors MXFP4 MoE from a monolithic 1299-line class into an oracle pattern, aligning it with FP8 and NVFP4 implementations. It creates new backend-specific expert classes (`TrtLlmMxfp4ExpertsMonolithic`, `OAITritonMxfp4ExpertsMonolithic`), moves backend selection and weight conversion logic to `oracle/mxfp4.py`, and consolidates monolithic/modular kernel selection. Hidden-size rounding is moved from `layer.py` into the oracle, simplifying the codebase.

**Technical impact**  
The refactor improves code maintainability by reducing duplication and standardizing the MXFP4 MoE backend selection mechanism. It introduces a unified oracle pattern that supports multiple backends (FlashInfer TRTLLM/CUTLASS, Triton, Marlin, CK) with proper priority ordering and configuration validation. Kernel assembly and weight conversion are now centralized, making it easier to add or modify backends.

**Potential risks**  
- The removal of the `_can_support_mxfp4` function may affect compatibility checks for advanced routing features (grouped top-k, custom routing, etc.).  
- Hidden-size rounding logic relocation could introduce subtle alignment issues if not consistently applied across all backends.  
- The refactor touches many files (18 changed), increasing the risk of integration errors, especially with environment variable handling and backend fallbacks.

**Key insights**  
- Developers should verify that all MXFP4 backends (especially Triton and Marlin) correctly handle the new oracle pattern and support the same feature set (e.g., expert mapping, activation types).  
- The centralized rounding logic in `mxfp4_round_up_hidden_size_and_intermediate_size` must be reviewed to ensure it matches all backend requirements (e.g., alignment to 128/256).  
- Testing should focus on edge cases like LoRA-enabled models, multi-GPU configurations (TP/DP/EP), and different activation formats to ensure no regression in kernel selection or performance.

---

## 6. [[Frontend] Remove librosa from audio dependency](https://github.com/vllm-project/vllm/pull/37058)


### Base Information

- **PR Number:** #37058
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-03-20 20:36:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37058/files) (18):**
  - `requirements/test.in`
  - `requirements/test.txt`
  - `setup.py`
  - `tests/entrypoints/openai/speech_to_text/test_transcription_validation.py`
  - `tests/entrypoints/openai/test_run_batch.py`
  - `tests/models/multimodal/generation/vlm_utils/builders.py`
  - `tests/multimodal/media/test_audio.py`
  - `tests/multimodal/test_audio.py`
  - `vllm/assets/audio.py`
  - `vllm/assets/video.py`
  - `vllm/benchmarks/datasets.py`
  - `vllm/entrypoints/openai/speech_to_text/speech_to_text.py`
  - `vllm/model_executor/models/nano_nemotron_vl.py`
  - `vllm/multimodal/audio.py`
  - `vllm/multimodal/media/audio.py`
  - `vllm/multimodal/parse.py`
  - `vllm/renderers/base.py`
  - `vllm/transformers_utils/processors/fireredasr2.py`

### Summary

**What changed and why**  
This PR removes `librosa` as an audio dependency to eliminate LGPL licensing concerns for downstream projects like `vllm-omni`. The changes replace `librosa` with `resampy` (BSD-3) and `pyav` (LGPL-compatible) for audio loading and resampling operations, while maintaining existing functionality.

**Technical impact**  
The audio processing pipeline now uses `resampy` as the default resampling method (replacing `librosa`), with `pyav` as an alternative for video/audio extraction. Audio loading now follows a fallback strategy: `soundfile` first, then `pyav` for unsupported formats. This changes the internal implementation but maintains the same public API for audio processing.

**Potential risks**  
The new `resample_audio_pyav` implementation has a minimum sample requirement (1024 samples) that could affect very short audio clips. The fallback logic in `load_audio` may have different error handling characteristics than the previous `librosa`-based approach. Test changes show slight differences in expected transcription outputs, indicating potential behavioral changes in audio processing.

**Key insights**  
Developers should update their dependencies to include `resampy` for audio tests. The default resampling method changed from `"librosa"` to `"resampy"`, and the audio parser now uses `"pyav"` instead of `"librosa"`. All audio-related code should be tested thoroughly as the underlying libraries have different implementation details that could affect edge cases.

---

## 7. [Add get_device_uuid for rocm](https://github.com/vllm-project/vllm/pull/37694)


### Base Information

- **PR Number:** #37694
- **Author:** [tmm77](https://github.com/tmm77)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-20 20:33:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37694/files) (1):**
  - `vllm/platforms/rocm.py`

### Summary

**What changed and why**  
Added a `get_device_uuid` method to the ROCm platform class to provide a unique identifier for AMD GPUs, similar to the existing CUDA implementation. This is required by the Verl application for PPO and Fully Async use cases when using the vLLM backend, ensuring consistent device identification across platforms.

**Technical impact**  
The change extends the ROCm platform interface to match the CUDA platform's capabilities, enabling Verl's device management code to work uniformly across different hardware backends. It integrates with the existing `amdsmi` library to query GPU UUIDs and maintains error handling consistent with other ROCm platform methods.

**Potential risks**  
If `amdsmi_get_gpu_device_uuid` fails, the method returns an uninitialized `device_uuid` variable, which could cause undefined behavior or crashes. Additionally, the error logging for UUID query failures does not return a fallback value, potentially leading to empty or invalid UUIDs being propagated.

**Key insights**  
Ensure the method returns a safe default (e.g., an empty string) in the second exception block to prevent uninitialized variable usage. Consider validating the `device_id` input range before querying handles to avoid index errors. This implementation aligns with cross-platform design but requires robust error handling to maintain system stability.

---

## 8. [elastic_ep: Fix issues with repeated scale up/down cycles](https://github.com/vllm-project/vllm/pull/37131)


### Base Information

- **PR Number:** #37131
- **Author:** [itayalroy](https://github.com/itayalroy)
- **Merged By:** [tlrmchlsmth](https://github.com/tlrmchlsmth)
- **Merged time:** 2026-03-20 16:13:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37131/files) (10):**
  - `vllm/distributed/device_communicators/cuda_communicator.py`
  - `vllm/distributed/device_communicators/pynccl.py`
  - `vllm/distributed/elastic_ep/elastic_execute.py`
  - `vllm/distributed/elastic_ep/elastic_state.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/executor/multiproc_executor.py`
  - `vllm/v1/executor/ray_executor.py`
  - `vllm/v1/executor/uniproc_executor.py`
  - `vllm/v1/worker/gpu_worker.py`
  - `vllm/v1/worker/worker_base.py`

### Summary

**What changed and why**  
This PR fixes two critical issues in Elastic EP's repeated scale up/down cycles: (1) PyNCCL communicators were never destroyed, causing CUDA OOM over time, and (2) EPLB suppression logic was scattered and buggy, leading to incorrect state after scale down. The changes centralize EPLB suppression management in `elastic_execute`, clean up the new-worker load path, and add explicit NCCL communicator destruction.

**Technical impact**  
The refactoring consolidates Elastic EP scaling logic, making the system more maintainable and predictable. Key improvements include moving new-worker initialization through a unified executor path, relocating weight‑transfer logic to the state machine, and ensuring proper cleanup of CUDA graphs and NCCL resources during reconfiguration. This reduces complexity and aligns scale‑up/down workflows.

**Potential risks**  
Introducing a new `PRE_KV_INIT` state and moving logic could affect state transition timing, especially if barriers or synchronization are misaligned. The explicit NCCL destruction assumes `destroy()` is safe to call repeatedly; if not, it may cause crashes. Additionally, the DBO (UBatchWrapper) path remains unsupported, which could limit compatibility.

**Key insights**  
Developers should note that EPLB suppression is now solely managed by `elastic_execute._set_eplb_suppressed()`, eliminating scattered state bugs. The new‑worker load path is significantly cleaner, but careful testing of scale‑up/down sequences is advised to ensure state transitions and memory cleanup work correctly. The NCCL destructor addition is critical for long‑running services.

---

## 9. [[BugFix] Allow qk_nope_head_dim=192 in FlashInfer MLA backend checks](https://github.com/vllm-project/vllm/pull/37475)


### Base Information

- **PR Number:** #37475
- **Author:** [kjiang249](https://github.com/kjiang249)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-20 15:14:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37475/files) (2):**
  - `vllm/v1/attention/backends/mla/flashinfer_mla.py`
  - `vllm/v1/attention/backends/mla/flashinfer_mla_sparse.py`

### Summary

**What changed and why**  
Updated FlashInfer MLA backend validation to support `qk_nope_head_dim=192` in addition to existing values (64, 128). This aligns vLLM with FlashInfer's recent support for GLM-5 models, enabling proper kernel selection and execution for these models.

**Technical impact**  
The changes expand model compatibility by allowing GLM-5 variants with 192-dimensional query/key heads to use FlashInfer MLA kernels. Both dense and sparse MLA backends now accept the new dimension, ensuring correct fallback behavior and preventing unnecessary rejection of supported models.

**Potential risks**  
If FlashInfer's underlying kernel does not fully support 192 dimensions on all GPU architectures or configurations, runtime errors or performance degradation could occur. Additionally, the sparse kernel now allows both 128 and 192, but sparse support for 192 may have different constraints (e.g., index_topk requirements) that aren't fully validated.

**Key insights**  
Always verify that backend kernel constraints match upstream library capabilities. Consider adding architecture-specific checks if FlashInfer's 192-dimension support has hardware limitations. Ensure test coverage includes edge cases for sparse attention with the new dimension.

---

## 10. [[Refactor] Remove unused dead code](https://github.com/vllm-project/vllm/pull/36171)


### Base Information

- **PR Number:** #36171
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-20 15:12:51
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36171/files) (3):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/moriio/moriio_connector.py`
  - `vllm/model_executor/layers/fused_moe/deepep_ll_prepare_finalize.py`
  - `vllm/model_executor/layers/fused_moe/utils.py`

### Summary

**What changed and why**  
Removed three unused private functions (`_is_last_layer`, `_map_local_to_global_ids`, and `_validate_scale_shape`) from the codebase. This is a straightforward dead code cleanup to reduce maintenance burden and improve code clarity.

**Technical impact**  
The changes are purely subtractive and remove internal helper methods that are not referenced elsewhere. This reduces the overall code footprint without affecting any functionality, as these methods were not being called.

**Potential risks**  
Low risk, as the functions were private and unused. However, there is a minor risk if these functions were intended for future use or debugging, but their private nature suggests they were internal implementation details.

**Key insights**  
This cleanup is a positive step for code hygiene. Developers should ensure no external or internal systems indirectly depended on these methods via reflection or dynamic calls. Future changes should continue to proactively identify and remove dead code.

---

## 11. [[Model Runner V2] Support Streaming Inputs](https://github.com/vllm-project/vllm/pull/37028)


### Base Information

- **PR Number:** #37028
- **Author:** [santiramos27](https://github.com/santiramos27)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-03-20 13:42:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37028/files) (7):**
  - `tests/v1/streaming_input/test_gpu_model_runner_v2_streaming.py`
  - `vllm/model_executor/models/whisper_causal.py`
  - `vllm/v1/worker/gpu/attn_utils.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/model_states/default.py`
  - `vllm/v1/worker/gpu/model_states/interface.py`
  - `vllm/v1/worker/gpu/states.py`

### Summary

**What changed and why**  
This PR fixes Model Runner V2 (MRv2) to properly handle streaming input updates for realtime/speech models. The main issue was that `add_requests()` didn't detect existing requests with the same `req_id` during streaming updates, causing slot leaks and eventual crashes. Additional fixes enable realtime models (Voxtral, Qwen3-ASR) by passing `cache_dtype` through KV cache initialization, initializing lazy CPU fields in attention metadata, and advertising realtime/transcription tasks.

**Technical impact**  
The changes ensure MRv2 correctly recycles request slots during streaming updates, preventing memory leaks and crashes in long-running sessions. KV cache initialization now respects `cache_dtype`, and attention metadata fields are properly materialized for Whisper's block-pooling builder. The task detection logic now matches V1 behavior, allowing realtime endpoints to register.

**Potential risks**  
The `_remove_request` method now returns a boolean; callers must handle the return value if they depend on removal success. The lazy CPU field checks in Whisper could mask deeper initialization issues if `_seq_lens_cpu` or `_num_computed_tokens_cpu` are unexpectedly `None`. Streaming updates assume the scheduler correctly folds intermediate tokens into `prefill_token_ids`.

**Key insights**  
Developers should ensure streaming updates always call `_remove_request` before re-adding to avoid state corruption. The `cache_dtype` propagation is critical for models with specialized KV cache layouts. The task support override in `DefaultModelState` is a template for other model states needing custom generation tasks.

---

## 12. [[ROCm][CI] Setting some mi325_4 tests back to optional (in parity with upstream)](https://github.com/vllm-project/vllm/pull/37711)


### Base Information

- **PR Number:** #37711
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 12:25:09
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37711/files) (1):**
  - `.buildkite/test-amd.yaml`

### Summary

**What changed and why**  
Seven test steps in the AMD CI configuration were marked as `optional: true` to reduce congestion on the `mi325_4` agent pool. This change aligns with upstream vLLM CI practices, where these test groups are already optional.

**Technical impact**  
These tests will no longer block CI pipeline progression if they fail or are skipped, potentially speeding up overall CI runtime and reducing resource contention. The change only affects test execution priority, not the tests themselves or their pass/fail reporting.

**Potential risks**  
Optional tests may be skipped more frequently, potentially allowing regressions to go unnoticed if failures are not monitored. There is also a risk of reduced test coverage over time if optional tests are consistently deprioritized.

**Key insights**  
This is a resource optimization change that mirrors upstream behavior. Developers should ensure optional test failures are still reviewed periodically and consider implementing alerts for repeated failures. The change helps balance CI efficiency with resource constraints.

---

## 13. [[Bugfix] Disable monolithic TRTLLM MoE for Renormalize routing (#37591)](https://github.com/vllm-project/vllm/pull/37605)


### Base Information

- **PR Number:** #37605
- **Author:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-20 12:19:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37605/files) (5):**
  - `.buildkite/test_areas/lm_eval.yaml`
  - `tests/evals/gsm8k/configs/Qwen3.5-35B-A3B-DEP2.yaml`
  - `tests/evals/gsm8k/configs/Qwen3.5-35B-A3B-FP8-DEP2.yaml`
  - `tests/evals/gsm8k/configs/models-qwen35-blackwell.txt`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`

### Summary

**What changed and why**  
This PR disables the monolithic TRTLLM MoE kernel (`trtllm_fp8_block_scale_moe`) for `Renormalize` and `RenormalizeNaive` routing methods due to a bug where all-negative router logits produce incorrect expert routing. The fix removes these routing methods from the monolithic kernel's supported list, forcing a fallback to the modular kernel (`TrtLlmFp8ExpertsModular`) which handles routing correctly in Python.

**Technical impact**  
The change affects kernel selection for FP8-quantized MoE models using `Renormalize` routing, shifting execution from a monolithic CUDA kernel to a modular approach with Python-based routing. This ensures correct softmax computation and expert assignment, restoring model accuracy (e.g., Qwen3.5-35B-A3B-FP8 GSM8K accuracy from 0% to 86%). The modular kernel may have different performance characteristics but maintains functional correctness.

**Potential risks**  
The modular kernel may introduce performance overhead due to Python-based routing and separate kernel launches. There is a risk of regression for other routing methods if the monolithic kernel's bug affects them similarly. Additionally, the fix is conditional on specific quantization schemes, so similar issues could arise if new schemes are added without validation.

**Key insights**  
Always validate kernel behavior for edge cases like all-negative logits. The monolithic kernel's internal routing logic has a correctness bug that the modular kernel avoids by offloading routing to Python. Developers should monitor performance when falling back to modular kernels and consider upstream fixes in the FlashInfer library. The added test configurations ensure ongoing validation for affected models.

---

## 14. [[compile] Initialize passes at VllmBackend init](https://github.com/vllm-project/vllm/pull/35216)


### Base Information

- **PR Number:** #35216
- **Author:** [angelayi](https://github.com/angelayi)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-03-20 11:40:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35216/files) (3):**
  - `tests/test_config.py`
  - `vllm/compilation/backends.py`
  - `vllm/compilation/decorators.py`

### Summary

**What changed and why**  
The changes move the initialization of post-grad passes (including `AllReduceFusionPass`) from `VllmBackend.__call__` to `VllmBackend.__init__`. This ensures expensive one-time setup operations, such as FlashInfer workspace allocation, are performed during backend initialization rather than during `torch.compile` execution, preventing them from being counted toward compilation latency. A related fix in `decorators.py` prevents duplicate initialization of compile wrappers.

**Technical impact**  
Compilation latency measurements will now more accurately reflect actual compilation time by excluding one-time setup costs. The backend's `__call__` method is simplified, and the pass configuration becomes a one-time initialization step. This change also affects test code, which no longer needs to manually call `configure_post_pass()`.

**Potential risks**  
If `VllmBackend` is instantiated but never called, the pass initialization cost is still incurred, which could be wasteful. There is a risk of duplicate initialization if the `hasattr` check in `decorators.py` fails to catch all edge cases. Additionally, any stateful dependencies in `configure_post_pass()` must be thread-safe if multiple backends are created concurrently.

**Key insights**  
This optimization improves the accuracy of compilation latency profiling and is a good practice for separating setup from execution. Developers should ensure that `configure_post_pass()` remains idempotent and lightweight where possible. The test update reflects the new initialization flow, and future changes should maintain this separation of concerns.

---

## 15. [[Model] Update Kimi-K25 and Isaac processors to fit HF-style](https://github.com/vllm-project/vllm/pull/37693)


### Base Information

- **PR Number:** #37693
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 11:30:22
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37693/files) (5):**
  - `vllm/model_executor/models/isaac.py`
  - `vllm/model_executor/models/kimi_k25.py`
  - `vllm/transformers_utils/processors/isaac.py`
  - `vllm/transformers_utils/processors/kimi_k25.py`
  - `vllm/transformers_utils/processors/step3_vl.py`

### Summary



---

## 16. [[Bugfix] Disable --calculate-kv-scales for hybrid GDN/Mamba+Attention…](https://github.com/vllm-project/vllm/pull/37565)


### Base Information

- **PR Number:** #37565
- **Author:** [Young-Leo](https://github.com/Young-Leo)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-20 11:28:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37565/files) (1):**
  - `vllm/model_executor/models/config.py`

### Summary

**What changed and why**  
Added a defensive guard in `HybridAttentionMambaModelConfig.verify_and_update_config()` to disable `--calculate-kv-scales` for hybrid models (e.g., Qwen3.5). This prevents corrupted FP8 KV cache scales caused by uninitialized recurrent state (GDN/Mamba/SSM) during calibration passes, which was leading to severe inference corruption.

**Technical impact**  
The change automatically disables `calculate_kv_scales` for any registered hybrid model, forcing use of the default scale (1.0). This ensures FP8 KV caching works correctly without silent scale corruption, while maintaining backward compatibility for non-hybrid models.

**Potential risks**  
Hybrid models now permanently lose KV scale calibration, potentially affecting quantization accuracy for FP8 caches. The warning may be missed in logs, and the fix assumes default scale 1.0 is always safe—which should be validated across diverse hybrid architectures.

**Key insights**  
Since `--calculate-kv-scales` is deprecated (slated for removal in v0.19), this is a stopgap safety measure. Developers should verify that recurrent layers in hybrid models initialize state properly if scale calibration is ever reintroduced. Consider adding a runtime check for recurrent state initialization in calibration passes for future designs.

---

## 17. [refactor: abstract deepgemm support into platform](https://github.com/vllm-project/vllm/pull/37519)


### Base Information

- **PR Number:** #37519
- **Author:** [SherryC41](https://github.com/SherryC41)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-03-20 10:54:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37519/files) (3):**
  - `vllm/platforms/cuda.py`
  - `vllm/platforms/interface.py`
  - `vllm/utils/deep_gemm.py`

### Summary

**What changed and why**  
This PR abstracts DeepGEMM hardware support checking into the platform interface. A new `support_deep_gemm()` method was added to both the platform interface and the CUDA platform implementation, replacing direct capability checks in `deep_gemm.py`. This simplifies adding DeepGEMM support for other hardware backends that use plugins.

**Technical impact**  
The change centralizes DeepGEMM capability detection within the platform abstraction layer, making the code more modular and maintainable. The `is_deep_gemm_supported()` function now delegates to the platform-specific implementation, reducing duplication and aligning with the existing platform pattern used for other hardware features.

**Potential risks**  
If the platform interface method is not correctly implemented for a new backend, DeepGEMM support may be incorrectly reported. The current CUDA implementation only checks for capability 90 (Hopper) or family 100 (Blackwell), which could miss future architectures unless the method is updated accordingly.

**Key insights**  
This refactor follows good abstraction principles by moving hardware-specific logic into the platform layer. Developers adding new platforms should ensure they implement `support_deep_gemm()` appropriately. The change is minimal and focused, but future work should include tests to validate the method across different hardware configurations.

---

## 18. [[Attention] Support distinguishing between short extends and decodes](https://github.com/vllm-project/vllm/pull/37303)


### Base Information

- **PR Number:** #37303
- **Author:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged By:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged time:** 2026-03-20 10:49:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37303/files) (9):**
  - `.buildkite/test_areas/engine.yaml`
  - `tests/v1/attention/test_batch_reordering.py`
  - `tests/v1/e2e/test_hybrid_chunked_prefill.py`
  - `vllm/v1/attention/backend.py`
  - `vllm/v1/attention/backends/mamba_attn.py`
  - `vllm/v1/attention/backends/utils.py`
  - `vllm/v1/worker/gpu_input_batch.py`
  - `vllm/v1/worker/gpu_model_runner.py`
  - `vllm/v1/worker/mamba_utils.py`

### Summary

**What changed and why**  
This PR enhances batch reordering logic to distinguish between short extends (chunked prefills with few tokens) and actual decodes. Previously, requests with `num_scheduled <= threshold` were all treated as decodes, which could misclassify short prefills. The new logic introduces four categories: decode, short_extend, long_extend, and prefill, ensuring proper handling in attention backends like Mamba.

**Technical impact**  
The batch order is now structured as decode → short_extend → long_extend → prefill, enabling backends to differentiate short extends from decodes via the new `is_prefilling` flag in `CommonAttentionMetadata`. This improves accuracy for architectures requiring distinct processing paths. The Mamba backend now treats short extends as prefills (`treat_short_extends_as_decodes=False`), while other backends default to the original behavior.

**Potential risks**  
If `is_prefilling` is not properly populated or backends misinterpret the flag, short extends could be incorrectly routed, leading to performance degradation or errors. The removal of `update_accepted_tokens_for_prefill_as_decode` logic may affect speculative decoding edge cases if not fully validated. Increased test complexity with four-way categorization could mask subtle ordering bugs.

**Key insights**  
Developers must ensure `num_prompt_tokens` is accurately tracked in `GPUInputBatch` to compute `is_prefilling`. Backends using `split_decodes_and_prefills` should review the `treat_short_extends_as_decodes` parameter to align with architectural needs. The PR simplifies Mamba-specific handling by leveraging the new categorization, but thorough testing is advised for mixed workloads with speculative decoding.

---

## 19. [fix CUDAGraph memory being counted twice](https://github.com/vllm-project/vllm/pull/37426)


### Base Information

- **PR Number:** #37426
- **Author:** [panpan0000](https://github.com/panpan0000)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-03-20 10:36:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37426/files) (1):**
  - `vllm/v1/worker/gpu_worker.py`

### Summary

**What changed and why**  
The fix addresses double counting of CUDAGraph memory in KV cache recommendations. When `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=1`, the estimated CUDAGraph memory was being added to `self.peak_activation_memory` during profiling, then later added again as `self.cudagraph_memory_estimate` when calculating `non_kv_cache_memory`, causing an overestimation.

**Technical impact**  
This correction ensures accurate calculation of available memory for KV cache sizing by preventing duplicate accounting. The change only affects memory profiling and recommendation logic—specifically leading to more appropriate KV cache upper bounds—without impacting actual runtime memory allocation or causing OOMs.

**Potential risks**  
The primary risk is that if the CUDAGraph memory estimate was intentionally meant to be included in `peak_activation_memory` for other purposes, removing it could affect other memory calculations. Additionally, developers must ensure the `cudagraph_memory_estimate` field is properly utilized elsewhere to avoid undercounting in different code paths.

**Key insights**  
Always verify that memory profiling variables aren't redundantly summed across different stages. When working with estimated and actual memory values, clearly separate their usage to prevent double counting. Consider adding a comment in the code to clarify that `peak_activation_memory` should exclude CUDAGraph estimates when they are handled separately.

---

## 20. [[MRV2] Avoid recompilation of _gather_block_tables_kernel](https://github.com/vllm-project/vllm/pull/37645)


### Base Information

- **PR Number:** #37645
- **Author:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-20 10:31:46
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37645/files) (1):**
  - `vllm/v1/worker/gpu/block_table.py`

### Summary

**What changed and why**  
The change adds a `do_not_specialize` parameter to the Triton JIT decorator for the `_gather_block_tables_kernel` function, specifically excluding the `num_reqs` argument from specialization. This prevents Triton from compiling separate kernel versions for different values of `num_reqs`, addressing the issue where the kernel was being compiled twice (once for `num_reqs=1` and once for other values).

**Technical impact**  
This modification reduces runtime compilation overhead by ensuring only one kernel version is compiled, regardless of the `num_reqs` value. It maintains the kernel's functional behavior while improving performance in scenarios where the kernel is called with varying `num_reqs`, such as during dynamic batching or request processing.

**Potential risks**  
If the kernel's performance is highly sensitive to the `num_reqs` value, disabling specialization could lead to suboptimal execution for specific cases (e.g., `num_reqs=1`). Additionally, any implicit assumptions in the kernel logic that depend on `num_reqs` specialization may need validation to ensure correctness across all inputs.

**Key insights**  
This is a targeted optimization that trades potential specialized performance gains for reduced compilation latency. Developers should verify that the kernel remains efficient for all expected `num_reqs` values and consider profiling to confirm no regressions. Similar `do_not_specialize` usage could be applied to other Triton kernels with parameters that vary frequently but don't benefit from specialization.

---

## 21. [[Perf] Eliminate redundant SparseMatrix creation in gpt_oss_triton_kernels](https://github.com/vllm-project/vllm/pull/37683)


### Base Information

- **PR Number:** #37683
- **Author:** [xyang16](https://github.com/xyang16)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-20 10:28:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37683/files) (2):**
  - `tests/kernels/moe/test_gpt_oss_triton_kernels.py`
  - `vllm/model_executor/layers/fused_moe/gpt_oss_triton_kernels_moe.py`

### Summary

**What changed and why**  
The PR eliminates redundant SparseMatrix creation in MoE routing by modifying `legacy_routing` to reuse the `SparseMatrix` object returned by `topk`. Previously, `topk` created a SparseMatrix internally, and then `legacy_routing_from_bitmatrix` reconstructed another SparseMatrix from its components, causing duplicate kernel launches for metadata computation. A new function `legacy_routing_from_sparsematrix` is introduced to process the existing SparseMatrix directly.

**Technical impact**  
This change reduces GPU kernel launches by avoiding duplicate executions of `_sum_bitmatrix_rows`, `_bitmatrix_metadata_compute_stage1`, and `_bitmatrix_metadata_compute_stage2`. It improves end-to-end throughput by ~4% in batch size 1 benchmarks on H200, with no functional regression. The existing `legacy_routing_from_bitmatrix` remains untouched for other callers, preserving backward compatibility.

**Potential risks**  
The refactoring assumes the `SparseMatrix` structure from `triton_kernels.topk` is stable and matches the expected format for routing data extraction. Any future changes to Triton's `SparseMatrix` internals could break the new function. Additionally, the new function relies on internal attributes like `mask_metadata.row_sorted_indx`, which may not be guaranteed as public API.

**Key insights**  
The optimization successfully removes a performance bottleneck in MoE routing with minimal code changes. Developers should ensure the `SparseMatrix` interface from Triton remains consistent across versions. The added unit tests validate correctness but should be expanded to cover edge cases like varying tensor shapes and expert counts. Consider adding a comment linking to the Triton source for future maintainers.

---

## 22. [[ROCm][CI] Fix accuracy for llama-nemotron-vl pooling tests](https://github.com/vllm-project/vllm/pull/37613)


### Base Information

- **PR Number:** #37613
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 10:16:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37613/files) (1):**
  - `tests/models/multimodal/pooling/test_llama_nemotron_vl.py`

### Summary

**What changed and why**  
This PR addresses accuracy differences in llama-nemotron-vl pooling tests on ROCm platforms by adjusting the relative tolerance for score comparisons and adding ROCm-specific engine configurations. The changes were prompted by observed numerical variance between Hugging Face and vLLM attention backends on ROCm hardware.

**Technical impact**  
The modifications conditionally apply a slightly looser tolerance threshold (0.022 vs 0.02) when running on ROCm platforms, while maintaining the original tolerance for other platforms. Additionally, ROCm-specific engine arguments are now passed to the vLLM model initialization in both embedding and reranker test functions.

**Potential risks**  
The increased tolerance threshold could potentially mask genuine accuracy regressions on ROCm platforms. There's also a risk that the ROCm-specific engine arguments might not be appropriate for all test scenarios or could interfere with other platform-specific configurations.

**Key insights**  
The changes demonstrate platform-aware testing by conditionally adjusting tolerance based on the current platform. Developers should monitor whether this tolerance adjustment is a temporary workaround or reflects inherent numerical differences between backends. Consider documenting the rationale for the specific tolerance values chosen.

---

## 23. [Fix various config related issues for Transformers v5](https://github.com/vllm-project/vllm/pull/37681)


### Base Information

- **PR Number:** #37681
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 09:30:13
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37681/files) (8):**
  - `tests/entrypoints/openai/chat_completion/test_completion_with_function_calling.py`
  - `vllm/transformers_utils/configs/colpali.py`
  - `vllm/transformers_utils/configs/deepseek_vl2.py`
  - `vllm/transformers_utils/configs/mistral.py`
  - `vllm/transformers_utils/configs/parakeet.py`
  - `vllm/transformers_utils/configs/qwen3_asr.py`
  - `vllm/transformers_utils/configs/speculators/base.py`
  - `vllm/transformers_utils/model_arch_config_convertor.py`

### Summary

**What changed and why**  
This PR fixes various configuration-related issues for Transformers v5 compatibility. The changes address problems with config field inheritance, initialization order, attribute access, and parameter validation across multiple model configurations. These fixes ensure proper config handling during model loading and initialization.

**Technical impact**  
The changes improve config robustness by fixing initialization order issues (Qwen3ASRConfig), removing invalid fields (ColPaliConfig, DeepSeekVL2Config), handling null values properly (mistral config), and adapting to Transformers v5's dataclass-based config system (SpeculatorsConfig). The attribute access fix prevents errors when checking optional config fields.

**Potential risks**  
The SpeculatorsConfig change introduces version detection logic that could break if Transformers' internal implementation changes. Removing fields like `projection_dim` from ColPaliConfig might affect downstream code expecting those attributes. The mistral fix assumes `max_source_positions` is only problematic when explicitly null, but other invalid values could still cause issues.

**Key insights**  
Always use `getattr()` with default values when accessing optional config attributes to avoid AttributeErrors. Ensure config initialization follows proper order: set subclass-specific attributes before calling `super().__init__()`. When adapting to Transformers v5, be aware that `PretrainedConfig` now validates kwargs against dataclass fields. Test configs with both null and missing values to ensure robustness.

---

## 24. [[compile] Add compiled artifact counter for VLLM_USE_MEGA_AOT_ARTIFACT=1.](https://github.com/vllm-project/vllm/pull/37589)


### Base Information

- **PR Number:** #37589
- **Author:** [zhxchen17](https://github.com/zhxchen17)
- **Merged By:** [zou3519](https://github.com/zou3519)
- **Merged time:** 2026-03-20 09:22:46
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37589/files) (2):**
  - `tests/compile/test_startup.py`
  - `vllm/compilation/caching.py`

### Summary



---

## 25. [[Metrics] Some small refactoring for better maintainability](https://github.com/vllm-project/vllm/pull/33898)


### Base Information

- **PR Number:** #33898
- **Author:** [hickeyma](https://github.com/hickeyma)
- **Merged By:** [markmc](https://github.com/markmc)
- **Merged time:** 2026-03-20 09:11:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/33898/files) (7):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/metrics.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl_connector.py`
  - `vllm/v1/metrics/loggers.py`
  - `vllm/v1/metrics/perf.py`
  - `vllm/v1/metrics/ray_wrappers.py`
  - `vllm/v1/metrics/utils.py`
  - `vllm/v1/spec_decode/metrics.py`

### Summary

**What changed and why**  
This PR refactors metrics-related code to improve maintainability and consistency. The changes include: renaming `KVConnectorPrometheus` to `KVConnectorProm` for naming consistency, consolidating duplicate `make_per_engine` helper functions into a single `create_metric_per_engine` function in a new `utils.py` module, and updating all callers to use this centralized function.

**Technical impact**  
The refactoring reduces code duplication by centralizing the `create_metric_per_engine` function, which creates labeled metric instances for each engine. This improves maintainability as future changes to per-engine metric creation only need to be made in one location. The naming change (`Prometheus` → `Prom`) aligns with existing conventions in the codebase.

**Potential risks**  
The main risk is that the consolidated function might not handle all edge cases previously covered by individual implementations, though the function signature appears identical. There's also a risk of import errors since the new `utils.py` module must be properly imported across multiple files. The changes affect metric collection across distributed components, so any bugs could impact monitoring visibility.

**Key insights**  
This is a well-executed refactoring that follows DRY principles and improves code organization. Developers should ensure all metric-related helper functions are now centralized in `vllm/v1/metrics/utils.py`. When adding new per-engine metrics, always use `create_metric_per_engine` instead of creating custom implementations. The naming convention change (`Prom`) should be applied consistently if similar classes exist elsewhere.

---

## 26. [[compile] Fix aot test failures with torch 2.12.](https://github.com/vllm-project/vllm/pull/37604)


### Base Information

- **PR Number:** #37604
- **Author:** [zhxchen17](https://github.com/zhxchen17)
- **Merged By:** [zou3519](https://github.com/zou3519)
- **Merged time:** 2026-03-20 09:06:30
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37604/files) (2):**
  - `tests/compile/test_aot_compile.py`
  - `vllm/compilation/caching.py`

### Summary

**What changed and why**  
The PR fixes AOT test failures with PyTorch 2.12 by refactoring serialization/deserialization logic. It extracts graph module serialization into separate methods (`serialize_graph_module`/`deserialize_graph_module`) and updates test handling for slice operations and mega artifact configurations.

**Technical impact**  
These changes improve code modularity by separating graph module serialization from broader artifact serialization. The test updates ensure compatibility with PyTorch 2.12's serialization behavior and properly skip tests when using mega artifacts (which bypass AOT Autograd).

**Potential risks**  
The refactoring could introduce subtle serialization inconsistencies if the new graph module methods don't fully replicate previous behavior. The conditional test skipping might mask issues when `VLLM_USE_MEGA_AOT_ARTIFACT` is enabled, requiring careful validation in both configurations.

**Key insights**  
Developers should verify that the extracted serialization methods maintain backward compatibility with existing cached artifacts. The test changes highlight the need to handle PyTorch version-specific serialization details, particularly for slice operations and fake tensor modes.

---

## 27. [[Pixtral] Enable Pixtral language model support Eagle3](https://github.com/vllm-project/vllm/pull/37182)


### Base Information

- **PR Number:** #37182
- **Author:** [Flechman](https://github.com/Flechman)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 08:50:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37182/files) (1):**
  - `vllm/model_executor/models/pixtral.py`

### Summary

**What changed and why**  
The changes enable Pixtral language model support for EAGLE-3 speculative decoding by implementing the `SupportsEagle3` interface. This allows Pixtral to use an Eagle3 draft model when the underlying language model returns multiple auxiliary hidden states.

**Technical impact**  
Pixtral now inherits from `SupportsEagle3` and implements its required methods (`set_aux_hidden_state_layers` and `get_eagle3_aux_hidden_state_layers`). These methods delegate to the language model after verifying it supports Eagle3 via `supports_eagle3()`, ensuring compatibility with speculative decoding workflows.

**Potential risks**  
If the underlying language model doesn't support Eagle3, calls to the new methods will raise a `RuntimeError`. There's also a risk of incomplete integration if other components (like the draft model) aren't properly configured to work with Pixtral's auxiliary hidden states.

**Key insights**  
The implementation correctly delegates Eagle3 functionality to the language model, maintaining separation of concerns. Developers should ensure the language model used with Pixtral actually supports Eagle3 before enabling speculative decoding. The error messages are clear and will help debug compatibility issues.

---

## 28. [[Bugfix] Fix ConchLinearKernel channelwise quantization (group_size=-1)](https://github.com/vllm-project/vllm/pull/37329)


### Base Information

- **PR Number:** #37329
- **Author:** [mgehre-amd](https://github.com/mgehre-amd)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-20 08:32:21
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37329/files) (1):**
  - `vllm/model_executor/kernels/linear/mixed_precision/conch.py`

### Summary

**What changed and why**  
The fix addresses a bug in channelwise quantization where `group_size=-1` was incorrectly passed to the Triton kernel. This caused negative stride calculations, leading to out-of-bounds memory accesses and silently corrupted outputs. The solution maps `group_size=-1` to the actual input dimension `K` before kernel invocation, ensuring proper scale value indexing.

**Technical impact**  
This change ensures correct behavior for per-channel quantized models (e.g., W8A16, W4A16) by aligning the kernel's stride logic with the intended single scale per channel. It maintains the existing API while fixing silent data corruption, enabling reliable inference for channelwise-quantized linear layers.

**Potential risks**  
If other parts of the codebase rely on `group_size=-1` as a special sentinel value, this mapping could introduce unintended side effects. Additionally, the fix assumes `x.shape[-1]` always represents the channel dimension `K`, which may not hold for all tensor layouts or future modifications.

**Key insights**  
Always validate sentinel values before passing them to low-level kernels, especially when they affect memory access patterns. Consider adding assertions or documentation for `group_size` semantics to prevent similar issues. Testing with diverse quantized models is crucial, as silent corruption can be difficult to detect.

---

## 29. [[Bugfix] Reject channelwise quantization (group_size <= 0) in ExllamaLinearKernel](https://github.com/vllm-project/vllm/pull/37331)


### Base Information

- **PR Number:** #37331
- **Author:** [mgehre-amd](https://github.com/mgehre-amd)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-20 08:31:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37331/files) (1):**
  - `vllm/model_executor/kernels/linear/mixed_precision/exllama.py`

### Summary

**What changed and why**  
Added an explicit guard in `ExllamaLinearKernel.can_implement` to reject channelwise quantization (where `group_size <= 0`). Previously, the modulo check `c.full_weight_shape[0] % c.group_size != 0` incorrectly passed for `group_size=-1` due to Python's modulo semantics, leading to a runtime crash when negative dimensions were computed.

**Technical impact**  
This change ensures Exllama kernels properly report unsupported configurations during kernel selection, allowing the system to fall back to compatible kernels like `ConchLinearKernel`. It prevents silent failures and runtime tensor allocation errors, improving robustness in mixed-precision quantization scenarios.

**Potential risks**  
If other kernels have similar missing guards, channelwise quantization might still cause issues elsewhere. The fix assumes `group_size <= 0` exclusively indicates channelwise quantization; future quantization schemes with non-positive group sizes could be incorrectly rejected.

**Key insights**  
Always validate input constraints explicitly rather than relying on downstream arithmetic checks. Kernel compatibility methods should fail fast with clear error messages to aid debugging and enable proper fallback mechanisms. Consider auditing other kernels for analogous edge cases with group_size validation.

---

## 30. [[ROCm] Enable wvSplitK skinny GEMM kernel for RDNA4/gfx1x decode](https://github.com/vllm-project/vllm/pull/34709)


### Base Information

- **PR Number:** #34709
- **Author:** [laudney](https://github.com/laudney)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-20 08:11:24
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34709/files) (4):**
  - `csrc/rocm/skinny_gemms.cu`
  - `tests/kernels/quantization/test_rocm_skinny_gemms.py`
  - `tests/model_executor/layers/test_rocm_unquantized_gemm.py`
  - `vllm/model_executor/layers/utils.py`

### Summary

**What changed and why**  
This PR enables the `wvSplitK` and `wvSplitKQ` skinny GEMM kernels on RDNA4/gfx1x hardware for decode-phase GEMMs (M=1..4). Previously these kernels were only available on gfx9 (MI-series), causing RDNA to fall back to slower generic implementations. The changes add compile-time and runtime detection for RDNA architectures, adapt kernel parameters for wave32 execution, and implement architecture-specific instructions (e.g., DPP reductions and dot product operations).

**Technical impact**  
The modifications extend the existing skinny GEMM framework to support RDNA4/gfx1x hardware, improving decode performance by ~15% on gfx1201. The code maintains backward compatibility with gfx9 through conditional compilation guards and runtime dispatch. Kernel launch parameters are now parameterized to handle both wave64 (gfx9) and wave32 (RDNA) execution models, while FP8 support is specifically enabled for gfx12 via built-in dot product intrinsics.

**Potential risks**  
Conditional compilation based on `__HIP__GFX9__` and `__HIP__GFX1X__` macros could lead to maintenance complexity if not carefully synchronized. The DPP reduction replacement using `__shfl_xor` for wave32 may have different performance characteristics or correctness edge cases. There is a risk of regression on gfx9 hardware if the guards or runtime detection fail, though the test plan includes verification.

**Key insights**  
Developers should ensure that any future architecture additions follow the same pattern of separate compile-time macros and runtime detection. The parameterized launch configuration (THRDS, WvPrGrp) is critical for supporting different wave sizes. Testing should prioritize cross-architecture validation to confirm that both gfx9 and RDNA paths behave identically for supported inputs.

---

## 31. [[ROCm][Quantization] make quark ocp mx dtype parser robust for weight-only quantization](https://github.com/vllm-project/vllm/pull/36232)


### Base Information

- **PR Number:** #36232
- **Author:** [xuebwang-amd](https://github.com/xuebwang-amd)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-20 08:10:03
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36232/files) (2):**
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`
  - `vllm/model_executor/layers/quantization/quark/schemes/quark_ocp_mx.py`

### Summary

**What changed and why**  
This PR enhances robustness for weight-only quantization in Quark's OCP MX scheme by handling cases where `input_quant_spec` is `None`. The changes prevent `NoneType` errors by adding null checks and adjusting the initialization logic to support weight-only quantization configurations.

**Technical impact**  
The modifications allow the quantization scheme to properly function without input quantization specifications, enabling weight-only quantization workflows. The code now safely handles `input_quant_spec` being optional, defaults `input_dtype` to `None`, and provides a pass-through function for input tensors when no input quantization is applied.

**Potential risks**  
If `input_quant_spec` is `None` but the code path expects a valid dictionary elsewhere, runtime errors could still occur. The `static_input_scales` logic defaults to `False` for `None` input specs, which might conflict with other assumptions in the system. The lambda pass-through function may introduce minor overhead or debugging complexity.

**Key insights**  
Always validate optional parameters before accessing their attributes. The pattern of using conditional initialization for `input_dtype` and `quant_dequant_func` is a good defensive programming practice. Developers should ensure all code paths that interact with `QuarkOCP_MX` are updated to handle the optional `input_quant_spec` parameter correctly.

---

## 32. [[Model] Add LFM2-ColBERT-350M support](https://github.com/vllm-project/vllm/pull/37528)


### Base Information

- **PR Number:** #37528
- **Author:** [ieBoytsov](https://github.com/ieBoytsov)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-20 07:57:58
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37528/files) (6):**
  - `docs/models/pooling_models/specific_models.md`
  - `docs/models/pooling_models/token_embed.md`
  - `tests/models/language/pooling/test_colbert.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/colbert.py`
  - `vllm/model_executor/models/registry.py`

### Summary

**What changed and why**  
This PR adds support for the LFM2-ColBERT-350M model by implementing a new `ColBERTLfm2Model` class. The changes integrate this model into vLLM's ColBERT framework, enabling its use as a cross-lingual retriever. Documentation and test updates ensure the model is properly registered and usable.

**Technical impact**  
The implementation extends the existing ColBERT architecture by adding a new backbone model type (LFM2). It correctly handles the hybrid nature of LFM2 (attention + SSM layers) by inheriting from `HasInnerState` and `IsHybrid` interfaces, ensuring proper cache initialization. The model leverages existing ColBERT late interaction capabilities while integrating with LFM2's specific weight loading and state management.

**Potential risks**  
The weight loading logic strips "model." prefixes from weight names, which could cause issues if other parts of the system expect different naming conventions. The auto-loading of sentence-transformers projector weights (from `1_Dense/`) introduces implicit dependencies on external file structures. The hybrid nature of LFM2 requires careful handling of mamba states and cache configurations.

**Key insights**  
This is a well-structured addition that follows existing patterns for ColBERT model integration. Developers should note that this model requires `--trust-remote-code` flag due to its custom architecture. The implementation properly handles the projection layer auto-loading from sentence-transformers format, which is a common pattern for ColBERT models.

---

## 33. [Fix attribute error in `isaac_patch_hf_runner`](https://github.com/vllm-project/vllm/pull/37685)


### Base Information

- **PR Number:** #37685
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 07:49:40
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37685/files) (1):**
  - `tests/models/multimodal/generation/vlm_utils/model_utils.py`

### Summary

**What changed and why**  
The PR fixes an `AttributeError` in `isaac_patch_hf_runner` by replacing a call to `self._update_causal_mask` with `create_causal_mask` from `transformers.masking_utils`. This change aligns with an upstream update in the HuggingFace model repository where `_update_causal_mask` was removed.

**Technical impact**  
The patch updates the attention mask preparation logic to use a standardized utility function, decoupling it from the model's internal method. This ensures compatibility with the latest model version and maintains the intended causal masking behavior for generation tasks.

**Potential risks**  
While this resolves the `AttributeError`, the PR notes that other changes in the upstream model may still cause a `RuntimeError` due to missing weights. Additionally, the new function's parameter signature and behavior must match the original to avoid subtle masking issues, especially with variable sequence lengths or caching.

**Key insights**  
Developers should verify that `create_causal_mask` produces identical results to the previous method under all inference scenarios. The fix is a direct adaptation of an upstream change, so monitoring future HuggingFace updates for similar breaking changes is recommended.

---

## 34. [[UX] Enable torch_profiler_with_stack](https://github.com/vllm-project/vllm/pull/37571)


### Base Information

- **PR Number:** #37571
- **Author:** [jeejeelee](https://github.com/jeejeelee)
- **Merged By:** [youkaichao](https://github.com/youkaichao)
- **Merged time:** 2026-03-20 04:17:27
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37571/files) (2):**
  - `docs/contributing/profiling.md`
  - `vllm/config/profiler.py`

### Summary

**What changed and why**  
The PR changes the default value of `torch_profiler_with_stack` from `False` to `True` in the profiler configuration. This enables stack tracing by default when using the PyTorch Profiler, making it more useful for debugging. The documentation is also updated to clarify when to use different profilers.

**Technical impact**  
Profiling sessions will now automatically include stack traces, providing richer debugging information (e.g., call stacks, memory usage, tensor shapes) without requiring manual configuration. This aligns with the intent to make profiling more informative for developers, though it may increase overhead compared to lightweight profilers like Nsight Systems.

**Potential risks**  
Enabling stack tracing by default increases profiling overhead, which could affect performance measurements and benchmarking accuracy. Users who rely on profiling for performance tuning may inadvertently capture more data than needed, slowing down inference. The change also assumes that debugging utility outweighs the overhead cost for most profiling use cases.

**Key insights**  
Developers should be aware that profiling now includes stack traces by default, which is beneficial for debugging but not ideal for pure performance benchmarking. To minimize overhead, use `--profiler-config.torch_profiler_with_stack=false` when profiling for performance. The updated documentation clearly distinguishes between profilers, helping users choose the right tool for their needs.

---

## 35. [[Misc] Use logger.info_once for auto tool choice log message](https://github.com/vllm-project/vllm/pull/37661)


### Base Information

- **PR Number:** #37661
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 03:40:36
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37661/files) (1):**
  - `vllm/parser/parser_manager.py`

### Summary

**What changed and why**  
Changed `logger.info()` to `logger.info_once()` for the auto tool choice log message to prevent repeated logging of the same message. This reduces log verbosity while still informing users that auto tool choice is enabled.

**Technical impact**  
The change ensures the log message appears only once per session/process instead of potentially multiple times, improving log clarity and reducing noise. It leverages a once-logging mechanism that suppresses duplicate messages.

**Potential risks**  
If the logger's `info_once` implementation has issues (e.g., incorrect deduplication), the message might not appear when needed or could still repeat. Additionally, developers might miss the log if it's buried among other once-logged messages.

**Key insights**  
Use `logger.info_once()` for infrequent, non-critical status messages to avoid log spam. Ensure the logging library's once-logging behavior is well-understood and tested. Consider if this message should remain visible in debug modes for troubleshooting.

---

## 36. [[Refactor] Relocate tests from tests/v1/entrypoints/ to tests/entrypoints/](https://github.com/vllm-project/vllm/pull/37500)


### Base Information

- **PR Number:** #37500
- **Author:** [sfeng33](https://github.com/sfeng33)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 02:50:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37500/files) (16):**
  - `.buildkite/scripts/hardware_ci/run-tpu-v1-test-part2.sh`
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/distributed.yaml`
  - `.buildkite/test_areas/entrypoints.yaml`
  - `.buildkite/test_areas/model_runner_v2.yaml`
  - `.github/CODEOWNERS`
  - `.github/mergify.yml`
  - `tests/conftest.py`
  - `tests/entrypoints/llm/test_struct_output_generate.py`
  - `tests/entrypoints/openai/chat_completion/test_chat_completion.py`
  - `tests/entrypoints/openai/chat_completion/test_completion_with_image_embeds.py`
  - `tests/entrypoints/openai/completion/test_completion.py`
  - `tests/entrypoints/openai/test_multi_api_servers.py`
  - `tests/v1/entrypoints/__init__.py`
  - `tests/v1/entrypoints/conftest.py`
  - `tests/v1/entrypoints/llm/__init__.py`

### Summary

**What changed and why**  
This PR consolidates test files by moving all entrypoint tests from `tests/v1/entrypoints/` to `tests/entrypoints/`. The purpose is to centralize entrypoint tests into a single folder structure, eliminating the version-specific subdirectory. This includes relocating test files to appropriate subdirectories (e.g., `llm/`, `openai/chat_completion/`) and updating all CI configurations and ownership files to reflect the new paths.

**Technical impact**  
The changes simplify the test directory hierarchy by removing the `v1/` layer for entrypoint tests, making the test organization more intuitive. CI pipelines are updated to point to the new locations, and duplicate test runs are avoided by adding explicit ignore rules for `test_multi_api_servers.py`. The `sample_json_schema` fixture is moved from a dedicated conftest file into `tests/conftest.py` and the test file itself, reducing dependency on version-specific fixtures.

**Potential risks**  
There is a risk of missing updates to any hardcoded references in other scripts or documentation not covered in this PR. The inline duplication of fixture data (e.g., `SAMPLE_JSON_SCHEMA`) could lead to maintenance overhead if the schema needs to be updated in multiple places. Additionally, the removal of the `v1/entrypoints` directory might break any external tooling or scripts that rely on the old paths.

**Key insights**  
Developers should verify that all CI jobs referencing the old paths have been updated, particularly in non-AMD configurations. The consolidation improves test discoverability, but care must be taken to keep shared test data (like JSON schemas) consistent. Future test additions should follow the new structure, and any remaining references to `tests/v1/entrypoints` should be cleaned up.

---

## 37. [[Refactor] Move serve entrypoint tests under tests/entrypoints/serve/](https://github.com/vllm-project/vllm/pull/37595)


### Base Information

- **PR Number:** #37595
- **Author:** [sfeng33](https://github.com/sfeng33)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 02:10:48
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37595/files) (11):**
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/entrypoints.yaml`
  - `tests/entrypoints/serve/instrumentator/__init__.py`
  - `tests/entrypoints/serve/instrumentator/test_basic.py`
  - `tests/entrypoints/serve/instrumentator/test_metrics.py`
  - `tests/entrypoints/serve/instrumentator/test_optional_middleware.py`
  - `tests/entrypoints/serve/instrumentator/test_orca_metrics.py`
  - `tests/entrypoints/serve/instrumentator/test_sleep.py`
  - `tests/entrypoints/serve/render/__init__.py`
  - `tests/entrypoints/serve/render/test_render.py`
  - `tests/entrypoints/serve/render/test_render_multimodal.py`

### Summary

**What changed and why**  
This PR reorganizes test files by moving `instrumentator` and `openai/cpu` test directories under a new `tests/entrypoints/serve/` subdirectory. The changes update CI configuration files (Buildkite pipelines) to reflect the new paths and fix import statements in the moved test files to use absolute imports (`from tests.utils` instead of relative imports).

**Technical impact**  
The restructuring creates a clearer logical grouping for serve-related entrypoint tests, improving maintainability. CI pipelines are updated to ensure tests continue to run in the correct locations, and import corrections prevent potential module resolution issues after the move.

**Potential risks**  
If any CI configurations or test runners outside the modified files still reference the old paths, tests could be missed or fail. The import changes assume `tests.utils` is accessible from the new location, which should be validated to avoid runtime import errors.

**Key insights**  
This is a straightforward refactoring that enhances code organization. Developers should verify all test references are updated and run a full test suite to confirm no regressions. Future test additions under `serve` should follow the same import pattern.

---

## 38. [[ROCm][CI] Fix granite_speech test for gfx90a by selecting compatible attention backend](https://github.com/vllm-project/vllm/pull/37611)


### Base Information

- **PR Number:** #37611
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 02:07:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37611/files) (1):**
  - `tests/models/multimodal/generation/test_granite_speech.py`

### Summary

**What changed and why**  
The change modifies the attention backend selection logic for Granite Speech tests on ROCm platforms. Previously, all ROCm configurations used the "ROCM_AITER_FA" backend, but now it only uses this backend on MI3xx GPUs (like MI300 series), defaulting to "TRITON_ATTN" for other ROCm GPUs like MI250 (gfx90a). This fixes a compatibility issue that was causing test failures on MI250 systems.

**Technical impact**  
This update introduces GPU architecture-aware backend selection, making the test suite more robust across different ROCm hardware generations. The system now dynamically chooses between ROCm-specific and Triton attention implementations based on the detected GPU architecture, which improves cross-platform compatibility without affecting non-ROCm platforms.

**Potential risks**  
The main risk is that the `on_mi3xx()` function might not accurately detect all MI3xx variants or could have false positives/negatives. Additionally, if other GPU architectures (beyond MI3xx and MI250) are introduced, they would default to TRITON_ATTN which might not be optimal. There's also a risk of import errors if the `on_mi3xx()` function is unavailable or changes signature.

**Key insights**  
This fix demonstrates the importance of hardware-specific optimization in heterogeneous computing environments. Developers should consider implementing similar architecture detection patterns for other ROCm-specific features. The solution is minimal and surgical, but would benefit from adding a comment explaining why MI250 requires TRITON_ATTN instead of ROCM_AITER_FA for future maintainability.

---

## 39. [[ROCm][CI] Remove deepep DBO tests on gfx90a](https://github.com/vllm-project/vllm/pull/37614)


### Base Information

- **PR Number:** #37614
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 02:07:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37614/files) (1):**
  - `.buildkite/test-amd.yaml`

### Summary

**What changed and why**  
Removed DeepEP (Distributed Backend Optimizer) tests from gfx90a (MI250) CI configurations and migrated the MI250 distributed test to use MI325 hardware. This change addresses compatibility issues since DeepEP is not supported on gfx90a architecture, as evidenced by CI failures.

**Technical impact**  
The CI pipeline now excludes incompatible DeepEP tests from gfx90a hardware, preventing test failures and ensuring accurate test results. The MI250 distributed test job now runs on MI325 hardware (`agent_pool: mi325_2`), maintaining test coverage while avoiding the architectural limitation.

**Potential risks**  
Removing the `optional: true` flag from the MI325 test step could cause CI failures if those tests become unstable. There's a risk that other gfx90a compatibility issues might surface in remaining tests, and the hardware change for MI250 tests might mask performance or behavior differences between MI250 and MI325 architectures.

**Key insights**  
This is a targeted fix for a known hardware compatibility issue. Developers should verify that DeepEP functionality is properly documented as incompatible with gfx90a. Consider adding architecture-specific test skips in the test code itself rather than just CI configuration to prevent accidental execution on unsupported hardware.

---

## 40. [[ROCm][CI] Update GSM8K eval config to use fp8-and-mixed models list](https://github.com/vllm-project/vllm/pull/37619)


### Base Information

- **PR Number:** #37619
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-20 02:06:53
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37619/files) (2):**
  - `.buildkite/test-amd.yaml`
  - `tests/evals/gsm8k/configs/models-mi3xx-fp8-and-mixed.txt`

### Summary

**What changed and why**  
The change updates the AMD CI pipeline's GSM8K correctness evaluation to use the `models-mi3xx-fp8-and-mixed.txt` configuration list instead of `models-mi3xx-fp8.txt`. This is a follow-up to address a failure in the `mi325_2: LM Eval Small Models (B200-MI325)` CI step, ensuring the pipeline tests a broader set of models that include both FP8 and mixed-precision configurations.

**Technical impact**  
This modification expands the scope of models evaluated in the GSM8K correctness test to include mixed-precision models alongside FP8-only models. It ensures the CI pipeline more comprehensively validates model performance and correctness across different precision configurations supported by AMD hardware, aligning the test with the intended model coverage.

**Potential risks**  
If the `models-mi3xx-fp8-and-mixed.txt` list includes models that are not fully compatible or stable with the evaluation setup, it could introduce new failures or flakiness in the CI pipeline. Additionally, the change might increase test execution time due to evaluating a larger set of models, potentially impacting CI resource usage.

**Key insights**  
The update reflects a proactive adjustment to CI configuration to maintain test coverage across evolving model precision support. Developers should verify that all models in the new config list are correctly configured and stable to prevent regressions. Monitoring CI performance and failure rates after this change is recommended to ensure it achieves the intended stability improvements.

---

## 41. [[Model] Deprecate the score task (this will not affect users).](https://github.com/vllm-project/vllm/pull/37537)


### Base Information

- **PR Number:** #37537
- **Author:** [noooop](https://github.com/noooop)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-20 01:07:56
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37537/files) (22):**
  - `docs/models/pooling_models/README.md`
  - `docs/models/pooling_models/classify.md`
  - `docs/models/pooling_models/scoring.md`
  - `tests/test_pooling_params.py`
  - `vllm/config/model.py`
  - `vllm/entrypoints/llm.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/pooling/__init__.py`
  - `vllm/entrypoints/pooling/score/protocol.py`
  - `vllm/entrypoints/pooling/score/serving.py`
  - `vllm/entrypoints/sagemaker/api_router.py`
  - `vllm/model_executor/layers/pooler/activations.py`
  - `vllm/model_executor/layers/pooler/seqwise/heads.py`
  - `vllm/model_executor/layers/pooler/seqwise/methods.py`
  - `vllm/model_executor/layers/pooler/seqwise/poolers.py`
  - `vllm/model_executor/layers/pooler/special.py`
  - `vllm/model_executor/layers/pooler/tokwise/heads.py`
  - `vllm/model_executor/layers/pooler/tokwise/poolers.py`
  - `vllm/model_executor/models/interfaces_base.py`
  - `vllm/pooling_params.py`
  - `vllm/tasks.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The PR deprecates the `score` pooling task as part of consolidating multi-task support. Cross-encoder (reranker) models are now treated as a subset of classification models with `num_labels == 1`, so the engine no longer differentiates between `classify` and `score`. The `score` task will be removed in v0.20, and users should migrate to `classify`.

**Technical impact**  
The `score` task is removed from the pooling task definitions, and all related logic now routes through `classify`. Scoring APIs remain functional but are conditionally enabled only for classification models with `num_labels == 1`. Code changes include updates to pooling heads, activation functions, API routing, and documentation to reflect this unification.

**Potential risks**  
- Backward compatibility: Existing code using the `score` task will receive deprecation warnings but may break after v0.20.  
- Conditional API enabling: The scoring API now depends on checking `num_labels == 1`, which could affect models incorrectly configured or dynamically adapted.  
- Documentation updates are extensive; any missed references could confuse users.

**Key insights**  
- Developers must update any explicit references to `task="score"` to `task="classify"`.  
- Scoring functionality is preserved but now gated by model configuration (`num_labels == 1`).  
- Review integration points (e.g., custom pooling layers) to ensure they align with the updated task support.

---

## 42. [[Model Runner V2] Fix draft logits not populated during cudagraph replay](https://github.com/vllm-project/vllm/pull/37639)


### Base Information

- **PR Number:** #37639
- **Author:** [TheEpicDolphin](https://github.com/TheEpicDolphin)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-20 00:43:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37639/files) (3):**
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/spec_decode/eagle/speculator.py`
  - `vllm/v1/worker/gpu/states.py`

### Summary

**What changed and why**  
The fix addresses a bug where draft logits were not properly captured during CUDA graph replay when using Eagle speculative decoding with probabilistic rejection sampling. The draft logits were stored in `RequestState` but not passed to `EagleCudaGraphManager`, causing incorrect sampling behavior. The solution moves `draft_logits` from `RequestState` to `EagleSpeculator`, aligning with the existing pattern for `draft_tokens` and enabling future CUDA graph enhancements.

**Technical impact**  
This change ensures draft logits are included in CUDA graph captures, enabling correct probabilistic rejection sampling for all speculative steps. It simplifies the data flow by centralizing draft-related tensors in `EagleSpeculator`, reducing dependencies on `RequestState` and paving the way for full CUDA graph support in Eagle prefill.

**Potential risks**  
Removing `draft_logits` from `RequestState` could affect other components relying on it, though the PR indicates no such dependencies. The fix assumes `EagleSpeculator` is always available when rejection sampling is active, which is validated via assertions. Edge cases may arise if `draft_logits` is accessed before initialization in `EagleSpeculator`.

**Key insights**  
The bug highlights the importance of ensuring all required tensors are captured in CUDA graphs for correctness. Developers should verify that data structures align with usage patterns across components, especially when enabling features like probabilistic rejection sampling. Adding accuracy tests for draft logits, as noted in the PR, is recommended to prevent similar issues.

---

## 43. [[CI] Removing deprecated rlhf examples reference](https://github.com/vllm-project/vllm/pull/37585)


### Base Information

- **PR Number:** #37585
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-20 00:20:33
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37585/files) (2):**
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/distributed.yaml`

### Summary

**What changed and why**  
This PR removes references to deprecated RLHF (Reinforcement Learning from Human Feedback) example files from CI/CD configuration files. The changes clean up forgotten references after previous repositioning of the Distributed test group, complementing earlier PRs #36188 and #37532.

**Technical impact**  
The changes remove three lines from two Buildkite pipeline configuration files, specifically excluding `examples/offline_inference/rlhf.py` and `examples/offline_inference/rlhf_colocate.py` from distributed test runs. This reduces test execution scope and aligns CI with the current codebase structure.

**Potential risks**  
If the RLHF examples are still being used elsewhere or have been moved to different locations, their exclusion from CI could lead to untested code paths. There's also a risk that related dependencies or imports in other test files might fail if they reference these removed examples.

**Key insights**  
This is a cleanup PR that should be reviewed alongside the referenced previous changes to ensure consistency. Developers should verify that all references to these deprecated examples have been removed from the entire codebase, not just CI configurations, to prevent broken references.

---

## 44. [[Bug] Fix FlashInfer allreduce fusion workspace uninitialized error](https://github.com/vllm-project/vllm/pull/37461)


### Base Information

- **PR Number:** #37461
- **Author:** [wzhao18](https://github.com/wzhao18)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-20 00:09:21
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37461/files) (2):**
  - `vllm/compilation/passes/fusion/allreduce_rms_fusion.py`
  - `vllm/distributed/device_communicators/flashinfer_all_reduce.py`

### Summary

**What changed and why**  
The PR fixes an uninitialized workspace error when torch compile loads a cached compiled module, bypassing pass initialization. Previously, the FlashInfer allreduce fusion workspace was created only in `AllReduceFusionPass.__init__`, but now workspace initialization is also moved into the kernel call (`call_trtllm_fused_allreduce_norm`) and the standalone allreduce backend, ensuring the workspace is available regardless of compilation caching.

**Technical impact**  
The changes refactor workspace management to be lazy-initialized on demand rather than eagerly in the pass constructor. This ensures the workspace is created when needed, even if the pass is skipped due to cache hits. The architecture now centralizes workspace creation in helper functions (`get_fi_ar_workspace` and `get_fi_ar_quant_workspace`) that handle initialization and reuse, improving robustness across different execution paths.

**Potential risks**  
There is a risk of redundant workspace creation if multiple calls occur before initialization completes, though the global variables and locking mitigate this. Additionally, the lazy initialization could introduce a slight performance overhead on the first kernel invocation. The changes also assume that workspace parameters (like `max_token_num` and `hidden_dim`) are consistent across calls, which may not hold if tensor shapes vary significantly.

**Key insights**  
Developers should ensure that workspace parameters passed to `get_fi_ar_workspace` are accurate and consistent with the actual tensor dimensions used in kernels. The fix highlights the importance of decoupling initialization from compilation passes when dealing with cached modules. Future changes should maintain the lazy initialization pattern to avoid similar cache-related issues.

---

## 45. [[XPU] bump vllm-xpu-kernels to v0.1.4](https://github.com/vllm-project/vllm/pull/37641)


### Base Information

- **PR Number:** #37641
- **Author:** [jikunshang](https://github.com/jikunshang)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-03-20 00:04:39
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37641/files) (1):**
  - `requirements/xpu.txt`

### Summary

**What changed and why**  
The PR updates the dependency `vllm-xpu-kernels` from version 0.1.3 to 0.1.4 in the XPU requirements file. This change incorporates accuracy fixes from the new kernel release, as indicated in the linked release notes.

**Technical impact**  
This update affects only XPU-based deployments by replacing the underlying kernel binaries. It may improve numerical accuracy for GPU operations on Intel XPU hardware, but should not alter the core vLLM codebase or non-XPU execution paths.

**Potential risks**  
The new wheel filename now specifies `manylinux_2_28_x86_64` instead of `linux_x86_64`, which could cause compatibility issues on older Linux distributions. Additionally, any regressions in the 0.1.4 kernels might affect XPU performance or correctness, though the release notes emphasize accuracy fixes.

**Key insights**  
Developers should verify that the target deployment environment supports the `manylinux_2_28` platform tag. The XPU CI test plan is appropriate, but consider adding accuracy validation for XPU-specific operations to ensure the fixes work as expected.

---

