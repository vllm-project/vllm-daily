# vLLM Merged PR Report

**Report Date:** 2026-03-27 PDT

**Total Merged PRs:** 20

---

## 1. [[ROCm] [Release] Update ROCm variant from rocm700 to rocm721](https://github.com/vllm-project/vllm/pull/38413)


### Base Information

- **PR Number:** #38413
- **Author:** [tjtanaa](https://github.com/tjtanaa)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-27 23:07:03
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38413/files) (2):**
  - `.buildkite/release-pipeline.yaml`
  - `tools/vllm-rocm/generate-rocm-wheels-root-index.sh`

### Summary

**What changed and why**  
The PR updates the ROCm variant from `rocm700` to `rocm721` in the release pipeline configuration and associated script. This change follows up on a previous PR (#38252) to align the build system with a newer ROCm version for release artifacts.

**Technical impact**  
This modifies the default ROCm version used for building and publishing vLLM's ROCm wheels and Docker images. The release pipeline will now generate artifacts targeting ROCm 7.2.1 instead of 7.0.0, which may include updated dependencies, performance improvements, or compatibility with newer hardware.

**Potential risks**  
If the new ROCm version introduces breaking changes or incompatibilities with existing vLLM code, it could affect users who rely on the ROCm builds. The conditional addition (`if: build.env("NIGHTLY") == "1"`) for publishing nightly images might cause confusion if not properly documented or tested.

**Key insights**  
This is a straightforward version bump that should be validated with thorough testing of the ROCm 7.2.1 builds. Developers should ensure all ROCm-specific tests pass with the new version and consider updating any version-specific documentation. The change to conditional nightly publishing is a good practice for release management.

---

## 2. [[Bugfix]fix output Nan/Inf in marlin if dtype=float16](https://github.com/vllm-project/vllm/pull/33972)


### Base Information

- **PR Number:** #33972
- **Author:** [ir1ka](https://github.com/ir1ka)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 16:36:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/33972/files) (8):**
  - `csrc/moe/marlin_moe_wna16/kernel.h`
  - `csrc/moe/marlin_moe_wna16/marlin_template.h`
  - `csrc/moe/marlin_moe_wna16/ops.cu`
  - `csrc/quantization/activation_kernels.cu`
  - `csrc/quantization/marlin/kernel.h`
  - `csrc/quantization/marlin/marlin.cu`
  - `csrc/quantization/marlin/marlin_template.h`
  - `vllm/model_executor/layers/quantization/utils/marlin_utils_fp4.py`

### Summary

**What changed and why**  
The changes fix NaN/Inf outputs in Marlin kernels when using `dtype=float16` with NVFP4 quantized models. The issue arises because float16's limited dynamic range causes overflow during GEMM operations. The solution dynamically scales inputs before GEMM to prevent overflow, primarily by modifying global scale handling from half-precision to float32 and adjusting scaling logic.

**Technical impact**  
Global scales are now stored and processed as float32 instead of half-precision (uint16/float16), improving numerical stability. The kernel applies scaling in float32 before converting to output precision, reducing overflow risk. Additionally, `use_fp16_accum` is disabled for NVFP4 and certain 4-bit group cases on CUDA 7.5 to avoid precision loss.

**Potential risks**  
The changes assume float32 scaling is sufficient to prevent all overflow scenarios; extreme activations might still cause issues. Disabling `use_fp16_accum` could impact performance on older GPUs (CUDA 7.5). Inconsistent scaling between dense and MoE models (where MoE previously worked) may introduce subtle behavioral differences.

**Key insights**  
Always use float32 for scaling factors when working with float16 activations to mitigate dynamic range limitations. Verify that performance on CUDA 7.5 hardware remains acceptable after disabling FP16 accumulation. Ensure thorough testing across various model architectures, as the fix addresses dense models but MoE models were already functional.

---

## 3. [[Perf] Use torch compile to fuse pack topk in trtllm moe](https://github.com/vllm-project/vllm/pull/37695)


### Base Information

- **PR Number:** #37695
- **Author:** [wzhao18](https://github.com/wzhao18)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-27 16:30:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37695/files) (3):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_nvfp4_moe.py`
  - `vllm/model_executor/layers/fused_moe/utils.py`

### Summary

**What changed and why**  
The PR introduces a torch.compile-optimized function `trtllm_moe_pack_topk_ids_weights` to fuse the packing of top-k expert IDs and weights into a single int32 tensor. This replaces duplicate packing logic in both FP8 and NVFP4 TRT-LLM MoE implementations with a shared, compiled function, aiming for a ~2% performance improvement as observed in benchmarks.

**Technical impact**  
This change centralizes the packing logic into a single utility function, reducing code duplication. Using `torch.compile` with dynamic shape support allows PyTorch's compiler to optimize the packing operation, potentially fusing memory operations and improving kernel efficiency during MoE routing. The impact is limited to the specific packing step within the TRT-LLM MoE execution path.

**Potential risks**  
The `torch.compile` backend uses `current_platform.simple_compile_backend`, which may have varying behavior across different hardware or PyTorch versions. The dynamic compilation could introduce slight overhead on the first run (compilation time). There's also a risk if the compiled function's output format doesn't exactly match the kernel's expected input, though the logic appears unchanged.

**Key insights**  
The performance improvement (~2%) validates the optimization's value. Developers should ensure the `torch.compile` backend is appropriate for all deployment targets. Consider adding a fallback mechanism or configuration flag in case compilation fails. This pattern of consolidating and compiling performance-critical operations could be applied to other similar tensor manipulation patterns in the codebase.

---

## 4. [Add nvidia h800 moe config](https://github.com/vllm-project/vllm/pull/31201)


### Base Information

- **PR Number:** #31201
- **Author:** [lengrongfu](https://github.com/lengrongfu)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 16:28:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/31201/files) (2):**
  - `vllm/model_executor/layers/fused_moe/configs/E=128,N=192,device_name=NVIDIA_H800,dtype=fp8_w8a8.json`
  - `vllm/model_executor/layers/fused_moe/configs/E=128,N=384,device_name=NVIDIA_H100_80GB_HBM3.json`

### Summary

**What changed and why**  
Two new configuration files have been added for fused MoE (Mixture of Experts) kernels: one for NVIDIA H800 with FP8 data type (E=128, N=192) and another for NVIDIA H100 80GB HBM3 (E=128, N=384). These files contain tuned Triton kernel parameters (e.g., block sizes, warps, stages) optimized for specific hardware and model configurations to improve performance.

**Technical impact**  
The additions extend the kernel auto-tuning system to support H800 and H100 GPUs for MoE layers, potentially enhancing inference speed and efficiency for models like Qwen3. The configurations are hardware-specific, ensuring optimal memory access patterns and parallelism for different tensor parallel sizes and data types (FP8 vs. FP16/BF16).

**Potential risks**  
If the configurations are incorrectly tuned or applied to unsupported hardware/datatypes, they could degrade performance or cause runtime errors. The PR lacks validation details (e.g., benchmark results comparison), making it hard to verify the claimed improvements. Additionally, the H100 config uses Triton 3.5.1 while the H800 uses 3.5.0, which may cause inconsistencies if Triton behavior differs between versions.

**Key insights**  
Developers should ensure these configs are only loaded for matching hardware and model settings (E, N, dtype). The PR highlights the importance of hardware-specific tuning for MoE kernels. Future updates should include benchmark results to validate performance gains and consider standardizing Triton versions across configs.

---

## 5. [[ROCm][Documentation] update quickstart and installation to include rocm nightly docker tips](https://github.com/vllm-project/vllm/pull/38367)


### Base Information

- **PR Number:** #38367
- **Author:** [hongxiayang](https://github.com/hongxiayang)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-27 16:20:20
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38367/files) (2):**
  - `docs/getting_started/installation/gpu.rocm.inc.md`
  - `docs/getting_started/quickstart.md`

### Summary

**What changed and why**  
Updated ROCm documentation to include the new nightly Docker image (`vllm/vllm-openai-rocm:nightly`) alongside the stable release. Deprecated the previous AMD-provided Docker images (`rocm/vllm` and `rocm/vllm-dev`) in favor of the official vLLM images, with a clear migration warning.

**Technical impact**  
Users now have clear guidance on using official vLLM Docker images for both stable and nightly builds. The deprecation notice directs users away from the older AMD-hosted images, streamlining support and reducing confusion about image sources and tags.

**Potential risks**  
Users relying on the deprecated AMD images may need to adjust their workflows, though the warning provides adequate notice. The nightly image carries inherent instability risks, but the documentation clearly labels it as a preview build for latest features/fixes.

**Key insights**  
The documentation now better reflects the current Docker image availability and encourages adoption of the official vLLM release pipeline. Developers should update any automation or scripts using `rocm/vllm*` images to use `vllm/vllm-openai-rocm` with appropriate tags (`latest` or `nightly`).

---

## 6. [[ROCm][CI/Build] ROCm 7.2.1 release version; torch 2.10; triton 3.6](https://github.com/vllm-project/vllm/pull/38252)


### Base Information

- **PR Number:** #38252
- **Author:** [gshtras](https://github.com/gshtras)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-27 16:03:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38252/files) (2):**
  - `docker/Dockerfile.rocm`
  - `docker/Dockerfile.rocm_base`

### Summary

**What changed and why**  
Updated ROCm base image from 7.0 to 7.2.1, upgraded PyTorch to release/2.10, and updated Triton to a newer commit. Added a workaround for pytest exiting with status code 0 despite test failures by injecting a custom `conftest.py`. Also included a cherry-picked Triton fix and a custom Kineto submodule for profiler compatibility.

**Technical impact**  
These changes align the ROCm environment with newer library versions, potentially improving performance, stability, and hardware support. The pytest workaround ensures CI correctly detects test failures, while the Triton and Kineto modifications address specific compatibility or functional issues in the ROCm stack.

**Potential risks**  
The pytest workaround uses `os._exit()` which may bypass cleanup routines. Upgrading multiple core dependencies (ROCm, PyTorch, Triton) simultaneously could introduce integration issues or regressions. The custom Kineto submodule and cherry-picked commits may deviate from upstream, requiring careful tracking.

**Key insights**  
The changes are a coordinated upgrade of the ROCm ecosystem; thorough testing across hardware and workloads is essential. The pytest workaround should be temporary—root cause investigation is needed. Ensure the custom Kineto and Triton modifications are documented and considered for future upstream alignment.

---

## 7. [[NVIDIA] Fix DGX Spark logic](https://github.com/vllm-project/vllm/pull/38126)


### Base Information

- **PR Number:** #38126
- **Author:** [johnnynunez](https://github.com/johnnynunez)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 15:26:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38126/files) (3):**
  - `CMakeLists.txt`
  - `cmake/external_projects/qutlass.cmake`
  - `cmake/utils.cmake`

### Summary

**What changed and why**  
This PR fixes build issues for NVIDIA's DGX Spark (SM121/Blackwell) architecture by updating arch guard checks and improving suffix handling in CMake utilities. The changes ensure that kernels like NVFP4, scaled_mm, and MLA are correctly built when targeting SM12.1 variants (e.g., `12.1a`/`12.1f`), which were previously skipped due to missing arch guards and improper suffix matching.

**Technical impact**  
The modifications extend arch guard lists to include SM12.1 variants across multiple kernel families (Marlin FP8, scaled_mm, NVFP4, CUTLASS MLA, etc.) and enhance `cuda_archs_loose_intersection` to handle cross-suffix matching (e.g., `12.1f` ↔ `12.1a`) and preserve target suffixes when source lists lack them. This ensures correct compilation flags are generated and prevents silent kernel exclusions for SM12x-family architectures.

**Potential risks**  
- The suffix-matching logic now permits cross-suffix matches (e.g., `12.1f` matching `12.1a`), which assumes functional equivalence but may introduce subtle behavioral differences if the suffixes imply distinct hardware capabilities.  
- The regex update in `qutlass.cmake` (`12\\.[01][af]?`) could inadvertently match unrelated arch versions if the pattern is not tightly scoped.  
- Changes to arch guard lists must remain synchronized with CUDA version constraints (e.g., CUDA ≥12.8 for certain kernels) to avoid enabling unsupported features.

**Key insights**  
- Developers should verify that all SM12x-family arch guards are consistently updated when adding new kernel families or CUDA versions.  
- The enhanced `cuda_archs_loose_intersection` function now handles suffix preservation and cross-matching more robustly, but its logic is complex; future modifications should include unit tests for edge cases.  
- When building for Blackwell architectures, ensure `TORCH_CUDA_ARCH_LIST` includes the appropriate suffix (e.g., `12.1a`) to leverage these fixes.

---

## 8. [[Model Runner V2] Rebuild attention metadata before eagle decode full…](https://github.com/vllm-project/vllm/pull/38311)


### Base Information

- **PR Number:** #38311
- **Author:** [TheEpicDolphin](https://github.com/TheEpicDolphin)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-27 13:46:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38311/files) (1):**
  - `vllm/v1/worker/gpu/spec_decode/eagle/speculator.py`

### Summary

**What changed and why**  
This PR fixes low-quality draft tokens at positions >0 in EAGLE speculative decoding by ensuring attention metadata is rebuilt before each FULL cudagraph execution. Previously, stale attention metadata from prior runs degraded draft token quality. The changes refactor the draft decoding path to rebuild metadata even in FULL mode, improving acceptance rates for later draft tokens.

**Technical impact**  
The refactor centralizes CUDA graph dispatch and attention metadata construction into dedicated helper methods (`_dispatch_and_sync_dp`, `_build_draft_attn_metadata`). This ensures attention metadata builders/backends are updated with fresh state before each decode step, regardless of cudagraph mode. The draft generation path now explicitly handles FULL mode separately, maintaining correctness while preserving performance.

**Potential risks**  
If the attention metadata rebuilding introduces significant overhead, it could impact latency in high-concurrency scenarios. Additionally, the refactored code assumes `decode_batch_desc.num_reqs` may be `None` (falling back to `num_reqs`), which should be validated. Edge cases with `dp_size > 1` and varying token counts need careful testing to ensure synchronization works correctly.

**Key insights**  
Always rebuild attention metadata for speculative decoding to avoid stale state, even in optimized paths like FULL cudagraph. The refactor improves code clarity and maintainability by separating concerns. Developers should verify that the metadata construction does not regress performance in benchmarks, especially under high concurrency.

---

## 9. [[CI] Skip failing test](https://github.com/vllm-project/vllm/pull/38369)


### Base Information

- **PR Number:** #38369
- **Author:** [NickLucche](https://github.com/NickLucche)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-03-27 13:25:19
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38369/files) (1):**
  - `tests/models/registry.py`

### Summary

**What changed and why**  
A single line was added to the test registry to specify a specific revision (`refs/pr/14`) for the Ernie4_5_VL model's Hugging Face example. This change pins the test to a particular PR revision, likely to bypass a transient failure caused by upstream model changes or inconsistencies.

**Technical impact**  
The test now explicitly uses a fixed model revision instead of the default (likely `main`). This ensures test consistency by avoiding automatic updates from the model repository, but it decouples the test from the latest model version, which may hide integration issues with newer releases.

**Potential risks**  
If the pinned revision is deleted or becomes inaccessible, the test will fail. Additionally, this approach may mask regressions or compatibility problems that arise when the model is updated, as the test no longer validates against the current canonical version.

**Key insights**  
This is a temporary workaround rather than a fix. Consider investigating the root cause of the test failure—such as model architecture changes or dependency issues—and implement a robust solution. Document the reason for pinning the revision to aid future maintenance.

---

## 10. [[QeRL] Compose online quantization with quantized reloading](https://github.com/vllm-project/vllm/pull/38032)


### Base Information

- **PR Number:** #38032
- **Author:** [kylesayrs](https://github.com/kylesayrs)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 13:22:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38032/files) (10):**
  - `tests/model_executor/model_loader/test_reload.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/mxfp8.py`
  - `vllm/model_executor/model_loader/base_loader.py`
  - `vllm/model_executor/model_loader/dummy_loader.py`
  - `vllm/model_executor/model_loader/reload/__init__.py`
  - `vllm/model_executor/model_loader/reload/layerwise.py`
  - `vllm/model_executor/model_loader/reload/meta.py`
  - `vllm/model_executor/model_loader/reload/types.py`
  - `vllm/model_executor/model_loader/weight_utils.py`

### Summary

**What changed and why**  
This PR enables online quantization to reuse the layerwise reloading infrastructure. Previously, online quantization had custom weight loading logic; now it leverages the existing reloading framework's buffering and processing mechanisms. The changes allow online quantized reloading (quantizing during weight reload) and unify the lifecycle management for both reloading and quantization.

**Technical impact**  
The architecture now treats online quantization as a special case of layerwise processing, where weights are buffered and quantized after full loading but without copying back to kernel tensors (since quantization happens during initial load). This reduces code duplication and ensures consistent behavior between reloading and quantization paths. The `initialize_online_processing` function is now shared, and `finalize_layerwise_processing` handles edge cases like attention layers and padding.

**Potential risks**  
There's a performance risk with parameters like `e_score_correction_bias` that may be loaded twice during online quantization, as noted in the edge case handling. The changes to `CopyCounter` to track all `copy_` operations (not just meta device) could have unintended side effects if other parts of the system rely on the previous behavior. The conditional logic around `kernel_tensors` being `None` in `finalize_layerwise_processing` adds complexity that must be carefully maintained.

**Key insights**  
Developers should note that online quantization now follows the same lifecycle as reloading, with the key difference being the skipping of the kernel tensor copy step. The `finalize_layerwise_processing` function is now central and may eventually replace `process_weights_after_loading`. Testing has been expanded to cover online quantized reloading scenarios, but the commented-out mxfp8 and mxfp4 tests indicate pending support.

---

## 11. [Add short flag `-sc` for `--speculative-config` argument](https://github.com/vllm-project/vllm/pull/38380)


### Base Information

- **PR Number:** #38380
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 12:04:23
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38380/files) (1):**
  - `vllm/engine/arg_utils.py`

### Summary

**What changed and why**  
Added a short flag `-sc` as an alias for the existing `--speculative-config` command-line argument to improve usability and maintain consistency with other CLI tools that provide abbreviated options for frequently used parameters.

**Technical impact**  
This change extends the argument parser's functionality by adding an alternative way to specify the speculative configuration without altering the underlying argument behavior or validation logic. The existing argument parsing tests continue to cover this addition.

**Potential risks**  
Minimal risk since this only adds an alias without modifying existing functionality. However, there's a slight chance of conflict if `-sc` is already used elsewhere in the argument parser, though this appears unlikely given the context.

**Key insights**  
This is a user experience improvement that follows established CLI conventions. Developers should ensure they document this new alias in relevant user-facing documentation and consider applying similar consistency improvements to other long-form arguments if appropriate.

---

## 12. [[ROCm] Fix GPT-OSS import for triton 3.6](https://github.com/vllm-project/vllm/pull/37453)


### Base Information

- **PR Number:** #37453
- **Author:** [gshtras](https://github.com/gshtras)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-27 11:00:57
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37453/files) (1):**
  - `vllm/model_executor/layers/quantization/utils/mxfp4_utils.py`

### Summary

**What changed and why**  
The code adds a try-except import block to handle a breaking change in Triton 3.6 where `GFX950MXScaleLayout` was renamed to `CDNA4MXScaleLayout`. This ensures backward compatibility with older Triton versions while supporting the newer version.

**Technical impact**  
This change maintains compatibility across Triton versions without altering the functional behavior of the MXFP4 quantization utilities. The system will automatically adapt to the available layout class based on the installed Triton version.

**Potential risks**  
If Triton 3.6 introduces additional API changes beyond the renamed class, those may not be caught by this fallback. The `on_gfx950()` condition still determines which path is taken, so any underlying changes in the layout classes themselves could cause subtle issues.

**Key insights**  
Always test with both Triton versions to ensure full compatibility. Consider adding version detection logic for clearer maintenance. This pattern of graceful fallback imports is a good practice for handling upstream library changes.

---

## 13. [{ROCm]: gpt-oss fusion/padding fixes](https://github.com/vllm-project/vllm/pull/38043)


### Base Information

- **PR Number:** #38043
- **Author:** [Rohan138](https://github.com/Rohan138)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-27 09:19:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38043/files) (3):**
  - `vllm/config/vllm.py`
  - `vllm/model_executor/layers/fused_moe/oracle/mxfp4.py`
  - `vllm/model_executor/layers/quantization/utils/mxfp4_utils.py`

### Summary

**What changed and why**  
This PR fixes two issues related to GPT-OSS optimizations on ROCm. First, it changes the MXFP4 padding alignment for ROCm from a dynamic check (128/256 based on GPU architecture) to a fixed 256 bytes, aligning with MI300 optimizations. Second, it re-enables RMSNorm+padding fusion for both Triton and wvsplitKrc GEMMs by removing the previous restriction that disabled fusion when Triton GEMMs were enabled.

**Technical impact**  
The padding change ensures consistent 256-byte alignment for hidden and intermediate sizes on ROCm, improving memory access patterns for MI300 GPUs. The fusion fix expands optimization coverage, allowing RMSNorm+padding fusion to work with all GEMM backends (Triton and wvsplitKrc), potentially increasing performance for GPT-OSS models (hidden size 2880) on ROCm platforms.

**Potential risks**  
Fixed 256-byte padding may over-pad on non-MI300 ROCm GPUs (e.g., older architectures), slightly increasing memory usage. The fusion change assumes #34304 resolved previous compatibility issues—if any latent bugs exist, they could affect numerical correctness or performance. Removing architecture detection reduces flexibility for future GPU variants.

**Key insights**  
Developers should verify that the 256-byte padding aligns with all targeted ROCm GPU memory subsystems. The fusion fix is a positive step but requires thorough testing across GEMM backends to ensure no regressions. Consider adding a runtime warning if non-MI300 ROCm GPUs use padded sizes, and monitor performance metrics post-deployment.

---

## 14. [Remove need for explicit `\n` in docstring lists for `--help` formatting](https://github.com/vllm-project/vllm/pull/38350)


### Base Information

- **PR Number:** #38350
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-27 08:38:00
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38350/files) (10):**
  - `vllm/config/cache.py`
  - `vllm/config/kernel.py`
  - `vllm/config/load.py`
  - `vllm/config/model.py`
  - `vllm/config/multimodal.py`
  - `vllm/config/parallel.py`
  - `vllm/config/profiler.py`
  - `vllm/config/scheduler.py`
  - `vllm/config/utils.py`
  - `vllm/utils/argparse_utils.py`

### Summary

**What changed and why**  
This PR removes explicit `\n` characters from docstrings in configuration classes and updates the `SortedHelpFormatter._split_lines` method to handle list formatting automatically. The changes improve docstring readability in source code while maintaining proper formatting in CLI `--help` output.

**Technical impact**  
The formatter now recognizes unordered lists (starting with `-`, `*`, `+`) and ordered lists (starting with `1.`, `2.`, etc.) as line boundaries, preventing unwanted line merging. This allows docstrings to use natural Markdown-like list syntax without manual line breaks. The mypy fix ensures proper type checking for the `@config` decorator.

**Potential risks**  
If the regex pattern fails to match edge cases (e.g., nested lists, custom bullet styles), list items may be incorrectly merged. The formatter changes could affect other argparse-based help outputs beyond config classes. There is also a risk of unintended behavior if docstrings contain mixed formatting not covered by the pattern.

**Key insights**  
Developers should now write docstrings with clean Markdown-style lists without explicit `\n`. The regex pattern should be extended if new list formats are introduced. Verify that all `--help` outputs render correctly, especially for complex nested structures. The mypy fix ensures decorator compatibility but does not affect runtime behavior.

---

## 15. [enable skipping of SW attention layers when using FP8 KV cache](https://github.com/vllm-project/vllm/pull/33695)


### Base Information

- **PR Number:** #33695
- **Author:** [jmkuebler](https://github.com/jmkuebler)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-27 06:25:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/33695/files) (4):**
  - `tests/quantization/test_fp8.py`
  - `vllm/config/cache.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/model_executor/layers/attention/attention.py`

### Summary

**What changed and why**  
This PR adds support for selectively skipping KV cache quantization on specific attention layers when using FP8 KV cache. The primary motivation is to keep Sliding Window Attention layers in BF16 while quantizing Full Attention layers, as SW layers offer minimal latency/memory benefits from quantization but still incur overhead. This conservative approach aims to improve inter-token latency (ITL) and reduce accuracy degradation risks.

**Technical impact**  
The changes introduce a new configuration parameter `kv_cache_dtype_skip_layers` that accepts layer indices or attention type names (e.g., "sliding_window"). When enabled, skipped layers revert to "auto" dtype (BF16) instead of FP8, affecting memory usage and compute precision selectively. The implementation modifies attention layer initialization to conditionally override quantization settings based on the skip list.

**Potential risks**  
If the skip list includes invalid layer indices or patterns, it could lead to runtime errors or unintended quantization behavior. The feature assumes sliding window attention detection is accurate; misclassification might skip quantization incorrectly. Additionally, mixing quantized and non-quantized layers could introduce subtle performance inconsistencies across different model architectures.

**Key insights**  
Benchmarks show consistent slight ITL improvements when skipping SW layers, validating the optimization. Developers should ensure the skip list aligns with the model's layer structure and attention types. This feature is particularly valuable for models where SW layers are prevalent, but thorough testing is recommended to confirm accuracy and performance gains across diverse workloads.

---

## 16. [fix(security): Add VLLM_MAX_N_SEQUENCES environment variable and enforce limit](https://github.com/vllm-project/vllm/pull/37952)


### Base Information

- **PR Number:** #37952
- **Author:** [jperezdealgaba](https://github.com/jperezdealgaba)
- **Merged By:** [russellb](https://github.com/russellb)
- **Merged time:** 2026-03-27 06:02:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37952/files) (5):**
  - `docs/usage/security.md`
  - `tests/entrypoints/openai/chat_completion/test_chat.py`
  - `tests/test_envs.py`
  - `vllm/envs.py`
  - `vllm/sampling_params.py`

### Summary

**What changed and why**  
This change introduces a new `VLLM_MAX_N_SEQUENCES` environment variable (default: 16384) to limit the maximum number of output sequences (`n`) per request. The `SamplingParams` class now validates `n` against this limit, raising an error if exceeded. This prevents excessive resource consumption and potential denial-of-service attacks from large `n` values.

**Technical impact**  
The validation occurs early in the request pipeline within `SamplingParams.__post_init__`, ensuring requests with oversized `n` are rejected before resource allocation. The limit is configurable via environment variable, allowing deployment-specific tuning. Documentation updates provide security guidance, and comprehensive tests verify both default and custom limits.

**Potential risks**  
If the default limit (16384) is too high for resource-constrained deployments, it may still allow resource exhaustion. Environment variable caching could cause stale values if changed at runtime without proper cache invalidation. The validation only covers the `SamplingParams` path; other entry points or internal APIs might bypass this check.

**Key insights**  
Public-facing deployments should set a lower limit (e.g., 64–128) based on expected workloads. Consider complementing this with reverse-proxy validation and monitoring for anomalous resource patterns. Ensure all request paths enforce the limit consistently, and document cache-clearing procedures for dynamic environment updates.

---

## 17. [[EPLB] Cleanup the transfer logic for the various eplb maps](https://github.com/vllm-project/vllm/pull/34520)


### Base Information

- **PR Number:** #34520
- **Author:** [SageMoore](https://github.com/SageMoore)
- **Merged By:** [tlrmchlsmth](https://github.com/tlrmchlsmth)
- **Merged time:** 2026-03-27 02:18:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34520/files) (3):**
  - `.buildkite/test_areas/expert_parallelism.yaml`
  - `tests/distributed/test_eplb_utils.py`
  - `vllm/distributed/eplb/eplb_state.py`

### Summary

**What changed and why**  
This PR refactors EPLB (Expert Parallel Load Balancing) map transfer logic by extracting duplicated code into dedicated functions. Previously, the logic for committing new expert maps into `EplbModelState` was duplicated across sync and async paths. Now `_commit_eplb_maps` handles multi-layer commits for sync operations, while `_commit_eplb_maps_for_layer` handles single-layer commits for async operations. The old `_update_layer_mapping_from_new` function has been removed.

**Technical impact**  
The refactoring improves code maintainability by centralizing map commitment logic and eliminating duplication. The architecture now clearly separates sync (full model update) and async (per-layer update) pathways. Critical assertions have been added to ensure physical expert count consistency during async operations, preventing race conditions when GPU resources change dynamically.

**Potential risks**  
The assertion in `_commit_eplb_maps_for_layer` that prevents physical expert count changes during async EPLB could potentially block legitimate scaling scenarios if not properly handled. The `_pad_out_tensor` helper function assumes padding with -1 is always appropriate, which may not hold for all tensor types. The removal of `_update_layer_mapping_from_new` requires careful verification that all its edge cases are covered by the new functions.

**Key insights**  
The refactoring successfully reduces code duplication while maintaining functional equivalence. Developers should note the strict assertion preventing physical expert count changes during async mode - this is a deliberate safety measure. The test coverage is comprehensive, covering shape changes, padding behavior, and layer-specific updates. Future modifications to map commitment logic should now be made in the centralized functions rather than at call sites.

---

## 18. [[P/D] Mooncake: Add unit tests and minor fixes for mooncake connector](https://github.com/vllm-project/vllm/pull/36946)


### Base Information

- **PR Number:** #36946
- **Author:** [dtcccc](https://github.com/dtcccc)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-03-27 01:26:40
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36946/files) (4):**
  - `tests/v1/kv_connector/unit/test_mooncake_connector.py`
  - `tests/v1/kv_connector/unit/utils.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/mooncake_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/mooncake_utils.py`

### Summary

**What changed and why**  
This PR adds comprehensive unit tests for the Mooncake connector and implements two key fixes: deferring import checks to avoid requiring Mooncake installation for tests, and fixing a double-send bug where failed requests were incorrectly included in ok_reqs. The test suite includes interface tests, bootstrap server validation, and mock-based worker testing.

**Technical impact**  
The changes improve test coverage and reliability of the Mooncake KV transfer system. The import deferral allows tests to run without Mooncake dependencies, while the double-send fix prevents data corruption during error conditions. The bootstrap server tests validate distributed coordination logic, and the mock infrastructure enables isolated unit testing of complex async components.

**Potential risks**  
The mock-based testing approach might not catch all integration issues with the actual Mooncake library. The deferred import check could mask missing Mooncake dependencies in production environments if error handling isn't robust. The bootstrap server tests rely on network ports which could cause flaky tests in CI environments.

**Key insights**  
The test suite effectively validates both happy paths and edge cases like partial blocks and error conditions. Developers should ensure the mock objects accurately reflect real Mooncake behavior. The import pattern change (from raising ImportError to logging warning) requires careful deployment verification to ensure missing dependencies are properly detected in production.

---

## 19. [[CI/Build] Move nightly wheel index generation to a single post-build step](https://github.com/vllm-project/vllm/pull/38322)


### Base Information

- **PR Number:** #38322
- **Author:** [Harry-Chen](https://github.com/Harry-Chen)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-27 00:44:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38322/files) (3):**
  - `.buildkite/release-pipeline.yaml`
  - `.buildkite/scripts/generate-and-upload-nightly-index.sh`
  - `.buildkite/scripts/upload-nightly-wheels.sh`

### Summary

**What changed and why**  
The PR splits the nightly wheel upload process into two separate steps: individual wheel uploads and centralized index generation. Previously, each concurrent build step would both upload its wheel and regenerate indices, creating TOCTOU (time-of-check to time-of-use) races. Now, each build step only uploads its wheel, while a new dedicated post-build step generates indices once after all wheels are built.

**Technical impact**  
This change improves pipeline reliability by eliminating race conditions during concurrent builds. The new index generation step depends on the entire `build-wheels` group with `allow_dependency_failure: true`, ensuring that a single wheel failure doesn't block index generation for successfully built wheels. The architecture now separates concerns: upload steps are lightweight and idempotent, while index generation is centralized and deterministic.

**Potential risks**  
The `allow_dependency_failure` setting could mask underlying build problems if wheels fail silently. The index generation script assumes all wheels are present in S3 by the time it runs, which might lead to incomplete indices if uploads are delayed. The Python version detection logic and regex module hack are fragile and could break with environment changes.

**Key insights**  
This is a well-structured concurrency fix that follows the single-responsibility principle. Developers should monitor the new step for any S3 eventual consistency issues. Consider adding validation to ensure all expected wheels are present before generating indices. The regex hack should be replaced with a proper dependency management solution.

---

## 20. [[Doc] Clarify Helm chart location in deployment guide](https://github.com/vllm-project/vllm/pull/38328)


### Base Information

- **PR Number:** #38328
- **Author:** [utsumi-fj](https://github.com/utsumi-fj)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-27 00:43:02
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38328/files) (1):**
  - `docs/deployment/frameworks/helm.md`

### Summary

**What changed and why**  
This documentation-only change adds a clarifying sentence with a relative link to specify the exact Helm chart location (`examples/online_serving/chart-helm`) used in the deployment guide. The purpose is to eliminate potential ambiguity for users following the installation instructions.

**Technical impact**  
The change has no technical impact on the codebase, runtime behavior, or system architecture. It only improves the documentation's clarity by explicitly pointing users to the correct Helm chart resource before they execute the installation commands.

**Potential risks**  
The primary risk is a broken link if the referenced chart directory is moved or renamed in the future. There is also a minor risk that users might still reference an incorrect or outdated chart if they have multiple chart versions locally, though the explicit path mitigates this.

**Key insights**  
Always use relative links in documentation to maintain integrity across documentation versions and repository clones. For documentation changes, ensure links are validated during the PR review process. Consider adding a periodic link-checking step to the CI/CD pipeline to catch broken references automatically.

---

