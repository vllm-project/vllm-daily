# vLLM Merged PR Report

**Report Date:** 2026-03-17 PDT

**Total Merged PRs:** 49

---

## 1. [[kv_offload+HMA][0/N]: Support block-level preemption handling](https://github.com/vllm-project/vllm/pull/34805)


### Base Information

- **PR Number:** #34805
- **Author:** [orozery](https://github.com/orozery)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-03-17 23:49:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34805/files) (7):**
  - `tests/v1/kv_connector/unit/test_multi_connector.py`
  - `tests/v1/kv_connector/unit/test_offloading_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/base.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/multi_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py`
  - `vllm/v1/worker/gpu/kv_connector.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The PR modifies the `handle_preemptions` connector API to accept `KVConnectorMetadata` instead of a set of preempted request IDs. This enables handling of arbitrary events—specifically sliding-window layer blocks that may be evicted from GPU KV cache but preserved by a connector—by passing richer metadata.

**Technical impact**  
This change generalizes preemption handling to support block-level evictions beyond full request preemptions. It affects all connector implementations (base, multi, offloading) and integrates metadata flow through the scheduler output, ensuring connectors receive structured data for precise cache management.

**Potential risks**  
If metadata is malformed or missing required fields (e.g., `reqs_to_flush`), connectors may silently skip necessary flush operations. The order of operations in `pre_forward` changed (`handle_preemptions` now called after `bind_connector_metadata`), which could introduce race conditions if not all connectors are idempotent.

**Key insights**  
Developers must ensure their connector metadata includes all necessary fields for eviction handling. The updated API is more flexible but requires careful validation of metadata consistency across distributed components. Testing should verify that both request-level preemptions and block-level evictions are correctly processed.

---

## 2. [[XPU] skip unsupported ut and update test_nixl_connector](https://github.com/vllm-project/vllm/pull/37179)


### Base Information

- **PR Number:** #37179
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-03-17 22:32:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37179/files) (2):**
  - `.buildkite/scripts/hardware_ci/run-xpu-test.sh`
  - `tests/v1/kv_connector/unit/test_nixl_connector.py`

### Summary

**What changed and why**  
Two changes were made to fix NIXL (Neural Interface for KV Cache) unit test issues on XPU hardware. The test script now skips cross-layer tests that require HND (Hierarchical Neural Device) support unavailable on XPU, and explicitly configures Ray to allocate GPU resources in non-CUDA environments.

**Technical impact**  
The changes modify test execution behavior specifically for XPU hardware. The CI script now excludes additional tests (`test_worker_memory_snapshot.py` and cross-layer NIXL tests) and adds a `--max-model-len` parameter for a specific model test. The test code itself ensures Ray properly initializes with GPU resources on non-CUDA platforms by explicitly setting `num_gpus=1`.

**Potential risks**  
The explicit `num_gpus=1` Ray configuration assumes test environments always have at least one GPU device available, which could cause initialization failures in CPU-only test environments. Skipping multiple tests reduces XPU test coverage, potentially hiding platform-specific issues. The `-k "not (test_register_kv_caches and FLASH_ATTN and True)"` filter syntax appears complex and may not correctly match the intended test cases.

**Key insights**  
These changes highlight platform-specific limitations in the test infrastructure, particularly around Ray resource detection on non-CUDA accelerators. Developers should verify that skipped tests have appropriate platform guards rather than being universally excluded. The Ray initialization pattern could be abstracted into a utility function for consistent cross-platform test setup.

---

## 3. [[responsesAPI] parser.extract_response_outputs can take in token IDs](https://github.com/vllm-project/vllm/pull/37130)


### Base Information

- **PR Number:** #37130
- **Author:** [qandrew](https://github.com/qandrew)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-03-17 22:31:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37130/files) (2):**
  - `vllm/entrypoints/openai/responses/serving.py`
  - `vllm/parser/abstract_parser.py`

### Summary

**What changed and why**  
The changes modify the `extract_response_outputs` method in the parser interface to accept token IDs (`model_output_token_ids`) alongside the existing string output. This prepares the codebase for models like Harmony that require token-based parsing instead of string-based parsing, as referenced in the related GitHub issue.

**Technical impact**  
This is a backward-compatible extension to the parser API—adding a new optional parameter with a default value ensures existing implementations continue to work. The change enables future parsers to leverage token IDs for more accurate or efficient output extraction, particularly for models where token-level granularity is necessary.

**Potential risks**  
If downstream parsers do not properly handle the new `model_output_token_ids` parameter (e.g., ignoring it when required), it could lead to incorrect parsing for token-dependent models. Additionally, the parameter is added as positional-only (`*`) which enforces keyword usage, but existing calls must be updated to avoid breaking changes.

**Key insights**  
Developers should update any custom parser implementations to accept and utilize `model_output_token_ids` where relevant. The change is a proactive step toward supporting token-aware parsing, so ensure thorough testing when integrating models that depend on token IDs. The PR maintains no functional changes currently, aligning with its preparatory nature.

---

## 4. [[CI] Stabilize test_cpu_offloading by waiting for async offload before cache reset](https://github.com/vllm-project/vllm/pull/37335)


### Base Information

- **PR Number:** #37335
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-17 22:26:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37335/files) (1):**
  - `tests/v1/kv_offload/test_cpu_offloading.py`

### Summary

**What changed and why**  
The changes address intermittent test failures in `test_cpu_offloading` by ensuring asynchronous GPU-to-CPU offload operations complete before resetting the prefix cache. Previously, `reset_prefix_cache()` could return `False` (silently ignored) while offload was still in progress, causing the GPU cache to remain uncleared and leading to assertion failures. The fix introduces a waiting mechanism with platform-specific timeouts and reduces batch size variability on ROCm.

**Technical impact**  
These modifications make the test more robust by synchronizing asynchronous operations, preventing race conditions between cache reset and ongoing offloads. Platform-specific adjustments (longer timeouts for ROCm, batch size reduction) account for hardware differences in CI environments. The changes are confined to test logic and do not affect production code behavior.

**Potential risks**  
The increased timeouts (up to 60 seconds) could mask deeper performance issues or deadlocks if they occur. The silent failure of `reset_prefix_cache()` being ignored remains a potential concern in production scenarios. Hard-coded platform-specific constants may require maintenance if test environments change.

**Key insights**  
Always synchronize asynchronous operations before dependent actions in tests. Platform-aware tuning is essential for CI stability across different hardware. Consider adding logging or explicit error handling for `reset_prefix_cache()` failures in production code to avoid silent issues. The test now provides clearer failure messages when assertions fail.

---

## 5. [[ROCm][CI] Add ROCM_EXTRA_ARGS to audio_in_video test server fixture](https://github.com/vllm-project/vllm/pull/37349)


### Base Information

- **PR Number:** #37349
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 21:55:40
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37349/files) (1):**
  - `tests/entrypoints/openai/chat_completion/test_audio_in_video.py`

### Summary

**What changed and why**  
Added `ROCM_EXTRA_ARGS` to the server fixture for the `test_audio_in_video` test. This change ensures that on ROCm platforms, prefix caching is disabled and sequence count is limited to 1, reducing batch variance that can cause early end-of-sequence (EOS) due to `stop` instead of `length`.

**Technical impact**  
The modification integrates platform-specific arguments into the test server's launch parameters, aligning ROCm test behavior with GPU-specific optimizations. This ensures consistent test outcomes by mitigating hardware-dependent variability in batch processing.

**Potential risks**  
If `ROCM_EXTRA_ARGS` is not properly defined or contains incompatible arguments for non-ROCm environments, it could lead to server startup failures or unintended side effects. Additionally, hardcoding these args may obscure test logic if future changes to ROCM_EXTRA_ARGS affect other tests.

**Key insights**  
Always validate that platform-specific constants like `ROCM_EXTRA_ARGS` are clearly documented and tested across environments. Consider isolating such fixtures to avoid cross-platform contamination and ensure maintainability as ROCm configurations evolve.

---

## 6. [[Hardware][TPU] Add supports_async_scheduling() method to Executor interface so that it can be extended for Executor implementations.](https://github.com/vllm-project/vllm/pull/36924)


### Base Information

- **PR Number:** #36924
- **Author:** [gxd3](https://github.com/gxd3)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 21:53:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36924/files) (5):**
  - `tests/v1/executor/test_executor.py`
  - `vllm/config/vllm.py`
  - `vllm/v1/executor/abstract.py`
  - `vllm/v1/executor/multiproc_executor.py`
  - `vllm/v1/executor/uniproc_executor.py`

### Summary

**What changed and why**  
A `supports_async_scheduling()` class method was added to the base `Executor` interface, returning `False` by default. This method is overridden to return `True` in `UniProcExecutor`, `ExecutorWithExternalLauncher`, and `MultiprocExecutor`. The change enables TPU-inference's custom executor to declare async scheduling compatibility by overriding this method, replacing a hardcoded backend list check with dynamic executor class detection.

**Technical impact**  
The configuration validation logic in `vllm.py` now queries the executor class directly via `supports_async_scheduling()` instead of checking against a static list of backend strings. This makes the system more extensible, allowing new executor implementations to integrate without modifying the core configuration validation code. Async scheduling compatibility is now determined by the executor's capability rather than its backend identifier.

**Potential risks**  
If a custom executor overrides `supports_async_scheduling()` incorrectly (e.g., returning `True` without proper async support), it could lead to runtime errors or undefined behavior. The change also assumes all existing subclasses (`UniProcExecutor`, `MultiprocExecutor`, etc.) correctly implement async scheduling; any hidden incompatibilities in these classes may now surface. The error messages are less specific (no longer listing supported backends), which could reduce debugging clarity for users.

**Key insights**  
This refactor improves extensibility by decoupling async scheduling validation from hardcoded backend checks. Developers adding new executor types should carefully evaluate async scheduling compatibility and override `supports_async_scheduling()` appropriately. Ensure thorough testing of async scheduling with custom executors, as the runtime now relies on this method for correctness. Consider adding documentation or type hints to clarify the contract for this method.

---

## 7. [[Perf] Enable dual stream execution of input projection for Qwen3](https://github.com/vllm-project/vllm/pull/36795)


### Base Information

- **PR Number:** #36795
- **Author:** [xyang16](https://github.com/xyang16)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 20:13:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36795/files) (3):**
  - `vllm/model_executor/models/qwen3_5.py`
  - `vllm/model_executor/models/qwen3_next.py`
  - `vllm/utils/multi_stream_utils.py`

### Summary

**What changed and why**  
This PR introduces dual-stream execution for the input projection layers in Qwen3 models to improve performance. It parallelizes the independent `in_proj_qkvz` and `in_proj_ba` operations using separate CUDA streams, wrapped in a custom op for `torch.compile` compatibility.

**Technical impact**  
The changes modify the forward pass to execute two linear projections concurrently, reducing kernel launch overhead and improving GPU utilization. A new utility (`multi_stream_utils.py`) manages stream synchronization, and custom ops ensure the optimization works with compiled graphs. This primarily affects Qwen3 and Qwen3.5 model implementations.

**Potential risks**  
Stream synchronization adds event overhead that may negate gains in low-concurrency scenarios. The custom op relies on `forward_context.no_compile_layers`, which could break if the context management changes. Slight accuracy variations observed in benchmarks (e.g., Qwen3 strict-match dropped 0.0068) warrant monitoring.

**Key insights**  
The optimization shows measurable throughput improvements (e.g., ~1.1-2.5% higher token throughput) but slightly increases TTFT, likely due to stream setup costs. Developers should verify correctness on diverse hardware and consider conditional activation based on batch size to avoid overhead in small workloads.

---

## 8. [[ROCm][CI] Skip trtllm kvfp8 dequant tests on ROCm](https://github.com/vllm-project/vllm/pull/37330)


### Base Information

- **PR Number:** #37330
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 20:12:26
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37330/files) (1):**
  - `tests/kernels/attention/test_trtllm_kvfp8_dequant.py`

### Summary

**What changed and why**  
Added a module-level pytest skip for ROCm platforms in `test_trtllm_kvfp8_dequant.py` to exclude unsupported trtllm kvfp8 dequant tests. This follows the same skip pattern previously implemented in `test_flashinfer.py`, ensuring consistency in handling ROCm-specific test exclusions.

**Technical impact**  
The change prevents test failures on ROCm by skipping the entire test module when running on ROCm platforms. This maintains CI stability and avoids false negatives, while preserving test coverage for other platforms (e.g., CUDA) where the functionality is supported.

**Potential risks**  
If the skip condition is overly broad, it might inadvertently hide platform-specific regressions or mask issues that could arise if ROCm support is added later. Additionally, developers must ensure the skip reason remains accurate if future ROCm updates include support for this feature.

**Key insights**  
This is a standard practice for managing platform-dependent test suites. Developers should document the skip rationale and periodically review skipped tests to assess whether platform support has been added. Consider adding a TODO comment linking to a tracking issue for future ROCm support.

---

## 9. [[Performance] Add --enable-ep-weight-filter CLI option](https://github.com/vllm-project/vllm/pull/37351)


### Base Information

- **PR Number:** #37351
- **Author:** [esmeetu](https://github.com/esmeetu)
- **Merged By:** [esmeetu](https://github.com/esmeetu)
- **Merged time:** 2026-03-17 18:36:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37351/files) (3):**
  - `vllm/config/parallel.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/model_executor/model_loader/default_loader.py`

### Summary

**What changed and why**  
Added a new `--enable-ep-weight-filter` CLI option that makes expert parallel weight filtering an explicit opt-in feature. This allows users to skip loading non-local expert weights during model initialization when expert parallelism is active, reducing I/O for per-expert MoE models like DeepSeek and Mixtral.

**Technical impact**  
The change introduces a new configuration flag that gates the existing weight filtering logic. Without the flag, behavior remains identical to main; with it, each rank loads only its assigned expert shards, potentially significantly reducing storage I/O during model loading for compatible MoE architectures.

**Potential risks**  
If users enable the flag without understanding its purpose, they might expect performance improvements on non-MoE models or fused-expert checkpoints where it has no effect. There's also a risk of confusion if the flag is enabled without `--enable-expert-parallel`, as the filter requires both conditions to activate.

**Key insights**  
This is a well-scoped performance optimization that maintains backward compatibility. Developers should document that this flag only benefits per-expert MoE models and requires expert parallelism to be enabled. The implementation correctly isolates the feature behind both model type and configuration checks.

---

## 10. [[Kernel][Helion] [16/N] Refactor register_kernel API to be more Dynamo-friendly](https://github.com/vllm-project/vllm/pull/36705)


### Base Information

- **PR Number:** #36705
- **Author:** [gmagogsfm](https://github.com/gmagogsfm)
- **Merged By:** [zou3519](https://github.com/zou3519)
- **Merged time:** 2026-03-17 18:23:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36705/files) (6):**
  - `tests/kernels/helion/helpers.py`
  - `tests/kernels/helion/test_autotune.py`
  - `tests/kernels/helion/test_pattern_matching.py`
  - `tests/kernels/helion/test_register.py`
  - `vllm/kernels/helion/ops/silu_mul_fp8.py`
  - `vllm/kernels/helion/register.py`

### Summary

**What changed and why**  
The PR refactors the Helion kernel registration API to move initialization logic (GPU platform detection, config file reading) from lazy evaluation into the `__init__` phase of `HelionKernelWrapper`. This makes the API more Dynamo/torch.compile-friendly by eliminating side effects during graph capture. The `config_picker` function is now a mandatory constructor argument instead of being registered lazily via a separate method.

**Technical impact**  
Kernel initialization now happens eagerly during wrapper construction, which improves compatibility with PyTorch compilation. The `HelionKernelWrapper` can now be disabled at construction time if configs are missing, with clear error messages. The API changes from a decorator with separate registration methods (`register_config_picker`, `register_input_generator`) to a single decorator call with all required parameters.

**Potential risks**  
The mandatory `config_picker` parameter breaks backward compatibility for existing kernel registrations. The eager initialization could cause performance overhead if many kernels are created but not used. The disabled wrapper state adds complexity to error handling paths, requiring callers to handle `RuntimeError` exceptions.

**Key insights**  
Developers must update all kernel registrations to pass `config_picker` (and optionally `input_generator`) directly to `register_kernel()`. The new initialization pattern ensures better torch.compile integration but requires careful testing of both custom-op and HOP paths. The disabled wrapper mechanism provides clearer diagnostics for missing configurations.

---

## 11. [[ROCm][Quantization] add fp8xfp8 attn support for rocm_aiter_unified_attn](https://github.com/vllm-project/vllm/pull/36927)


### Base Information

- **PR Number:** #36927
- **Author:** [divakar-amd](https://github.com/divakar-amd)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-17 17:49:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36927/files) (1):**
  - `vllm/v1/attention/backends/rocm_aiter_unified_attn.py`

### Summary

**What changed and why**  
This PR adds FP8×FP8 attention support for the ROCm AIter unified attention backend by handling quantization scale adjustments. Since the underlying triton kernel expects FP8 queries but doesn't support separate Q scales, the implementation absorbs `q_scale` and `k_scale` into `softmax_scale`, and either absorbs `v_scale` into `output_scale` or applies it post-attention.

**Technical impact**  
The changes enable mixed-precision FP8 attention computations in ROCm environments, improving performance for quantized models. The backend now dynamically adjusts scaling factors based on query dtype, maintaining numerical correctness while leveraging kernel constraints. This extends the quantization support introduced in PR #35316.

**Potential risks**  
If `output_scale` is provided but `v_scale` absorption leads to precision loss due to division, numerical stability could be affected. Edge cases where `layer._v_scale_float` is zero or extremely small may cause division errors or overflow. The post-attention rescale path adds a conditional operation that could impact performance uniformity.

**Key insights**  
Developers should verify that `layer._v_scale_float` is non-zero and well-conditioned to avoid numerical issues. The conditional rescale logic introduces a minor branching overhead; consider profiling to ensure it doesn't become a bottleneck. This implementation assumes the triton kernel's scaling behavior remains stable—any kernel updates may require revisiting these adjustments.

---

## 12. [[Kernel] Add non-gated support for NVFP4 CUTLASS MoE](https://github.com/vllm-project/vllm/pull/37320)


### Base Information

- **PR Number:** #37320
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-17 15:12:05
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37320/files) (8):**
  - `csrc/ops.h`
  - `csrc/quantization/w8a8/cutlass/moe/moe_data.cu`
  - `csrc/quantization/w8a8/cutlass/scaled_mm_entry.cu`
  - `csrc/torch_bindings.cpp`
  - `tests/evals/gsm8k/configs/moe-refactor/Nemotron-Nano-30B-NvFp4-ModelOpt-vllm-cutlass.yaml`
  - `tests/evals/gsm8k/configs/moe-refactor/config-b200.txt`
  - `vllm/_custom_ops.py`
  - `vllm/model_executor/layers/fused_moe/cutlass_moe.py`

### Summary

**What changed and why**  
Added support for non-gated MoE models (e.g., Nemotron-Nano) in the CUTLASS nvfp4 kernel by introducing an `is_gated` parameter to adjust the first GEMM's output dimension (`n` vs `2*n`). This allows the kernel to compute the correct problem size without an extra launch, and enables `no_act_and_mul` activation support in `CutlassExpertsFp4`.

**Technical impact**  
The changes modify the GEMM problem size calculation in the CUDA kernel to dynamically scale based on gating, affecting memory allocation and tensor shapes in the fused MoE pipeline. This extends the `--moe-backend=cutlass` compatibility to non-gated models while maintaining backward compatibility with existing gated activations.

**Potential risks**  
Incorrect `is_gated` flag propagation could lead to shape mismatches or silent errors in GEMM computations. The assumption that `w1_n = 2*n` for gated and `n` for non-gated must hold consistently across all call sites; any deviation may cause runtime failures or incorrect outputs.

**Key insights**  
Developers must ensure the `is_gated` flag is correctly derived from the model's activation type (e.g., via `activation.is_gated`). The changes are minimal and focused, but verification of tensor shape assertions in `run_cutlass_moe_fp4` is critical to prevent regressions. The added test configuration provides a validation baseline for non-gated models.

---

## 13. [[ROCm] Validate block_size for explicitly selected attention backends](https://github.com/vllm-project/vllm/pull/36846)


### Base Information

- **PR Number:** #36846
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [gshtras](https://github.com/gshtras)
- **Merged time:** 2026-03-17 15:04:40
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36846/files) (1):**
  - `vllm/platforms/rocm.py`

### Summary

**What changed and why**  
The change removes two lines that were stripping `block_size` from `attn_selector_config` before validating user-selected attention backends. This fixes a regression where explicit backend selection bypassed block_size validation, breaking the expected contract for backend support checks.

**Technical impact**  
Restoring block_size validation for explicitly selected backends ensures that user-specified backends are properly checked for compatibility with the given block_size. The auto-selection path remains unaffected, as block_size is still stripped later in that flow to allow for dynamic determination.

**Potential risks**  
If block_size is not properly validated in other code paths or if there are inconsistencies in how backends report support, this could lead to runtime errors or performance degradation when using explicitly selected backends with incompatible block sizes.

**Key insights**  
This fix highlights the importance of maintaining separate validation paths for auto-selection versus explicit user selection. Developers should ensure that contract-breaking changes are thoroughly tested across both selection modes to prevent similar regressions.

---

## 14. [[Bugfix][ROCm] Fix worker startup OOM on ROCm by skipping unreliable cudagraph memory profiling](https://github.com/vllm-project/vllm/pull/36720)


### Base Information

- **PR Number:** #36720
- **Author:** [JartX](https://github.com/JartX)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-03-17 14:55:34
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36720/files) (1):**
  - `vllm/v1/worker/gpu_worker.py`

### Summary

**What changed and why**  
The change disables CUDA graph memory profiling on ROCm/HIP platforms by adding a platform check. This prevents the use of `profile_cudagraph_memory()` on ROCm, where `torch.cuda.mem_get_info()` and graph pool handles produce unreliable or negative memory estimates that were causing OOM errors during worker startup.

**Technical impact**  
On ROCm, `cudagraph_memory_estimate` is now forced to zero, bypassing the problematic profiling logic and allowing accurate calculation of available KV cache memory. On CUDA platforms, behavior remains unchanged—profiling still occurs unless eager execution is enforced or the environment variable `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS` is false.

**Potential risks**  
If CUDA graphs are later enabled on ROCm, memory estimates may become inaccurate since profiling is skipped. The fix assumes ROCm’s graph memory behavior is negligible or consistent, which could lead to over- or under-allocation if this assumption fails. Additionally, platform-specific logic increases maintenance complexity.

**Key insights**  
This is a targeted workaround for a platform-specific bug, not a universal solution. Developers should monitor ROCm’s graph memory handling for future improvements and consider whether a more robust detection mechanism for unreliable profiling is needed across platforms. The change highlights the importance of validating memory profiling assumptions on different hardware backends.

---

## 15. [Make KV connector metadata build overridable via plugin](https://github.com/vllm-project/vllm/pull/37336)


### Base Information

- **PR Number:** #37336
- **Author:** [sarckk](https://github.com/sarckk)
- **Merged By:** [heheda12345](https://github.com/heheda12345)
- **Merged time:** 2026-03-17 14:29:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37336/files) (3):**
  - `tests/v1/core/utils.py`
  - `tests/v1/kv_connector/unit/test_scheduler_kv_connector_override.py`
  - `vllm/v1/core/sched/scheduler.py`

### Summary

**What changed and why**  
The PR extracts the `connector.build_connector_meta` call in the scheduler's `schedule` method into a separate `_build_kv_connector_meta` method. This allows plugins to override the method and inject custom metadata (like block hashes) into the `SchedulerOutput` without modifying the base struct. The test demonstrates a plugin that adds block hashes to the metadata.

**Technical impact**  
This change introduces a new extension point in the scheduler, enabling plugins to customize KV connector metadata generation. It maintains backward compatibility by defaulting to the original connector behavior. The test utilities are updated to support string-based KV connector configuration for easier plugin testing.

**Potential risks**  
If plugins incorrectly override `_build_kv_connector_meta`, they could break metadata generation or introduce side effects affecting scheduler output. The test uses monkey-patching on the scheduler class, which could interfere with other tests if not properly isolated. The dynamic addition of attributes to `SchedulerOutput` (like `block_hashes_by_req`) may cause type-checking issues.

**Key insights**  
This is a well-designed extension mechanism that keeps the core scheduler clean while enabling plugin-specific metadata. Developers should ensure plugin overrides are thread-safe and avoid modifying shared scheduler state. The test pattern provides a template for future plugin integrations, but care must be taken to reset global plugin state between tests.

---

## 16. [[Model] Add ColQwen3.5 4.5B support](https://github.com/vllm-project/vllm/pull/36887)


### Base Information

- **PR Number:** #36887
- **Author:** [athrael-soju](https://github.com/athrael-soju)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-17 14:17:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36887/files) (8):**
  - `docs/models/pooling_models.md`
  - `docs/models/supported_models.md`
  - `examples/pooling/score/colqwen3_5_rerank_online.py`
  - `tests/models/multimodal/pooling/test_colqwen3_5.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/colqwen3_5.py`
  - `vllm/model_executor/models/config.py`
  - `vllm/model_executor/models/registry.py`

### Summary

**What changed and why**  
This PR adds support for the ColQwen3.5 4.5B late-interaction model for multimodal document retrieval and reranking. The implementation follows the existing ColQwen3 pattern, introducing a new model class that extends Qwen3.5 with a ColBERT-style projection head to produce per-token L2-normalized embeddings for MaxSim scoring.

**Technical impact**  
The changes integrate a new multimodal pooling model into vLLM's architecture, enabling token-level embedding extraction and late-interaction scoring via existing `/rerank` and `/score` endpoints. The model leverages Qwen3.5's hybrid backbone and supports both text and image inputs, expanding vLLM's retrieval capabilities.

**Potential risks**  
The custom weight-loading logic for projection layers may be fragile if future model variants use different naming conventions. The forced use of `Qwen3VLProcessor` overrides the model's native processor, which could cause compatibility issues if the model expects different preprocessing. Additionally, the 462-second test runtime indicates significant computational overhead for validation.

**Key insights**  
Developers should note that ColQwen3.5 is registered as a pooling model and uses the same late-interaction scoring pattern as ColQwen3. The implementation includes robust testing for embedding normalization and scoring accuracy. Ensure any future updates to the model's processor or weight structure are reflected in the custom loading logic to maintain compatibility.

---

## 17. [[Models] Cohere ASR](https://github.com/vllm-project/vllm/pull/35809)


### Base Information

- **PR Number:** #35809
- **Author:** [ekagra-ranjan](https://github.com/ekagra-ranjan)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 14:04:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35809/files) (14):**
  - `examples/offline_inference/audio_language.py`
  - `tests/entrypoints/openai/correctness/test_transcription_api_correctness.py`
  - `tests/models/registry.py`
  - `vllm/benchmarks/datasets.py`
  - `vllm/entrypoints/openai/speech_to_text/protocol.py`
  - `vllm/inputs/data.py`
  - `vllm/inputs/preprocess.py`
  - `vllm/model_executor/models/cohere_asr.py`
  - `vllm/model_executor/models/registry.py`
  - `vllm/multimodal/processing/processor.py`
  - `vllm/renderers/base.py`
  - `vllm/transformers_utils/model_arch_config_convertor.py`
  - `vllm/transformers_utils/processors/__init__.py`
  - `vllm/transformers_utils/processors/cohere_asr.py`

### Summary

**What changed and why**  
This PR adds Cohere ASR (Automatic Speech Recognition) model support to vLLM. It implements the model architecture, processor, and tokenizer since Cohere ASR lacks a Hugging Face implementation. The changes leverage vLLM's existing variable-length encoder infrastructure to handle audio inputs of varying durations.

**Technical impact**  
The integration extends vLLM's multimodal capabilities to include a new encoder-decoder ASR model. It introduces a custom model class (`CohereASRForConditionalGeneration`), a feature extractor, and a processor for audio preprocessing. The modifications also update the encoder-decoder input pipeline to optionally skip the decoder start token (via `skip_decoder_start_token`) and adjust the transcription API to support broader `vllm_xargs` types.

**Potential risks**  
- The model weights are not yet publicly available (noted with TODOs), which could block immediate usage.  
- The custom processor and feature extractor are complex and may introduce audio preprocessing bugs or performance issues.  
- The `skip_decoder_start_token` logic, while necessary for Cohere ASR, could affect other encoder-decoder models if not properly isolated.

**Key insights**  
- Cohere ASR is integrated as a first-class model in vLLM, similar to other non-HF models.  
- Developers should ensure the `skip_decoder_start_token` flag is only applied to compatible models to avoid breaking existing encoder-decoder workflows.  
- The WER correctness test is temporarily disabled for Cohere ASR; it should be re-enabled once weights are released and validated.

---

## 18. [[Bugfix] Rescale NVFP4 weight scales to fix BF16 dequant underflow](https://github.com/vllm-project/vllm/pull/34577)


### Base Information

- **PR Number:** #34577
- **Author:** [ricky-chaoju](https://github.com/ricky-chaoju)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-17 13:48:43
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34577/files) (1):**
  - `vllm/model_executor/layers/quantization/utils/marlin_utils_fp4.py`

### Summary



---

## 19. [[Bugfix] Fix mock.patch resolution failure for standalone_compile.FakeTensorMode on Python <= 3.10](https://github.com/vllm-project/vllm/pull/37158)


### Base Information

- **PR Number:** #37158
- **Author:** [dbari](https://github.com/dbari)
- **Merged By:** [zou3519](https://github.com/zou3519)
- **Merged time:** 2026-03-17 13:13:06
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37158/files) (1):**
  - `vllm/compilation/compiler_interface.py`

### Summary

**What changed and why**  
The fix replaces `patch("torch._inductor.standalone_compile.FakeTensorMode", ...)` with `patch.object(sys.modules["torch._inductor.standalone_compile"], "FakeTensorMode", ...)`. This addresses a Python version discrepancy where string-based `mock.patch` incorrectly resolves to a wrapper function instead of the module in Python ≤3.10, causing an `AttributeError`.

**Technical impact**  
The change ensures consistent behavior across Python versions by directly targeting the module object from `sys.modules`. This prevents the crash during Torch Dynamo compilation when `FakeTensorMode` patching is required, maintaining compatibility with the existing compilation pipeline.

**Potential risks**  
If `torch._inductor.standalone_compile` is not present in `sys.modules` (e.g., due to lazy loading), the patch could fail. Additionally, the fix assumes the module structure remains stable; future changes to Torch’s internal organization could break this resolution.

**Key insights**  
Always use explicit module references when patching in cross‑version Python code. Consider adding a fallback or validation to ensure the module exists before patching. This fix highlights the importance of testing under all supported Python versions to catch environment‑specific issues early.

---

## 20. [[Perf] Set Flashinfer sparse MLA as default backend for FP8 kv cache](https://github.com/vllm-project/vllm/pull/37252)


### Base Information

- **PR Number:** #37252
- **Author:** [wzhao18](https://github.com/wzhao18)
- **Merged By:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged time:** 2026-03-17 13:09:21
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37252/files) (3):**
  - `docs/design/attention_backends.md`
  - `tools/pre_commit/generate_attention_backend_docs.py`
  - `vllm/platforms/cuda.py`

### Summary



---

## 21. [[Deprecation] Deprecate `--calculate-kv-scales` option](https://github.com/vllm-project/vllm/pull/37201)


### Base Information

- **PR Number:** #37201
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-17 12:57:24
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37201/files) (1):**
  - `vllm/config/cache.py`

### Summary

**What changed and why**  
The PR deprecates the `--calculate-kv-scales` CLI option by adding a deprecation warning to the field validator and updating the docstring. This feature dynamically calibrates FP8 KV cache scales but is not broadly useful and complicates dummy runs as a special case.

**Technical impact**  
The changes maintain backward compatibility while signaling future removal. The option still functions but now emits a `DeprecationWarning` when enabled, and the docstring clearly indicates deprecation status. Existing functionality remains unchanged until the planned removal in v0.19.

**Potential risks**  
If users rely on dynamic FP8 KV cache scale calibration, they will need to migrate to alternative approaches before v0.19. The warning could potentially be missed in automated environments if warnings are filtered or not monitored. There's also a risk that the deprecation warning might affect existing test suites that check for specific warning patterns.

**Key insights**  
This is a well-executed deprecation with clear communication through both runtime warnings and documentation. Developers should update any automation or scripts using this flag and prepare for its removal. The test plan appropriately focuses on warning emission and backward compatibility rather than testing the deprecated functionality itself.

---

## 22. [[Model] Remove unused `handle_oov_mm_token`](https://github.com/vllm-project/vllm/pull/37321)


### Base Information

- **PR Number:** #37321
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 12:44:53
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37321/files) (3):**
  - `tests/models/multimodal/processing/test_qwen2_5_omni_embed.py`
  - `vllm/model_executor/models/fireredasr2.py`
  - `vllm/model_executor/models/kimi_audio.py`

### Summary



---

## 23. [bump compressed-tensors version to 0.14.0.1](https://github.com/vllm-project/vllm/pull/36988)


### Base Information

- **PR Number:** #36988
- **Author:** [brian-dellabetta](https://github.com/brian-dellabetta)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-03-17 12:36:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36988/files) (1):**
  - `requirements/common.txt`

### Summary

**What changed and why**  
The PR updates the `compressed-tensors` dependency from version 0.13.0 to 0.14.0.1. This change is intended to incorporate a backwards-compatible release that adds support for offloading models during quantization, includes bug fixes, and enhances MXFP4 support.

**Technical impact**  
Upgrading to version 0.14.0.1 may introduce new features related to model offloading and improved quantization handling, which could affect compression workflows and memory usage during model quantization. The change is confined to a dependency version bump, so existing APIs and integrations should remain compatible as described.

**Potential risks**  
Although the release is noted as backwards-compatible, there is always a risk of unintended side effects from new bug fixes or enhanced features, such as changes in behavior for specific compression formats or edge cases in offloading. Additionally, any undiscovered issues in the new version could impact quantization processes.

**Key insights**  
Developers should verify that the new version's offloading and MXFP4 enhancements align with existing quantization pipelines. It is recommended to run relevant compression and quantization tests to ensure no regressions occur. Keeping the dependency pinned to a specific version helps maintain reproducibility, but monitoring for any downstream issues is advised.

---

## 24. [[Bug] Fix FlashInfer MNNVL socket collisions under concurrent vLLM jobs](https://github.com/vllm-project/vllm/pull/36674)


### Base Information

- **PR Number:** #36674
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-03-17 12:19:53
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36674/files) (1):**
  - `vllm/distributed/device_communicators/flashinfer_all_reduce.py`

### Summary

**What changed and why**  
The fix addresses socket collisions in FlashInfer MNNVL when multiple concurrent vLLM jobs run. The issue occurred because `random.randint(...)` was used with a deterministic global RNG seed (often `seed=0`) to generate IPC `opId`s for socket names, causing identical socket paths across jobs. The change temporarily seeds Python's random module with entropy from `os.urandom` during workspace creation to ensure unique socket identifiers.

**Technical impact**  
This modification localizes the random seed change to the `initialize_fi_ar_workspace` function, preserving the global RNG state for the rest of the application. It ensures that each vLLM job instance generates distinct IPC socket names, preventing `Address already in use` errors and collective state inconsistencies during distributed initialization.

**Potential risks**  
If `os.urandom` fails or returns insufficient entropy, the random seed may not be adequately unique, though this is rare. The `try/finally` block ensures the global RNG state is always restored, but any exception within the `try` block before the state restoration could leave the global RNG in an unexpected state—though the current structure minimizes this risk.

**Key insights**  
The fix is a targeted workaround for a concurrency issue in a third-party library (FlashInfer). Developers should be aware that this approach assumes the underlying `flashinfer_comm.create_allreduce_fusion_workspace` uses Python's `random` module. A more robust long-term solution would involve patching FlashInfer to use a proper random source or allow explicit `opId` specification.

---

## 25. [[Torch 2.11] Migrate torch._C._cpu calls to public   torch.cpu API](https://github.com/vllm-project/vllm/pull/35673)


### Base Information

- **PR Number:** #35673
- **Author:** [atalman](https://github.com/atalman)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-03-17 11:47:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35673/files) (9):**
  - `benchmarks/kernels/cpu/benchmark_cpu_attn.py`
  - `benchmarks/kernels/cpu/benchmark_cpu_fused_moe.py`
  - `tests/kernels/attention/test_cpu_attn.py`
  - `tests/kernels/moe/test_cpu_fused_moe.py`
  - `vllm/model_executor/kernels/linear/mixed_precision/cpu.py`
  - `vllm/model_executor/layers/fused_moe/cpu_fused_moe.py`
  - `vllm/model_executor/layers/quantization/cpu_wna16.py`
  - `vllm/model_executor/layers/utils.py`
  - `vllm/v1/attention/backends/cpu_attn.py`

### Summary

**What changed and why**  
This PR migrates all calls from the private `torch._C._cpu._is_amx_tile_supported()` API to the public `torch.cpu._is_amx_tile_supported()` API. The change is necessary because PyTorch 2.11 removed the private API, causing `AttributeError` crashes. The public API is available in both older and newer PyTorch versions, ensuring compatibility.

**Technical impact**  
The changes maintain identical functionality for AMX tile support detection across the codebase. This ensures the system correctly selects optimized CPU kernels (AMX vs. vectorized) based on hardware capabilities. The migration improves code maintainability by using stable public APIs instead of internal ones.

**Potential risks**  
There's a low risk if the public API behaves differently in edge cases, though this is unlikely given its purpose. The changes are widespread (9 files), so any missed instances of the private API could still cause crashes. The PR should be verified to cover all occurrences, including any dynamically generated or conditional imports.

**Key insights**  
Always prefer public PyTorch APIs over private `_C` modules for long-term stability. This migration pattern is already established in the PyTorch ecosystem, as seen in the linked torchao PR. Developers should audit their codebases for similar private API usage to prevent breakage during PyTorch upgrades.

---

## 26. [[CI] Split Distributed Tests (4 GPUs) and Kernel MoE tests](https://github.com/vllm-project/vllm/pull/37100)


### Base Information

- **PR Number:** #37100
- **Author:** [avinashsingh77](https://github.com/avinashsingh77)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-17 11:44:56
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37100/files) (2):**
  - `.buildkite/test_areas/distributed.yaml`
  - `.buildkite/test_areas/kernels.yaml`

### Summary

**What changed and why**  
The changes split the monolithic "Distributed (2 GPUs)" CI job into three smaller jobs (DP Tests, Compile+RPC, Torchrun+Shutdown) with 20-minute timeouts each, and increased the parallelism of the "Kernels MoE Test" from 2 to 5 while reducing its timeout from 60 to 25 minutes. This aims to improve CI reliability by reducing flakiness from timeouts and enabling faster, more parallel test execution.

**Technical impact**  
Splitting the distributed tests allows each job to focus on a specific test category, which can lead to more predictable runtimes and easier debugging. Increasing parallelism for MoE tests spreads the workload across more agents, potentially cutting overall CI duration. However, this also increases resource consumption (more concurrent GPU agents) and requires careful sharding to avoid test drops.

**Potential risks**  
The 20-minute timeout for each new distributed job may be too aggressive if tests grow or become slower, risking premature failures. Increasing MoE parallelism to 5 without adjusting shard logic could cause uneven test distribution or missed tests if the shard count isn't properly aligned. There's also a risk of accidentally dropping tests during the split if dependencies or commands are misassigned.

**Key insights**  
Verify that the total test count remains unchanged after the split to ensure no regressions. Monitor the new jobs for timeout issues; consider dynamic timeouts or further splitting if any exceed 20 minutes. Ensure the MoE test sharding (using `--shard-id`/`--num-shards`) correctly balances load across 5 parallel jobs to maximize efficiency.

---

## 27. [[Perf] Optimize top-k search in apply_top_k_top_p_triton sampler](https://github.com/vllm-project/vllm/pull/37225)


### Base Information

- **PR Number:** #37225
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-03-17 11:35:18
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37225/files) (1):**
  - `vllm/v1/sample/ops/topk_topp_triton.py`

### Summary



---

## 28. [[CI] Fix GPU memory leak when RemoteOpenAIServer fails to start in __init__](https://github.com/vllm-project/vllm/pull/37230)


### Base Information

- **PR Number:** #37230
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-17 09:08:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37230/files) (1):**
  - `tests/utils.py`

### Summary

**What changed and why**  
The changes fix a critical GPU memory leak that occurred when `RemoteOpenAIServer.__init__` failed (e.g., due to health-check timeouts). Previously, if initialization raised an exception, the `__exit__` method was never invoked, leaving server and EngineCore subprocesses alive and holding GPU memory. This caused cascading out-of-memory (OOM) failures in subsequent tests. The fix extracts cleanup logic into a shared `_shutdown()` method, improves orphaned-process termination, and removes silent fallbacks to ensure leaks cause immediate test failures.

**Technical impact**  
The code now guarantees cleanup in both normal and exceptional paths, preventing GPU memory leaks across test runs. The replacement of `_kill_orphaned_children` with `/proc`-based process group scanning ensures reliable cleanup even after parent process reaping. The stricter `_wait_for_gpu_memory_release` raises `RuntimeError` on timeout, making memory leaks visible immediately rather than allowing poisoned test environments. Increased timeouts accommodate slower model loading and GPU driver reclaim latency, particularly for ROCm.

**Potential risks**  
Scanning `/proc` is Linux-specific and may fail on non-Linux CI systems. The aggressive SIGKILL approach could theoretically affect unrelated processes sharing the same process group ID in edge cases. The removal of the stabilization fallback may increase test flakiness on shared GPU environments where background processes cause memory fluctuations, though this is intentional to surface leaks.

**Key insights**  
Always ensure resource cleanup in `__init__` exception handlers when using context managers. Relying on process group IDs and `/proc` is more robust than parent-child process tracking for cleanup. Failing fast on resource leaks is preferable to silent fallbacks that cause cascading failures. Consider platform compatibility if tests are run outside Linux.

---

## 29. [[Bugfix] Standardize custom HF Processor init](https://github.com/vllm-project/vllm/pull/37289)


### Base Information

- **PR Number:** #37289
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 08:38:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37289/files) (7):**
  - `vllm/model_executor/models/deepseek_ocr.py`
  - `vllm/model_executor/models/deepseek_ocr2.py`
  - `vllm/model_executor/models/glm4v.py`
  - `vllm/model_executor/models/qwen_vl.py`
  - `vllm/tokenizers/qwen_vl.py`
  - `vllm/transformers_utils/processors/glm4v.py`
  - `vllm/transformers_utils/processors/qwen_vl.py`

### Summary

**What changed and why**  
This PR standardizes Hugging Face processor initialization across vision-language models. It ensures per-request kwargs override config kwargs (fixed in DeepSeek OCR models) and separates image processor initialization from the main processor constructor for GLM4V and Qwen-VL to follow standard HF conventions.

**Technical impact**  
The changes create a more consistent initialization pattern where image processors are constructed separately before being passed to the main processor. This improves maintainability and aligns with Hugging Face's processor design patterns. The kwargs merging order fix ensures runtime parameters properly override default configurations.

**Potential risks**  
The QwenVLProcessor changes remove dynamic property access in favor of direct attribute assignment, which could break if the tokenizer lacks these attributes. The modified initialization signatures in processor classes could affect any external code that directly instantiates these processors with old parameter orders.

**Key insights**  
Developers should verify that all tokenizer instances have the required image tag attributes before processor initialization. The standardized pattern makes future processor implementations more predictable, but careful testing is needed for backward compatibility with existing model loading workflows.

---

## 30. [[BugFix] PyTorch Compilation Tests should error if any test fails](https://github.com/vllm-project/vllm/pull/37300)


### Base Information

- **PR Number:** #37300
- **Author:** [zou3519](https://github.com/zou3519)
- **Merged By:** [zou3519](https://github.com/zou3519)
- **Merged time:** 2026-03-17 08:26:39
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37300/files) (1):**
  - `.buildkite/test_areas/pytorch.yaml`

### Summary

**What changed and why**  
The change replaces a `find -exec` command with `find -print0 \| xargs -0 -n1 -I{}` in a Buildkite CI step for PyTorch compilation tests. This ensures the pipeline correctly propagates failure exit codes when any individual test fails, preventing silent passes.

**Technical impact**  
The fix addresses a known issue where `find -exec` does not propagate non-zero exit codes from executed commands, which could allow CI to pass even if tests fail. Using `xargs` with null-delimited input (`-print0` and `-0`) maintains safe handling of filenames with special characters while ensuring the overall step fails on test failures.

**Potential risks**  
If the `xargs` command is misconfigured (e.g., missing `-0` or `-n1`), it could break on filenames with spaces or newlines. Additionally, the change assumes all test files are Python files matching the pattern; any non-test files inadvertently included could cause unexpected behavior.

**Key insights**  
This is a critical fix for CI reliability, mirroring a proven solution from another PR. Developers should adopt the `-print0 \| xargs -0` pattern for similar `find`-based test executions to ensure exit code integrity. Verify that the command works across all supported environments, as `xargs` options may vary slightly between platforms.

---

## 31. [[Bugfix] Fix DP MTP Dummy Run](https://github.com/vllm-project/vllm/pull/35243)


### Base Information

- **PR Number:** #35243
- **Author:** [benchislett](https://github.com/benchislett)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-03-17 08:16:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35243/files) (1):**
  - `vllm/v1/worker/gpu_worker.py`

### Summary

**What changed and why**  
The fix addresses a DP dummy run failure when MTP (Multi-Token Prediction) is enabled. Previously, the dummy run used `num_tokens=1`, causing incorrect padding and a failed assertion in uniform batch decoding. The change ensures the dummy run uses the correct token count (`uniform_decode_query_len`), which equals `1 + num_speculative_tokens` when MTP is active.

**Technical impact**  
This ensures the dummy run’s token count aligns with the padded query length used during uniform batch decoding, preventing assertion failures. It maintains compatibility with speculative decoding configurations and avoids disrupting the DP initialization process.

**Potential risks**  
If `uniform_decode_query_len` is not properly initialized (e.g., missing or zero), the dummy run may still fail or use an invalid token count. Edge cases with non-uniform batch configurations or disabled MTP could also be affected if the attribute behaves unexpectedly.

**Key insights**  
Always validate padding logic when mixing uniform batch decoding with speculative methods. Consider adding a fallback (e.g., defaulting to 1) if `uniform_decode_query_len` is unavailable, and ensure dummy runs mirror actual execution parameters to avoid mismatches.

---

## 32. [[openapi] remove redundant exception stack trace[4/N]](https://github.com/vllm-project/vllm/pull/37157)


### Base Information

- **PR Number:** #37157
- **Author:** [andyxning](https://github.com/andyxning)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 08:06:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37157/files) (1):**
  - `vllm/entrypoints/openai/server_utils.py`

### Summary

**What changed and why**  
The PR modifies the exception handler in `server_utils.py` to replace `logger.exception` with `logger.error`. This change prevents duplicate stack traces in logs, as the full traceback is already logged by Starlette's `ServerErrorMiddleware`. The handler now logs only the request ID for context.

**Technical impact**  
This refactor eliminates redundant logging by separating concerns: the exception handler logs request metadata, while the middleware handles stack trace output. It reduces log noise and aligns with Starlette's error-handling architecture, where unhandled exceptions are re-raised and logged by the middleware layer.

**Potential risks**  
If `log_error_stack` is disabled, no stack trace will appear in logs at all, potentially hindering debugging. Additionally, any custom exception handlers that rely on the previous logging behavior may lose visibility into error details without adjusting their configuration.

**Key insights**  
Developers should ensure `log_error_stack` is enabled in production for debugging. This change simplifies log analysis by removing duplication, but teams must verify that all necessary error context is captured by the middleware or other monitoring tools.

---

## 33. [[`UltraVox`] Fix output type](https://github.com/vllm-project/vllm/pull/37224)


### Base Information

- **PR Number:** #37224
- **Author:** [vasqu](https://github.com/vasqu)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-17 07:51:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37224/files) (2):**
  - `vllm/model_executor/models/ultravox.py`
  - `vllm/transformers_utils/configs/ultravox.py`

### Summary

**What changed and why**  
The changes update the UltraVox model to handle a transformer refactor where layer outputs are now direct hidden states instead of tuples. Backward compatibility is maintained by checking if outputs are tuples and extracting the first element when needed. Additionally, a configuration attribute initialization order was fixed.

**Technical impact**  
These modifications ensure the UltraVox model remains compatible with both old (tuple output) and new (direct hidden state) transformer layer implementations. The changes affect the forward pass logic in both the main model and its encoder, making the code more robust to upstream API changes without breaking existing functionality.

**Potential risks**  
The type check `isinstance(hidden_states, tuple)` adds minimal overhead but could mask deeper issues if layers return unexpected tuple structures. There's also a risk that other parts of the codebase might still expect tuple outputs, potentially causing silent errors elsewhere.

**Key insights**  
The backward compatibility check is a pragmatic solution for transitional periods. Developers should verify that all transformer layers consistently return either tuples or hidden states across the entire codebase. Consider adding logging or warnings when tuple outputs are detected to aid in migration monitoring.

---

## 34. [[Chore] Replace all base64 usages with faster pybase64 package](https://github.com/vllm-project/vllm/pull/37290)


### Base Information

- **PR Number:** #37290
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 07:44:19
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37290/files) (37):**
  - `examples/online_serving/openai_chat_completion_client_for_multimodal.py`
  - `examples/online_serving/openai_realtime_client.py`
  - `examples/online_serving/openai_realtime_microphone_client.py`
  - `examples/pooling/embed/embedding_requests_base64_online.py`
  - `examples/pooling/embed/vision_embedding_online.py`
  - `examples/pooling/plugin/prithvi_geospatial_mae_io_processor.py`
  - `examples/pooling/plugin/prithvi_geospatial_mae_online.py`
  - `examples/pooling/score/colqwen3_rerank_online.py`
  - `examples/pooling/token_embed/colqwen3_token_embed_online.py`
  - `tests/benchmarks/test_random_multimodal_dataset_video.py`
  - `tests/distributed/test_weight_transfer.py`
  - `tests/entrypoints/openai/chat_completion/test_audio_in_video.py`
  - `tests/entrypoints/openai/chat_completion/test_vision_embeds.py`
  - `tests/entrypoints/openai/completion/test_completion_with_prompt_embeds.py`
  - `tests/entrypoints/openai/test_realtime_validation.py`
  - `tests/entrypoints/pooling/embed/test_cohere_online.py`
  - `tests/entrypoints/pooling/embed/test_cohere_online_vision.py`
  - `tests/entrypoints/pooling/embed/test_online.py`
  - `tests/entrypoints/pooling/embed/test_protocol.py`
  - `tests/entrypoints/pooling/pooling/test_online.py`
  - `tests/models/multimodal/generation/vlm_utils/model_utils.py`
  - `tests/models/multimodal/pooling/test_colpali.py`
  - `tests/models/multimodal/pooling/test_colqwen3.py`
  - `tests/models/multimodal/pooling/test_llama_nemotron_vl.py`
  - `tests/multimodal/media/test_audio.py`
  - `tests/multimodal/media/test_connector.py`
  - `tests/plugins/prithvi_io_processor_plugin/prithvi_io_processor/prithvi_processor.py`
  - `tests/plugins_tests/test_terratorch_io_processor_plugins.py`
  - `tests/renderers/test_sparse_tensor_validation.py`
  - `tools/pre_commit/check_forbidden_imports.py`
  - `vllm/benchmarks/datasets.py`
  - `vllm/distributed/weight_transfer/ipc_engine.py`
  - `vllm/entrypoints/openai/realtime/connection.py`
  - `vllm/entrypoints/openai/run_batch.py`
  - `vllm/entrypoints/pooling/embed/protocol.py`
  - `vllm/multimodal/media/audio.py`
  - `vllm/multimodal/media/video.py`

### Summary

**What changed and why**  
This PR replaces all `import base64` statements with `import pybase64 as base64` across 37 files to standardize on the faster `pybase64` package. It also adds `base64` to the forbidden imports checker to prevent future usage of the standard library module.

**Technical impact**  
The changes improve base64 encoding/decoding performance consistently across the codebase, particularly in multimodal and pooling-related components that frequently process media data. The import substitution maintains API compatibility while leveraging optimized C implementations.

**Potential risks**  
Some edge cases may arise if `pybase64` behaves differently from the standard library for malformed inputs or specific encoding scenarios. The forbidden import check could incorrectly flag legitimate uses if not carefully configured, though the current regex appears robust.

**Key insights**  
Developers should ensure `pybase64` is included in all relevant dependency lists. The change is largely transparent but warrants verification in performance-critical paths. Future imports of `base64` will be caught by pre-commit checks, enforcing consistency.

---

## 35. [[Misc][LoRA] Add --lora-target-modules to restrict LoRA to specific modules](https://github.com/vllm-project/vllm/pull/34984)


### Base Information

- **PR Number:** #34984
- **Author:** [bhoomit](https://github.com/bhoomit)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-03-17 07:36:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34984/files) (9):**
  - `docs/features/lora.md`
  - `tests/entrypoints/openai/test_cli_args.py`
  - `tests/lora/test_lora_manager.py`
  - `tests/lora/test_lora_utils.py`
  - `vllm/config/lora.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/lora/model_manager.py`
  - `vllm/lora/utils.py`
  - `vllm/lora/worker_manager.py`

### Summary

**What changed and why**  
This PR adds a `--lora-target-modules` CLI parameter and corresponding `LoRAConfig.target_modules` field to allow deployment-time restriction of LoRA application to specific model modules (e.g., `o_proj`, `qkv_proj`). The change enables performance tuning by reducing computational overhead when only a subset of modules require LoRA adaptation.

**Technical impact**  
The modification introduces a two‑stage filtering mechanism: first, modules are checked against the model’s supported LoRA modules; second, they are filtered by the deployment‑time `target_modules` list. This prevents unnecessary LoRA wrapper instantiation for excluded modules, directly reducing memory and compute overhead, as evidenced by the benchmark showing up to 47% TPOT reduction in eager mode.

**Potential risks**  
If users specify `target_modules` that exclude modules present in a loaded adapter, those adapter weights will be silently ignored (with a warning), potentially leading to unexpected model behavior. Additionally, the suffix‑based matching could inadvertently exclude modules with identical suffixes but different full paths if the model architecture uses nested naming conventions.

**Key insights**  
The implementation cleanly separates model‑level support checks from deployment‑time restrictions, ensuring backward compatibility when `target_modules` is `None`. Developers should note that adapter‑level restriction alone is ineffective—vLLM always wraps all supported modules unless explicitly filtered via this new parameter. The warning system helps detect mismatches between adapter contents and active modules.

---

## 36. [Fix Phi3 test that fails with Transformers v5](https://github.com/vllm-project/vllm/pull/37298)


### Base Information

- **PR Number:** #37298
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-17 07:29:24
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37298/files) (1):**
  - `tests/models/multimodal/pooling/test_phi3v.py`

### Summary

**What changed and why**  
A test for the Phi3 model was failing due to a missing import in Transformers v5. The function `is_flash_attn_greater_or_equal_2_10` was removed from the Transformers library, but custom modeling code for `TIGER-Lab/VLM2Vec-Full` still references it. This patch redefines the function locally in the test file to maintain compatibility.

**Technical impact**  
The change is isolated to the test environment and does not affect production code. It ensures the test can generate the Hugging Face reference output by providing a backward-compatible implementation of the deleted function, allowing the test to run successfully with both Transformers v4 and v5.

**Potential risks**  
If the patched function's behavior differs from the original, it could lead to inaccurate test results. There is also a risk that other tests or modules might depend on the same function, requiring similar patches elsewhere. The fix is temporary and may need to be revisited if Transformers introduces further breaking changes.

**Key insights**  
This is a targeted workaround for a test-specific dependency issue. Developers should be aware that the custom modeling code relies on a deprecated Transformers utility. Consider updating the modeling code or test references in the future to avoid similar issues. The patch is safe as it only affects test execution and not runtime behavior in vLLM.

---

## 37. [[Bugfix] Fix unclean shutdown crash with AllReduce Fusion workspace](https://github.com/vllm-project/vllm/pull/36955)


### Base Information

- **PR Number:** #36955
- **Author:** [siewcapital](https://github.com/siewcapital)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-17 07:22:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36955/files) (1):**
  - `vllm/distributed/device_communicators/flashinfer_all_reduce.py`

### Summary

**What changed and why**  
The changes address a crash during unclean shutdown when using AllReduce Fusion on B200 hardware. The issue occurred because global workspace objects (`_fi_ar_workspace`, `_fi_ar_quant_workspace`) were being cleaned up via `__del__` during Python interpreter shutdown, after the import system was already torn down. The fix registers an `atexit` handler to explicitly release these resources before shutdown begins, preventing the `ImportError`.

**Technical impact**  
This ensures deterministic cleanup of FlashInfer workspace resources during interpreter shutdown, aligning with existing patterns in the codebase (e.g., `pynccl_allocator.py`). The addition of a threading lock (`_fi_ar_workspace_lock`) guarantees thread-safe access to the global workspaces during cleanup, though the primary use case is single-threaded at exit.

**Potential risks**  
If `destroy_fi_ar_workspace` is called concurrently from multiple threads during normal operation (not just at exit), the lock could introduce minor overhead. Additionally, while the `atexit` handler is registered unconditionally, it assumes `flashinfer` modules remain importable until shutdown—this is safe given the handler runs early in shutdown sequencing.

**Key insights**  
The fix is minimal and follows established best practices for resource cleanup in Python. Developers should note that similar global resource patterns elsewhere in the codebase may benefit from analogous `atexit` registration. The lock ensures idempotency and thread safety, but its impact is negligible since cleanup typically occurs once at shutdown.

---

## 38. [pick up tuned prefill configs for FP8 FA3](https://github.com/vllm-project/vllm/pull/36265)


### Base Information

- **PR Number:** #36265
- **Author:** [jmkuebler](https://github.com/jmkuebler)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-17 07:00:27
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36265/files) (1):**
  - `cmake/external_projects/vllm_flash_attn.cmake`

### Summary

**What changed and why**  
Updated the Git commit hash for the `vllm-flash-attn` dependency from `1488682bb545f7d020e958a33116b1419d1cfc83` to `29210221863736a08f71a866459e368ad1ac4a95`. This change pulls in a newer version of the flash-attention repository to incorporate tuned prefill configurations for FP8 FA3, as referenced in the linked PR.

**Technical impact**  
This updates the underlying flash-attention library to a version that includes performance optimizations for FP8 operations, specifically targeting prefill phase improvements. The change may affect computational efficiency and memory usage during attention computations in FP8 precision mode.

**Potential risks**  
The new commit may introduce regressions or compatibility issues if the updated flash-attention version has breaking changes not fully aligned with vLLM's current integration. Additionally, any undiscovered bugs in the referenced PR could propagate into the main build.

**Key insights**  
Always verify that the new flash-attention version maintains API compatibility with existing vLLM code. Consider running extended integration tests beyond CI to ensure stability, and monitor performance benchmarks to confirm the expected gains in FP8 prefill operations.

---

## 39. [[Frontend] Complete OpenAI render delegation](https://github.com/vllm-project/vllm/pull/37287)


### Base Information

- **PR Number:** #37287
- **Author:** [sagearc](https://github.com/sagearc)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 06:53:56
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37287/files) (11):**
  - `tests/entrypoints/openai/chat_completion/test_chat_error.py`
  - `tests/entrypoints/openai/test_serving_responses.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/openai/engine/serving.py`
  - `vllm/entrypoints/openai/generate/api_router.py`
  - `vllm/entrypoints/openai/responses/serving.py`
  - `vllm/entrypoints/pooling/__init__.py`
  - `vllm/entrypoints/pooling/pooling/serving.py`
  - `vllm/entrypoints/serve/disagg/serving.py`
  - `vllm/entrypoints/serve/render/serving.py`
  - `vllm/entrypoints/serve/tokenize/serving.py`

### Summary

**What changed and why**  
This PR completes the frontend disaggregation by delegating all remaining OpenAI preprocessing methods from `OpenAIServing` to `OpenAIServingRender`. The base class methods (`_preprocess_completion`, `_preprocess_cmpl`, `_preprocess_chat`, `_validate_chat_template`, `_render_next_turn`, and `_generate_with_builtin_tools`) are removed, and dependent classes now call the render layer directly. This consolidates preprocessing logic into a single GPU-less render layer, eliminating duplication.

**Technical impact**  
The changes centralize preprocessing logic in `OpenAIServingRender`, making it the canonical source for rendering across all OpenAI-compatible endpoints. This simplifies the architecture by removing redundant code from `OpenAIServing` and ensures consistent behavior between generate-mode and render-only servers. The refactor also improves maintainability, as future preprocessing updates need only be applied in one location.

**Potential risks**  
The removal of base class methods could introduce regression if any downstream code still references them, though the PR updates all callers. The delegation adds a dependency on `OpenAIServingRender` being properly initialized in all serving classes; missing injections (e.g., in tests) could cause runtime failures. Additionally, the `tool_parser` logic now supports `ResponsesRequest` in `OpenAIServingRender.preprocess_chat`, which must be validated for correctness.

**Key insights**  
Developers should ensure `OpenAIServingRender` is injected into all dependent classes during initialization, as seen in the updated constructors. The render layer’s public methods (`preprocess_*`, `validate_chat_template`) are now the single source of truth for preprocessing. When extending preprocessing logic or adding new request types, modifications should be made exclusively in `OpenAIServingRender` to maintain architectural consistency.

---

## 40. [[Misc] Use VLLMValidationError in batch, pooling, and tokenize protocol validators](https://github.com/vllm-project/vllm/pull/36256)


### Base Information

- **PR Number:** #36256
- **Author:** [umut-polat](https://github.com/umut-polat)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 06:52:30
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36256/files) (3):**
  - `vllm/entrypoints/openai/run_batch.py`
  - `vllm/entrypoints/pooling/base/protocol.py`
  - `vllm/entrypoints/serve/tokenize/protocol.py`

### Summary

**What changed and why**  
This PR replaces `ValueError` with `VLLMValidationError` in protocol-level model validators across three files: batch transcription/translation requests, pooling, and tokenize endpoints. The change standardizes error handling for client input validation by using a framework-specific exception type, completing a migration series across all OpenAI-compatible endpoints.

**Technical impact**  
The changes improve error consistency by using `VLLMValidationError` which likely provides structured error information (including parameter names in some cases) that can be properly formatted in API responses. This enables better error categorization and potentially more informative error messages for API consumers compared to generic `ValueError` exceptions.

**Potential risks**  
The parameter name is only specified in batch validators (`parameter="file"`) but not in the pooling/tokenize validators, creating inconsistency in error reporting. There's also a risk if upstream error handling code doesn't properly handle the new exception type, though previous PRs in the series suggest this migration is well-established.

**Key insights**  
This completes a systematic migration to framework-specific validation errors. Developers should ensure consistent use of the `parameter` argument across all `VLLMValidationError` instances for uniform error reporting. The changes are minimal and focused, maintaining backward compatibility while improving error specificity.

---

## 41. [[1/2] Move InternVL-based processors](https://github.com/vllm-project/vllm/pull/37260)


### Base Information

- **PR Number:** #37260
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 06:50:56
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37260/files) (20):**
  - `tests/models/multimodal/processing/test_h2ovl.py`
  - `tests/models/multimodal/processing/test_internvl.py`
  - `tests/models/multimodal/processing/test_nemotron_vl.py`
  - `vllm/model_executor/models/eagle2_5_vl.py`
  - `vllm/model_executor/models/h2ovl.py`
  - `vllm/model_executor/models/internvl.py`
  - `vllm/model_executor/models/nano_nemotron_vl.py`
  - `vllm/model_executor/models/nemotron_parse.py`
  - `vllm/model_executor/models/nemotron_vl.py`
  - `vllm/model_executor/models/nvlm_d.py`
  - `vllm/model_executor/models/skyworkr1v.py`
  - `vllm/transformers_utils/processors/__init__.py`
  - `vllm/transformers_utils/processors/eagle2_5_vl.py`
  - `vllm/transformers_utils/processors/h2ovl.py`
  - `vllm/transformers_utils/processors/internvl.py`
  - `vllm/transformers_utils/processors/nano_nemotron_vl.py`
  - `vllm/transformers_utils/processors/nemotron_parse.py`
  - `vllm/transformers_utils/processors/nemotron_vl.py`
  - `vllm/transformers_utils/processors/nvlm_d.py`
  - `vllm/transformers_utils/processors/skyworkr1v.py`

### Summary

**What changed and why**  
This PR moves processor classes for InternVL-based multimodal models (Eagle2.5-VL, H2OVL, InternVL, NanoNemotronVL, NemotronParse, NemotronVL, NVLM-D, SkyworkR1V) from their respective modeling files to a new `vllm/transformers_utils/processors/` directory. The goal is to improve code readability and prepare for future refactoring to separate image-specific and video-specific logic in a Transformers-like style, making it easier to call them with token inputs later.

**Technical impact**  
The change centralizes processor logic into a dedicated module, reducing duplication and improving maintainability. Model files now import processors from the new location, and the `__init__.py` exports the moved classes. This refactor does not alter the external API or behavior—processors remain functionally identical but are now organized under a common structure. Test files are updated to reflect the new import paths.

**Potential risks**  
There is a risk of import errors if any downstream code relies on the old import paths from model files. The large number of moved files increases the chance of missing an import update or introducing subtle differences in processor initialization. Additionally, the PR description lacks test results, making it harder to verify that the refactor doesn’t break existing functionality.

**Key insights**  
This is a foundational step toward a cleaner separation of concerns in multimodal processing. Developers should update any direct imports from model files to use the new processor module. Ensure all processor dependencies (e.g., PIL, torchvision) are correctly handled in the new location. Future PRs should include comprehensive testing to validate that the moved processors behave identically to their previous versions.

---

## 42. [Bugfix for offloading+prefetch for GLM-4.7-FP8](https://github.com/vllm-project/vllm/pull/37178)


### Base Information

- **PR Number:** #37178
- **Author:** [sfbemerk](https://github.com/sfbemerk)
- **Merged By:** [esmeetu](https://github.com/esmeetu)
- **Merged time:** 2026-03-17 06:22:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37178/files) (1):**
  - `vllm/model_executor/offloader/prefetch.py`

### Summary

**What changed and why**  
This fix addresses an AttributeError when using offload+prefetch with the GLM-4.7-FP8 model, where transient parameters (like `k_scale`/`v_scale`) created during KV-cache initialization are deleted after processing. The error occurred because the offloader tried to sync CPU storage for parameters that no longer exist. The solution marks such offloaders as deleted and prunes them from the offloading pool.

**Technical impact**  
The changes modify the offloading prefetch logic to handle transient parameters gracefully. A new `_param_deleted` flag is added to `_ParamOffloader` to track deleted parameters, and `_ModuleOffloader.sync_cpu_storage()` now cleans up stale offloaders. This ensures buffer-pool allocation and prefetching only involve persistent parameters.

**Potential risks**  
If the `AttributeError` catching is too broad, it might mask legitimate attribute access issues unrelated to parameter deletion. Additionally, improper cleanup of deleted offloaders could lead to memory leaks if references persist elsewhere. The fix assumes transient parameters are always deleted after `process_weights_after_loading`, which may not hold for all model architectures.

**Key insights**  
Developers should ensure that parameter lifecycle management (creation/deletion) aligns with the offloader’s assumptions. The fix is model-specific (GLM-4.7-FP8) but may benefit other models with similar transient parameters. Consider adding logging or metrics to monitor pruned offloaders in production to catch unexpected behavior.

---

## 43. [Add gigachat 3.1 tool parser + fix gigachat3 tool parser](https://github.com/vllm-project/vllm/pull/36664)


### Base Information

- **PR Number:** #36664
- **Author:** [ajpqs](https://github.com/ajpqs)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-03-17 05:03:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36664/files) (2):**
  - `tests/entrypoints/openai/tool_parsers/test_gigachat3_tool_parser.py`
  - `vllm/tool_parsers/gigachat3_tool_parser.py`

### Summary

**What changed and why**  
This PR adds a tool parser for Gigachat 3.1 models and fixes the existing parser for Gigachat 3 models. The changes ensure proper parsing of function‑call outputs that use model‑specific special tokens (`<\|message_sep\|>`, `<\|role_sep\|>`, `<\|function_call\|>`). Previously, the parser did not handle these tokens correctly, leading to incorrect extraction of tool calls.

**Technical impact**  
The modifications introduce two distinct regex patterns to match the output formats of Gigachat 3 and 3.1, and they adjust the streaming and non‑streaming extraction logic accordingly. The parser now correctly separates content from tool‑call JSON, handles end‑of‑sequence tokens, and validates JSON incrementally during streaming. The test suite has been expanded to cover both model versions and edge cases like mixed content, parameterless calls, and large streaming steps.

**Potential risks**  
If the special tokens are not consistently added to the tokenizer’s vocabulary, parsing may fail. The regex‑based approach assumes a strict output format; any deviation in the model’s generation (e.g., extra whitespace or unexpected token ordering) could lead to missed tool calls or incorrect content extraction. Additionally, the streaming logic relies on incremental JSON validation, which might be fragile if the model streams JSON in non‑standard chunks.

**Key insights**  
Developers should ensure that the tokenizer is always configured with the required special tokens when using these parsers. The updated tests provide a comprehensive reference for expected behavior—use them to validate any future changes. Consider adding more robust JSON parsing (e.g., using a stateful parser) if the model output format becomes more complex or variable.

---

## 44. [[perf][connector] optimize build_connector_meta when host buffer transfer is not used](https://github.com/vllm-project/vllm/pull/37165)


### Base Information

- **PR Number:** #37165
- **Author:** [youkaichao](https://github.com/youkaichao)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-03-17 04:59:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37165/files) (1):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl_connector.py`

### Summary

**What changed and why**  
The PR optimizes `build_connector_meta` by extracting the logic that processes `scheduler_output` into a new helper method `_build_save_meta`. This processing is only required when host buffer transfer is enabled (`self.use_host_buffer` is True). The change avoids iterating over potentially hundreds of items in `scheduler_output` when host buffer transfer is not used, improving performance.

**Technical impact**  
This refactoring reduces unnecessary computational overhead in scenarios where host buffer transfer is disabled. The main `build_connector_meta` method now conditionally calls `_build_save_meta` only when needed, while the core request metadata building logic remains unchanged. This maintains the same functional behavior but with better performance for non-host-buffer configurations.

**Potential risks**  
If the `self.use_host_buffer` flag is incorrectly set or if there are hidden dependencies between the extracted logic and other parts of `build_connector_meta`, it could lead to incomplete metadata generation. The refactoring assumes a clean separation between host-buffer-specific and general metadata building, which should be validated through thorough testing.

**Key insights**  
This is a targeted performance optimization that follows good separation of concerns by isolating host-buffer-specific logic. Developers should ensure that the `use_host_buffer` configuration is correctly propagated and tested in all deployment scenarios. The pattern of conditionally executing expensive operations based on feature flags is a useful performance optimization technique.

---

## 45. [[Frontend] Delegate tokenization serving preprocessing to OpenAIServingRender](https://github.com/vllm-project/vllm/pull/37266)


### Base Information

- **PR Number:** #37266
- **Author:** [sagearc](https://github.com/sagearc)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 04:22:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37266/files) (5):**
  - `tests/entrypoints/openai/chat_completion/test_chat_error.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/openai/generate/api_router.py`
  - `vllm/entrypoints/serve/render/serving.py`
  - `vllm/entrypoints/serve/tokenize/serving.py`

### Summary

**What changed and why**  
This PR centralizes preprocessing logic by making `OpenAIServingTokenization` delegate to `OpenAIServingRender` instead of inheriting duplicate methods from `OpenAIServing`. It also moves `OpenAIServingRender` construction earlier to `init_app_state` so it's shared across all serving classes from startup.

**Technical impact**  
The changes eliminate code duplication by having tokenization endpoints use the same preprocessing implementation as chat/completion endpoints. Moving `OpenAIServingRender` initialization earlier ensures consistent preprocessing behavior across all OpenAI-compatible endpoints and simplifies dependency injection.

**Potential risks**  
The method name changes from `_preprocess_chat` to `preprocess_chat` (and similar for other methods) could break external code that directly calls these now-public methods. The test file modification shows mock setup needed updating, indicating potential test maintenance overhead.

**Key insights**  
This is a clean architectural improvement that establishes `OpenAIServingRender` as the single source of truth for preprocessing. Developers should note the method visibility change (private to public) and ensure all endpoint classes consistently delegate preprocessing through this shared component rather than implementing their own logic.

---

## 46. [[Bugfix](xpu): prevent “selected index k out of range” in TP decode path](https://github.com/vllm-project/vllm/pull/37259)


### Base Information

- **PR Number:** #37259
- **Author:** [zhejiangxiaomai](https://github.com/zhejiangxiaomai)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-03-17 04:14:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37259/files) (1):**
  - `vllm/_xpu_ops.py`

### Summary

**What changed and why**  
The fix addresses a runtime error in XPU tensor parallel (TP) inference where the decode path's top-k operation could request more tokens (`k`) than the available logits dimension on a shard. This occurred because TP sharding reduces the logits width per device. The solution clamps the `k` value to the actual logits dimension before calling `topk`, mirroring existing defensive logic in the prefill path.

**Technical impact**  
This change ensures robustness in the decode phase under tensor parallelism by preventing out-of-range tensor accesses. It aligns the decode path's error handling with the prefill path, maintaining consistency across different inference stages and supporting stable execution when `topk_tokens` exceeds the shard's logits width.

**Potential risks**  
If `topk_tokens` is consistently larger than the shard's logits width, the effective `k` will be reduced, potentially altering the model's sampling behavior (e.g., fewer candidates considered). This could affect output quality in edge cases, though it prioritizes avoiding crashes. The fix assumes the masking logic (`masked_fill`) correctly handles invalid indices afterward.

**Key insights**  
Developers should verify that the clamping behavior is intentional for all TP configurations and consider whether a warning should be logged when `real_topk < topk_tokens`. This fix highlights the importance of validating tensor dimensions in sharded environments, especially for operations like `topk` that depend on the last dimension's size.

---

## 47. [[Feature]: Support for multiple embedding types in a single inference call](https://github.com/vllm-project/vllm/pull/35829)


### Base Information

- **PR Number:** #35829
- **Author:** [staugust](https://github.com/staugust)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-17 02:05:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35829/files) (7):**
  - `tests/plugins/bge_m3_sparse_plugin/bge_m3_sparse_processor/sparse_embeddings_processor.py`
  - `tests/plugins/bge_m3_sparse_plugin/bge_m3_sparse_processor/types.py`
  - `tests/plugins_tests/test_bge_m3_sparse_io_processor_plugins.py`
  - `vllm/model_executor/layers/pooler/special.py`
  - `vllm/model_executor/models/roberta.py`
  - `vllm/pooling_params.py`
  - `vllm/tasks.py`

### Summary

**What changed and why**  
This PR adds support for generating both sparse and dense embeddings in a single inference call for the BGE-M3 model. The changes extend the existing sparse embedding processor to handle multiple embedding types (sparse, dense, or both) by introducing a new `embed_task` parameter and modifying the pooling layer to combine outputs.

**Technical impact**  
The architecture now supports a unified `embed&token_classify` pooling task, which concatenates dense and sparse embedding outputs. The processor dynamically configures pooling parameters based on the requested task and validates dimensions for matryoshka models. This reduces inference overhead when both embedding types are needed.

**Potential risks**  
The `embed_request_queue` management assumes sequential request processing; concurrent or out-of-order requests could cause mismatches. The dimension validation logic may incorrectly reject valid configurations if `matryoshka_dimensions` is empty but dimensions are set. The removal of task validation in `pooling_params.py` (line 99) could allow invalid tasks if plugins misconfigure them.

**Key insights**  
Developers should ensure thread safety for the `embed_request_queue` in concurrent scenarios. The new `BgeM3Pooler` demonstrates a composable pattern for multi-task pooling. When extending this feature, validate that `embed_task` and pooling parameters are consistently propagated across online/offline request paths.

---

## 48. [[Bugfix][ResponsesAPI] Fix crash when tool_choice=required exceeds max_output_tokens](https://github.com/vllm-project/vllm/pull/37258)


### Base Information

- **PR Number:** #37258
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-17 01:54:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37258/files) (2):**
  - `tests/entrypoints/openai/responses/test_function_call.py`
  - `vllm/parser/abstract_parser.py`

### Summary

**What changed and why**  
This PR fixes a crash that occurs when `tool_choice="required"` is set but the generated output exceeds `max_output_tokens`. The crash happened because the parser attempted to validate incomplete or malformed JSON as a tool call. The fix adds graceful error handling to skip validation when the content is invalid, aligning the behavior with OpenAI's API.

**Technical impact**  
The changes modify the `_parse_tool_calls` method in `abstract_parser.py` to suppress `ValidationError` when parsing fails, returning an empty tool call list instead. This ensures that incomplete outputs due to token limits do not cause server crashes and produce a consistent `incomplete_details` response. A new test validates this behavior under token-constrained conditions.

**Potential risks**  
Suppressing validation errors could mask legitimate parsing issues unrelated to token limits, making debugging harder. The fix assumes that any validation failure is due to truncated output, which may not always be true. Additionally, the empty content check (`content = content or ""`) might obscure cases where `content` is `None` for other reasons.

**Key insights**  
The fix prioritizes stability over strict validation when outputs are truncated, which is appropriate for handling `max_output_tokens` gracefully. Developers should ensure that validation errors are logged or monitored to distinguish between expected truncations and actual parsing bugs. The test provides a good pattern for verifying API compatibility with OpenAI under constrained generation scenarios.

---

## 49. [Fix infinite recursive search issue in quark.py](https://github.com/vllm-project/vllm/pull/32779)


### Base Information

- **PR Number:** #32779
- **Author:** [xiao-llm](https://github.com/xiao-llm)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-17 00:19:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/32779/files) (1):**
  - `vllm/model_executor/layers/quantization/quark/quark.py`

### Summary

**What changed and why**  
The fix addresses an infinite recursion issue in `_find_matched_config()` when processing sharded modules. The original code recursively called itself for all shard names, including the original layer name, leading to unbounded recursion. The change adds a conditional check to use a global quant config when the shard name matches the original layer name, breaking the recursion.

**Technical impact**  
This prevents `RecursionError` in environments with sharded model configurations (e.g., ROCm/VLLM with FP8 models). The recursion depth is now bounded, ensuring stable execution for quantized models. The logic preserves the original behavior for distinct shard names while handling self-referential cases gracefully.

**Potential risks**  
If the global quant config is missing or incorrectly structured, the fallback may cause quantization errors. The fix assumes shard names are derived predictably; unexpected naming schemes could bypass the condition and reintroduce recursion. Additionally, the deep comparison of shard configs may become a performance bottleneck with many shards.

**Key insights**  
Always validate recursion termination conditions in hierarchical searches. Consider adding a recursion depth limit or iterative approach for robustness. Review shard naming conventions in related modules to ensure consistency. Test with varied model architectures to confirm the fix generalizes across environments.

---

