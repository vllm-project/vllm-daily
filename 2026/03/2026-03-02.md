# vLLM Merged PR Report

**Report Date:** 2026-03-02 PST

**Total Merged PRs:** 47

---

## 1. [add regression test](https://github.com/vllm-project/vllm/pull/35834)


### Base Information

- **PR Number:** #35834
- **Author:** [hallerite](https://github.com/hallerite)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 23:32:15
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35834/files) (1):**
  - `tests/entrypoints/openai/test_tokenization_vlm.py`

### Summary

**What changed and why**  
A new regression test was added to verify that the `/tokenize` endpoint correctly expands image placeholder tokens for Vision-Language Models (VLMs). This test ensures that image placeholders in multimodal messages are processed into the expected number of tokens, addressing a bug where only ~26 tokens were returned instead of the correct 1451.

**Technical impact**  
The test validates that the multimodal processor is properly integrated into the tokenization flow for VLMs. It confirms that the fix from PR #34560 is effective and prevents regression in future releases. The test uses a real VLM model with an image asset to simulate actual tokenization behavior.

**Potential risks**  
The test relies on a specific image (`stop_sign.jpg`) producing exactly 1451 tokens, which may be sensitive to changes in the model's tokenizer or image processing pipeline. If the model or tokenizer version changes, the expected token count may need adjustment. Additionally, the test requires external dependencies (model download, asset server) which could introduce flakiness in CI environments.

**Key insights**  
This test is critical for ensuring multimodal tokenization correctness in the OpenAI-compatible API. Developers should update the expected token count if the underlying model or tokenizer is updated. Consider adding tolerance ranges or checks for token count ranges if exact counts are too fragile, and ensure the test environment consistently provides the required assets.

---

## 2. [[ROCm] [Release] Change the package from `aiter` to `amd-aiter`](https://github.com/vllm-project/vllm/pull/35198)


### Base Information

- **PR Number:** #35198
- **Author:** [tjtanaa](https://github.com/tjtanaa)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-02 23:13:40
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35198/files) (2):**
  - `.buildkite/scripts/annotate-rocm-release.sh`
  - `tools/vllm-rocm/pin_rocm_dependencies.py`

### Summary

**What changed and why**  
The PR updates references from the old package name `aiter` to the new name `amd-aiter` (and `amd_aiter` in wheel filenames). This change aligns with an upstream update where the AITER library renamed its package.

**Technical impact**  
The modifications ensure compatibility with the renamed package by updating both the release annotation script (`annotate-rocm-release.sh`) and the dependency pinning logic (`pin_rocm_dependencies.py`). This affects ROCm release pipelines and dependency resolution during builds.

**Potential risks**  
If the new `amd-aiter` package is not available in the specified S3 bucket or has incompatible API changes, installation or runtime errors may occur. Additionally, any hardcoded references to `aiter` elsewhere in the codebase could cause failures.

**Key insights**  
Developers should verify that all references to `aiter` (e.g., in imports, configuration files, or documentation) are updated. The test plan should include validating the installation and functionality of the new package in a ROCm environment.

---

## 3. [Fix TYPE_CHECKING stub defaults in envs.py to match actual runtime defaults](https://github.com/vllm-project/vllm/pull/35645)


### Base Information

- **PR Number:** #35645
- **Author:** [lin-shh](https://github.com/lin-shh)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-02 21:59:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35645/files) (1):**
  - `vllm/envs.py`

### Summary

**What changed and why**  
Updated three environment variable stub defaults in the TYPE_CHECKING block to match their actual runtime defaults. The variables `VLLM_USAGE_SOURCE`, `VLLM_CPU_OMP_THREADS_BIND`, and `VLLM_USE_BYTECODE_HOOK` previously had incorrect stub values, which misled static type checkers and IDE tooling.

**Technical impact**  
This change ensures static analysis tools (mypy, pyright) and IDE autocompletion/documentation features display accurate default values for these environment variables. No runtime behavior is altered, as the actual environment variable resolution logic in the `environment_variables` dictionary remains unchanged.

**Potential risks**  
The primary risk is minimal, as this is a documentation/stub-only fix. However, developers who previously relied on the incorrect stub values in their IDE for understanding defaults may have had inaccurate assumptions. There is no risk of breaking existing code or changing system behavior.

**Key insights**  
Always ensure TYPE_CHECKING stubs accurately reflect runtime behavior to maintain developer tooling reliability. Consider implementing automated checks to validate consistency between stub defaults and runtime lambda defaults during CI/CD. The fix for `VLLM_USE_BYTECODE_HOOK` is particularly important since it changes from `False` to `True`, which could significantly affect developer expectations.

---

## 4. [[Misc] Fix typos in comments: explict→explicit, paramaters→parameters](https://github.com/vllm-project/vllm/pull/35648)


### Base Information

- **PR Number:** #35648
- **Author:** [lin-shh](https://github.com/lin-shh)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-02 21:59:14
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35648/files) (3):**
  - `vllm/model_executor/layers/fused_moe/oracle/fp8.py`
  - `vllm/model_executor/layers/fused_moe/oracle/nvfp4.py`
  - `vllm/model_executor/layers/fused_moe/router/router_factory.py`

### Summary

**What changed and why**  
Three spelling errors in code comments were corrected: "explict" to "explicit" in two fused MoE kernel files, and "paramaters" to "parameters" in a router factory file. These changes improve documentation clarity without altering any functional code.

**Technical impact**  
The changes have no impact on system behavior, performance, or architecture since they only affect comments. They enhance readability and maintain professional code documentation standards.

**Potential risks**  
There are no functional risks as the modifications are purely textual and confined to comments. However, any future global text searches for the incorrect spellings might miss these updated locations.

**Key insights**  
While minor, consistent spelling in comments aids readability and reduces cognitive load for developers. It's good practice to periodically review documentation for such issues, possibly via automated linting tools.

---

## 5. [[MISC] Removed unused function find_all_indices() from tool_parsers/utils.py](https://github.com/vllm-project/vllm/pull/35683)


### Base Information

- **PR Number:** #35683
- **Author:** [taneem-ibrahim](https://github.com/taneem-ibrahim)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 21:58:42
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35683/files) (1):**
  - `vllm/tool_parsers/utils.py`

### Summary

**What changed and why**  
Removed the unused `find_all_indices()` function from `tool_parsers/utils.py` to eliminate dead code. The function was identified as not being referenced anywhere in the codebase, so its removal helps reduce clutter and maintenance overhead.

**Technical impact**  
This change has no functional impact on the system since the function was never called. It simplifies the module by removing unnecessary code, which may slightly improve readability and reduce the cognitive load for developers working in this file.

**Potential risks**  
The primary risk is minimal—if the function was indirectly used via dynamic imports or reflection, it could cause runtime errors. However, given the PR description states it's unused, this is unlikely. Another consideration is that any future feature needing similar substring indexing logic would require reimplementation.

**Key insights**  
This is a straightforward cleanup that aligns with good code hygiene practices. Developers should ensure removal is verified via static analysis or grep to confirm no references exist. Consider adding a linter rule to flag unused functions proactively to prevent similar accumulation of dead code.

---

## 6. [[BugFix] Fix cmake based incremental install (wrong vllm install dir)](https://github.com/vllm-project/vllm/pull/35773)


### Base Information

- **PR Number:** #35773
- **Author:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-02 21:58:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35773/files) (1):**
  - `cmake/external_projects/vllm_flash_attn.cmake`

### Summary

**What changed and why**  
The fix addresses a bug where CMake-based incremental installs incorrectly appended `/vllm/` twice to the installation path. This occurred because the original code ran per-component install rules that modified `CMAKE_INSTALL_PREFIX` multiple times when all components were installed simultaneously via `cmake --build --target install`. The change replaces component-specific install rules with `ALL_COMPONENTS` to ensure the prefix modification and restoration happen only once.

**Technical impact**  
This ensures consistent installation paths for FlashAttention components (`_vllm_fa2_C` and `_vllm_fa3_C`) regardless of whether components are installed individually or together. The build system now correctly nests these components under a single `/vllm/` subdirectory, aligning with the expected directory structure and preventing path corruption.

**Potential risks**  
If any future changes introduce component-specific install logic, the switch to `ALL_COMPONENTS` might inadvertently affect other components. Additionally, the fix assumes all relevant install steps are synchronous; any asynchronous or conditional install steps could still lead to path mismatches.

**Key insights**  
The use of `ALL_COMPONENTS` is a robust solution for batch install operations, simplifying maintenance and reducing redundancy. Developers should verify that no component-specific side effects are lost in the consolidation. This change highlights the importance of testing both incremental and full install scenarios in CMake-based projects.

---

## 7. [[CI/Build] Trigger processor tests on registry update](https://github.com/vllm-project/vllm/pull/35824)


### Base Information

- **PR Number:** #35824
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 21:55:58
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35824/files) (2):**
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/models_multimodal.yaml`

### Summary

**What changed and why**  
This PR updates Buildkite CI configuration to trigger multimodal processor tests when the model registry (`tests/models/registry.py`) changes. The change was prompted by a previous PR (#35763) that merged without running these tests, causing a regression. Additionally, the unused "Custom Models Test" section was removed from the configuration.

**Technical impact**  
The changes ensure that modifications to the model registry file will now properly trigger relevant multimodal processor tests in CI. This improves test coverage reliability by establishing correct file dependency tracking between the registry and processor tests. The removal of the unused test section cleans up the CI configuration.

**Potential risks**  
If the registry file contains dependencies beyond multimodal processors, other test areas might also need similar updates. There's a minor risk that the removed "Custom Models" section, while marked as optional, might have been used by developers for ad-hoc testing during PR development.

**Key insights**  
This fix addresses a critical CI gap where registry changes weren't triggering necessary tests. Developers should verify that all test areas with registry dependencies have been updated. Consider documenting the registry file's role in test triggering to prevent similar oversights in the future.

---

## 8. [[ROCm][CI] Fix Assertion Logic For `test_gpt_oss`](https://github.com/vllm-project/vllm/pull/35806)


### Base Information

- **PR Number:** #35806
- **Author:** [micah-wil](https://github.com/micah-wil)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 21:08:05
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35806/files) (1):**
  - `tests/models/quantization/test_gpt_oss.py`

### Summary

**What changed and why**  
The PR fixes a flaky test assertion that incorrectly rejected measured accuracy scores higher than expected. Previously, the test required measured accuracy to be within ±0.02 of the expected value, but this failed when measured accuracy exceeded expectations (e.g., 0.91 vs. 0.89). The change now only checks that measured accuracy is not below the lower bound (expected - tolerance).

**Technical impact**  
This update aligns the test logic with typical accuracy validation, where exceeding the target is acceptable. It resolves CI failures in AMD's Quantized Models Test suite that emerged after quark installation enabled previously skipped tests. The fix ensures tests pass when models perform better than baseline.

**Potential risks**  
If the tolerance (rtol=0.02) is too lenient, it could mask genuine regressions where accuracy drops slightly but remains above the threshold. Additionally, the change assumes higher accuracy is always valid, which may not hold if the metric has an upper bound or specific expected range.

**Key insights**  
Developers should verify that the tolerance value is appropriate for the model and use case. Consider adding an upper-bound check if accuracy significantly above expectations indicates test setup issues. This fix highlights the importance of validating assertion logic when enabling previously skipped tests.

---

## 9. [[Refactor] Fix maxsim cuda platform and add cli to control it](https://github.com/vllm-project/vllm/pull/35427)


### Base Information

- **PR Number:** #35427
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-02 20:48:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35427/files) (5):**
  - `vllm/entrypoints/cli/serve.py`
  - `vllm/entrypoints/openai/cli_args.py`
  - `vllm/entrypoints/pooling/__init__.py`
  - `vllm/entrypoints/pooling/score/serving.py`
  - `vllm/entrypoints/pooling/score/utils.py`

### Summary

**What changed and why**  
This PR adds a CLI flag `--use-gpu-for-pooling-score` to control whether MaxSim pooling scores are computed on GPU, improving performance for late-interaction scoring. It also includes a safety check that prevents using this feature with multiple API servers due to current limitations.

**Technical impact**  
The changes introduce a new configuration option that propagates from CLI arguments through the pooling initialization to the MaxSim computation logic. When enabled, GPU computation is used via `current_platform.device_type` instead of defaulting to CPU, potentially accelerating scoring operations significantly.

**Potential risks**  
The feature is incompatible with multi-server deployments (`api_server_count > 1`), which is enforced via a runtime error. There's also a risk if `current_platform.is_cpu()` incorrectly identifies the platform, leading to unintended device selection. The TODO comment indicates this is not yet fully tested.

**Key insights**  
Developers should be aware that GPU pooling is currently experimental and restricted to single-server setups. The implementation correctly uses the platform abstraction for device selection, but thorough testing is needed before removing the restriction. Consider adding logging to confirm device selection in production use.

---

## 10. [[CI/Build] Automatically patch video metadata for multimodal processor test](https://github.com/vllm-project/vllm/pull/35822)


### Base Information

- **PR Number:** #35822
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 20:27:45
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35822/files) (1):**
  - `tests/models/multimodal/processing/test_common.py`

### Summary

**What changed and why**  
The PR consolidates video metadata patching logic by replacing multiple model-specific patch functions with a single `add_video_metadata` function. This function is now conditionally applied based on the data parser's `video_needs_metadata` attribute, aiming to prevent issues like those previously seen in #35763 by centralizing the video metadata handling.

**Technical impact**  
This change simplifies the test infrastructure by removing hardcoded model-specific video patches from `MM_DATA_PATCHES` dictionary. The logic now dynamically checks if video metadata is required via the processor's data parser, making the system more maintainable and reducing duplication. The architecture shifts from explicit model registration to a capability-based approach.

**Potential risks**  
If the `video_needs_metadata` attribute is not correctly implemented or returns false positives/negatives for certain models, tests could fail or produce incorrect behavior. The removal of explicit model mappings might make it harder to trace which models require video metadata patching during debugging.

**Key insights**  
The refactor promotes cleaner separation of concerns by leveraging the data parser's capabilities. Developers should ensure the `video_needs_metadata` property is accurately defined for all relevant multimodal processors. Consider adding logging or assertions to validate this attribute's behavior during test execution to catch regressions early.

---

## 11. [[Core] Add optional flags to check for repetitive token patterns in engine output](https://github.com/vllm-project/vllm/pull/35451)


### Base Information

- **PR Number:** #35451
- **Author:** [aykoppol](https://github.com/aykoppol)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 20:23:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35451/files) (7):**
  - `tests/v1/core/test_repetition_detection.py`
  - `vllm/entrypoints/openai/chat_completion/protocol.py`
  - `vllm/entrypoints/openai/completion/protocol.py`
  - `vllm/sampling_params.py`
  - `vllm/v1/core/sched/utils.py`
  - `vllm/v1/engine/__init__.py`
  - `vllm/v1/request.py`

### Summary

**What changed and why**  
This PR introduces optional repetition detection to identify and halt generation when repetitive token patterns (like nonsensical repeated sentences) appear in model output. The feature addresses hallucination failures in models, particularly improving throughput by ~33% for error-prone multimodal inputs. It's implemented as an opt-in parameter in sampling configurations to avoid breaking existing behavior.

**Technical impact**  
The changes add a new `RepetitionDetectionParams` class to define detection thresholds (pattern size, repetition count) and integrate it into the sampling pipeline. The core logic in `check_stop` now evaluates token sequences for repetitive N-grams, setting a new `FINISHED_REPETITION` status when detected. This extends the engine’s finish reasons and is exposed via OpenAI-compatible endpoints.

**Potential risks**  
Overly sensitive detection could prematurely stop valid generations, especially for models with repetitive but meaningful outputs (e.g., lists, poetry). The algorithm’s complexity grows with `max_pattern_size`, which may impact latency for very large patterns. Edge cases like overlapping patterns or partial repetitions might not be handled as intended (see test `test_partial_repetition_not_detected`).

**Key insights**  
The feature is well-encapsulated and disabled by default, minimizing risk to existing workflows. Developers should tune `min_pattern_size` and `min_count` based on model behavior and use case. Consider adding a buffer or confidence threshold to reduce false positives in production. The integration with `min_tokens` ensures early stops respect minimum generation requirements.

---

## 12. [[Model Runner V2] Use ModelState.prepare_attn() for cuda graph capture [5/N]](https://github.com/vllm-project/vllm/pull/35774)


### Base Information

- **PR Number:** #35774
- **Author:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-02 20:06:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35774/files) (5):**
  - `vllm/v1/worker/gpu/cudagraph_utils.py`
  - `vllm/v1/worker/gpu/input_batch.py`
  - `vllm/v1/worker/gpu/model_runner.py`
  - `vllm/v1/worker/gpu/spec_decode/eagle/cudagraph.py`
  - `vllm/v1/worker/gpu/spec_decode/eagle/speculator.py`

### Summary

**What changed and why**  
The changes centralize attention preparation logic by replacing direct calls to `build_attn_metadata` with `ModelState.prepare_attn()` during CUDA graph capture. This refactor removes hardcoded assumptions (like `uniform_decode_query_len`) and leverages the existing `InputBatch.make_dummy()` for consistent dummy input creation.

**Technical impact**  
This simplifies the CUDA graph capture flow by delegating attention metadata construction to the model state abstraction, improving code reuse and reducing duplication. The changes also ensure proper handling of distributed context parallelism (DCP) by integrating `prepare_dcp_local_seq_lens` and removing manual buffer manipulations.

**Potential risks**  
Removing the `uniform_decode_query_len` parameter could affect specialized decoding scenarios if not handled within `ModelState.prepare_attn()`. The dependency on `ModelState` being correctly initialized in all capture contexts (including Eagle speculator) introduces a new failure point if the state is inconsistent.

**Key insights**  
The refactor promotes cleaner separation of concerns, but developers must verify that `ModelState.prepare_attn()` correctly supports all capture modes (e.g., uniform decode, DCP). Testing should focus on edge cases like varying request counts and token distributions to ensure no regression in graph capture behavior.

---

## 13. [[V0 deprecation] Remove Swin model](https://github.com/vllm-project/vllm/pull/35821)


### Base Information

- **PR Number:** #35821
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-02 20:03:42
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35821/files) (1):**
  - `vllm/model_executor/models/swin.py`

### Summary

**What changed and why**  
The PR removes the Swin model implementation (`swin.py`) as part of the v0 deprecation effort. This aligns with the stated purpose of removing this encoder alongside Donut models during the deprecation cleanup.

**Technical impact**  
This deletion eliminates support for Swin Transformer models within the vLLM codebase. It reduces code complexity and maintenance overhead but will break any existing workflows or integrations that depend on the Swin model. The removal is a breaking change that requires users to migrate away from Swin-based models.

**Potential risks**  
- Existing code or configurations referencing the Swin model will fail, potentially causing runtime errors.
- If any dependencies or downstream projects rely on this model, they will be impacted without a direct replacement in the codebase.
- The PR lacks test results or a rollback plan, increasing risk if issues arise post-merge.

**Key insights**  
- Ensure all references to the Swin model are removed from configuration files, documentation, and model registries.
- Communicate this breaking change clearly to users, as it may affect production pipelines.
- Consider providing migration guidance or alternatives for users who previously utilized Swin models.

---

## 14. [[Docs][Model Runner V2] Add Design Docs](https://github.com/vllm-project/vllm/pull/35819)


### Base Information

- **PR Number:** #35819
- **Author:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-02 19:58:14
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35819/files) (6):**
  - `docs/assets/design/model_runner_v2/async_no_race_condition.png`
  - `docs/assets/design/model_runner_v2/async_race_condition.png`
  - `docs/assets/design/model_runner_v2/async_sched.png`
  - `docs/assets/design/model_runner_v2/persistent_batch_mrv2.png`
  - `docs/assets/design/model_runner_v2/persistent_batch_v1.png`
  - `docs/design/model_runner_v2.md`

### Summary

**What changed and why**  
This PR adds comprehensive design documentation for Model Runner V2 (MRV2), converted from original Google Docs. The document explains the architectural improvements over V1, addressing persistent batch inefficiencies, async scheduling limitations, and technical debt accumulated from feature additions.

**Technical impact**  
The documentation provides a clear reference for developers working on MRV2, detailing decoupled persistent batch design, async-first execution, and GPU-native input processing. It establishes design principles that will guide future implementation and ensure consistency across the codebase.

**Potential risks**  
Since this is documentation only, there are no direct code risks. However, the document describes MRV2 as not yet feature-complete or rigorously tested, which could lead to misunderstandings if readers assume the described system is fully production-ready.

**Key insights**  
Developers should treat this as a living design guide when implementing MRV2 features. The emphasis on modularity, explicit CUDA graph management, and elimination of async barriers provides critical architectural direction. Future work should align with these principles rather than porting V1 behaviors directly.

---

## 15. [[ModelRunnerV2] Rename sampler functions and variables for clarity](https://github.com/vllm-project/vllm/pull/35459)


### Base Information

- **PR Number:** #35459
- **Author:** [andylolu2](https://github.com/andylolu2)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-02 19:48:56
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35459/files) (7):**
  - `vllm/v1/worker/gpu/sample/bad_words.py`
  - `vllm/v1/worker/gpu/sample/gumbel.py`
  - `vllm/v1/worker/gpu/sample/logit_bias.py`
  - `vllm/v1/worker/gpu/sample/min_p.py`
  - `vllm/v1/worker/gpu/sample/penalties.py`
  - `vllm/v1/worker/gpu/sample/sampler.py`
  - `vllm/v1/worker/gpu/sample/states.py`

### Summary

**What changed and why**  
The PR renames variables and parameters from `idx_mapping` to `expanded_idx_mapping` across multiple sampler modules (bad_words, gumbel, logit_bias, min_p, penalties, sampler, states). This clarifies that the mapping is already expanded (i.e., per-token) rather than per-request, which is important for spec decode integration and reduces confusion about tensor shapes.

**Technical impact**  
These changes improve code clarity without altering functionality. The renamed variables consistently reflect that the mapping operates at the token level (`num_tokens` replaces `num_reqs` in shape comments), aligning with the expanded nature of the data structures used in ModelRunnerV2. Kernel launch configurations and indexing logic remain unchanged.

**Potential risks**  
Low risk since the changes are purely renaming and do not modify logic. However, any missed references to the old variable names in downstream code or external integrations could cause runtime errors. The PR touches core sampling components, so thorough testing is required to ensure no regressions in sampling behavior.

**Key insights**  
The renaming enhances maintainability by making the data flow more explicit—developers can now easily distinguish between per-request and per-token mappings. When working with these modules, always verify that `expanded_idx_mapping` is indeed expanded (length = `num_tokens`) and not a per-request index. This change sets a clearer foundation for spec decode features.

---

## 16. [[CI] Temporarily Disable Nightly Failures](https://github.com/vllm-project/vllm/pull/35770)


### Base Information

- **PR Number:** #35770
- **Author:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-02 17:49:13
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35770/files) (4):**
  - `.buildkite/test_areas/distributed.yaml`
  - `.buildkite/test_areas/lm_eval.yaml`
  - `.buildkite/test_areas/weight_loading.yaml`
  - `tests/evals/gsm8k/configs/moe-refactor/config-h100.txt`

### Summary

**What changed and why**  
This PR temporarily disables several CI test steps due to infrastructure issues (A100 runners not responding) and a specific test failure (llama4 issue). The changes comment out three A100-based test configurations and modify one example test command, while fixing a formatting issue in a configuration file.

**Technical impact**  
The CI pipeline will skip the disabled A100 test suites, reducing test coverage for large model evaluation and weight loading on A100 hardware. The pipeline remains functional for H100 tests and other configurations, but critical distributed testing capabilities are temporarily reduced.

**Potential risks**  
Disabling these tests could allow regressions to go undetected in A100-specific code paths, particularly for large model handling and weight loading scenarios. The RLHF async test is also disabled without a clear timeline for re-enablement, potentially masking serialization or distributed computation issues.

**Key insights**  
This is a temporary infrastructure mitigation, not a code fix. Developers should monitor A100 runner availability and re-enable tests promptly. The commented-out tests should be tracked with clear TODOs or issue references to ensure they're restored once underlying issues are resolved.

---

## 17. [[Tool Parser] Fix Qwen3Coder streaming parameter loss with speculative decode](https://github.com/vllm-project/vllm/pull/35615)


### Base Information

- **PR Number:** #35615
- **Author:** [voipmonitor](https://github.com/voipmonitor)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-03-02 17:40:37
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35615/files) (2):**
  - `vllm/entrypoints/openai/chat_completion/serving.py`
  - `vllm/tool_parsers/qwen3coder_tool_parser.py`

### Summary

**What changed and why**  
This PR fixes a critical bug where Qwen3Coder's streaming tool parser silently dropped parameters when speculative decoding was enabled. The root causes were: 1) parameter processing occurred after the function close check, causing final parameters to be lost when delivered with `</function>`; 2) a single-pass parameter extraction that discarded complete parameters when encountering an incomplete one; and 3) name-based deduplication preventing multiple calls to the same function.

**Technical impact**  
The changes fundamentally restructure the parser's execution flow to ensure all complete parameters are emitted before handling function closure. The switch from single-pass `if` to a `while` loop with fragment accumulation ensures speculative decoding bursts don't cause parameter loss. The removal of name-based deduplication and addition of proper index tracking enables multiple calls to the same function.

**Potential risks**  
The reordering of close-check logic could potentially affect edge cases where function closure needs immediate handling. The removal of the `parameter_prefix` check in `json_started` might cause issues if opening braces appear in unexpected contexts. The always-append approach to `prev_tool_call_arr` could increase memory usage with many tool calls.

**Key insights**  
The fix demonstrates the importance of processing order in streaming parsers—parameter extraction must precede closure detection. The solution highlights how speculative decoding exacerbates existing timing issues in incremental parsers. Developers should validate that the parser handles both single-token streaming and speculative decoding scenarios, and ensure existing tests cover parallel tool calls with identical function names.

---

## 18. [[Model Runner V2][Perf] align dummy_run tokens to uniform decode for dp cudagraph](https://github.com/vllm-project/vllm/pull/35376)


### Base Information

- **PR Number:** #35376
- **Author:** [izhuhaoran](https://github.com/izhuhaoran)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-03-02 17:10:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35376/files) (1):**
  - `vllm/v1/worker/gpu/model_runner.py`

### Summary

**What changed and why**  
The PR modifies `_dummy_run` to align dummy batch token counts with `uniform_decode_query_len` when `uniform_decode=True`. This ensures idle data-parallel (DP) ranks use the same token distribution as active ranks during CUDA graph execution, preventing graph misses and performance degradation in speculative decoding scenarios.

**Technical impact**  
This change ensures CUDA graph consistency across DP ranks by matching the token allocation pattern used in uniform decode mode. It maintains the existing behavior for non-uniform decode cases while enabling correct graph caching, which is critical for maintaining high-throughput inference with speculative decoding and DP.

**Potential risks**  
If `uniform_decode_query_len` is not properly initialized or varies unexpectedly, it could lead to mismatched token counts or allocation errors. The change assumes uniform decode is only used with CUDA graphs and DP; other configurations might inadvertently trigger the new logic with unintended side effects.

**Key insights**  
Developers should verify that `uniform_decode_query_len` is correctly set in the CUDA graph manager and that uniform decode is only enabled in compatible DP+graph scenarios. This fix highlights the importance of synchronizing dummy execution patterns across distributed workers to avoid silent performance regressions.

---

## 19. [[XPU][NIXL] Add GPUDirect RDMA support for XPU](https://github.com/vllm-project/vllm/pull/35270)


### Base Information

- **PR Number:** #35270
- **Author:** [zhenwei-intel](https://github.com/zhenwei-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-03-02 16:42:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35270/files) (3):**
  - `docker/Dockerfile.xpu`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl_connector.py`
  - `vllm/platforms/xpu.py`

### Summary

**What changed and why**  
This PR adds GPUDirect RDMA support for XPU devices in the NIXL connector by modifying the Dockerfile to build UCX and NIXL from source with specific configurations, updating the NIXL connector to recognize XPU as a valid memory type, and setting an environment variable to disable UCX's memory type cache for XPU platforms. The changes enable direct GPU-to-GPU communication via RDMA, which improves performance for distributed inference.

**Technical impact**  
The modifications allow XPU devices to utilize GPUDirect RDMA, enabling faster KV cache transfers between GPUs over InfiniBand. This reduces latency and increases throughput, as evidenced by the 1.65x improvement in request throughput under SLO constraints. The architecture now treats XPU similarly to CUDA for memory type mapping, extending NIXL's capabilities to Intel GPUs.

**Potential risks**  
The implementation depends on a specific UCX commit that includes necessary fixes, and manual configuration of `UCX_NET_DEVICES` is required for optimal performance until upstream changes are merged. Additionally, ze-ipc support is currently unsupported, which may limit functionality in certain scenarios. Disabling `UCX_MEMTYPE_CACHE` could have minor performance implications in other contexts.

**Key insights**  
Developers must ensure the specified UCX version is used and configure network devices appropriately. The changes are backward compatible but require careful environment setup. Performance gains are significant, but the solution is interim until UCX fully supports the required features; monitor upstream UCX PRs for updates.

---

## 20. [[Model] Add support for nvidia/llama-nemotron-rerank-vl-1b-v2](https://github.com/vllm-project/vllm/pull/35735)


### Base Information

- **PR Number:** #35735
- **Author:** [jzakrzew](https://github.com/jzakrzew)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-03-02 16:32:14
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35735/files) (9):**
  - `docs/models/pooling_models.md`
  - `docs/models/supported_models.md`
  - `examples/pooling/score/template/nemotron-vl-rerank.jinja`
  - `tests/models/multimodal/pooling/test_llama_nemotron_vl.py`
  - `tests/models/multimodal/pooling/test_llama_nemotron_vl_embed.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/config.py`
  - `vllm/model_executor/models/nemotron_vl.py`
  - `vllm/model_executor/models/registry.py`

### Summary

**What changed and why**  
Added support for the `nvidia/llama-nemotron-rerank-vl-1b-v2` multimodal reranking model, which uses a bidirectional Llama backbone with SigLIP vision encoder and a sequence-classification head for cross-encoder scoring. The changes include model implementation, documentation updates, comprehensive tests, and a custom Jinja template for the reranking API.

**Technical impact**  
This extends the pooling model family to support a new multimodal reranker architecture (`LlamaNemotronVLForSequenceClassification`). The implementation reuses the existing `LlamaNemotronVLForEmbedding` backbone and adds a classification head, while maintaining compatibility with the existing score/rerank APIs. The model registry and configuration systems are updated to recognize the new architecture.

**Potential risks**  
The reranker model requires a custom chat template override (`nemotron-vl-rerank.jinja`) because the bundled tokenizer template is unsuitable for scoring. Tests now use `half` precision instead of `bfloat16`, which may affect numerical stability comparisons. The weight loading logic includes manual initialization for unused inner LM classification head parameters, which could lead to inconsistencies if not properly handled.

**Key insights**  
Developers must use the provided Jinja template when serving this model. The test consolidation into a single file improves maintainability but changes precision requirements. The implementation demonstrates effective reuse of existing multimodal embedding infrastructure while adding cross-encoding capabilities through a well-defined interface (`SupportsCrossEncoding`).

---

## 21. [[ROCm][CI] Fix backslash-continuation in pytest marker re-quoting and treat exit code 5 as success](https://github.com/vllm-project/vllm/pull/35798)


### Base Information

- **PR Number:** #35798
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-02 16:07:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35798/files) (1):**
  - `.buildkite/scripts/hardware_ci/run-amd-test.sh`

### Summary

**What changed and why**  
This PR fixes two issues in the AMD test runner script. First, it corrects how backslash-continuation characters in pytest commands are handled during marker re-quoting, preventing syntax errors. Second, it treats pytest exit code 5 (no tests collected) as a success, which is valid for sharded CI runs where a shard may legitimately have zero matching tests.

**Technical impact**  
The changes improve the robustness of command parsing in multi-line pytest invocations and adjust the CI failure criteria to accommodate legitimate empty test shards. This ensures the CI pipeline doesn't fail unnecessarily and correctly processes complex pytest command formats with line continuations.

**Potential risks**  
The exit code handling change could mask legitimate failures if exit code 5 occurs due to misconfigured test markers or environment issues. The backslash handling logic might not cover all edge cases of shell command parsing, particularly with nested quotes or complex escaping patterns.

**Key insights**  
Always treat pytest exit code 5 as a non-failure in sharded test environments. When processing shell commands programmatically, pay special attention to line continuation characters as they create unique tokenization challenges. Consider adding logging for exit code 5 scenarios to distinguish between legitimate empty shards and configuration errors.

---

## 22. [[All Reduce] Change default backend of Flashinfer All Reduce to trtllm](https://github.com/vllm-project/vllm/pull/35793)


### Base Information

- **PR Number:** #35793
- **Author:** [hjjq](https://github.com/hjjq)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-02 15:57:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35793/files) (1):**
  - `vllm/envs.py`

### Summary

**What changed and why**  
The default backend for Flashinfer All Reduce has been changed from "auto" (which defaulted to "mnnvl") to "trtllm". This change addresses stability issues with the "mnnvl" backend, specifically problems related to cudagraph compatibility as reported in issue #35772.

**Technical impact**  
This modification ensures that the Flashinfer All Reduce operations default to the more stable "trtllm" backend across all deployments unless explicitly overridden. It maintains backward compatibility by keeping the environment variable configurable, but shifts the default behavior to avoid known issues.

**Potential risks**  
Switching to "trtllm" may impact performance in scenarios where "mnnvl" was functioning correctly, as "mnnvl" was noted to perform similarly or better in non-problematic cases. Additionally, if the underlying cudagraph issue in "mnnvl" is resolved, the default may need to be re-evaluated to avoid missing optimizations.

**Key insights**  
This is a stability-focused change that prioritizes reliability over potential performance gains. Developers should monitor the linked GitHub issue for updates on "mnnvl" backend fixes. Consider documenting this change in release notes to inform users of the shifted default and its rationale.

---

## 23. [[Bugfix] Fix MM processor test for Qwen3.5](https://github.com/vllm-project/vllm/pull/35797)


### Base Information

- **PR Number:** #35797
- **Author:** [ywang96](https://github.com/ywang96)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-02 15:05:08
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35797/files) (1):**
  - `tests/models/multimodal/processing/test_common.py`

### Summary

**What changed and why**  
This PR adds Qwen3.5 and Qwen3.5 MoE model identifiers to the video metadata patching registry in the multimodal processor test. The change fixes a test failure that occurred because these models were not registered for the patching function after being added to CI in a previous PR (#35763).

**Technical impact**  
The change ensures that Qwen3.5 models receive the same video metadata preprocessing as other Qwen-based vision-language models during testing. This maintains consistency in test behavior across similar model architectures and prevents test failures in the CI pipeline.

**Potential risks**  
The main risk is that if Qwen3.5 models have different video processing requirements than Qwen3_VL models, using the same patching function could mask actual compatibility issues. Additionally, there's a risk that other test scenarios might need similar updates that were overlooked.

**Key insights**  
This is a straightforward fix that aligns test infrastructure with recent model additions. Developers should verify that Qwen3.5 models truly share the same video processing requirements as Qwen3_VL models. Consider adding a comment explaining why these models share the same patching function to prevent future confusion.

---

## 24. [[MoE][Perf] Wrap DSV3 QKVAProj GEMM in custom op for torch.compile](https://github.com/vllm-project/vllm/pull/35751)


### Base Information

- **PR Number:** #35751
- **Author:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-02 15:03:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35751/files) (1):**
  - `vllm/model_executor/models/deepseek_v2.py`

### Summary

**What changed and why**  
The change wraps the data-dependent conditional dispatch in `DeepSeekV2FusedQkvAProj.forward` into a custom op (`min_latency_fused_qkv_a_proj`). Previously, the conditional `0 < num_tokens <= 16` caused graph breaks under `torch.compile` because it depended on tensor shape at trace time. The custom op hides this dynamic dispatch from the compiler while preserving the same runtime behavior.

**Technical impact**  
This enables `torch.compile` to trace through the forward pass without graph breaks, improving performance for compiled execution. The custom op includes both a real implementation (`_min_latency_fused_qkv_a_proj_impl`) that retains the original dynamic logic and a fake implementation (`_min_latency_fused_qkv_a_proj_fake`) that returns the correct output shape for tracing. The `_use_min_latency_gemm` flag remains outside the custom op as a compile-time guard.

**Potential risks**  
If the custom op registration fails or is incorrectly implemented, it could lead to silent correctness issues or performance regressions. The fake implementation must accurately reflect output shape and metadata to avoid tracing errors. There is also a risk that future changes to the GEMM kernels or tensor shapes might require updates to the custom op.

**Key insights**  
Using custom ops is an effective pattern for hiding dynamic, data-dependent control flow from `torch.compile`. Developers should ensure fake implementations match real output characteristics precisely. This approach maintains both eager-mode correctness and compiled-mode performance, but requires careful testing of both code paths.

---

## 25. [clean unused cudagraph_batch_sizes](https://github.com/vllm-project/vllm/pull/35552)


### Base Information

- **PR Number:** #35552
- **Author:** [BoyuanFeng](https://github.com/BoyuanFeng)
- **Merged By:** [BoyuanFeng](https://github.com/BoyuanFeng)
- **Merged time:** 2026-03-02 14:00:17
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35552/files) (1):**
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The PR removes the `cudagraph_batch_sizes` attribute and its initialization logic from the GPU model runner. This cleanup follows a previous change (#32946) that made this variable obsolete, eliminating dead code to improve maintainability.

**Technical impact**  
These changes reduce memory usage slightly by removing an unused data structure and simplify the codebase by eliminating redundant logic. The core functionality remains unaffected since the variable was no longer referenced elsewhere.

**Potential risks**  
The removal appears safe as the variable was unused, but developers should verify that no downstream dependencies or external modules indirectly relied on this attribute. Additionally, any conditional logic that previously depended on the presence of `cudagraph_batch_sizes` should be reviewed to ensure no hidden side effects.

**Key insights**  
This is a straightforward cleanup that enhances code clarity. Developers should leverage static analysis tools to identify similar unused variables in the codebase. Future modifications to CUDA graph-related features should ensure all deprecated attributes are fully phased out to prevent accumulation of technical debt.

---

## 26. [[Bugfix] Fix missing sequence_lengths in qwen3_omni_moe_thinker](https://github.com/vllm-project/vllm/pull/35741)


### Base Information

- **PR Number:** #35741
- **Author:** [yeqcharlotte](https://github.com/yeqcharlotte)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-03-02 13:11:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35741/files) (1):**
  - `vllm/model_executor/models/qwen3_omni_moe_thinker.py`

### Summary

**What changed and why**  
The fix addresses a missing `sequence_lengths` parameter in `Qwen3OmniMoeThinkerVisionBlock.forward()` that was introduced in PR #34580 for the FlashInfer cuDNN backend. Without this update, loading Qwen3-Omni models crashes with a TypeError. The changes add the parameter to the vision block's forward method and compute `sequence_lengths` in the encoder loop using `MMEncoderAttention.maybe_compute_sequence_lengths()`, mirroring the pattern in `qwen3_vl.py`.

**Technical impact**  
This ensures consistency across all Qwen vision model implementations that use the FlashInfer cuDNN backend, preventing runtime crashes. The computation of `cu_seqlens_np` directly from `grid_thw` in numpy avoids a GPU->CPU synchronization overhead, maintaining performance while aligning with the updated attention mechanism requirements.

**Potential risks**  
If `sequence_lengths` is computed incorrectly (e.g., due to mismatched dimensions in `grid_thw`), it could lead to attention mechanism failures or silent errors. The reliance on numpy for `cu_seqlens_np` computation introduces a CPU dependency that might affect performance in GPU-heavy pipelines, though the non-blocking tensor transfer mitigates this.

**Key insights**  
Developers should verify that `grid_thw` structure aligns with expectations across all Qwen model variants to prevent data corruption. This fix highlights the importance of updating all related model files when modifying shared components like attention backends—consider adding a test to catch similar omissions in future changes.

---

## 27. [[BUG] Fix rlhf_async example](https://github.com/vllm-project/vllm/pull/35788)


### Base Information

- **PR Number:** #35788
- **Author:** [hao-aaron](https://github.com/hao-aaron)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-03-02 12:36:41
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35788/files) (1):**
  - `examples/offline_inference/new_weight_syncing/rlhf_async_new_apis.py`

### Summary

**What changed and why**  
The change increases the sleep duration from 0.2 seconds to 5 seconds in the `pause_after_n_tokens` method of an RLHF async example. This fixes a test failure observed in a CI build, likely addressing a race condition or timing issue where the previous delay was insufficient for proper synchronization.

**Technical impact**  
This adjustment directly affects the timing behavior during generation pauses in asynchronous RLHF workflows. It ensures that the system has adequate time to stabilize or complete internal state transitions before proceeding, which is critical for the correctness of the new weight syncing mechanism in offline inference scenarios.

**Potential risks**  
Increasing the sleep time may introduce unnecessary latency in the overall generation process, potentially slowing down throughput in production environments. If the underlying issue is a race condition, a fixed sleep might not be fully robust across different hardware or load conditions, and a more deterministic synchronization approach could be preferable.

**Key insights**  
While this fix resolves an immediate CI failure, consider investigating the root cause of the timing issue to implement a more reliable solution, such as using condition variables or explicit state checks instead of arbitrary sleeps. Monitor performance metrics to ensure the increased delay does not negatively impact system efficiency.

---

## 28. [[ci] Add Ray compatibility check informational CI job](https://github.com/vllm-project/vllm/pull/34672)


### Base Information

- **PR Number:** #34672
- **Author:** [jeffreywang-anyscale](https://github.com/jeffreywang-anyscale)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-03-02 12:06:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34672/files) (2):**
  - `.buildkite/scripts/check-ray-compatibility.sh`
  - `.buildkite/test_areas/ray_compat.yaml`

### Summary

**What changed and why**  
Added an informational CI job that proactively checks if Ray LLM can generate lock files compatible with vLLM's current dependency constraints. This catches dependency conflicts early by fetching Ray's requirement files, extracting vLLM's installed dependencies (including audio extras), and running `uv pip compile` to test resolution feasibility.

**Technical impact**  
This introduces a non-blocking CI step (`soft_fail: true`) that runs after the image build. It uses `uv` for dependency resolution, treating vLLM's constraints as fixed bounds to verify if Ray's requirements can be satisfied within them. The step provides Buildkite annotations and Slack notifications on failure, improving cross-project dependency awareness.

**Potential risks**  
The script assumes Ray uses `vllm[audio]` and strips extra markers accordingly; changes in Ray's dependency specification could affect accuracy. Network dependencies (fetching remote requirement files) may introduce flakiness. The Slack notification requires a secret (`RAY_COMPAT_SLACK_WEBHOOK_URL`) configured post-merge, which could delay initial alerts.

**Key insights**  
This is a proactive compatibility safeguard that shifts dependency conflict detection left. Developers should monitor failures as early warnings of breaking changes in vLLM's dependencies that affect Ray integration. Ensure the Slack webhook secret is promptly configured after merge to enable notifications. The script's resolution logic is a robust simulation of Ray's lock file generation process.

---

## 29. [[KVConnector] Auto-downgrade to PIECEWISE cudagraph mode for layerwise async ops](https://github.com/vllm-project/vllm/pull/31057)


### Base Information

- **PR Number:** #31057
- **Author:** [yashwantbezawada](https://github.com/yashwantbezawada)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-02 12:04:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/31057/files) (4):**
  - `vllm/config/vllm.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/base.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/lmcache_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/multi_connector.py`

### Summary

**What changed and why**  
Added logic to automatically downgrade CUDA graph mode to PIECEWISE when using KV connectors with layerwise async operations. This prevents data races during CUDA graph replay, where Python-side synchronization calls like `wait_for_layer_load()` are skipped, leaving KV cache data unsynchronized.

**Technical impact**  
The change introduces a new class method `requires_piecewise_for_cudagraph()` in the KV connector base class, allowing connectors to declare incompatibility with FULL CUDA graph mode. The VllmConfig validation now checks this flag and downgrades the mode automatically, ensuring safe execution for connectors like LMCache with `use_layerwise=True`.

**Potential risks**  
If other connectors implement non-trivial layerwise operations but forget to override the method, they could still encounter data races. The lazy import in VllmConfig might cause issues if KVConnectorFactory isn't available in all runtime contexts. Additionally, silent downgrades to PIECEWISE could impact performance unexpectedly if users are unaware of the mode change.

**Key insights**  
This follows established patterns for handling CUDA graph incompatibilities (similar to DCP/pooler models). Developers adding new KV connectors must assess if they use layerwise async ops and override the method accordingly. Consider adding a debug log or metric to track automatic downgrades in production to aid performance monitoring.

---

## 30. [[Spec Decode] Add hidden states extraction system](https://github.com/vllm-project/vllm/pull/33736)


### Base Information

- **PR Number:** #33736
- **Author:** [fynnsu](https://github.com/fynnsu)
- **Merged By:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged time:** 2026-03-02 11:29:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/33736/files) (16):**
  - `examples/offline_inference/extract_hidden_states.py`
  - `tests/models/registry.py`
  - `tests/v1/kv_connector/extract_hidden_states_integration/__init__.py`
  - `tests/v1/kv_connector/extract_hidden_states_integration/predictable_llama.py`
  - `tests/v1/kv_connector/extract_hidden_states_integration/test_extraction.py`
  - `tests/v1/spec_decode/test_extract_hidden_states.py`
  - `vllm/config/speculative.py`
  - `vllm/distributed/kv_events.py`
  - `vllm/distributed/kv_transfer/kv_connector/factory.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/example_hidden_states_connector.py`
  - `vllm/model_executor/models/extract_hidden_states.py`
  - `vllm/model_executor/models/registry.py`
  - `vllm/transformers_utils/configs/extract_hidden_states.py`
  - `vllm/v1/outputs.py`
  - `vllm/v1/spec_decode/extract_hidden_states.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
Added a hidden states extraction system for speculative decoding. This introduces a new `extract_hidden_states` speculative method that uses a dummy drafter model to cache intermediate hidden states from the target model into a custom KV connector, enabling external storage and retrieval of hidden states for downstream tasks.

**Technical impact**  
The system extends the speculative decoding framework with a non‑speculative pathway that extracts and saves hidden states via a custom `ExampleHiddenStatesConnector`. It adds a new model architecture (`ExtractHiddenStatesModel`) with a cache‑only attention backend, modifies KV‑connector output merging to support dual contexts (target and drafter), and updates configuration handling to support the new method. This enables hidden‑state extraction without affecting existing speculative‑decoding performance.

**Potential risks**  
- The temporary workaround of creating a second KV‑connector context for the drafter model may cause issues in future KV‑transfer or disaggregation scenarios.  
- The system currently only supports a limited set of target model architectures (e.g., Llama, Qwen).  
- The `ExampleHiddenStatesConnector` is designed for debugging and may have performance overhead.  
- Edge cases with data‑parallel execution or CUDA graphs need thorough testing.

**Key insights**  
- This implementation provides a foundation for hidden‑state extraction, useful for debugging, analysis, or training auxiliary models.  
- Developers should note the architectural constraints: the method only works with `CacheOnlyAttentionLayer` and requires `eagle_aux_hidden_state_layer_ids` in the draft config.  
- Future work should integrate KV‑connector support for draft models more cleanly to avoid the dual‑context workaround.  
- The example connector and integration tests offer a template for building production‑ready hidden‑state connectors.

---

## 31. [[torch.compile] Improve cold and warm start compile tests](https://github.com/vllm-project/vllm/pull/35709)


### Base Information

- **PR Number:** #35709
- **Author:** [zou3519](https://github.com/zou3519)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-02 11:27:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35709/files) (5):**
  - `tests/compile/test_cold_start.py`
  - `tests/compile/test_startup.py`
  - `tests/conftest.py`
  - `vllm/compilation/compiler_interface.py`
  - `vllm/compilation/counter.py`

### Summary

**What changed and why**  
The test suite was refactored to combine cold and warm start compilation tests into a single file (`test_startup.py`). The cold start test now runs in a forked child process to isolate in-memory state, while the warm start test runs in the parent process using the cached artifacts. A new counter (`num_compiled_artifacts_loaded`) was added to track cache hits, and temporary directories are used for both VLLM and inductor caches to ensure test isolation.

**Technical impact**  
These changes improve test accuracy by properly separating cold and warm start scenarios, preventing state leakage. The addition of `num_compiled_artifacts_loaded` provides better observability into cache utilization. Using `VllmRunner` ensures proper GPU memory cleanup, and fresh cache directories eliminate interference between test runs.

**Potential risks**  
The warm start test currently expects one artifact to be saved (`num_compiled_artifacts_saved=1`), which is noted as a bug (issue #35708). Forking before CUDA initialization is required but may cause portability issues on non-Linux systems. Relying on internal PyTorch counters (`aot_autograd`) makes the test fragile to upstream changes.

**Key insights**  
The refactoring enhances test reliability and coverage, but developers should monitor the warm start artifact-saving bug. The forking approach is necessary for state isolation but may need adaptation for cross-platform support. Using fresh caches is a best practice that should be adopted in other compilation tests.

---

## 32. [[BugFix][Model]Fix the garbled code in Ernie4.5-VL caused by fast_moe_cold_start](https://github.com/vllm-project/vllm/pull/35587)


### Base Information

- **PR Number:** #35587
- **Author:** [CSWYF3634076](https://github.com/CSWYF3634076)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-02 10:56:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35587/files) (1):**
  - `vllm/model_executor/models/config.py`

### Summary

**What changed and why**  
A new configuration class `Ernie4_5_VLMoeForConditionalGenerationConfig` was added to disable `fast_moe_cold_start` for the Ernie4.5-VL model. This fixes garbled output caused by the model's conditional execution of text/vision MoE branches, which violates the optimization's assumption that all MoE layers execute in a fixed order during initialization.

**Technical impact**  
The change directly modifies the compilation configuration for this specific model, forcing `fast_moe_cold_start` to `False`. This ensures correct execution order in MoE layers but may reduce initialization performance for Ernie4.5-VL. No other models are affected, maintaining the optimization's benefits elsewhere.

**Potential risks**  
If other models with similar conditional MoE execution patterns exist, they may encounter the same issue silently. The fix is model-specific, so future variants or similar architectures might require similar patches. There's also a risk that disabling this optimization could impact cold-start latency in deployment scenarios.

**Key insights**  
This fix highlights a critical edge case where MoE optimizations must account for conditional execution paths. Developers should verify that `fast_moe_cold_start` assumptions hold for any new MoE-based models added to the codebase. Consider adding a validation mechanism or documentation note for models with non-standard MoE layer execution.

---

## 33. [[CI/Build] Enable Qwen3.5 tests on CI](https://github.com/vllm-project/vllm/pull/35763)


### Base Information

- **PR Number:** #35763
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 09:43:53
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35763/files) (1):**
  - `tests/models/registry.py`

### Summary

**What changed and why**  
The PR updates the Qwen3.5 model test configurations in the registry by removing the `min_transformers_version="5.1.0"` constraint and switching from instruction-tuned variants (e.g., `Qwen3.5-9B-Instruct`) to base model variants (e.g., `Qwen3.5-0.8B`). This change allows CI tests to run with Transformers 4.57.6, as the model configuration has been ported and no longer requires a newer Transformers version.

**Technical impact**  
These modifications enable the Qwen3.5 test suite to execute successfully in CI environments using older Transformers versions (≥4.57), broadening compatibility. The switch to base model variants may affect test coverage by focusing on non-instruction-tuned models, potentially altering the behavior tested during initialization.

**Potential risks**  
Using base models instead of instruction-tuned variants could miss edge cases specific to instruction-tuned configurations, such as prompt formatting or special token handling. Additionally, if the ported config relies on features only available in newer Transformers versions, subtle incompatibilities might arise despite passing basic tests.

**Key insights**  
Developers should verify that the base model variants adequately represent the intended test scenarios, especially for instruction-tuned use cases. It’s also advisable to monitor for any downstream issues in model initialization or inference that could stem from the version or model variant changes.

---

## 34. [[CI][HPU] Pin vllm commit compatible with vllm-gaudi - HPU tests](https://github.com/vllm-project/vllm/pull/35307)


### Base Information

- **PR Number:** #35307
- **Author:** [PatrykWo](https://github.com/PatrykWo)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 09:02:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35307/files) (1):**
  - `.buildkite/scripts/hardware_ci/run-hpu-test.sh`

### Summary

**What changed and why**  
Added dynamic commit pinning for vllm in HPU CI tests to maintain compatibility with the vllm-gaudi plugin. The script now fetches a target commit SHA from a vllm-gaudi repository file and conditionally checks out that commit during Docker image build, preventing test failures when upstream vllm API changes break the plugin.

**Technical impact**  
This change decouples the CI pipeline from upstream vllm instability by allowing vllm-gaudi maintainers to control the tested vllm version via an external file. The Docker build process now includes a conditional git checkout step, making the HPU tests more resilient to breaking changes in vllm's main branch.

**Potential risks**  
If the fetched commit SHA is invalid or the vllm-gaudi repository becomes inaccessible, the build may fail. The `git fetch --unshallow` command could fail silently due to the `2>/dev/null \|\| true` suppression, potentially masking network or repository issues. There's also a risk of the pinning mechanism becoming stale if maintainers forget to update it.

**Key insights**  
This is a pragmatic solution for managing dependencies between closely coupled projects. Developers should monitor the pinning file's content and ensure it's updated promptly when vllm-gaudi adapts to new vllm APIs. Consider adding validation for the fetched commit SHA and logging for the unshallow operation to improve debuggability.

---

## 35. [[Doc] Improve UX of `--enable-log-requests`](https://github.com/vllm-project/vllm/pull/35723)


### Base Information

- **PR Number:** #35723
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-03-02 08:24:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35723/files) (5):**
  - `benchmarks/auto_tune/auto_tune.sh`
  - `benchmarks/multi_turn/README.md`
  - `vllm/engine/arg_utils.py`
  - `vllm/entrypoints/logger.py`
  - `vllm/entrypoints/openai/cli_args.py`

### Summary

**What changed and why**  
This PR improves the UX of the `--enable-log-requests` flag by adding clarifying help text and runtime warnings. It addresses user confusion about why prompt inputs are not logged by explaining the dependency on the `DEBUG` log level. It also removes the deprecated `--disable-log-requests` flag from the CLI and associated documentation/scripts.

**Technical impact**  
The changes modify the argument parser's help text to explicitly document the log level dependency (INFO for request metadata, DEBUG for prompts). A new initialization check in `RequestLogger` warns users if the configured log level is too high for the requested logging. The deprecated flag removal is a cleanup that simplifies the CLI interface.

**Potential risks**  
The warning logic uses `logger.warning_once` and `logger.info_once`, which rely on internal state to prevent spam. If these mechanisms fail, it could lead to repeated log messages. Removing the deprecated flag may break any external scripts or user workflows that still reference `--disable-log-requests`, though it was marked deprecated for six months.

**Key insights**  
The PR effectively clarifies a non-intuitive behavior by documenting it upfront and providing runtime feedback. Developers should note that request logging now has a clear relationship with the `VLLM_LOGGING_LEVEL` environment variable. All references to the removed `--disable-log-requests` flag should be purged from user documentation and automation scripts.

---

## 36. [[Core] Move test utility to test file](https://github.com/vllm-project/vllm/pull/35672)


### Base Information

- **PR Number:** #35672
- **Author:** [wjabbour](https://github.com/wjabbour)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-03-02 07:56:03
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35672/files) (4):**
  - `tests/kernels/moe/test_gpt_oss_triton_kernels.py`
  - `tests/kernels/moe/test_modular_oai_triton_moe.py`
  - `tests/kernels/moe/utils.py`
  - `vllm/model_executor/layers/utils.py`

### Summary

**What changed and why**  
The `shuffle_weight` utility function was removed from the production `vllm/model_executor/layers/utils.py` and relocated to a test-specific `tests/kernels/moe/utils.py` file. This change was made because the function is no longer used in production code—weight shuffling is now handled by ROCm's AITER operations—and is only required for unit tests.

**Technical impact**  
This refactoring reduces dead code in the production codebase, improving maintainability. The function remains available for test purposes, ensuring existing unit tests continue to pass without modification. No impact on runtime behavior or production performance is expected.

**Potential risks**  
If any production code still implicitly depends on this function (e.g., via dynamic imports or undocumented use), it could cause runtime failures. Additionally, future test files that import `shuffle_weight` from the old location will break unless updated to use the new test utility path.

**Key insights**  
This is a clean-up change that aligns with good software hygiene by removing unused code. Developers should verify that no other modules import `shuffle_weight` from the old location and update any test-related imports accordingly. The function's relocation to a test-specific utility file appropriately segregates test-only logic from production code.

---

## 37. [[CI] Fix mypy for vllm/device allocator](https://github.com/vllm-project/vllm/pull/35518)


### Base Information

- **PR Number:** #35518
- **Author:** [hickeyma](https://github.com/hickeyma)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-02 07:53:19
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35518/files) (2):**
  - `tools/pre_commit/mypy.py`
  - `vllm/device_allocator/cumem.py`

### Summary

**What changed and why**  
Fixed mypy type errors in `vllm/device_allocator/cumem.py` by correcting type annotations and variable assignments. Removed the file from the mypy exclusion list (`tools/pre_commit/mypy.py`) to enforce type checking.

**Technical impact**  
The changes improve type safety and clarity in the CUDA memory allocator module. Key fixes include updating callback signatures, adding proper `Iterator` return types for context managers, and correcting optional type annotations (e.g., `instance: "CuMemAllocator \| None"`). This ensures the code aligns with mypy’s static type checks and reduces runtime errors.

**Potential risks**  
If the corrected type signatures do not match the actual runtime behavior of external dependencies (e.g., `torch.cuda.memory.CUDAPluggableAllocator`), it could lead to mismatches between static and dynamic types. Additionally, removing the file from the exclusion list may expose new mypy errors in future changes.

**Key insights**  
Always ensure context managers explicitly return an `Iterator` (or `Generator`) for compatibility with `@contextmanager`. Use `\| None` for optional instance variables to avoid assignment errors. Verify that callback signatures match their actual usage in external libraries to prevent type mismatches.

---

## 38. [[Performance] Extract kv update ops from MLA attention backends](https://github.com/vllm-project/vllm/pull/34627)


### Base Information

- **PR Number:** #34627
- **Author:** [ElizaWszola](https://github.com/ElizaWszola)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-03-02 07:43:19
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34627/files) (3):**
  - `vllm/config/compilation.py`
  - `vllm/model_executor/layers/attention/mla_attention.py`
  - `vllm/v1/attention/backend.py`

### Summary

**What changed and why**  
This PR extracts KV cache update operations from MLA attention backends into separate custom ops, similar to previous optimizations for standard attention. The changes introduce a new `unified_mla_kv_cache_update` operator and modify the attention forward pass to ensure proper ordering between KV cache updates and attention computations under `torch.compile`.

**Technical impact**  
The refactoring decouples KV cache updates from the attention computation path, allowing for better optimization by the compiler and potentially improving performance. The addition of a dummy tensor dependency ensures that `torch.compile` preserves the execution order between the cache update and attention forward pass, maintaining correctness while enabling graph optimizations.

**Potential risks**  
If the dummy dependency mechanism fails or the slot mapping is incorrectly structured, it could lead to race conditions or incorrect KV cache state. The changes assume `slot_mapping` is a dictionary, which may break if the data structure changes. Additionally, the performance gains depend heavily on the compiler's ability to optimize the separated ops.

**Key insights**  
Developers should verify that the slot mapping remains a dictionary across all call sites and ensure the dummy dependency does not introduce unnecessary overhead. The approach aligns with prior optimizations (PR #25954), promoting consistency. Performance testing shows minimal regression, but further benchmarking across diverse hardware and model configurations is recommended.

---

## 39. [[Fix Bug]`num_active_loras` always equals to zero](https://github.com/vllm-project/vllm/pull/34119)


### Base Information

- **PR Number:** #34119
- **Author:** [RunkaiTao](https://github.com/RunkaiTao)
- **Merged By:** [jeejeelee](https://github.com/jeejeelee)
- **Merged time:** 2026-03-02 07:17:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34119/files) (7):**
  - `tests/lora/test_fused_moe_lora_kernel.py`
  - `tests/lora/test_gptoss_tp.py`
  - `vllm/lora/ops/triton_ops/fused_moe_lora_op.py`
  - `vllm/lora/ops/triton_ops/lora_expand_op.py`
  - `vllm/lora/ops/triton_ops/lora_kernel_metadata.py`
  - `vllm/lora/ops/triton_ops/lora_shrink_op.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The fix addresses a torch.compile compatibility issue where `num_active_loras` was incorrectly baked as a constant zero during tracing. Previously, `num_active_loras` was a Python integer, which torch.compile treated as static and captured its initial value (zero) during the first forward pass. This caused all subsequent kernel calls to receive zero regardless of the actual active LoRA count. The solution replaces the Python int with a CPU tensor (`num_active_loras_cpu`), ensuring torch.compile reads the tensor's current value dynamically on each call.

**Technical impact**  
This change ensures LoRA kernels correctly adapt to varying numbers of active LoRAs during inference, particularly when `specialize_active_lora` is enabled. The kernel grid dimensions now scale dynamically based on the actual active LoRA count, improving performance (as shown by the ~5% TTFT improvement in benchmarks). The modification maintains API compatibility by updating all related kernel functions and test utilities to accept a CPU tensor instead of an integer.

**Potential risks**  
If the CPU tensor is not properly synchronized or updated before kernel launches, it could lead to incorrect grid dimensions or kernel errors. Additionally, the change introduces a small overhead from tensor creation and device transfers, though this is minimal. Edge cases include ensuring the tensor remains on CPU and is not accidentally moved to GPU, which would break the kernel API.

**Key insights**  
Developers should treat mutable state that affects kernel behavior as tensors (not Python primitives) when using torch.compile to avoid constant baking. The fix also highlights the importance of aligning kernel specialization with dynamic runtime values. When modifying LoRA-related code, verify that all kernel call sites and tests are updated to use the tensor API, and ensure proper tensor initialization in `LoRAKernelMeta`.

---

## 40. [[Docs] Add breadcrumbs for better UX](https://github.com/vllm-project/vllm/pull/35749)


### Base Information

- **PR Number:** #35749
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 06:31:55
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35749/files) (1):**
  - `mkdocs.yaml`

### Summary

**What changed and why**  
Added `navigation.path` to the MkDocs theme configuration to enable breadcrumb navigation, enhancing user experience by providing better navigation context and hierarchy within the documentation site.

**Technical impact**  
This change activates the breadcrumbs feature in the MkDocs Material theme, which will display the current page's location within the site structure. It requires the theme to support this feature and may affect the layout and styling of the navigation bar.

**Potential risks**  
If the MkDocs Material theme version does not support the `navigation.path` feature, it could cause rendering errors or unexpected behavior. Additionally, existing custom CSS or JavaScript might conflict with the new breadcrumbs display.

**Key insights**  
Ensure the MkDocs Material theme is updated to a version that supports breadcrumbs. Test the documentation site thoroughly to verify that breadcrumbs appear correctly and do not break existing navigation or layout elements.

---

## 41. [[MyPy][BugFix] Check profiler is assigned before calling start() on it](https://github.com/vllm-project/vllm/pull/35505)


### Base Information

- **PR Number:** #35505
- **Author:** [hickeyma](https://github.com/hickeyma)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-02 05:23:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35505/files) (1):**
  - `vllm/v1/worker/gpu_worker.py`

### Summary

**What changed and why**  
The fix addresses a mypy type checking error where `self.profiler` could be `None` when calling `start()`. The code restructured the conditional logic to ensure `self.profiler` is properly initialized before attempting to start it, replacing a warning for unrecognized profiler types with a `ValueError`.

**Technical impact**  
This change improves type safety by eliminating a potential `None` access path. The architecture now enforces that `self.profiler` must be assigned (either as `TorchProfilerWrapper` or `CudaProfilerWrapper`) before `start()` is called, making the code more robust and self-documenting.

**Potential risks**  
The replacement of `logger.warning` with `ValueError` for unrecognized profiler types changes error handling from a soft warning to a hard exception, which could crash the application if invalid configuration slips through validation. Additionally, the logic still assumes `self.profiler_config.profiler` validation occurs elsewhere.

**Key insights**  
Developers should ensure upstream configuration validation is robust since the code now relies on it. The fix demonstrates good defensive programming by eliminating `None` paths, but consider whether the `ValueError` should be caught and logged at a higher level to maintain system stability with invalid configurations.

---

## 42. [Fix unresolved-import errors when using Astral's ty by removing src.root](https://github.com/vllm-project/vllm/pull/35681)


### Base Information

- **PR Number:** #35681
- **Author:** [tlrmchlsmth](https://github.com/tlrmchlsmth)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-03-02 02:26:48
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35681/files) (1):**
  - `pyproject.toml`

### Summary

**What changed and why**  
Removed the `root = "./vllm"` setting from the `[tool.ty.src]` section in `pyproject.toml`. This configuration was causing Astral's ty tool to incorrectly resolve imports prefixed with `vllm.` by searching in `./vllm/vllm/` instead of the correct module path, leading to `unresolved-import` errors. The change aligns with the deprecation of `src.root` and resolves cross-module import validation issues.

**Technical impact**  
The removal eliminates the misconfigured module search path, allowing ty to correctly resolve first-party imports like `import vllm.config`. This ensures static analysis and linting tools function properly without false positives for unresolved imports, improving developer experience and code validation accuracy.

**Potential risks**  
If other developers or CI environments rely on the old `src.root` behavior for custom module resolution, they might encounter inconsistent linting results. Additionally, if the codebase uses unconventional import structures, further adjustments to ty's configuration might be needed to handle all import patterns correctly.

**Key insights**  
This change addresses a deprecated configuration that was actively causing tooling errors. Developers should verify that their local ty installations are up-to-date to support the updated configuration. Consider documenting this change in team workflows to prevent confusion and ensure consistent linting across all environments.

---

## 43. [[Feat] Supports Anthropic Messages count_tokens API](https://github.com/vllm-project/vllm/pull/35588)


### Base Information

- **PR Number:** #35588
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 01:48:55
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35588/files) (3):**
  - `vllm/entrypoints/anthropic/api_router.py`
  - `vllm/entrypoints/anthropic/protocol.py`
  - `vllm/entrypoints/anthropic/serving.py`

### Summary

**What changed and why**  
This PR adds support for Anthropic's `count_tokens` API endpoint (`/v1/messages/count_tokens`). The implementation introduces new request/response models, a dedicated route handler, and refactors the existing message conversion logic to be reusable for both token counting and message generation. The purpose is to resolve issue #29915 by providing token counting functionality compatible with Anthropic's API specification.

**Technical impact**  
The changes extend the Anthropic-compatible API surface by adding a new endpoint that leverages the existing tokenization infrastructure. The refactoring of `_convert_anthropic_to_openai_request` into modular helper methods improves code maintainability and reusability. The system now supports token counting for Anthropic message formats, including complex content blocks (text, images, tool use/results) and system messages.

**Potential risks**  
The error handling in `count_tokens` catches all exceptions and returns a generic internal error, which may obscure specific failure causes. The refactored conversion logic, while more modular, introduces additional complexity and potential for regression in existing message generation flows. There is also a risk of incomplete support for all Anthropic content block types if the conversion logic does not handle all edge cases.

**Key insights**  
The PR successfully implements a new API endpoint while improving code structure through decomposition of a large method. Developers should ensure comprehensive testing of both token counting and message generation with varied input types (especially tool use and multimodal content). Consider enhancing error reporting in `count_tokens` to provide more specific error types for better debugging. The changes are backward compatible and do not affect existing functionality.

---

## 44. [[Misc] Cleanup useless `current_platform` import](https://github.com/vllm-project/vllm/pull/35715)


### Base Information

- **PR Number:** #35715
- **Author:** [wangxiyuan](https://github.com/wangxiyuan)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 01:36:54
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35715/files) (5):**
  - `vllm/compilation/passes/fusion/sequence_parallelism.py`
  - `vllm/config/model.py`
  - `vllm/distributed/parallel_state.py`
  - `vllm/v1/attention/backends/fa_utils.py`
  - `vllm/v1/attention/backends/flashinfer.py`

### Summary

**What changed and why**  
This PR removes unnecessary imports of `current_platform` from five files. The imports were either unused (as in `sequence_parallelism.py` where `FP8_DTYPE` was defined but not used) or redundant because `current_platform` is already imported elsewhere in the module or accessed via the global namespace.

**Technical impact**  
The changes reduce code clutter and eliminate unused variables, improving maintainability. Since `current_platform` is typically a globally accessible module, removing these imports does not affect functionality—it only cleans up redundant or dead code.

**Potential risks**  
Low risk, as the removed imports were either unused or already available in the module’s scope. However, developers should verify that `current_platform` is indeed accessible in each file’s context (e.g., through earlier imports or global initialization) to avoid `NameError` issues.

**Key insights**  
This cleanup highlights the importance of regularly removing dead code to keep the codebase lean. Developers should use linting tools to detect unused imports and variables proactively. Ensure that any future changes do not reintroduce similar redundancies.

---

## 45. [[CPU][Distributed] Fix Enable _CPUSHMDistributed only when TP/PP ranks share the same SHM group name](https://github.com/vllm-project/vllm/pull/34169)


### Base Information

- **PR Number:** #34169
- **Author:** [charlesashby](https://github.com/charlesashby)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-03-02 01:34:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34169/files) (1):**
  - `vllm/distributed/device_communicators/cpu_communicator.py`

### Summary

**What changed and why**  
Added a check `_all_group_ranks_share_shm_group_name()` to ensure all ranks in a TP/PP group compute the same shared‑memory group name before enabling the CPU SHM communicator. This prevents attempts to use SHM across different nodes (where names differ) and forces a fallback to torch.distributed, fixing a crash when ranks are on separate machines.

**Technical impact**  
The change introduces a lightweight all‑gather of the local SHM group name within each device group. If names mismatch, the communicator reverts to torch.distributed, preserving correctness while sacrificing the performance benefit of SHM for cross‑node groups. Single‑node deployments continue to use the fast SHM path.

**Potential risks**  
The all‑gather adds a small synchronization overhead during communicator initialization. Edge cases where ranks are on the same physical machine but different nodes (e.g., separate containers) will also fall back to torch.distributed, potentially missing SHM opportunities that could be enabled with a more stable group‑name generation scheme.

**Key insights**  
This is a safety‑first fix that ensures robustness in multi‑node CPU deployments. Developers should be aware that SHM is now only used when all ranks agree on the group name, which effectively means they must be on the same node. Future work could refine the naming logic to allow SHM for same‑machine multi‑node setups.

---

## 46. [[Misc] Bound NIXL upper bound version](https://github.com/vllm-project/vllm/pull/35495)


### Base Information

- **PR Number:** #35495
- **Author:** [NickLucche](https://github.com/NickLucche)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-03-02 00:57:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35495/files) (1):**
  - `requirements/kv_connectors.txt`

### Summary

**What changed and why**  
The change adds an upper version bound (`< 0.10.0`) to the `nixl` dependency in the `kv_connectors.txt` requirements file. This is a temporary measure to prevent upgrades to NIXL 0.10.0 or higher, as internal reports indicate agent disconnection issues (`nixlRemoteDisconnectError`) with newer versions.

**Technical impact**  
This restricts the dependency resolution to NIXL versions between 0.7.1 and 0.9.x, ensuring compatibility with the current system. It provides immediate stability by avoiding a known problematic version, but it may delay access to future bug fixes or features in NIXL 0.10.0+.

**Potential risks**  
If the underlying issue in NIXL 0.10.0 is not resolved promptly, this bound could become a long-term constraint, potentially causing dependency conflicts or security vulnerabilities if older versions are not maintained. Additionally, other dependencies that require NIXL >= 0.10.0 could break.

**Key insights**  
This is a reactive but necessary fix for stability. The team should investigate the root cause of the disconnection error and either work with the NIXL maintainers for a fix or plan an internal mitigation. Consider adding a comment with a reference to the issue report and a timeline for re-evaluating the bound.

---

## 47. [[CI] Defining extended V1 e2e + engine tests](https://github.com/vllm-project/vllm/pull/35580)


### Base Information

- **PR Number:** #35580
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-03-02 00:10:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35580/files) (3):**
  - `.buildkite/test-amd.yaml`
  - `.buildkite/test_areas/engine.yaml`
  - `tests/v1/e2e/test_spec_decode.py`

### Summary

**What changed and why**  
This PR adds dedicated CI steps for multi-GPU V1 e2e tests that were previously silently skipped. It introduces separate 2-GPU and 4-GPU test jobs targeting specific tensor parallelism and Eagle correctness tests, while keeping the existing 1-GPU step unchanged. This ensures proper CI coverage for multi-GPU functionality.

**Technical impact**  
The changes improve test coverage by executing previously skipped multi-GPU tests in appropriate hardware pools. The architecture now separates test runs by GPU requirements, allowing targeted execution and better resource utilization. The 1-GPU step continues to run all tests with multi-GPU tests self-skipping via decorators.

**Potential risks**  
The new 4-GPU test still runs on 8-GPU machines for stability (as noted), which may mask resource contention issues. There's a timeout comment indicating test stability needs verification before adjusting timeouts. The selective test filtering (`-k "tensor_parallelism"`) could miss future multi-GPU tests if naming conventions change.

**Key insights**  
Developers should ensure new multi-GPU tests follow the established naming patterns to be automatically included. The optional flag suggests these are initially non-blocking; monitor stability before promoting to required status. The GPU requirement annotations in test files must remain synchronized with CI configuration to maintain coverage.

---

