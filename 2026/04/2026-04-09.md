# vLLM Merged PR Report

**Report Date:** 2026-04-09 PDT

**Total Merged PRs:** 39

---

## 1. [[Core] Change max_model_len in EngineCoreReadyResponse to be non-None](https://github.com/vllm-project/vllm/pull/39442)


### Base Information

- **PR Number:** #39442
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 23:34:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39442/files) (2):**
  - `vllm/v1/engine/__init__.py`
  - `vllm/v1/engine/core_client.py`

### Summary

**What changed and why**  
The changes update the `EngineCoreReadyResponse` class to make `max_model_len` a required integer field instead of an optional field with a default `None` value. This follows up on previous PRs that established this field is never actually `None` in practice, making the type annotation more accurate.

**Technical impact**  
This simplifies the code by removing null checks and making the type system more precise. The `_apply_ready_response` method no longer needs conditional logic to handle a potential `None` value, resulting in cleaner and more straightforward code.

**Potential risks**  
If there are any edge cases or initialization paths where `max_model_len` could legitimately be `None`, this change could introduce runtime errors. The assumption that this field is always populated must hold true across all engine configurations and deployment scenarios.

**Key insights**  
The type system should accurately reflect runtime behavior to improve code clarity and catch errors early. Developers should verify that all code paths populating `EngineCoreReadyResponse` always set `max_model_len` to a valid integer value, especially in distributed or multi-engine setups.

---

## 2. [Add platform manual_seed_all API](https://github.com/vllm-project/vllm/pull/38468)


### Base Information

- **PR Number:** #38468
- **Author:** [yma11](https://github.com/yma11)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-09 22:43:51
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38468/files) (18):**
  - `benchmarks/kernels/benchmark_moe_align_block_size.py`
  - `benchmarks/kernels/deepgemm/benchmark_fp8_block_dense_gemm.py`
  - `tests/kernels/attention/test_lightning_attn.py`
  - `tests/kernels/core/test_fused_quant_layernorm.py`
  - `tests/kernels/moe/test_marlin_vs_trtllm_mxint4.py`
  - `tests/kernels/moe/test_moe.py`
  - `tests/kernels/moe/test_shared_fused_moe_routed_transform.py`
  - `tests/kernels/test_fused_quant_activation.py`
  - `tests/models/language/pooling/test_token_classification.py`
  - `tests/v1/e2e/spec_decode/test_lora_with_spec_decode.py`
  - `tests/v1/spec_decode/test_tree_attention.py`
  - `tools/pre_commit/check_torch_cuda.py`
  - `vllm/platforms/cpu.py`
  - `vllm/platforms/cuda.py`
  - `vllm/platforms/interface.py`
  - `vllm/platforms/rocm.py`
  - `vllm/platforms/xpu.py`
  - `vllm/utils/torch_utils.py`

### Summary

**What changed and why**  
This PR introduces a platform-agnostic `manual_seed_all` API across all platform implementations (CUDA, ROCm, XPU, CPU) to eliminate direct CUDA binding dependencies in tests and benchmarks. It replaces scattered `torch.cuda.manual_seed_all` calls with a unified `set_random_seed` utility that uses the new platform API, ensuring consistent seeding across different hardware backends.

**Technical impact**  
The changes decouple seeding logic from specific hardware APIs, making the codebase more portable and maintainable. The `set_random_seed` function now delegates to `current_platform.manual_seed_all`, which is implemented appropriately for each platform (e.g., no-op for CPU, device-specific seeding for CUDA/ROCm/XPU). This centralizes RNG management and reduces platform-specific conditional checks.

**Potential risks**  
The CPU platform’s `manual_seed_all` is a no-op, which may lead to unintended RNG behavior if CPU-specific tests rely on CUDA-style seeding. Additionally, the pre-commit hook now flags `torch.cuda.manual_seed` usage, but developers might inadvertently reintroduce direct CUDA calls in new code. Cross-platform consistency should be validated for non-CUDA backends.

**Key insights**  
Developers should exclusively use `set_random_seed` for reproducibility in tests and benchmarks. The platform abstraction simplifies supporting new hardware without modifying seeding logic. Ensure that any new platform implementations (e.g., for custom accelerators) define `manual_seed_all` appropriately. Review the pre-commit hook output to catch regressions.

---

## 3. [[BugFix][CPU] Add CPU profiler summary file output](https://github.com/vllm-project/vllm/pull/38366)


### Base Information

- **PR Number:** #38366
- **Author:** [Elm8116](https://github.com/Elm8116)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-04-09 22:41:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38366/files) (1):**
  - `vllm/profiler/wrapper.py`

### Summary

**What changed and why**  
This PR adds CPU profiler summary file output to match existing CUDA profiling behavior. Previously, CPU profiling only logged output without generating a summary file, causing inconsistency and silent loss of profiler data. The changes introduce a fallback that writes a CPU summary sorted by `self_cpu_time_total` to `profiler_out_<rank>.txt`, limited to rank 0 to avoid file contention.

**Technical impact**  
The refactoring extracts common table-building and file-writing logic into reusable methods (`_build_profiler_table`, `_write_profiler_table`), improving code maintainability. CPU profiling now consistently produces output files similar to CUDA, enhancing cross-platform profiling usability. The architecture remains unchanged, with the same URI path handling and rank-based output restrictions preserved.

**Potential risks**  
If `_is_uri_path` incorrectly identifies a filesystem path as a URI, valid local directories might be skipped, causing silent failures. The rank 0 restriction for CPU output could hide profiling data from other ranks in multi-GPU CPU-only setups, though this aligns with CUDA behavior. The hardcoded `row_limit=50` for CPU summaries may truncate data compared to CUDA's default limit of 100.

**Key insights**  
The refactoring successfully reduces code duplication and improves consistency between CPU and GPU profiling paths. Developers should verify that `_is_uri_path` handles all expected storage backends correctly. Consider making the CPU row limit configurable to match CUDA behavior if detailed comparison across platforms is needed. The changes are backward compatible and maintain existing CUDA functionality.

---

## 4. [[Bugfix] Fix broken explicit unquantized kv cache dtype support](https://github.com/vllm-project/vllm/pull/38922)


### Base Information

- **PR Number:** #38922
- **Author:** [Isotr0py](https://github.com/Isotr0py)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-09 22:27:54
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38922/files) (3):**
  - `csrc/attention/dtype_fp8.cuh`
  - `csrc/quantization/w8a8/fp8/amd/quant_utils.cuh`
  - `csrc/quantization/w8a8/fp8/nvidia/quant_utils.cuh`

### Summary

**What changed and why**  
The PR fixes a bug where explicit unquantized KV cache data types (like `bfloat16`) were incorrectly handled, causing runtime errors. The issue stemmed from string-based dispatch logic that didn't properly map unquantized types to the `kAuto` enum variant. Changes introduce a helper function `get_fp8_kv_cache_data_type` to centralize dtype string parsing and refactor dispatch macros to use enum comparisons instead of string matching.

**Technical impact**  
This refactor improves maintainability by replacing scattered string comparisons with a single enum-based dispatch. The KV cache handling now correctly supports both quantized (FP8 variants) and unquantized (float16, bfloat16, auto) data types across NVIDIA and AMD backends. The changes ensure consistent behavior between the two quantization implementations.

**Potential risks**  
If the helper function's mapping is incomplete or incorrect for future data types, similar dispatch failures could occur. The refactored macros now rely on early enum conversion, which could mask mismatches between backend support (e.g., if one backend omits a dtype). No validation ensures the enum covers all possible input strings from Python configuration.

**Key insights**  
Centralizing dtype parsing reduces duplication and aligns with software engineering best practices. Developers should ensure any new KV cache dtype added to the Python layer is also added to `get_fp8_kv_cache_data_type`. Consider adding static assertions or unit tests to verify enum coverage against all possible configuration strings.

---

## 5. [[feat]: make DCP error msg clearer](https://github.com/vllm-project/vllm/pull/28443)


### Base Information

- **PR Number:** #28443
- **Author:** [WorldExplored](https://github.com/WorldExplored)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 22:27:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/28443/files) (1):**
  - `vllm/v1/worker/cp_utils.py`

### Summary

**What changed and why**  
The changes enhance error messaging for Decode Context Parallelism (DCP) compatibility checks. When DCP is enabled (`dcp_world_size > 1`), the validation now provides a clearer, more actionable error message if an attention implementation lacks softmax LSE support during decode, guiding users to adjust `VLLM_ATTENTION_BACKEND` or disable DCP.

**Technical impact**  
This update improves debuggability without altering functional behavior. The validation logic remains the same—still asserting `need_to_return_lse_for_decode`—but the error message now explicitly links the issue to DCP requirements and suggests concrete remediation steps, aiding users in configuration troubleshooting.

**Potential risks**  
The changes are low-risk as they only affect error messaging. However, if the underlying condition (`need_to_return_lse_for_decode`) is incorrectly evaluated, the same false positives/negatives could occur. Additionally, the message assumes users have control over `VLLM_ATTENTION_BACKEND`, which may not always be the case in constrained environments.

**Key insights**  
The update demonstrates a best practice in error messaging: providing clear failure reasons and actionable solutions. Developers should ensure similar validations elsewhere in the codebase follow this pattern. Also, consider documenting supported attention backends for DCP to preempt configuration issues.

---

## 6. [[KV Offload] Implement `shutdown()` in `OffloadingConnector` and related classes](https://github.com/vllm-project/vllm/pull/39182)


### Base Information

- **PR Number:** #39182
- **Author:** [ronensc](https://github.com/ronensc)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-04-09 22:06:23
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39182/files) (6):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/scheduler.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/worker.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py`
  - `vllm/v1/kv_offload/abstract.py`
  - `vllm/v1/kv_offload/worker/cpu_gpu.py`
  - `vllm/v1/kv_offload/worker/worker.py`

### Summary

**What changed and why**  
A `shutdown()` method has been added throughout the KV offloading stack to enable clean resource release during engine shutdown. The implementation propagates shutdown calls from the top-level `OffloadingConnector` down to worker and scheduler components, ultimately clearing job queues, synchronizing pending transfers, and releasing Python-side resources like streams and events.

**Technical impact**  
This change introduces a formal resource cleanup lifecycle for the offloading subsystem, ensuring that GPU streams, events, and pending jobs are properly finalized. The architecture now supports graceful shutdown without leaking resources or attempting unnecessary cache operations (e.g., deferred store jobs are dropped since CPU cache is non-persistent).

**Potential risks**  
If `shutdown()` is called while offloading operations are still in progress, it may lead to premature resource cleanup or synchronization issues. The default no-op implementation in base classes could mask missing concrete shutdown logic in future subclasses. Additionally, clearing queues without completing jobs might be acceptable for CPU cache but could be problematic if persistent storage is later introduced.

**Key insights**  
Developers should ensure `shutdown()` is called only after all offloading work is complete. Consider adding logging or metrics to track cleanup. The pattern of dropping deferred store jobs aligns with the non-persistent nature of CPU cache but should be documented. Future handlers must implement `shutdown()` to avoid resource leaks.

---

## 7. [[Mergify] Update model vendor auto-label rules](https://github.com/vllm-project/vllm/pull/39312)


### Base Information

- **PR Number:** #39312
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 21:25:38
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39312/files) (2):**
  - `.github/mergify.yml`
  - `.github/workflows/issue_autolabel.yml`

### Summary

**What changed and why**  
This PR updates Mergify configuration to add automatic labeling for Mistral model changes and expands existing model vendor label rules to include additional code directories. The changes ensure that PRs modifying Mistral-related files receive a `mistral` label and automatically ping relevant committers, while also updating DeepSeek, Llama, and Qwen rules to cover tool parsers, reasoning, and Transformers utilities directories.

**Technical impact**  
The modifications enhance the automation of PR triaging by ensuring consistent labeling across model-specific code changes. By expanding directory coverage, the rules now capture a broader range of model-related contributions, improving maintainability and ensuring appropriate reviewers are notified. The addition of Mistral-specific rules aligns its handling with existing model vendors.

**Potential risks**  
Overly broad file patterns could incorrectly label unrelated changes if directory structures evolve or if file naming conventions overlap with other models. The auto-ping feature for Mistral issues may generate noise if the configured users are not the correct point of contact for all Mistral-related changes. There is also a risk of missing edge cases where model changes occur in unanticipated directories.

**Key insights**  
Developers should verify that the expanded directory patterns accurately reflect the project's code organization for each model vendor. Consider adding similar rules for other supported models to maintain consistency. Monitor the auto-labeling behavior to ensure it reduces manual triaging effort without introducing false positives.

---

## 8. [Add EXAONE-4.5](https://github.com/vllm-project/vllm/pull/39388)


### Base Information

- **PR Number:** #39388
- **Author:** [lkm2835](https://github.com/lkm2835)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-09 20:53:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39388/files) (11):**
  - `docs/models/supported_models.md`
  - `examples/offline_inference/vision_language.py`
  - `examples/offline_inference/vision_language_multi_image.py`
  - `tests/models/registry.py`
  - `vllm/config/speculative.py`
  - `vllm/model_executor/models/exaone4.py`
  - `vllm/model_executor/models/exaone4_5.py`
  - `vllm/model_executor/models/exaone4_5_mtp.py`
  - `vllm/model_executor/models/exaone_moe_mtp.py`
  - `vllm/model_executor/models/registry.py`
  - `vllm/v1/spec_decode/eagle.py`

### Summary

**What changed and why**  
This PR adds support for the EXAONE-4.5-33B vision-language model from LG AI Research. The changes include new model implementations (`exaone4_5.py` and `exaone4_5_mtp.py`), updates to registry and configuration files, and integration into example scripts and documentation.

**Technical impact**  
The addition extends vLLM's multimodal capabilities by incorporating a new vision-language architecture. The model leverages existing components like `Qwen2_5_VisionTransformer` for the vision encoder and introduces a new attention mechanism (`EXAONE4_5_VisionAttention`). It also adds support for multi-token prediction (MTP) via `Exaone4_5_MTP`, aligning with the speculative decoding framework.

**Potential risks**  
The model's vision encoder uses a custom attention implementation that may require thorough validation for correctness and performance. The MTP support modifies the speculative configuration, and any misconfiguration could affect inference stability. Additionally, the example scripts assume specific prompt formats and placeholders (`<\|image_pad\|>`, `<\|video_pad\|>`), which must match the model's tokenizer expectations.

**Key insights**  
Developers should verify that the vision encoder's rotary embedding and attention computations align with the original model. The MTP integration should be tested with speculative decoding to ensure compatibility. The example scripts provide a reference for prompt formatting, but users must ensure the processor and tokenizer are correctly initialized from the Hugging Face repository.

---

## 9. [[BugFix] Fix OOB read in CUTLASS grouped GEMM with epilogue](https://github.com/vllm-project/vllm/pull/38571)


### Base Information

- **PR Number:** #38571
- **Author:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-09 20:52:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38571/files) (2):**
  - `csrc/cutlass_extensions/epilogue/broadcast_load_epilogue_array_c3x.hpp`
  - `csrc/cutlass_extensions/epilogue/broadcast_load_epilogue_c3x.hpp`

### Summary

**What changed and why**  
The fix addresses an out-of-bounds (OOB) read in CUTLASS grouped GEMM epilogue by correcting a shape mismatch between predicate tensors and the tensors being copied. Previously, the code used `filter()` to remove stride‑0 modes, but this could lead to inconsistent tensor shapes, causing illegal memory accesses. The new approach explicitly slices stride‑0 modes at index 0, ensuring predicate and data tensors have matching layouts.

**Technical impact**  
This change ensures memory safety during column‑broadcast operations by aligning tensor shapes before the `copy_if` operation. The static assertions verify that stride‑0 modes exist as expected, enforcing the assumed tensor layout. The fix maintains performance by avoiding redundant copies while eliminating the OOB risk.

**Potential risks**  
If the static assertions fail (i.e., stride‑0 modes are not present), compilation will halt, which is safe but may indicate an unexpected layout change. The slicing logic assumes specific mode indices; any future layout modifications could reintroduce similar shape mismatches. The fix is limited to column‑broadcast scenarios and may not address analogous issues in other broadcast modes.

**Key insights**  
Always ensure predicate and data tensors have identical shapes when using conditional copy operations. Explicit slicing is more reliable than `filter()` for removing stride‑0 dimensions. Developers should verify tensor layouts with static assertions where possible, and consider similar checks in related epilogue code paths to prevent analogous bugs.

---

## 10. [feat(cpu): add CPU support for draft model speculative decoding](https://github.com/vllm-project/vllm/pull/32662)


### Base Information

- **PR Number:** #32662
- **Author:** [ganeshr10](https://github.com/ganeshr10)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-04-09 20:49:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/32662/files) (7):**
  - `cmake/cpu_extension.cmake`
  - `csrc/cpu/spec_decode_utils.cpp`
  - `csrc/cpu/torch_bindings.cpp`
  - `vllm/utils/cpu_triton_utils.py`
  - `vllm/v1/spec_decode/eagle.py`
  - `vllm/v1/spec_decode/utils.py`
  - `vllm/v1/worker/cpu_model_runner.py`

### Summary

**What changed and why**  
This PR adds CPU support for speculative decoding with draft models by implementing PyTorch fallbacks for Triton kernels. It introduces C++ implementations of key speculative decoding operations (eagle_prepare_inputs_padded, rejection sampling, etc.) and integrates them into the CPU backend via Python adapters. The changes enable CPU-based speculative decoding, which previously relied on GPU-specific Triton kernels.

**Technical impact**  
The PR extends the CPU model runner to support speculative decoding by replacing Triton kernel calls with C++ OpenMP-parallelized implementations. It modifies build configurations to include new C++ sources and updates Python wrappers to handle dtype conversions (int32 ↔ int64) between PyTorch and C++. The CPU backend now supports the same speculative decoding pipeline as GPU, including draft model execution and rejection sampling.

**Potential risks**  
- Dtype mismatches between Python (int32) and C++ (int64) could cause silent data corruption if conversion wrappers fail.  
- OpenMP parallelism may introduce thread-safety issues or performance bottlenecks on non-x86 architectures.  
- The CPU implementations may not fully match GPU behavior in edge cases (e.g., tensor padding, boundary conditions).  
- Increased binary size and compilation complexity due to additional C++ sources.

**Key insights**  
- The C++ kernels are carefully designed to mirror GPU Triton logic but use OpenMP for parallelism.  
- Python adapters handle dtype conversions transparently, but developers must ensure consistent tensor types.  
- CPU speculative decoding performance depends on OpenMP efficiency; tuning may be needed for different hardware.  
- This change makes speculative decoding hardware-agnostic, expanding vLLM’s deployment flexibility.

---

## 11. [[CT][FP8][Marlin] refactor CompressedTensorsW8A16Fp8 to use kernel abstraction](https://github.com/vllm-project/vllm/pull/38244)


### Base Information

- **PR Number:** #38244
- **Author:** [jikunshang](https://github.com/jikunshang)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-09 18:58:35
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38244/files) (4):**
  - `vllm/model_executor/kernels/linear/__init__.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a16_fp8.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/utils.py`

### Summary

**What changed and why**  
The PR refactors `CompressedTensorsW8A16Fp8` to use a kernel abstraction (`FP8ScaledMMLinearKernel`) instead of hardcoded Marlin kernel calls. This aligns the W8A16-FP8 path with existing kernel-selection infrastructure, centralizes kernel registration, and fixes a bug where block quantization on pre-Ada GPUs caused shape mismatches due to duplicate scale attributes.

**Technical impact**  
This change decouples the compressed-tensors scheme from specific kernel implementations, enabling easier addition of new FP8 kernels for different platforms. The kernel selection logic is now unified via `init_wfp8_a16_linear_kernel`, and shared mapping dictionaries (`STRATEGY_TO_PARAMETER_TYPE`, `STRATEGY_TO_WEIGHT_QUANT_KEY`) promote consistency across quantization schemes.

**Potential risks**  
The bug fix deletes `weight_scale` before registering `weight_scale_inv` for block quantization; if other code paths still reference `weight_scale`, it could lead to attribute errors. Additionally, the refactor assumes all future FP8 kernels will use the same parameter names (`weight`, `weight_scale`, etc.), which may limit flexibility for kernels with different requirements.

**Key insights**  
Developers should note that new FP8 kernels can now be added by registering them in `_POSSIBLE_WFP8A16_KERNELS` without modifying the scheme class. The shared utility mappings reduce duplication but require careful updates if new quantization strategies are introduced. The bug fix highlights the importance of ensuring only one scale attribute exists when kernels process layers.

---

## 12. [[Feature] Add auto-detection for reasoning_config when only reasoning_parser is set](https://github.com/vllm-project/vllm/pull/38214)


### Base Information

- **PR Number:** #38214
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-04-09 18:36:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38214/files) (10):**
  - `docs/features/reasoning_outputs.md`
  - `tests/entrypoints/openai/chat_completion/test_thinking_token_budget.py`
  - `tests/v1/logits_processors/test_correctness.py`
  - `vllm/config/reasoning.py`
  - `vllm/config/vllm.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/reasoning/abs_reasoning_parsers.py`
  - `vllm/reasoning/basic_parsers.py`
  - `vllm/v1/engine/input_processor.py`
  - `vllm/v1/sample/logits_processor/builtin.py`

### Summary

**What changed and why**  
This PR adds auto-detection for `reasoning_config` when only `reasoning_parser` is set. Previously, users needed to explicitly configure both `--reasoning-parser` and `--reasoning-config`. Now, if only `--reasoning-parser` is provided, vLLM will attempt to automatically initialize the reasoning boundary tokens (`reasoning_start_str` and `reasoning_end_str`) from the parser's implementation, simplifying configuration.

**Technical impact**  
The changes introduce a new `enabled` property in `ReasoningConfig` to track successful token ID initialization. The `initialize_token_ids` method now queries the reasoning parser for boundary strings if they aren't explicitly configured. This creates a dependency chain where the parser must implement `reasoning_start_str` and `reasoning_end_str` properties for auto-detection to work. The engine config automatically creates a `ReasoningConfig` when only a parser is specified.

**Potential risks**  
If a reasoning parser doesn't implement the required `reasoning_start_str` and `reasoning_end_str` properties, auto-initialization will fail with a warning but won't crash. This could lead to silent failures where reasoning features appear configured but aren't actually enabled. The validation logic now checks `reasoning_config.enabled` instead of just `reasoning_config is None`, which could affect existing integrations that relied on the previous behavior.

**Key insights**  
Developers implementing custom reasoning parsers must now add `reasoning_start_str` and `reasoning_end_str` properties for auto-detection to work. The system gracefully degrades with warnings rather than errors when auto-detection fails. Test coverage has been expanded to verify both explicit and auto-detected configurations work correctly with the same test logic.

---

## 13. [[ROCm] Fix AITER ops fake impl and minor bugs](https://github.com/vllm-project/vllm/pull/36092)


### Base Information

- **PR Number:** #36092
- **Author:** [ChuanLi1101](https://github.com/ChuanLi1101)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-09 17:56:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36092/files) (1):**
  - `vllm/_aiter_ops.py`

### Summary

**What changed and why**  
Fixed three ROCm-specific bugs: 1) The fake implementation for `_rocm_aiter_fused_topk_fake` now returns properly shaped tensors instead of `None`, ensuring compatibility with `torch.compile` tracing. 2) Removed the incorrect `self` parameter from the `@staticmethod shuffle_weight`. 3) Added missing spaces in two `ValueError` messages for proper formatting.

**Technical impact**  
These changes ensure that the ROCm-specific custom ops behave consistently between real and fake implementations, preventing tracing failures in FakeTensor mode. The static method fix corrects parameter binding, and the error message improvements enhance debugging clarity without affecting functionality.

**Potential risks**  
The fake implementation now creates tensors with specific shapes and dtypes; if these don't exactly match the real op's output under all conditions, it could cause subtle discrepancies during compilation. The static method change could break any existing code that was incorrectly passing three arguments (though such usage would have been buggy).

**Key insights**  
Always ensure fake/meta implementations return tensors with matching shapes and dtypes to the real ops. Use `@staticmethod` correctly by omitting `self`. Maintain consistent error message formatting for better developer experience. These fixes are critical for ROCm compilation workflows.

---

## 14. [fix(gdn): Align prefill warmup with real prefill path](https://github.com/vllm-project/vllm/pull/39169)


### Base Information

- **PR Number:** #39169
- **Author:** [ibrahim1023](https://github.com/ibrahim1023)
- **Merged By:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged time:** 2026-04-09 17:49:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39169/files) (1):**
  - `vllm/model_executor/layers/mamba/gdn_linear_attn.py`

### Summary

**What changed and why**  
The PR aligns the Gated Delta Network (GDN) prefill warmup path with the actual prefill execution path. Previously, the warmup used different tensor construction (`fused_gdn_gating`) and kernel configuration (`use_qk_l2norm_in_kernel=True`), which could defer first-request work to the real prefill. Now, warmup mirrors real prefill by using `fused_post_conv_prep` and disables in-kernel L2 normalization.

**Technical impact**  
This ensures that kernel autotuning during warmup matches the real prefill’s computational graph, reducing first-request latency and improving consistency. The change affects only the warmup phase, leaving inference unchanged but ensuring cached kernel configurations are optimal for actual usage.

**Potential risks**  
If `fused_post_conv_prep` or the disabled L2 norm path have unhandled edge cases (e.g., numerical stability), warmup could fail or mislead autotuning. The reliance on dummy tensors may not fully replicate all real input distributions, though this is mitigated by mirroring the real path.

**Key insights**  
Developers should verify that the warmup’s tensor shapes and dtypes exactly match real prefill inputs. Consider adding validation that warmup kernel timings align with real execution. This fix highlights the importance of keeping warmup and production paths synchronized to avoid deferred computation.

---

## 15. [[Model][Perf] Enable checkpoints prefetching for Lustre FS by default](https://github.com/vllm-project/vllm/pull/39422)


### Base Information

- **PR Number:** #39422
- **Author:** [arpera](https://github.com/arpera)
- **Merged By:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged time:** 2026-04-09 17:48:00
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39422/files) (1):**
  - `vllm/model_executor/model_loader/weight_utils.py`

### Summary

**What changed and why**  
This PR enables automatic checkpoint prefetching for Lustre filesystems by default, extending the existing NFS support. It also significantly improves logging clarity by providing detailed filesystem detection, checkpoint size, and RAM availability information to help users understand prefetching decisions.

**Technical impact**  
The changes refactor the prefetching logic to be more modular with separate functions for filesystem type detection, checkpoint size calculation, and RAM availability checking. The decision logic now explicitly handles four scenarios based on filesystem type and RAM availability, providing appropriate logging for each case. This improves maintainability and makes the prefetching behavior more transparent.

**Potential risks**  
The Lustre filesystem detection relies on Linux-specific `/proc/mounts` parsing, which may fail on non-Linux systems or in containerized environments. The 90% RAM threshold is hardcoded and may not be optimal for all deployment scenarios. There's a risk of false positives if other network filesystems aren't recognized, though the logging improvements help mitigate this.

**Key insights**  
The refactoring into separate utility functions improves code organization and testability. The detailed logging will significantly help with debugging and understanding system behavior. Developers should consider making the RAM threshold configurable and potentially adding support for additional network filesystem types in future iterations.

---

## 16. [[LMCache] vLLM Block Allocation Event](https://github.com/vllm-project/vllm/pull/38856)


### Base Information

- **PR Number:** #38856
- **Author:** [Oasis-Git](https://github.com/Oasis-Git)
- **Merged By:** [ApostaC](https://github.com/ApostaC)
- **Merged time:** 2026-04-09 17:30:29
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38856/files) (1):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/lmcache_mp_connector.py`

### Summary

**What changed and why**  
Added block allocation event reporting to LMCache for observability. The changes introduce a new method `_report_block_allocation_deltas` that collects per-request block allocation data (for both new and cached requests) and sends it to LMCache via the scheduler adapter. This enables tracing of block allocations in vLLM.

**Technical impact**  
The change integrates vLLM's block allocation lifecycle with LMCache's monitoring capabilities. It captures allocation deltas—full token content for new requests and only newly appended blocks for cached requests—ensuring LMCache can map blocks to their actual token content. This enhances observability without affecting core scheduling logic.

**Potential risks**  
- The import logic for `RequestAllocationRecord` uses fallbacks that may cause confusion if LMCache's type definitions change.  
- If `tracker.all_token_ids` does not contain enough tokens (e.g., due to partial tracking), slicing could raise index errors.  
- Performance overhead may increase for workloads with many small requests due to per-request record construction and reporting.

**Key insights**  
- Ensure LMCache's `report_block_allocations` method is implemented and handles the provided records correctly.  
- Validate that `tracker.all_token_ids` always matches the expected length (blocks × block size) to avoid slicing errors.  
- Consider adding metrics to monitor the reporting overhead in production.

---

## 17. [[CI/Build] Don't auto-rebase PRs with CI failures](https://github.com/vllm-project/vllm/pull/39443)


### Base Information

- **PR Number:** #39443
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-04-09 13:57:37
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39443/files) (1):**
  - `.github/mergify.yml`

### Summary

**What changed and why**  
The PR modifies the Mergify configuration to prevent automatic rebasing of PRs with CI failures. It updates two condition names from `status-failure` to `check-failure` for accuracy and adds a new condition `#check-failure = 0` to the auto-rebase rule, ensuring only PRs without any failing checks are rebased.

**Technical impact**  
This change reduces CI workload by avoiding unnecessary rebases of PRs with existing CI failures, aligning the auto-rebase feature with its original purpose of preventing stale merges rather than continuously updating problematic PRs. The correction from `status-failure` to `check-failure` ensures proper condition evaluation according to Mergify documentation.

**Potential risks**  
If the `check-failure` condition doesn't correctly identify all CI failure types, some PRs might still be rebased inappropriately or fail to rebase when they should. There's also a risk that PRs with transient failures might remain stale longer if authors don't manually trigger updates.

**Key insights**  
The change optimizes CI resource usage by focusing rebases on healthy PRs only. Developers should ensure their PRs pass all checks before adding the `ready` label to benefit from auto-rebase. The condition name correction improves configuration reliability and should be verified against other rules that might use similar patterns.

---

## 18. [[Spec Decode] fix returning size mismatch on extract hidden states proposer](https://github.com/vllm-project/vllm/pull/38610)


### Base Information

- **PR Number:** #38610
- **Author:** [zzaebok](https://github.com/zzaebok)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-04-09 13:39:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38610/files) (1):**
  - `vllm/v1/spec_decode/extract_hidden_states.py`

### Summary

**What changed and why**  
The change fixes a shape mismatch in the speculative decoding proposer. The `propose` method was returning raw tensor dimensions that could be `[batch_size, 2]` during decode steps (containing both target and speculative verification tokens), but the caller expects `[batch_size, 1]` since `num_speculative_tokens=1`. The fix slices the tensor to return only the first column.

**Technical impact**  
This ensures the proposer output consistently matches the expected shape `[batch_size, 1]` for downstream operations, preventing runtime errors in speculative decoding. It maintains compatibility with the existing speculative decoding architecture while correcting a latent bug that manifested during decode steps.

**Potential risks**  
If the tensor shape ever deviates from the assumed `[batch_size, 2]` during decode steps (e.g., due to future changes in speculative token handling), the hard-coded slice `[:, :1]` could become incorrect. Additionally, this fix assumes `num_speculative_tokens` is always 1, which may limit flexibility if the proposer is later generalized.

**Key insights**  
The fix is minimal and targeted, but consider adding a shape assertion or documentation to clarify the expected tensor structure. Reviewers should verify that all speculative decoding tests pass and that no other code paths rely on the full `[batch_size, 2]` tensor. Future enhancements should parameterize the slice based on `num_speculative_tokens`.

---

## 19. [Add nightly b200 test for spec decode eagle correctness](https://github.com/vllm-project/vllm/pull/38577)


### Base Information

- **PR Number:** #38577
- **Author:** [puririshi98](https://github.com/puririshi98)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-04-09 13:09:10
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38577/files) (1):**
  - `.buildkite/test_areas/spec_decode.yaml`

### Summary

**What changed and why**  
Three new nightly test steps were added to the Buildkite pipeline configuration for spec decode functionality, specifically targeting B200 devices. These steps mirror existing tests for Eagle correctness, speculators+MTP, and draft model functionality, but run on B200 hardware as optional nightly jobs to expand test coverage across different GPU architectures.

**Technical impact**  
This change extends the CI/CD pipeline to include B200 GPU testing for spec decode features, ensuring compatibility and correctness across multiple hardware platforms. The tests maintain the same source dependencies and pytest commands as their H200 counterparts, providing consistent validation while leveraging different device capabilities.

**Potential risks**  
Running identical tests on different hardware could reveal device-specific issues or performance discrepancies that weren't previously captured. The optional nature of these tests means failures might be overlooked if not properly monitored. There's also a risk of increased resource consumption and pipeline runtime if these nightly tests become more frequent or extensive.

**Key insights**  
The addition demonstrates a strategic approach to multi-device testing, particularly for new GPU architectures like B200. Developers should ensure test results from these nightly runs are properly tracked and investigated. Consider establishing alerting mechanisms for optional test failures to prevent hardware-specific regressions from going unnoticed.

---

## 20. [[ASR] Fix spacing bw chunks in multi chunk audio transcription](https://github.com/vllm-project/vllm/pull/39116)


### Base Information

- **PR Number:** #39116
- **Author:** [ekagra-ranjan](https://github.com/ekagra-ranjan)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 12:46:33
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39116/files) (5):**
  - `tests/entrypoints/openai/speech_to_text/test_transcription_inter_chunk_spacing.py`
  - `vllm/entrypoints/openai/speech_to_text/serving.py`
  - `vllm/entrypoints/openai/speech_to_text/speech_to_text.py`
  - `vllm/model_executor/models/cohere_asr.py`
  - `vllm/model_executor/models/interfaces.py`

### Summary

**What changed and why**  
This PR fixes missing spacing between consecutive audio chunks in ASR transcription outputs. Previously, text from separate chunks was concatenated without spaces (e.g., "Hello, thisis vllm"), causing readability issues. The fix introduces language-aware spacing—adding a space for most languages but omitting it for languages like Chinese and Japanese that don't use spaces between words.

**Technical impact**  
The changes add a `separator` parameter to streaming generators and modify the text-joining logic in both streaming and non-streaming paths. A new helper function `asr_inter_chunk_separator()` determines the appropriate separator based on language, using a `no_space_languages` set defined in the `SupportsTranscription` interface and model implementations (e.g., Cohere ASR). This ensures consistent behavior across transcription and translation tasks.

**Potential risks**  
If a model’s `no_space_languages` set is incorrectly defined or missing, it could lead to improper spacing for certain languages. The fix assumes chunk boundaries align with word boundaries, which may not always hold, potentially inserting spaces mid-word in rare cases. Additionally, the streaming logic modifies `output.text` in place, which could affect downstream processing if the output object is reused elsewhere.

**Key insights**  
The solution elegantly handles both streaming and non-streaming cases with a single separator logic. Developers should ensure all ASR model implementations properly inherit and define `no_space_languages`. The extensive test suite provides good coverage, but real-world audio with rapid speech or ambiguous chunk cuts should be validated to ensure spacing remains natural.

---

## 21. [[XPU] check is_xccl_available before oneccl warmup](https://github.com/vllm-project/vllm/pull/39302)


### Base Information

- **PR Number:** #39302
- **Author:** [xinyu-intel](https://github.com/xinyu-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-09 12:42:18
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39302/files) (1):**
  - `vllm/v1/worker/xpu_worker.py`

### Summary

**What changed and why**  
Added a conditional check `torch.distributed.is_xccl_available()` before performing a oneccl warmup operation via `torch.distributed.all_reduce`. This prevents crashes when PyTorch is built without XCCL (XPU Collective Communications Library) support, ensuring the code only attempts XCCL operations when the library is available.

**Technical impact**  
The change makes the initialization of XPU workers more robust by avoiding unnecessary collective operations in environments lacking XCCL. It maintains existing functionality for systems with XCCL while gracefully skipping the warmup step when XCCL is unavailable, improving compatibility across different PyTorch builds.

**Potential risks**  
If `is_xccl_available()` returns `True` but XCCL is improperly configured or fails during the `all_reduce` call, the original crash risk may persist. Additionally, environments without XCCL will skip the warmup, which could lead to suboptimal performance if the warmup is critical for later collective operations, though this is likely a minor concern.

**Key insights**  
Always verify library availability before invoking hardware-specific collective operations to enhance portability and prevent runtime crashes. Consider adding logging or a warning when XCCL is unavailable to aid debugging. Ensure that skipping the warmup does not affect subsequent distributed operations in non-XCCL environments.

---

## 22. [[Refactor] Move NVFP4 GEMM management into NvFp4LinearKernel](https://github.com/vllm-project/vllm/pull/39129)


### Base Information

- **PR Number:** #39129
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-09 12:05:36
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39129/files) (11):**
  - `vllm/model_executor/kernels/linear/__init__.py`
  - `vllm/model_executor/kernels/linear/nvfp4/__init__.py`
  - `vllm/model_executor/kernels/linear/nvfp4/base.py`
  - `vllm/model_executor/kernels/linear/nvfp4/cutlass.py`
  - `vllm/model_executor/kernels/linear/nvfp4/emulation.py`
  - `vllm/model_executor/kernels/linear/nvfp4/fbgemm.py`
  - `vllm/model_executor/kernels/linear/nvfp4/flashinfer.py`
  - `vllm/model_executor/kernels/linear/nvfp4/marlin.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w4a4_nvfp4.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`
  - `vllm/model_executor/layers/quantization/utils/nvfp4_utils.py`

### Summary

**What changed and why**  
The refactor moves NVFP4 GEMM management into a unified kernel abstraction (`NvFp4LinearKernel`), aligning it with existing patterns for FP8/Int8/MP kernels. Each backend (CUTLASS, FlashInfer, Marlin, FBGEMM, emulation) is now a subclass, and kernel selection is centralized via `init_nvfp4_linear_kernel()`. The old backend enum and helper functions are removed.

**Technical impact**  
This change standardizes NVFP4 linear operations under the kernel/linear abstraction, improving code consistency and maintainability. It introduces a clear priority-based auto-selection mechanism (with environment variable overrides) and decouples backend-specific weight formatting and GEMM execution into isolated kernel classes.

**Potential risks**  
The removal of the old backend selection logic could introduce regressions if the new kernel auto-selection fails on untested hardware configurations. Additionally, the emulation fallback warning may be triggered incorrectly if environment variables or platform detection misbehave. Cross-platform support (ROCM) currently only includes the emulation kernel, which may impact performance.

**Key insights**  
Developers should verify that the kernel selection logic (`init_nvfp4_linear_kernel`) correctly prioritizes backends for their target hardware. The refactor simplifies adding new NVFP4 backends by following the established subclass pattern. Ensure that all kernel subclasses properly implement `is_supported()` and `can_implement()` to avoid runtime failures.

---

## 23. [[CI/Build] Update auto-rebase rule](https://github.com/vllm-project/vllm/pull/39429)


### Base Information

- **PR Number:** #39429
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-04-09 10:59:56
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39429/files) (1):**
  - `.github/mergify.yml`

### Summary

**What changed and why**  
The PR updates an automated GitHub workflow rule in Mergify configuration. It replaces the deprecated `rebase` action with `update` and increases the commit-behind threshold from 40 to 50 commits to accommodate higher daily commit volumes.

**Technical impact**  
This change modernizes the CI/CD automation by using the supported `update` method, which merges the target branch instead of rebasing. The increased threshold reduces unnecessary automation triggers, aligning the rule with current development velocity.

**Potential risks**  
If the daily commit rate exceeds 50, PRs may still fall too far behind main before auto-update triggers, potentially increasing merge conflicts. The `update` action could introduce merge commits instead of linear history, which may affect some team workflows.

**Key insights**  
Developers should monitor whether the 50-commit threshold remains appropriate as project activity evolves. Consider documenting the rationale for using `update` over `rebase` to ensure team awareness of the change in merge strategy.

---

## 24. [[ROCm] Disable fused_silu_mul_block_quant on ROCm](https://github.com/vllm-project/vllm/pull/39387)


### Base Information

- **PR Number:** #39387
- **Author:** [micah-wil](https://github.com/micah-wil)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-09 10:59:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39387/files) (1):**
  - `vllm/compilation/passes/fusion/act_quant_fusion.py`

### Summary

**What changed and why**  
The change modifies a conditional check from `is_cuda_alike()` to `is_cuda()` to disable the `SiluMulBlockQuantPattern` fusion specifically on ROCm platforms. This addresses failures on ROCm due to missing support for the `torch.ops._C.per_token_group_fp8_quant` operation required by certain FP8 quantization schemes.

**Technical impact**  
This selectively disables the fused `silu_mul_block_quant` kernel for ROCm while preserving it for NVIDIA CUDA platforms. The fusion remains active for CUDA but is now excluded on ROCm, preventing runtime assertion errors when using FP8 models with AITER or manual quantization configurations.

**Potential risks**  
ROCm users may experience performance degradation for FP8-quantized models that previously benefited from this fusion. There is also a risk of platform-specific code divergence if ROCm support is added later without re-enabling this path. The change assumes `is_cuda_alike()` incorrectly includes ROCm for this fusion, which may affect other platform checks.

**Key insights**  
The fix is a targeted workaround for missing ROCm operator support. Developers should track upstream ROCm progress on `per_token_group_fp8_quant` and re-enable the fusion once available. Consider adding a feature flag or runtime detection for this operator to avoid permanent platform branching.

---

## 25. [[Quantization] Support Quark W8A8 INT8 MoE inference](https://github.com/vllm-project/vllm/pull/36320)


### Base Information

- **PR Number:** #36320
- **Author:** [JoursBleu](https://github.com/JoursBleu)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-09 10:24:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36320/files) (4):**
  - `tests/quantization/test_quark.py`
  - `vllm/model_executor/layers/fused_moe/utils.py`
  - `vllm/model_executor/layers/quantization/quark/quark.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`

### Summary

**What changed and why**  
This PR adds support for loading MoE models quantized with AMD Quark's W8A8 INT8 scheme (per-channel weight + per-token dynamic activation) into vLLM. Previously, such models failed due to missing detection logic, lack of INT8 MoE methods, and restrictive assertions in quantization utilities. The changes introduce detection for dynamic per-token W8A8, a new `QuarkW8A8Int8MoEMethod` class, and relaxed branching in `_int8_quantize()` to handle various INT8 quantization paths.

**Technical impact**  
The modifications extend vLLM's quantization support to include dynamic per-token INT8 activation with both per-tensor and per-channel weight scaling for MoE models. This enables efficient inference of large MoE models like MiniMax-M2.1 (456B) with minimal accuracy loss, as shown by the test results. The architecture now accommodates three INT8 quantization variants: per-token dynamic, static per-tensor, and dynamic per-tensor.

**Potential risks**  
The defensive branching in `_int8_quantize()` assumes correct scale tensor handling; if `A_scale` is provided but invalid (e.g., wrong shape), it could lead to silent errors. The MoE weight scale merging logic for per-tensor schemes may introduce slight quantization error if original scales vary significantly. Additionally, the reliance on `trust_remote_code=True` for custom models could pose security considerations in some deployments.

**Key insights**  
Developers should verify that INT8 MoE models use symmetric quantization, as zero-points are discarded post-loading. The test results show excellent accuracy recovery (99.2% on MMLU), but performance should be validated across diverse MoE architectures. Ensure that any future INT8 quantization schemes align with the three supported paths to avoid assertion failures.

---

## 26. [[Model Runner V2] Fix flex attention kv blocks calculation issue](https://github.com/vllm-project/vllm/pull/39353)


### Base Information

- **PR Number:** #39353
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [LucasWilkinson](https://github.com/LucasWilkinson)
- **Merged time:** 2026-04-09 10:07:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39353/files) (1):**
  - `vllm/v1/attention/backends/flex_attention.py`

### Summary

**What changed and why**  
The fix addresses a calculation error in flex attention's KV blocks initialization. Previously, the code incorrectly used `max_model_len` (maximum sequence length for a single request) to determine the number of query blocks and KV indices. This caused a tensor shape mismatch when processing batched tokens. The fix replaces these calculations with `max_num_batched_tokens` (maximum tokens across all batched requests) and properly computes KV indices based on the query block size and page count.

**Technical impact**  
This change ensures the flex attention backend correctly allocates tensors for batched inference scenarios. The persistent KV indices tensor now has dimensions based on the maximum number of query groups (derived from batched tokens) and KV indices per query group, aligning with the actual memory requirements during multi-request processing. This enables the Model Runner V2 to handle batched requests without tensor shape errors.

**Potential risks**  
The fix assumes `max_num_batched_tokens` accurately represents the worst-case token count across all concurrent requests. If scheduler configurations change or edge cases emerge where actual token counts exceed this estimate, similar allocation issues could recur. Additionally, the relationship between `max_num_kv_indices` and `max_num_pages_per_seq` should be validated to ensure it covers all possible KV cache scenarios.

**Key insights**  
Always use batched token limits (`max_num_batched_tokens`) rather than per-request limits (`max_model_len`) when allocating resources for concurrent request processing. The fix demonstrates proper alignment between query processing groups and KV cache indexing. Developers should verify that similar calculations elsewhere in the attention backends follow this pattern to prevent hidden batch-processing bugs.

---

## 27. [[ROCm][CI] Resolved nvidia package deps issue](https://github.com/vllm-project/vllm/pull/39421)


### Base Information

- **PR Number:** #39421
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-09 09:06:06
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39421/files) (3):**
  - `.pre-commit-config.yaml`
  - `docker/Dockerfile.rocm`
  - `requirements/rocm-test.txt`

### Summary

**What changed and why**  
This PR addresses ROCm CI/Docker build failures caused by NVIDIA package naming inconsistencies. It expands `--no-emit-package` exclusions in `.pre-commit-config.yaml` and `rocm-test.txt` to cover all NVIDIA package variants (unsuffixed, `-cu12`, `-cu13`). Additionally, it modifies `Dockerfile.rocm` to persist the built vLLM wheel for later reinstallation instead of verifying PyTorch's ROCm build.

**Technical impact**  
The changes ensure that ROCm dependency resolution ignores all NVIDIA CUDA packages, preventing conflicts when building for AMD hardware. The Dockerfile modification improves build reliability by ensuring the vLLM wheel remains available after compiler removal, though it removes the explicit PyTorch ROCm verification step.

**Potential risks**  
Removing the PyTorch ROCm verification could allow CUDA-based PyTorch to be installed inadvertently, breaking ROCm compatibility. The extensive exclusion list may become outdated if NVIDIA introduces new package naming schemes or versions, requiring ongoing maintenance.

**Key insights**  
Developers should monitor NVIDIA package naming changes and update exclusions accordingly. Consider adding the PyTorch ROCm verification back in a later stage or as a separate check to maintain compatibility assurance. The wheel persistence approach is a practical solution for multi-stage Docker builds.

---

## 28. [[Performance Improvement] Update `batched_count_greater_than` to handle batch size 1 without recompile](https://github.com/vllm-project/vllm/pull/38933)


### Base Information

- **PR Number:** #38933
- **Author:** [Lucaskabela](https://github.com/Lucaskabela)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 08:51:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38933/files) (3):**
  - `tests/v1/sample/test_batched_count_greater_than.py`
  - `vllm/v1/sample/ops/logprobs.py`
  - `vllm/v1/sample/sampler.py`

### Summary

**What changed and why**  
The PR modifies `batched_count_greater_than` to avoid PyTorch Dynamo recompilation when batch size changes from 1 to larger values. Specifically, it removes `dynamic=True` from the `torch.compile` decorator and adds `torch._check` assertions on batch dimensions, while also adding `mark_unbacked` calls in `gather_logprobs` to prevent 0/1 specialization. A comprehensive test is added to verify no recompilation occurs.

**Technical impact**  
These changes eliminate a ~205ms recompilation overhead in RL use cases with batch size 1, improving runtime performance. The modifications ensure the batch dimension is treated symbolically, allowing a single compiled graph to handle varying batch sizes without re-specialization. The core counting logic remains unchanged.

**Potential risks**  
Using `torch._check` and `mark_unbacked` introduces reliance on internal PyTorch APIs, which may change in future versions. The test relies on monkey-patching and internal Dynamo counters, which could be fragile. Additionally, removing `dynamic=True` might affect other dynamic dimensions beyond batch size, though the added checks aim to constrain behavior.

**Key insights**  
The fix effectively addresses a known Dynamo specialization issue, but developers should monitor for PyTorch updates that could break these internal API usages. The test provides a good guard against regression but should be kept updated with PyTorch changes. Consider documenting the reliance on internal APIs for future maintenance.

---

## 29. [[CI/Build] Fix memory cleanup in MM test](https://github.com/vllm-project/vllm/pull/39411)


### Base Information

- **PR Number:** #39411
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-04-09 08:50:46
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39411/files) (2):**
  - `.buildkite/test_areas/models_multimodal.yaml`
  - `tests/models/multimodal/generation/test_memory_leak.py`

### Summary

**What changed and why**  
The PR fixes a CI failure by modifying the test execution strategy for multimodal memory leak tests. It splits the memory leak test (`test_memory_leak.py`) from the main test run and executes it separately, while also renaming a specific test function from `test_qwen3_vl_no_memory_leak` to the more generic `test_no_memory_leak`.

**Technical impact**  
This change isolates the memory leak test execution, which may help in debugging CI failures related to memory cleanup. The test suite structure now treats memory leak tests as a separate concern, potentially allowing for more controlled environment setup or resource monitoring during their execution.

**Potential risks**  
Splitting the test execution adds complexity to the CI pipeline and could lead to inconsistent test environments between the main run and the isolated memory leak test. The renamed test function might break existing test discovery patterns or documentation references if they relied on the specific function name.

**Key insights**  
The approach suggests memory leak tests require special handling, possibly due to resource constraints or cleanup requirements. Developers should ensure the isolated test execution maintains the same preconditions as the original test suite. Consider documenting why this separation is necessary for future maintenance.

---

## 30. [[UX] Improve error message for MM input too long](https://github.com/vllm-project/vllm/pull/39409)


### Base Information

- **PR Number:** #39409
- **Author:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged By:** [Isotr0py](https://github.com/Isotr0py)
- **Merged time:** 2026-04-09 06:20:19
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39409/files) (1):**
  - `vllm/v1/engine/input_processor.py`

### Summary

**What changed and why**  
The PR improves error message clarity by replacing "length" with "embedding tokens" when multimedia inputs exceed the encoder cache size. This addresses user confusion where "length" could be misinterpreted as video/audio duration rather than token count, as reported in issue #39408.

**Technical impact**  
The change is purely cosmetic and affects only error message wording. It doesn't alter any validation logic, cache behavior, or system functionality—just improves communication when validation fails due to multimedia input size constraints.

**Potential risks**  
No functional risks since validation logic remains unchanged. However, the new phrasing "embedding tokens" assumes users understand tokenization concepts, which might still cause confusion for some non-technical users interpreting multimedia inputs.

**Key insights**  
This is a UX-focused improvement that enhances error message specificity. Developers should ensure similar terminology updates are applied consistently across all multimedia-related error messages to maintain clarity. Consider adding documentation about tokenization concepts for multimedia inputs to further aid user understanding.

---

## 31. [[BugFix] fix tests/kernels/moe/test_moe_layer.py](https://github.com/vllm-project/vllm/pull/39404)


### Base Information

- **PR Number:** #39404
- **Author:** [zou3519](https://github.com/zou3519)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-04-09 05:49:00
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39404/files) (1):**
  - `vllm/model_executor/layers/fused_moe/runner/moe_runner_base.py`

### Summary

**What changed and why**  
The change modifies the condition in `get_layer_from_name` to only treat `"from_forward_context"` as a sentinel layer name when `HAS_OPAQUE_TYPE` is false. This fixes a test that broke after PR #39286, which stopped using `"from_forward_context"` as a sentinel for PyTorch 2.11+. The test was incorrectly using the literal string `"from_forward_context"` as a layer name, causing it to be misinterpreted.

**Technical impact**  
This update ensures backward compatibility with older PyTorch versions while allowing newer versions to treat `"from_forward_context"` as a normal layer name. It aligns the sentinel behavior with the PyTorch version check introduced in PR #39286, preventing unintended special handling of layers that happen to be named `"from_forward_context"` in newer PyTorch environments.

**Potential risks**  
If `HAS_OPAQUE_TYPE` is incorrectly set or the PyTorch version detection fails, the sentinel logic could be misapplied, leading to runtime errors. Additionally, any other tests or code that rely on the old sentinel behavior without considering `HAS_OPAQUE_TYPE` might need similar updates to avoid hidden bugs.

**Key insights**  
Developers should ensure that any direct usage of `"from_forward_context"` as a literal string in tests or code is reviewed for compatibility with the PyTorch version-specific sentinel logic. This fix highlights the importance of version-aware conditionals when deprecating sentinel values, and similar patterns should be audited across the codebase.

---

## 32. [[Tests] Add Qwen3-VL multimodal memory leak check](https://github.com/vllm-project/vllm/pull/39268)


### Base Information

- **PR Number:** #39268
- **Author:** [lalit10](https://github.com/lalit10)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-09 04:54:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39268/files) (3):**
  - `tests/models/multimodal/generation/test_memory_leak.py`
  - `vllm/utils/mem_constants.py`
  - `vllm/utils/mem_utils.py`

### Summary

**What changed and why**  
Added a new memory leak test for the Qwen3-VL multimodal model to detect GPU/CPU memory growth during repeated inference. The test establishes a post-warmup memory baseline and verifies that memory usage remains stable across subsequent rounds. This addresses issue #16353 by providing an engine-level leak check.

**Technical impact**  
Introduces a targeted integration test that validates memory management for multimodal workloads. The test uses weak references and explicit garbage collection to isolate memory measurements, ensuring accurate detection of leaks. It also adds utility constants (KB/KiB) and formatting functions to support memory reporting.

**Potential risks**  
The test may produce false positives in CI environments due to shared GPU memory or background processes. The strict zero-growth threshold (0 MiB) could be overly sensitive to minor memory fluctuations. Additionally, the test relies on platform-specific resource usage reporting, which may behave differently across operating systems.

**Key insights**  
This test provides a valuable pattern for memory leak detection that can be extended to other models. Developers should consider adding tolerance thresholds for CI stability and ensure the test runs in isolated GPU environments. The memory utility additions improve consistency in reporting across the codebase.

---

## 33. [nemotron-nano-vl: Allow `use_audio_in_video` to be passed at `vllm serve` time](https://github.com/vllm-project/vllm/pull/38538)


### Base Information

- **PR Number:** #38538
- **Author:** [askliar](https://github.com/askliar)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 04:44:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38538/files) (2):**
  - `vllm/model_executor/models/nano_nemotron_vl.py`
  - `vllm/transformers_utils/processors/nano_nemotron_vl.py`

### Summary

**What changed and why**  
The changes enable dynamic configuration of `use_audio_in_video` at serving time and fix a crash when audio is pre-populated by the chat completions endpoint. The implementation now resolves `use_audio_in_video` statically during initialization, handles videos without audio streams gracefully via a per-video `has_audio` mask, and properly injects audio context only for videos with valid audio.

**Technical impact**  
These modifications decouple audio extraction logic from per-request processor instantiation, improving performance by avoiding redundant HF processor creation. The system now supports both library usage (extracting audio from video bytes) and upstream pre-populated audio paths, ensuring consistent video-audio pairing. The changes also enhance robustness by validating audio-video correspondence and silently skipping videos without audio streams.

**Potential risks**  
If upstream provides pre-populated audio items that don't match the video count, validation will raise a `ValueError`, potentially breaking requests. The graceful handling of missing audio streams relies on exception catching, which could mask unexpected errors if the exception scope is too broad. Additionally, the `has_audio` mask logic assumes strict alignment between `<video>` tokens and video items, which may fail if the prompt structure diverges.

**Key insights**  
Developers should ensure that any upstream audio provisioning maintains a 1:1 correspondence with videos. The logging added for audio extraction and pre-populated usage aids debugging. Consider narrowing the exception catch in `_extract_audio_from_videos` to specific expected errors (e.g., `NoAudioStreamError`) to avoid suppressing unrelated failures. The static resolution of `use_audio_in_video` at init simplifies configuration but requires server restarts for changes.

---

## 34. [[Core] Simplify API server handshake](https://github.com/vllm-project/vllm/pull/39364)


### Base Information

- **PR Number:** #39364
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 03:56:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39364/files) (5):**
  - `vllm/entrypoints/cli/serve.py`
  - `vllm/v1/engine/__init__.py`
  - `vllm/v1/engine/core.py`
  - `vllm/v1/engine/core_client.py`
  - `vllm/v1/engine/utils.py`

### Summary

**What changed and why**  
This PR simplifies the API server handshake process by consolidating post-initialization configuration data (e.g., `max_model_len`, `num_gpu_blocks`, `dp_stats_address`) into a single `EngineCoreReadyResponse` message sent from engines to frontends. Previously, this data was passed through multiple handshakes, requiring delayed API server startup in external DP router/multi API-server scenarios.

**Technical impact**  
The changes streamline communication by removing the dual-handshake complexity and eliminating the need to delay API server startup in certain DP configurations. The `EngineCoreReadyResponse` is now a dataclass (instead of msgspec.Struct) and includes additional fields (`num_gpu_blocks`, `dp_stats_address`), while the handshake logic is simplified in `serve.py` and `core.py`.

**Potential risks**  
Removing the delayed API server startup could introduce race conditions if frontends rely on the `dp_stats_address` before it's received. The dataclass change may affect serialization/deserialization compatibility if not handled correctly by msgspec. There's also a risk of incorrect `num_gpu_blocks` summation in DP mode if engines report overlapping or inconsistent values.

**Key insights**  
Developers should verify that the `EngineCoreReadyResponse` dataclass is properly serialized by msgspec (note the removal of `array_like` and `omit_defaults`). The consolidation reduces code complexity but requires careful testing in external DP and hybrid load-balancing scenarios to ensure no regression in coordinator address propagation. The removal of `_ready_response_decoder` global variable in favor of inline decoding may slightly impact performance but improves code clarity.

---

## 35. [[Docs] Bring README updates into docs README](https://github.com/vllm-project/vllm/pull/39397)


### Base Information

- **PR Number:** #39397
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-09 03:35:01
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39397/files) (1):**
  - `docs/README.md`

### Summary

**What changed and why**  
This PR updates the documentation README to reflect recent improvements from the main repository, specifically enhancing the feature lists and broadening the project's description. The changes modernize the documentation by adding new supported features, quantization methods, hardware, and model architectures.

**Technical impact**  
The updates improve the accuracy and completeness of the public-facing documentation, ensuring it aligns with the current capabilities of vLLM. This provides users with a more comprehensive and up-to-date understanding of the library's features and supported ecosystems.

**Potential risks**  
There is a minor risk of the documentation becoming overly detailed or difficult to maintain if feature lists continue to expand rapidly. Additionally, some listed features (e.g., specific hardware plugins or model architectures) may require corresponding implementation details elsewhere in the docs to avoid confusion.

**Key insights**  
The documentation now better highlights vLLM's growth and active community, which is valuable for attracting users and contributors. Developers should ensure that any newly mentioned features (like specific kernels or hardware support) are consistently documented and tested across the codebase to maintain credibility.

---

## 36. [[Frontend][4/n] Improve pooling entrypoints \| pooling.](https://github.com/vllm-project/vllm/pull/39153)


### Base Information

- **PR Number:** #39153
- **Author:** [noooop](https://github.com/noooop)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-04-09 03:09:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39153/files) (43):**
  - `tests/entrypoints/openai/chat_completion/test_chat_error.py`
  - `tests/entrypoints/openai/chat_completion/test_serving_chat.py`
  - `tests/entrypoints/openai/completion/test_completion_error.py`
  - `tests/entrypoints/openai/completion/test_lora_resolvers.py`
  - `tests/entrypoints/openai/generative_scoring/test_generative_scoring.py`
  - `tests/entrypoints/openai/responses/test_serving_responses.py`
  - `tests/entrypoints/pooling/classify/test_offline.py`
  - `tests/entrypoints/pooling/classify/test_online.py`
  - `tests/entrypoints/pooling/embed/test_offline.py`
  - `tests/entrypoints/pooling/embed/test_online.py`
  - `tests/entrypoints/pooling/scoring/test_bi_encoder_online.py`
  - `tests/entrypoints/pooling/scoring/test_cross_encoder_online.py`
  - `tests/entrypoints/pooling/token_classify/test_offline.py`
  - `tests/entrypoints/pooling/token_classify/test_online.py`
  - `tests/entrypoints/pooling/token_embed/test_offline.py`
  - `tests/entrypoints/pooling/token_embed/test_online.py`
  - `tests/entrypoints/serve/disagg/test_generate_stream.py`
  - `tests/entrypoints/serve/lora/test_serving_models.py`
  - `tests/v1/engine/test_async_llm.py`
  - `vllm/engine/protocol.py`
  - `vllm/entrypoints/llm.py`
  - `vllm/entrypoints/openai/api_server.py`
  - `vllm/entrypoints/openai/engine/serving.py`
  - `vllm/entrypoints/openai/models/serving.py`
  - `vllm/entrypoints/pooling/__init__.py`
  - `vllm/entrypoints/pooling/base/io_processor.py`
  - `vllm/entrypoints/pooling/base/serving.py`
  - `vllm/entrypoints/pooling/classify/io_processor.py`
  - `vllm/entrypoints/pooling/classify/serving.py`
  - `vllm/entrypoints/pooling/embed/io_processor.py`
  - `vllm/entrypoints/pooling/embed/serving.py`
  - `vllm/entrypoints/pooling/io_processor_factories.py`
  - `vllm/entrypoints/pooling/pooling/api_router.py`
  - `vllm/entrypoints/pooling/pooling/io_processor.py`
  - `vllm/entrypoints/pooling/pooling/serving.py`
  - `vllm/entrypoints/pooling/scoring/io_processor.py`
  - `vllm/entrypoints/pooling/scoring/serving.py`
  - `vllm/entrypoints/pooling/typing.py`
  - `vllm/entrypoints/sagemaker/api_router.py`
  - `vllm/entrypoints/serve/render/serving.py`
  - `vllm/plugins/io_processors/__init__.py`
  - `vllm/v1/engine/async_llm.py`
  - `vllm/v1/engine/llm_engine.py`

### Summary

**What changed and why**  
This PR refactors pooling entrypoints to improve architecture and simplify the codebase. The main changes include removing the `io_processor` attribute from the engine protocol and related classes, consolidating pooling task handling into a unified `PoolingIOProcessor` framework, and updating test files to reflect these changes. The goal is to streamline the pooling API implementation and reduce redundancy.

**Technical impact**  
The removal of `io_processor` from the engine protocol decouples the engine from specific IO processing logic, shifting responsibility to pooling-specific processors. This enhances modularity and simplifies the engine’s interface. The introduction of a refined `PoolingServing` hierarchy and updated `init_pooling_io_processors` function centralizes pooling task management, improving consistency across embedding, classification, scoring, and plugin tasks.

**Potential risks**  
- The removal of `io_processor` from the engine may break existing custom integrations that directly depend on this attribute.
- Changes to error messages and task validation logic could affect client-side error handling.
- The consolidation of pooling logic might introduce regressions in edge cases, especially for plugin-based or multimodal tasks.

**Key insights**  
- Developers should update any custom code that references `engine.io_processor` to use the new pooling IO processor factories.
- The refactoring aligns with a cleaner separation of concerns, making the codebase more maintainable.
- Ensure thorough testing of all pooling tasks (embed, classify, token_embed, token_classify, plugin, scoring) to validate the new architecture.

---

## 37. [[CI] fix possible user permission issues in nightly index generation](https://github.com/vllm-project/vllm/pull/39390)


### Base Information

- **PR Number:** #39390
- **Author:** [Harry-Chen](https://github.com/Harry-Chen)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-09 01:14:07
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39390/files) (1):**
  - `.buildkite/scripts/generate-and-upload-nightly-index.sh`

### Summary

**What changed and why**  
The change modifies a Docker command in the CI script to run the container with the current user's UID and GID instead of the default root user (UID 0). This addresses permission errors that occurred when the container attempted to remove or modify files created by a non-root user in the host-mounted volume.

**Technical impact**  
This ensures file operations within the mounted volume respect the host user's permissions, preventing "Permission denied" errors during index generation. The change is isolated to the specific Docker invocation used when an older Python version is detected, maintaining compatibility while fixing the permission issue.

**Potential risks**  
If the host user's UID/GID doesn't exist inside the container, it could cause unexpected behavior (though slim images typically include a "nobody" user). Additionally, any container processes that require elevated privileges (e.g., installing system packages) may fail, but this is unlikely for the given Python script.

**Key insights**  
Always run CI containers with host user permissions when mounting volumes to avoid permission conflicts. Consider using `-u $(id -u):$(id -g)` as a best practice for similar Docker commands in CI pipelines. Verify that the container image supports non-root users for all required operations.

---

## 38. [Fix NUMA binding on non-CDMM Grace-Blackwell systems](https://github.com/vllm-project/vllm/pull/39361)


### Base Information

- **PR Number:** #39361
- **Author:** [soodoshll](https://github.com/soodoshll)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-04-09 00:36:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39361/files) (1):**
  - `vllm/platforms/cuda.py`

### Summary

**What changed and why**  
The fix addresses NUMA binding failures on non-CDMM Grace-Blackwell systems (e.g., GB200), where each GPU's HBM is exposed as a memory-only NUMA node without CPUs. Previously, `nvmlDeviceGetNumaNodeId()` returned these nodes, causing `numactl --cpunodebind` to fail. The change adds a check to verify if the NVML-reported NUMA node has CPUs; if not, it falls back to CPU-affinity-based detection to find the nearest CPU-bearing node.

**Technical impact**  
This modifies the NUMA node detection logic in `vllm/platforms/cuda.py` to handle heterogeneous NUMA topologies more robustly. The fallback mechanism ensures GPU binding works correctly on systems with memory-only NUMA nodes, maintaining compatibility with existing CPU-affinity detection for other platforms. The change is localized to the `get_device_numa_node` method and introduces a new helper method `_numa_node_has_cpus`.

**Potential risks**  
If the `cpulist` file read fails or returns unexpected data, the fallback may incorrectly assume no CPUs exist, potentially leading to suboptimal NUMA binding. Additionally, the debug logging could produce excessive output in multi-GPU environments. Edge cases involving non-standard sysfs paths or permissions might affect the helper method's reliability.

**Key insights**  
The solution elegantly handles a specific hardware topology without disrupting existing behavior. Developers should ensure the sysfs path `/sys/devices/system/node/node{node_id}/cpulist` is accessible in all deployment environments. Consider adding a configuration option to manually override NUMA node detection if future hardware variations arise.

---

## 39. [[ROCm] Remove unnecessary fp8 roundtrip in gather cache NHD dequant](https://github.com/vllm-project/vllm/pull/39122)


### Base Information

- **PR Number:** #39122
- **Author:** [Bortlesboat](https://github.com/Bortlesboat)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-09 00:12:22
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39122/files) (1):**
  - `vllm/v1/attention/backends/rocm_aiter_fa.py`

### Summary

**What changed and why**  
The change removes an unnecessary FP8 roundtrip in the NHD dequantization path of the gather cache kernel. Previously, after loading FP8 values and scaling them to float32, the results were cast back to FP8 before storage, only to be auto-cast to BF16 by Triton. Now, the scaled float32 values are directly cast to the output buffer's dtype (BF16), eliminating a precision-losing intermediate step.

**Technical impact**  
This optimization improves numerical precision by avoiding a lossy FP8 conversion that served no purpose. The kernel now correctly implements dequantization by converting directly from FP8 to float32 (via scaling) and then to the target dtype, matching the expected behavior where dequantization outputs higher-precision values. Triton's auto-casting on store handles the final conversion efficiently.

**Potential risks**  
If the output buffer's dtype differs unexpectedly from BF16 (e.g., FP16 or float32), the change relies on Triton's auto-casting behavior, which should be safe but is now more explicit. There is a minimal risk if `key_ptr_offset.dtype.element_ty` does not match the intended output dtype, though this is consistent with the buffer allocation.

**Key insights**  
This fix aligns the dequantization path with its purpose—converting compressed data to a higher-precision format without unnecessary intermediate steps. Developers should ensure that output buffer dtypes are correctly configured, as the kernel now depends on them for the final cast. The change is a clear precision improvement with no performance cost.

---
