# vLLM Merged PR Report

**Report Date:** 2026-04-03 PDT

**Total Merged PRs:** 29

---

## 1. [Refactor Arctic loading to use AutoWeightsLoader](https://github.com/vllm-project/vllm/pull/38955)


### Base Information

- **PR Number:** #38955
- **Author:** [lalit10](https://github.com/lalit10)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-03 22:01:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38955/files) (1):**
  - `vllm/model_executor/models/arctic.py`

### Summary

**What changed and why**  
The refactor moves Arctic-specific weight loading logic from `ArcticForCausalLM` to `ArcticModel` and replaces it with `AutoWeightsLoader`. This centralizes weight remapping (like stacked parameters and MoE expert handling) in the base model class while keeping `lm_head` handling in the causal LM class, aligning with a broader standardization effort (#15697).

**Technical impact**  
This change improves code maintainability by separating concerns: `ArcticModel` now handles complex weight mappings (MLP/MoE layers, stacked parameters), while `ArcticForCausalLM` uses a generic loader. The MoE layer detection now dynamically uses `config.moe_layer_frequency` instead of hardcoded layer parity, making the system more configurable and less brittle.

**Potential risks**  
The removal of the loading time logger message could affect user experience during long loads. There's a risk if `config.moe_layer_frequency` or `config.use_residual` are incorrectly set, as the weight mapping logic now depends on these. The conditional skip for `lm_head` when `tie_word_embeddings` is True must be verified to avoid double-loading.

**Key insights**  
The refactor successfully decouples model-specific weight remapping from the generic loading infrastructure. Developers should ensure the config attributes (`moe_layer_frequency`, `use_residual`) are correctly populated. Consider re-adding or relocating the informational loading message for user transparency if long load times are expected.

---

## 2. [[Bugfix] Fix DSV32 weight loading](https://github.com/vllm-project/vllm/pull/38870)


### Base Information

- **PR Number:** #38870
- **Author:** [zyongye](https://github.com/zyongye)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-04-03 19:57:52
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38870/files) (2):**
  - `vllm/model_executor/models/deepseek_mtp.py`
  - `vllm/model_executor/models/deepseek_v2.py`

### Summary

**What changed and why**  
The changes fix a KeyError during FP8 checkpoint loading by conditionally fusing the `wk` and `weights_proj` layers only for FP4 checkpoints. Previously, the fused `wk_weights_proj` layer was always created, causing missing weight keys when loading non-FP4 checkpoints. Now, separate `wk` and `weights_proj` layers are used for non-FP4 configurations.

**Technical impact**  
This modifies the model architecture dynamically based on quantization type, ensuring compatibility with both FP4 and non-FP4 checkpoints. The forward pass and weight loading logic now branch on `is_fp4_ckpt`, maintaining performance for FP4 while restoring correctness for other quantization schemes.

**Potential risks**  
If `is_fp4_ckpt` detection fails (e.g., due to changes in `quant_config.get_name()`), model loading could break. The branching logic increases complexity and must be kept consistent across initialization, forward pass, and weight loading. Edge cases with unknown quantization types may default to the non-FP4 path, which might not be optimal.

**Key insights**  
Always validate quantization configuration handling in model initialization. Consider centralizing the `is_fp4_ckpt` logic to avoid duplication. Ensure any future quantization methods are accounted for in the conditional checks. The fix is minimal and targeted, but thorough testing with various checkpoint formats is recommended.

---

## 3. [[ROCm][CI] Fix ROCm Dockerfile conftest generation for older Docker parsers](https://github.com/vllm-project/vllm/pull/38959)


### Base Information

- **PR Number:** #38959
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-03 19:41:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38959/files) (1):**
  - `docker/Dockerfile.rocm`

### Summary

**What changed and why**  
The change replaces a heredoc (`cat << 'EOF'`) with a `printf` command to generate the `conftest.py` file in the ROCm Dockerfile. This addresses compatibility issues with older Docker/Buildx frontends that may not correctly parse heredoc syntax during image builds.

**Technical impact**  
This modification ensures consistent file generation across different Docker parser versions, improving build reliability. The `printf` approach is more portable and less dependent on specific shell or Docker frontend features, reducing potential build failures in CI environments.

**Potential risks**  
While `printf` is widely supported, the multi-line formatting could become harder to maintain if the `conftest.py` content grows significantly. There is also a minor risk of syntax errors if quote escaping is not handled correctly in future edits, though the current change appears correct.

**Key insights**  
Always prefer portable shell commands over syntax that may vary across Docker versions. This fix highlights the importance of testing builds with different tooling versions. Developers should consider similar heredoc usage in other Dockerfiles and update them proactively to avoid hidden compatibility issues.

---

## 4. [[Bug] Fix workspace manager `_current_workspaces` size](https://github.com/vllm-project/vllm/pull/38853)


### Base Information

- **PR Number:** #38853
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-03 18:29:45
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38853/files) (1):**
  - `vllm/v1/worker/workspace.py`

### Summary

**What changed and why**  
The fix addresses an initialization issue where `_current_workspaces` was hardcoded to a list of two `None` values (`[None, None]`). It now correctly initializes the list with `self._num_ubatches` entries, each set to `None`, ensuring the workspace manager properly supports the configured number of micro-batch slots.

**Technical impact**  
This change aligns the internal workspace list size with the `num_ubatches` parameter passed during initialization. It ensures that workspace buffers are managed per active ubatch slot as intended, preventing potential index errors or incorrect buffer management when `num_ubatches` is not equal to 2.

**Potential risks**  
If `num_ubatches` is set to 0 or a negative value, the initialization `[None] * self._num_ubatches` would create an empty or invalid list, potentially leading to runtime errors. The code should validate `num_ubatches` is a positive integer. Additionally, existing code that implicitly relied on the hardcoded size of 2 may need review.

**Key insights**  
Always validate configuration parameters like `num_ubatches` at initialization. The update to the docstring clarifying "workspace ubatch slots" is helpful for maintainability. Consider adding a validation check, such as `assert self._num_ubatches > 0`, to prevent misuse.

---

## 5. [[Bug] Fix compile error for `swap_blocks_batch` in CUDA 13](https://github.com/vllm-project/vllm/pull/38915)


### Base Information

- **PR Number:** #38915
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-04-03 16:56:38
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38915/files) (1):**
  - `csrc/cache_kernels.cu`

### Summary

**What changed and why**  
The changes fix a CUDA 13 compilation error in `swap_blocks_batch` by updating tensor data pointer access to mutable pointers and adding conditional compilation for CUDA versions ≥13.0. This addresses an API change in CUDA 13 where `cuMemcpyBatchAsync` no longer accepts a `fail_idx` parameter.

**Technical impact**  
The code now uses `mutable_data_ptr<int64_t>()` instead of `data_ptr<int64_t>()` to avoid const-casting, improving type safety. The conditional compilation ensures compatibility across CUDA versions: CUDA ≥13.0 uses a simplified API call, while older versions retain the `fail_idx` parameter for error tracking.

**Potential risks**  
If the conditional macro `CUDA_VERSION` is incorrectly defined or missing, it could lead to compilation failures or runtime errors. The removal of `fail_idx` in CUDA ≥13.0 reduces error granularity, making debugging harder when copies fail. Additionally, the fallback path for older CUDA/ROCm remains unchanged, which may have performance implications.

**Key insights**  
Developers should verify CUDA version detection works correctly across all build environments. Consider adding runtime version checks if compile-time detection is insufficient. The fallback path should be monitored for performance, and future updates might unify error handling to maintain debuggability across CUDA versions.

---

## 6. [[Bugfix][LoRA] Fix missing in_proj_z in Qwen3_5ForConditionalGenerati…](https://github.com/vllm-project/vllm/pull/38927)


### Base Information

- **PR Number:** #38927
- **Author:** [elenalil-aws](https://github.com/elenalil-aws)
- **Merged By:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged time:** 2026-04-03 16:30:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38927/files) (1):**
  - `vllm/model_executor/models/qwen3_5.py`

### Summary

**What changed and why**  
Added a single line to include `in_proj_z` in the `packed_modules_mapping` dictionary when LoRA is enabled. This fixes a missing entry that caused `IndexError` during LoRA adapter loading under tensor parallelism, specifically for Qwen3.5 conditional generation models.

**Technical impact**  
Ensures that when LoRA is enabled, the model correctly maps both `in_proj_qkv` and `in_proj_z` projections, aligning the conditional generation variant's behavior with the base causal LM implementation. This resolves a root cause that previously required workarounds in downstream tensor parallelism logic.

**Potential risks**  
Minimal risk given the change is a direct mirror of existing correct logic from the base class. However, developers should verify that all Qwen3.5 variants (including MoE) now consistently handle LoRA mappings, as the fix only addresses the conditional generation path.

**Key insights**  
This fix directly addresses a root cause rather than masking symptoms, improving long-term maintainability. Developers should ensure similar mappings are reviewed in other model families to prevent analogous LoRA/tensor parallelism incompatibilities.

---

## 7. [[ROCm][CI] Minor missing import patch](https://github.com/vllm-project/vllm/pull/38951)


### Base Information

- **PR Number:** #38951
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-03 16:01:20
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38951/files) (1):**
  - `docker/Dockerfile.rocm`

### Summary

**What changed and why**  
Added `import sys` inside the `pytest_unconfigure` function to fix a missing import error. The function uses `sys.stdout.flush()` and `sys.stderr.flush()` but previously lacked the necessary import statement.

**Technical impact**  
This change ensures the `sys` module is available within the function scope, preventing a `NameError` during test teardown. It maintains the existing behavior of flushing output streams before exiting, which is critical for reliable test logging.

**Potential risks**  
The import is placed inside the function rather than at the module level, which could slightly impact performance if the function is called repeatedly, though this is negligible for a teardown hook. There's also a risk that other functions in the same module might rely on `sys` without proper imports, but this change is localized.

**Key insights**  
Always verify imports are in scope where they are used, especially in CI/CD scripts where missing dependencies can cause silent failures. Consider auditing the module for similar issues, and evaluate if moving the import to the module level would improve clarity without sacrificing functionality.

---

## 8. [[ROCm][CI] Added back missing common deps](https://github.com/vllm-project/vllm/pull/38937)


### Base Information

- **PR Number:** #38937
- **Author:** [AndreasKaratzas](https://github.com/AndreasKaratzas)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-03 15:58:57
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38937/files) (2):**
  - `requirements/rocm-test.in`
  - `requirements/rocm-test.txt`

### Summary

**What changed and why**  
This PR restores the `-r common.txt` dependency inclusion in the `rocm-test.in` file, which was accidentally removed in a previous commit. The change ensures that the ROCm test environment inherits all common dependencies, as reflected by the extensive updates in the generated `rocm-test.txt` file.

**Technical impact**  
The ROCm test environment now correctly includes all shared dependencies from `common.txt`, such as `anthropic`, `openai`, `fastapi`, `opentelemetry`, and numerous other packages. This aligns the ROCm test setup with other environments and ensures consistent dependency resolution across the project.

**Potential risks**  
The large number of added dependencies (+282 lines) could increase build times, image sizes, and potential version conflicts. Some newly added packages (e.g., `opentelemetry`, `model-hosting-container-standards`) may introduce unexpected behavior or compatibility issues in ROCm-specific testing scenarios.

**Key insights**  
Always verify that platform-specific requirement files correctly include common dependencies. Consider whether all added packages are strictly necessary for ROCm testing to avoid bloating the environment. Use dependency locking tools to prevent similar regressions in the future.

---

## 9. [[ci] Remove soft fail for AMD image build job](https://github.com/vllm-project/vllm/pull/38941)


### Base Information

- **PR Number:** #38941
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-03 13:42:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38941/files) (1):**
  - `.buildkite/hardware_tests/amd.yaml`

### Summary

**What changed and why**  
Removed the `soft_fail: true` configuration from the AMD image build job in the Buildkite pipeline. This change makes the job a hard requirement, meaning pipeline success now depends on this job passing.

**Technical impact**  
The AMD image build job will now block pipeline progression if it fails, ensuring stricter quality control for AMD-specific builds. This eliminates silent failures and enforces consistent build standards across all hardware targets.

**Potential risks**  
If the AMD build is inherently flaky or environment-dependent, this change could cause frequent pipeline failures and disrupt development workflows. Teams relying on quick iterations may experience delays until AMD build issues are resolved.

**Key insights**  
This change prioritizes reliability over convenience for AMD builds. Developers should verify the AMD build's stability and monitor initial pipeline runs closely. Consider adding robust logging or preemptive testing to catch issues early before they block the pipeline.

---

## 10. [Removed GPU state confirmation and cleanup steps.](https://github.com/vllm-project/vllm/pull/38238)


### Base Information

- **PR Number:** #38238
- **Author:** [dhonnappa-amd](https://github.com/dhonnappa-amd)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-03 13:11:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38238/files) (1):**
  - `.buildkite/scripts/hardware_ci/run-amd-test.sh`

### Summary

**What changed and why**  
Removed GPU state monitoring and reset functionality from the AMD hardware CI test script. This code interacted with `/opt/amdgpu/etc/gpu_state` to wait for clean GPU states and trigger resets, which is incompatible with Kubernetes pods where GPU lifecycle is managed by the device plugin and kubelet.

**Technical impact**  
The script no longer attempts to manage GPU state directly, aligning with Kubernetes best practices where pods should not access host-level GPU management files. This simplifies the script and removes dependencies on host filesystem access that would fail in containerized environments.

**Potential risks**  
If GPU state management is still needed at the test level, alternative mechanisms must be implemented through Kubernetes-native approaches. Tests might proceed with GPUs in an unexpected state if the device plugin doesn't guarantee clean initialization, potentially leading to flaky tests or resource contention.

**Key insights**  
This change correctly recognizes the separation of concerns between containerized applications and host hardware management. Developers should ensure GPU initialization and cleanup are handled at the Kubernetes orchestration level rather than within application pods. Consider adding validation that GPUs are properly initialized through ROCm info or other diagnostic commands available within the container.

---

## 11. [Remove MQ multi-node tests](https://github.com/vllm-project/vllm/pull/38934)


### Base Information

- **PR Number:** #38934
- **Author:** [jeffreywang-anyscale](https://github.com/jeffreywang-anyscale)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-03 13:00:09
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38934/files) (2):**
  - `.buildkite/test_areas/distributed.yaml`
  - `tests/distributed/test_mq_tcp_multinode.py`

### Summary

**What changed and why**  
The PR removes the MessageQueue TCP multi-node test suite from CI configuration and deletes the associated test file. This is done to unblock CI pipelines that were failing due to these specific tests, as indicated by the PR description stating "Remove a failed test to unblock CI."

**Technical impact**  
Removing these tests reduces test coverage for cross-node MessageQueue functionality using TCP fallback transport. The CI pipeline will no longer validate that MessageQueue correctly detects cross-node communication scenarios and falls back from shared memory to TCP transport when processes span multiple nodes with isolated `/dev/shm`.

**Potential risks**  
Without these tests, regressions in multi-node MessageQueue TCP functionality may go undetected. The tests specifically validated critical cross-node communication paths, large payload handling (>1 MiB chunks), and proper transport selection logic. Future changes to `shm_broadcast.py` or `parallel_state.py` could introduce bugs in multi-node environments that won't be caught by CI.

**Key insights**  
This appears to be a temporary fix to restore CI functionality, but the team should prioritize investigating and fixing the underlying test failures rather than permanently removing this coverage. Consider adding a tracking issue to restore these tests once the root cause is identified, as multi-node communication is essential for distributed vLLM deployments.

---

## 12. [[Model Runner V2] Add config validation for not-yet-supported features](https://github.com/vllm-project/vllm/pull/38758)


### Base Information

- **PR Number:** #38758
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-04-03 12:08:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38758/files) (2):**
  - `.buildkite/test_areas/model_runner_v2.yaml`
  - `vllm/config/vllm.py`

### Summary

**What changed and why**  
Added configuration validation for the V2 model runner to check for unsupported features when `VLLM_USE_V2_MODEL_RUNNER` is enabled. This prevents runtime errors by raising a `ValueError` if any incompatible configurations are detected. The BuildKite pipeline step for Model Runner V2 Pipeline Parallelism was also removed, likely because it requires fixes not yet merged.

**Technical impact**  
The validation runs early in the configuration process, ensuring that unsupported features (like hybrid/Mamba models, prefill context parallelism, certain speculative methods, and others) are caught before model execution. This improves developer experience by providing clear error messages and reduces the risk of silent failures or incorrect behavior when using the V2 model runner.

**Potential risks**  
If the validation is too restrictive or misses some unsupported features, it could block valid use cases or allow incompatible configurations to slip through. The list of unsupported features is noted as "not necessarily exhaustive," which means additional checks may be needed later. There's also a risk of the validation becoming outdated if the V2 model runner's capabilities change without corresponding updates.

**Key insights**  
Developers should update this validation method as new features are added to the V2 model runner. The PR references several open PRs (e.g., #38163, #35045, #38390) that will add support for some currently unsupported features—once merged, those checks should be removed. This approach is proactive but requires maintenance to stay aligned with the V2 model runner's development timeline.

---

## 13. [[Bugfix] Re-enable Renormalize routing for TRT-LLM MoE experts](https://github.com/vllm-project/vllm/pull/38859)


### Base Information

- **PR Number:** #38859
- **Author:** [yzong-rh](https://github.com/yzong-rh)
- **Merged By:** [ZJY0516](https://github.com/ZJY0516)
- **Merged time:** 2026-04-03 10:48:17
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38859/files) (2):**
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_bf16_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`

### Summary



---

## 14. [[vLLM IR] add `import_ir_kernels()` to support OOT platforms](https://github.com/vllm-project/vllm/pull/38807)


### Base Information

- **PR Number:** #38807
- **Author:** [wxsIcey](https://github.com/wxsIcey)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-04-03 10:25:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38807/files) (2):**
  - `vllm/config/kernel.py`
  - `vllm/platforms/interface.py`

### Summary

**What changed and why**  
The changes extract IR kernel registration from `IrOpPriorityConfig.set_priority()` into a new `Platform.import_ir_kernels()` classmethod. This refactoring allows out-of-tree (OOT) platforms to override the method and register their own IR op implementations, while preserving the default behavior of importing `vllm.kernels`.

**Technical impact**  
This introduces a pluggable architecture for IR kernel registration, decoupling kernel imports from the priority configuration logic. The platform abstraction layer now provides an extension point, enabling custom platforms to inject their kernel implementations without modifying core vLLM code.

**Potential risks**  
If an OOT platform overrides `import_ir_kernels()` incorrectly or fails to import necessary kernel modules, it could lead to missing IR op implementations and runtime failures. The default implementation's import statement uses `# noqa: F401`, which might suppress legitimate linting warnings about unused imports in custom implementations.

**Key insights**  
This is a well-structured change that follows the Open-Closed Principle, making the system extensible for new hardware platforms. Developers creating OOT platforms should ensure their `import_ir_kernels()` method imports all required kernel modules to register implementations properly. The change maintains backward compatibility while enabling future platform customization.

---

## 15. [Fix invalid logprobs with MTP enabled and sync scheduling](https://github.com/vllm-project/vllm/pull/38711)


### Base Information

- **PR Number:** #38711
- **Author:** [danisereb](https://github.com/danisereb)
- **Merged By:** [benchislett](https://github.com/benchislett)
- **Merged time:** 2026-04-03 09:24:37
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38711/files) (1):**
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The fix addresses a bug where MTP (Multi-Token Prediction) speculative decoding with sync scheduling produced invalid logprobs when sequences approached `max_model_len`. The root cause was that draft token zeroing only occurred in the async scheduling path when `input_fits_in_drafter=False`. The change ensures draft tokens are zeroed for all drafter types in this condition, preventing stale drafts from corrupting Mamba state.

**Technical impact**  
This ensures consistent and correct logprob calculations across all scheduling modes (sync and async) when sequences are near the model's maximum length. The fix maintains MTP performance while eliminating catastrophic logprob errors that could affect downstream tasks like scoring or reranking.

**Potential risks**  
The change introduces additional zeroing operations for all drafter types when input doesn't fit, which may have a negligible performance impact. Edge cases could arise if other speculative decoding configurations have similar conditional logic gaps. The fix assumes zeroing is always safe, but verifying this for all model architectures (especially non-Mamba models) is advisable.

**Key insights**  
Always handle edge conditions uniformly across all execution paths—async vs. sync discrepancies are a common source of subtle bugs. The custom TME metric provides an excellent validation approach for logprob accuracy. Consider adding a regression test for sequences near `max_model_len` with various sampling parameters to prevent recurrence.

---

## 16. [[Frontend] new online quantization frontend](https://github.com/vllm-project/vllm/pull/38138)


### Base Information

- **PR Number:** #38138
- **Author:** [vkuzo](https://github.com/vkuzo)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-03 08:58:39
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38138/files) (13):**
  - `tests/quantization/test_online.py`
  - `tests/quantization/utils.py`
  - `vllm/config/model.py`
  - `vllm/config/quantization.py`
  - `vllm/config/vllm.py`
  - `vllm/engine/arg_utils.py`
  - `vllm/entrypoints/llm.py`
  - `vllm/model_executor/layers/quantization/__init__.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/online/__init__.py`
  - `vllm/model_executor/layers/quantization/online/base.py`
  - `vllm/model_executor/layers/quantization/online/fp8.py`
  - `vllm/model_executor/model_loader/weight_utils.py`

### Summary

**What changed and why**  
This PR introduces a new online quantization frontend that enables quantizing FP16/BF16 model weights during loading without requiring pre-quantized checkpoints. It adds support for two FP8 schemes (per‑tensor and per‑block) with a flexible configuration API, allowing users to specify global schemes, override schemes per layer type (linear/MoE), and ignore specific layers via regex or exact name matching.

**Technical impact**  
The changes extend the quantization infrastructure with a new `OnlineQuantizationConfig` class that dispatches to scheme‑specific linear and MoE methods. The architecture cleanly separates configuration resolution, weight processing, and kernel selection, maintaining backward compatibility with existing quantization methods. Online quantization is integrated into the model loading pipeline via `quantization_config` and works with meta‑device initialization for just‑in‑time weight materialization.

**Potential risks**  
- The new online quantization path may conflict with existing checkpoint‑based quantization configs; validation logic prevents simultaneous use, but edge cases in hybrid scenarios need careful testing.  
- The per‑block FP8 implementation relies on hardware‑specific kernels (DeepGEMM, CUTLASS) and may have performance variability across platforms.  
- The `ignore` list uses regex matching that must align with unfused layer names; fused modules (like QKV) require special handling, which could confuse users.

**Key insights**  
- The design prioritizes extensibility: new quantization schemes can be added by extending `OnlineQuantScheme` and implementing corresponding method classes.  
- Developers should note that online quantization is incompatible with pre‑quantized checkpoints; the validation in `get_quant_config` ensures clear error messages.  
- Future work should consolidate duplicate FP8 implementations (existing `fp8.py` and new `online/fp8.py`) to reduce maintenance overhead.

---

## 17. [[KVConnector] Skip `register_kv_caches` on profiling](https://github.com/vllm-project/vllm/pull/38558)


### Base Information

- **PR Number:** #38558
- **Author:** [NickLucche](https://github.com/NickLucche)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-04-03 08:40:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38558/files) (1):**
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
The change skips the `register_kv_caches` call during profiling by adding a condition (`and not is_profiling`) to the existing `has_kv_transfer_group()` check. This prevents the KV connector from being initialized with dummy or minimal KV cache configurations during profiling runs, which could lead to incorrect behavior or resource accounting.

**Technical impact**  
This ensures that profiling operations do not interfere with the KV connector's state or GPU memory allocations. The KV connector will only be initialized during actual execution (non-profiling) scenarios, maintaining consistency between the profiled and runtime environments.

**Potential risks**  
If profiling relies on accurate GPU memory measurements that include KV connector allocations, this change might cause underestimation. Additionally, any code path that assumes the KV connector is always initialized after `initialize_kv_cache` could break during profiling, though such assumptions would be incorrect per the contract.

**Key insights**  
The fix is a defensive measure that aligns with the KV connector's contract—`register_kv_caches` is not required to be re-entrant. Developers should ensure profiling hooks account for any memory or state setup separately if needed. Consider introducing a dedicated profiling hook for KV connector initialization if accurate memory tracking is critical.

---

## 18. [[Bugfix] Fix AWQ models batch invariance issues](https://github.com/vllm-project/vllm/pull/38670)


### Base Information

- **PR Number:** #38670
- **Author:** [YM2132](https://github.com/YM2132)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-03 07:54:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38670/files) (4):**
  - `tests/v1/determinism/test_batch_invariance.py`
  - `vllm/model_executor/layers/batch_invariant.py`
  - `vllm/model_executor/layers/quantization/awq.py`
  - `vllm/model_executor/layers/quantization/awq_marlin.py`

### Summary

**What changed and why**  
This PR fixes batch invariance issues for AWQ quantized models by preventing automatic conversion to Marlin kernels when `VLLM_BATCH_INVARIANT=1` is enabled. It forces AWQ to use the dequant + `torch.matmul` path so Triton overrides can intercept operations, and addresses float16-specific bugs in the batch invariance implementation that were exposed by AWQ models.

**Technical impact**  
The changes ensure AWQ models work correctly in batch invariant mode by bypassing performance-optimized Marlin kernels in favor of deterministic matmul operations. This trades speed for determinism. The batch invariance module is also hardened to handle float16 data types properly, including shared memory constraints on specific GPU architectures.

**Potential risks**  
Forcing the dequant + `torch.matmul` path may significantly reduce inference performance for AWQ models when batch invariance is enabled. The shared memory adjustment for float16 is hardware-dependent and could affect performance on different GPU generations. There's also a risk that the float16 log_softmax fix might introduce subtle numerical differences.

**Key insights**  
Developers should be aware that enabling batch invariance with AWQ models incurs a performance penalty. The changes are well-contained but introduce environment-dependent behavior—test thoroughly across GPU architectures. Consider separating the float16 fixes into their own PR for cleaner maintenance. Ensure future quantization methods consider batch invariance compatibility during design.

---

## 19. [[XPU] bump up xpu-kernel v0.1.5, transpose moe weights](https://github.com/vllm-project/vllm/pull/38342)


### Base Information

- **PR Number:** #38342
- **Author:** [mayuyuace](https://github.com/mayuyuace)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-03 07:10:02
- **Type:** `None`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38342/files) (3):**
  - `requirements/xpu.txt`
  - `vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py`
  - `vllm/model_executor/layers/quantization/fp8.py`

### Summary



---

## 20. [[Kernel] Add swapAB support for SM120 CUTLASS blockwise FP8 GEMM](https://github.com/vllm-project/vllm/pull/38325)


### Base Information

- **PR Number:** #38325
- **Author:** [Nekofish-L](https://github.com/Nekofish-L)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-04-03 06:49:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38325/files) (1):**
  - `csrc/libtorch_stable/quantization/w8a8/cutlass/c3x/scaled_mm_blockwise_sm120_fp8_dispatch.cuh`

### Summary

**What changed and why**  
This PR adds `swap_ab` kernel support for SM120 (Blackwell) blockwise FP8 scaled GEMM to improve performance for decode-phase workloads with small batch sizes. By transposing the problem as `D = (B^T @ A^T)^T`, the small M dimension is moved to N, enabling better tile coverage and higher SM occupancy.

**Technical impact**  
The changes introduce a new template parameter `swap_ab` that conditionally swaps A/B tensors and their scales, adjusts layouts, and uses a dedicated tile configuration (128x32x128) for swapped operations. The dispatch logic now selects between three configurations: default (128x128x128), pingpong (64x128x128), and swap_ab (128x32x128) based on a heuristic (`M <= 64 \|\| M % 4 != 0`).

**Potential risks**  
The heuristic for enabling swap_ab is conservative and may not capture all beneficial shapes, potentially leaving performance gains untapped. There is also a risk of correctness issues if the layout transpositions or scale mappings are incorrectly handled in edge cases, especially when M and N dimensions differ significantly.

**Key insights**  
Performance benchmarks show substantial speedups (e.g., from 97 µs to 57 µs for M=16, K=16384) with maintained accuracy. Developers should consider expanding the heuristic to include more shapes (M > 64) and validate correctness across a broader range of dimensions. The swap_ab approach is a valuable optimization for small-M GEMMs in inference workloads.

---

## 21. [[GDN] Eliminate GPU->CPU sync in prepare_chunk_indices during prefill](https://github.com/vllm-project/vllm/pull/38361)


### Base Information

- **PR Number:** #38361
- **Author:** [arpera](https://github.com/arpera)
- **Merged By:** [vadiklyutiy](https://github.com/vadiklyutiy)
- **Merged time:** 2026-04-03 06:38:03
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38361/files) (10):**
  - `vllm/model_executor/layers/fla/ops/chunk.py`
  - `vllm/model_executor/layers/fla/ops/chunk_delta_h.py`
  - `vllm/model_executor/layers/fla/ops/chunk_o.py`
  - `vllm/model_executor/layers/fla/ops/chunk_scaled_dot_kkt.py`
  - `vllm/model_executor/layers/fla/ops/cumsum.py`
  - `vllm/model_executor/layers/fla/ops/kda.py`
  - `vllm/model_executor/layers/fla/ops/solve_tril.py`
  - `vllm/model_executor/layers/fla/ops/wy_fast.py`
  - `vllm/model_executor/layers/mamba/gdn_linear_attn.py`
  - `vllm/v1/attention/backends/gdn_attn.py`

### Summary

**What changed and why**  
This PR eliminates a GPU→CPU synchronization bottleneck in GDN prefill by pre-computing chunk indices and offsets on the CPU. Previously, `prepare_chunk_indices` called `.tolist()` on a GPU tensor, causing a blocking sync. Now these values are computed in `GDNAttentionMetadataBuilder.build()` where CPU data is already available, then asynchronously copied to GPU and passed through the FLA ops chain as optional parameters.

**Technical impact**  
The changes introduce a new data flow where pre-computed chunk metadata bypasses the `@tensor_cache` mechanism entirely for GDN prefill paths. This removes the initial sync per step while maintaining backward compatibility for other callers (KDA, OLMo Hybrid) through the existing cache fallback. The architecture now supports both cached and pre-computed paths, with the hardcoded chunk size 64 extracted into a `FLA_CHUNK_SIZE` constant.

**Potential risks**  
The async copy (`non_blocking=True`) assumes proper synchronization elsewhere in the pipeline. If downstream ops use these tensors before transfer completes, it could cause silent errors. Additionally, the conditional logic for when to compute/pre-pass these tensors adds complexity, and any mismatch between CPU-computed indices and GPU expectations could lead to subtle bugs.

**Key insights**  
The fix successfully eliminates the profiling-visible sync without performance regression. Developers should ensure the async transfers are properly synchronized in all usage contexts. The pattern of pre-computing metadata on CPU when possible is a valuable optimization strategy for other GPU→CPU sync points in the codebase.

---

## 22. [[Intel][Triton] Support `round_int8` for Intel backend](https://github.com/vllm-project/vllm/pull/38825)


### Base Information

- **PR Number:** #38825
- **Author:** [mieshkiwrk](https://github.com/mieshkiwrk)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-03 05:47:35
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38825/files) (1):**
  - `vllm/model_executor/layers/quantization/utils/int8_utils.py`

### Summary

**What changed and why**  
Added support for `round_int8` in the Intel Triton backend by introducing a new conditional branch for XPU platforms. This enables proper 8-bit integer rounding using Intel's libdevice library, aligning functionality with existing CUDA and HIP backends.

**Technical impact**  
The change extends quantization utilities to support Intel GPUs, ensuring consistent behavior across different hardware platforms. It maintains the existing code structure by adding a parallel implementation path without modifying other backends.

**Potential risks**  
If the Intel libdevice's `round` function behaves differently from CUDA/HIP implementations, it could cause numerical discrepancies. The change also assumes `current_platform.is_xpu()` correctly identifies all Intel GPU environments, which may not cover future Intel variants.

**Key insights**  
This is a straightforward hardware compatibility update that follows established patterns. Developers should verify numerical equivalence across backends and consider adding unit tests for Intel GPU rounding. The implementation correctly isolates platform-specific code without introducing cross-platform dependencies.

---

## 23. [[XPU][CI] Skip test_topp_only and test_topk_and_topp cases on Intel GPU in CI](https://github.com/vllm-project/vllm/pull/38904)


### Base Information

- **PR Number:** #38904
- **Author:** [zxd1997066](https://github.com/zxd1997066)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-03 05:44:53
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38904/files) (1):**
  - `.buildkite/intel_jobs/test-intel.yaml`

### Summary

**What changed and why**  
The PR modifies the Intel GPU CI test configuration to skip two additional test cases (`test_topp_only` and `test_topk_and_topp`) in the sampling module. This change aligns the test exclusion pattern with the PR's stated purpose of skipping these specific tests on Intel GPU CI runs.

**Technical impact**  
This update narrows the test scope for Intel GPU CI by excluding tests that likely involve top-p (nucleus) sampling functionality. It ensures CI passes without executing these specific test cases, which may be failing or unsupported on Intel GPU hardware.

**Potential risks**  
If these tests are skipped indefinitely, regressions in top-p sampling support on Intel GPUs could go undetected. There's also a risk that the skip pattern becomes overly broad if more sampling-related tests are added in the future without updating the exclusion logic.

**Key insights**  
The change is a tactical workaround for CI stability, but the team should track whether these tests are failing due to temporary issues or fundamental hardware limitations. Consider adding a comment linking to an issue tracking the root cause investigation or planned support for these features on Intel GPUs.

---

## 24. [[ROCm] Fix aiter persistent mode mla with q/o nhead<16 for kimi-k2.5 tp8](https://github.com/vllm-project/vllm/pull/38615)


### Base Information

- **PR Number:** #38615
- **Author:** [wufann](https://github.com/wufann)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-03 03:54:03
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38615/files) (1):**
  - `vllm/v1/attention/backends/mla/rocm_aiter_mla.py`

### Summary

**What changed and why**  
The fix addresses a shape mismatch in persistent mode MLA (Multi-Head Latent Attention) when the number of attention heads per GPU is less than 16 (e.g., 8 heads in Kimi-K2.5 with tensor parallelism 8). Previously, `AiterMLAImpl` padded queries to 16 heads for kernel compatibility, but metadata buffers were allocated for the original head count, causing inconsistency. The change ensures metadata allocation aligns with the padded head count.

**Technical impact**  
This update modifies the `_num_attention_heads` attribute to use a minimum of 16 heads, ensuring consistency between the padded tensor shape passed to `mla_decode_fwd` and the metadata buffers from `get_mla_metadata_info_v1`. It resolves runtime shape errors in persistent mode and maintains compatibility with the existing head-padding mechanism in the kernel.

**Potential risks**  
If other parts of the codebase rely on the original `num_heads` value (e.g., for memory calculations or non-persistent mode), unintended side effects could occur. Additionally, the hardcoded minimum of 16 may not generalize to future kernels with different padding requirements, though it aligns with current aiter kernel constraints.

**Key insights**  
Developers should verify that no other logic depends on the unpadded head count, as this change centralizes the padded value. Consider adding a comment or constant for the "16" threshold to clarify its origin (e.g., kernel-specific padding). Testing should include edge cases with varied head counts and tensor parallelism configurations to ensure robustness.

---

## 25. [[CPU] Replace OMP initialization](https://github.com/vllm-project/vllm/pull/36487)


### Base Information

- **PR Number:** #36487
- **Author:** [kot-begemot-uk](https://github.com/kot-begemot-uk)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-04-03 03:42:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36487/files) (7):**
  - `.buildkite/scripts/hardware_ci/run-cpu-distributed-smoke-test.sh`
  - `csrc/cpu/torch_bindings.cpp`
  - `csrc/cpu/utils.cpp`
  - `vllm/platforms/cpu.py`
  - `vllm/utils/ompmultiprocessing.py`
  - `vllm/v1/executor/multiproc_executor.py`
  - `vllm/v1/worker/cpu_worker.py`

### Summary

**What changed and why**  
The PR replaces ad-hoc OpenMP thread initialization with a standards-compliant approach using OMP_PLACES and OMP_PROC_BIND environment variables. It removes custom NUMA and CPU affinity management code that was causing hangs (#32651) and was ineffective after OMP's initial library load. A new `OMPProcessManager` class is introduced to handle CPU topology discovery and OMP environment configuration for multiprocessing.

**Technical impact**  
This change significantly simplifies CPU thread management by leveraging OpenMP's standard mechanisms instead of low-level POSIX affinity calls. The architecture now properly separates OMP configuration from worker initialization, ensuring environment variables are set before OMP initializes. It also removes platform-specific workarounds for libgomp/libiomp5 preloading and introduces simulated NUMA support for CI testing.

**Potential risks**  
The removal of manual affinity binding could affect performance on heterogeneous CPU architectures if OMP's automatic placement is suboptimal. The new `OMPProcessManager` introduces a global resource management approach that might have race conditions in concurrent initialization scenarios. The simulated NUMA splitting logic could fail if CPU masks cannot be divided evenly.

**Key insights**  
Developers should verify that OMP_PLACES and OMP_PROC_BIND work correctly across different OpenMP implementations (GCC, Intel, LLVM). The change makes CPU configuration more portable but requires thorough testing on diverse hardware. The `VLLM_CPU_OMP_THREADS_BIND` environment variable now accepts standard OMP place syntax rather than custom CPU ID lists.

---

## 26. [[Frontend] feat: add streaming support for token generation endpoint](https://github.com/vllm-project/vllm/pull/37171)


### Base Information

- **PR Number:** #37171
- **Author:** [hhk7734](https://github.com/hhk7734)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-04-03 03:20:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37171/files) (4):**
  - `tests/entrypoints/serve/disagg/test_generate_stream.py`
  - `tests/entrypoints/serve/disagg/test_serving_tokens.py`
  - `vllm/entrypoints/serve/disagg/protocol.py`
  - `vllm/entrypoints/serve/disagg/serving.py`

### Summary

**What changed and why**  
This PR implements streaming support for the disaggregated `/inference/v1/generate` endpoint by adding new protocol models (`GenerateStreamResponse`, `GenerateResponseStreamChoice`) and a streaming generator (`serve_tokens_stream_generator`). It follows the existing completion API streaming pattern and supports `stream_options` for usage statistics.

**Technical impact**  
The changes extend the token generation endpoint to support Server-Sent Events (SSE) streaming, aligning it with OpenAI-compatible streaming patterns. The architecture now handles both full and streaming responses, with proper error handling and usage tracking. The `SamplingParams.output_kind` is set to `DELTA` for streaming to ensure incremental token delivery.

**Potential risks**  
If the streaming generator encounters mid-generation errors (e.g., `finish_reason='error'`), error chunks are emitted but the stream may still close gracefully, potentially masking underlying issues. Empty token outputs are skipped, which could cause confusion if clients expect explicit empty deltas. The reliance on `as_list(delta_token_ids)` assumes `delta_token_ids` is always a sequence, which may not hold for all output types.

**Key insights**  
Developers should ensure that the `SamplingParams.output_kind` is correctly managed when toggling between streaming and non-streaming modes. The streaming implementation reuses existing error-handling utilities, but thorough testing of edge cases (like network interruptions) is recommended. The addition of comprehensive unit tests provides a solid foundation for validating streaming behavior.

---

## 27. [[XPU][CI] Skip test_topk_only cases on Intel GPU in CI](https://github.com/vllm-project/vllm/pull/38899)


### Base Information

- **PR Number:** #38899
- **Author:** [zxd1997066](https://github.com/zxd1997066)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-03 02:50:41
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38899/files) (1):**
  - `.buildkite/intel_jobs/test-intel.yaml`

### Summary

**What changed and why**  
The PR modifies the Intel GPU CI configuration to skip `test_topk_only` test cases by adding `-k "not test_topk_only"` to the pytest command for the `v1/sample` test suite. This change aims to exclude specific failing or problematic tests from CI execution on Intel GPU environments.

**Technical impact**  
This change reduces test coverage for Intel GPU CI by excluding `test_topk_only` tests, potentially masking underlying issues with top-k sampling functionality on Intel GPUs. The CI will no longer validate this specific feature during automated testing, which could lead to undetected regressions.

**Potential risks**  
Skipping tests may hide bugs or compatibility issues with top-k sampling on Intel GPUs, risking silent failures in production. If the test exclusion becomes permanent without addressing the root cause, it could degrade long-term code quality and platform support.

**Key insights**  
Developers should treat this as a temporary workaround and prioritize investigating why `test_topk_only` fails on Intel GPUs. Consider adding a skip reason or linking to an issue tracking the fix. Ensure other test suites continue to validate top-k sampling on supported platforms to maintain overall coverage.

---

## 28. [[ROCm][CI/Build] Fix the pytest hook to properly print out the summary](https://github.com/vllm-project/vllm/pull/38585)


### Base Information

- **PR Number:** #38585
- **Author:** [gshtras](https://github.com/gshtras)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-03 02:24:27
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38585/files) (1):**
  - `docker/Dockerfile.rocm`

### Summary

**What changed and why**  
The change modifies a pytest workaround in the ROCm Dockerfile to ensure proper test execution termination. Instead of immediately calling `os._exit()` in `pytest_sessionfinish`, it now stores the exit code and calls `os._exit()` later in `pytest_unconfigure`, after flushing output streams. This ensures the test summary is printed before termination while preserving the correct exit code.

**Technical impact**  
This improves the CI test reporting for ROCm builds by allowing pytest's normal output flushing and summary printing to complete. The architecture remains the same, but the hook execution order is adjusted to leverage pytest's built-in teardown sequence, making the workaround more robust and aligned with pytest's lifecycle.

**Potential risks**  
If `pytest_unconfigure` is not called due to an earlier crash or abnormal termination, the exit code may not be properly propagated. There's also a minor risk with the global variable `_exit_code` if pytest runs multiple sessions in the same process, though this is unlikely in CI. The dependency on `sys` module being imported is implicit in the new code.

**Key insights**  
The fix correctly addresses both exit code preservation and output completeness. Developers should ensure any future pytest hooks or modifications consider the execution order of `sessionfinish` versus `unconfigure`. The solution is elegant but relies on pytest's internal lifecycle; any changes to pytest's hook system in future versions should be monitored for compatibility.

---

## 29. [Fix Nano Nemotron VL regressions](https://github.com/vllm-project/vllm/pull/38655)


### Base Information

- **PR Number:** #38655
- **Author:** [netanel-haber](https://github.com/netanel-haber)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-03 00:22:06
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38655/files) (7):**
  - `tests/models/registry.py`
  - `tests/models/utils.py`
  - `vllm/model_executor/models/nano_nemotron_vl.py`
  - `vllm/model_executor/models/parakeet.py`
  - `vllm/model_executor/models/radio.py`
  - `vllm/transformers_utils/configs/parakeet.py`
  - `vllm/transformers_utils/processors/nano_nemotron_vl.py`

### Summary

**What changed and why**  
This PR fixes three regressions in the Nano Nemotron VL multimodal model: (1) replacing deep-copying of `VllmConfig` with `with_hf_config()` to avoid torch parameter copy errors during worker startup, (2) avoiding `get_hf_processor()` in metadata hot paths to prevent tokenizer borrowing conflicts, and (3) removing a hacky `feature_size_cache` in favor of `num_tokens_per_image`. It also adds the model to the HF example registry for regression testing.

**Technical impact**  
The changes improve robustness by eliminating deep-copy issues with live parameters and reducing tokenizer contention. The removal of `feature_size_cache` simplifies dynamic tiling logic, while updates to audio/video handling make the code more config-driven. Registry addition ensures future changes are tested against this model.

**Potential risks**  
Removing the `_video_embedder_loaded` flag could mask missing video weights if the model checkpoint lacks them. The `frames_indices` handling for ragged lists may introduce edge cases if not all code paths handle the list-of-tensors format. Changes to audio length calculation assume `hop_length` is always available in the config.

**Key insights**  
Use `VllmConfig.with_hf_config()` instead of deep-copying configs with live parameters. Avoid tokenizer access in hot paths to prevent borrowing errors. Ensure video weight loading is validated elsewhere since the safety flag was removed. Test multimodal models with dynamic resolution and ragged inputs thoroughly.

---

