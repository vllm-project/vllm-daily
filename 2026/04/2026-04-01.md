# vLLM Merged PR Report

**Report Date:** 2026-04-01 PDT

**Total Merged PRs:** 29

---

## 1. [[CPU] Support gelu act in cpu_fused_moe](https://github.com/vllm-project/vllm/pull/38770)


### Base Information

- **PR Number:** #38770
- **Author:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-04-01 23:14:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38770/files) (5):**
  - `csrc/cpu/cpu_fused_moe.cpp`
  - `tests/kernels/moe/test_cpu_fused_moe.py`
  - `vllm/envs.py`
  - `vllm/model_executor/layers/fused_moe/cpu_fused_moe.py`
  - `vllm/v1/attention/backends/cpu_attn.py`

### Summary

**What changed and why**  
This PR adds GELU (Gaussian Error Linear Unit) activation support to the CPU fused Mixture of Experts (MoE) kernel. The changes extend the existing activation options (SiLU and SwiGLU-OAI) to include GELU, enabling models that use GELU-based gated activations to leverage optimized CPU kernels.

**Technical impact**  
The implementation introduces a new `GeluAndMul` enum variant and corresponding dispatch logic in `cpu_fused_moe.cpp`, along with a vectorized `gelu_and_mul` kernel that computes GELU(x) * y using the error function. The Python layer is updated to map `MoEActivation.GELU` to a native PyTorch implementation, and tests are expanded to validate the new activation. Additionally, an unrelated environment variable `VLLM_CPU_ATTN_SPLIT_KV` is exposed in `envs.py` and used in the CPU attention backend.

**Potential risks**  
The GELU implementation uses `std::erf`, which may have platform-dependent numerical behavior or performance variations compared to optimized approximations. The vectorized kernel assumes 16-element alignment and fixed-width processing, which could cause issues if input dimensions are not multiples of 16. The unrelated attention split-KV environment variable change, while minor, introduces a configuration coupling that could affect other CPU attention optimizations.

**Key insights**  
Developers should verify that the GELU approximation (using `approximate="none"` in PyTorch) matches the expected behavior of target models. The kernel's reliance on `std::erf` may benefit from performance profiling across different CPU architectures. Ensure that the test coverage includes edge cases like odd dimension sizes to validate the vectorization logic. The attention split-KV toggle should be documented as it may influence performance for CPU-based attention layers.

---

## 2. [Revert "[Kernel] Add gpt-oss Router GEMM kernel (#37205)"](https://github.com/vllm-project/vllm/pull/38778)


### Base Information

- **PR Number:** #38778
- **Author:** [xyang16](https://github.com/xyang16)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 22:02:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38778/files) (13):**
  - `CMakeLists.txt`
  - `benchmarks/kernels/benchmark_router_gemm.py`
  - `csrc/moe/gpt_oss_router_gemm.cu`
  - `csrc/moe/gpt_oss_router_gemm.cuh`
  - `csrc/moe/moe_ops.h`
  - `csrc/moe/torch_bindings.cpp`
  - `tests/kernels/moe/test_router_gemm.py`
  - `vllm/_custom_ops.py`
  - `vllm/lora/layers/__init__.py`
  - `vllm/lora/layers/gate_linear.py`
  - `vllm/lora/utils.py`
  - `vllm/model_executor/layers/fused_moe/router/gate_linear.py`
  - `vllm/model_executor/models/gpt_oss.py`

### Summary

**What changed and why**  
This PR reverts commit b1169d7, which introduced a specialized GPT-OSS router GEMM kernel. The revert removes the kernel implementation, associated tests, benchmarks, and integration code due to reported accuracy issues with the gpt-oss-120b model. The changes effectively roll back to using standard linear operations for GPT-OSS router computations.

**Technical impact**  
The removal eliminates a performance-optimized kernel for GPT-OSS models on Hopper/Blackwell GPUs, falling back to generic GEMM implementations (cuBLAS or `F.linear`). This impacts throughput for GPT-OSS models with specific dimensions (e.g., 32/128 experts, 2880 hidden size) but ensures correctness. The GateLinear class now only supports DSV3 optimized kernels and cuBLAS fallbacks, simplifying the dispatch logic.

**Potential risks**  
Performance regression for GPT-OSS models on compatible hardware, as the specialized kernel is no longer available. The revert may reintroduce latent issues if the original kernel was added to address other problems (e.g., numerical stability). Additionally, the GateLinearWithLoRA class removal could affect LoRA fine-tuning for GPT-OSS, though the base ReplicatedLinear fallback remains functional.

**Key insights**  
The revert prioritizes model accuracy over performance, a critical trade-off in production systems. Developers should validate accuracy for GPT-OSS models post-revert and monitor performance metrics. Consider root-cause analysis of the kernel’s accuracy issues before attempting a reimplementation. Ensure LoRA integration tests pass, as GateLinearWithLoRA was removed but may be re-added if needed.

---

## 3. [[Kernel] [Helion] Use warning_once in get_gpu_name to prevent log spam](https://github.com/vllm-project/vllm/pull/38743)


### Base Information

- **PR Number:** #38743
- **Author:** [gmagogsfm](https://github.com/gmagogsfm)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 21:30:32
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38743/files) (1):**
  - `vllm/kernels/helion/utils.py`

### Summary

**What changed and why**  
The code replaces `logging.getLogger()` with `init_logger()` from vLLM's internal logger and changes `logger.warning()` to `logger.warning_once()` in `get_gpu_name()`. This prevents repeated warning logs when `device_id` is `None`, particularly in multi-node environments, while still notifying users of the potential device misidentification.

**Technical impact**  
Switching to `init_logger()` ensures consistent logging configuration across vLLM components. Using `warning_once()` reduces log noise by emitting the warning only once per process, improving readability in logs without suppressing the alert entirely. The function's behavior remains unchanged otherwise.

**Potential risks**  
If `warning_once()` relies on process-level state, the warning might still reappear in distributed setups with multiple processes or after restarts. Developers might overlook the issue if logs are not aggregated across nodes, as the warning appears only once per node/process. There's also a risk that the default device ID 0 could be incorrect in heterogeneous GPU environments.

**Key insights**  
This change effectively balances alerting users with maintaining clean logs. Developers should ensure `device_id` is explicitly passed in multi-GPU contexts to avoid reliance on the default. Consider documenting this behavior or adding a debug-level log for repeated calls to aid troubleshooting. The update aligns with vLLM's logging best practices.

---

## 4. [[ROCm][Bugfix] Fix ROCm runtime failure due to missing symbol](https://github.com/vllm-project/vllm/pull/38750)


### Base Information

- **PR Number:** #38750
- **Author:** [gshtras](https://github.com/gshtras)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 21:30:11
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38750/files) (2):**
  - `csrc/ops.h`
  - `csrc/torch_bindings.cpp`

### Summary

**What changed and why**  
The changes fix a ROCm runtime failure caused by an undefined symbol (`_Z28silu_and_mul_per_block_quant...`) when importing the vllm extension. The issue occurs because the `silu_and_mul_per_block_quant` function is not built for ROCm, but its declaration and binding were previously included unconditionally. The fix conditionally excludes this function from ROCm builds by wrapping its declaration in `#ifndef USE_ROCM` and moving its Torch library registration inside the same guard.

**Technical impact**  
This ensures the ROCm build does not attempt to compile or link the unsupported `silu_and_mul_per_block_quant` function, preventing the undefined symbol error. The function remains available for non-ROCm builds (e.g., CUDA), preserving existing functionality. The change is minimal and localized to build configuration, avoiding broader architectural impacts.

**Potential risks**  
If ROCm builds later require this quantization function, its absence could cause missing feature errors. Additionally, any code that indirectly depends on this function in ROCm builds may fail at runtime. The conditional guard must remain synchronized across both the header declaration and binding registration to avoid declaration/definition mismatches.

**Key insights**  
Always conditionally exclude platform-specific functions at both declaration and binding levels. Verify that all build configurations (CUDA/ROCm) are tested after such changes. Consider documenting ROCm limitations for missing ops to guide future development or porting efforts.

---

## 5. [[Bugfix] Use dedicated MM processor cache in /tokenize to prevent sender-cache pollution](https://github.com/vllm-project/vllm/pull/38545)


### Base Information

- **PR Number:** #38545
- **Author:** [sergey-zinchenko](https://github.com/sergey-zinchenko)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 21:14:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38545/files) (4):**
  - `tests/entrypoints/serve/tokenize/test_tokenize_then_chat_vlm.py`
  - `vllm/entrypoints/serve/render/serving.py`
  - `vllm/entrypoints/serve/tokenize/serving.py`
  - `vllm/renderers/base.py`

### Summary

**What changed and why**  
The fix addresses a bug where `/tokenize` endpoint calls with multimodal inputs (e.g., images) pollute the sender cache, causing subsequent `/v1/chat/completions` requests to fail. The solution introduces a dedicated `BaseMultiModalProcessor` with its own `processor_only_cache` for tokenize requests, isolating them from the main sender cache. A `skip_mm_cache=True` flag is threaded through the rendering chain to route tokenize requests to this isolated processor.

**Technical impact**  
This change creates a clear separation between tokenization and inference caches, preventing state contamination. The architecture now supports two independent multimodal processors: one for regular inference (with shared sender cache) and one for tokenization (with a read-only cache). This ensures tokenize operations do not leave stale entries that interfere with chat completions.

**Potential risks**  
- The deep copy of the tokenizer for the read-only processor could introduce overhead or subtle inconsistencies if tokenizer state is not fully isolated.  
- If the `skip_mm_cache` flag is incorrectly propagated or omitted in future code changes, cache pollution could reoccur.  
- The added complexity in the renderer call chain increases the risk of missing the flag in new rendering paths or async variants.

**Key insights**  
- Cache isolation is critical for endpoints that preprocess but do not execute inference.  
- The pattern of using a dedicated processor with a separate cache is a robust solution for similar state pollution issues.  
- Developers should ensure any new rendering or preprocessing methods consistently propagate the `skip_mm_cache` flag to maintain isolation.

---

## 6. [[Perf] DSV3.2 Indexer Fused Weights Projection](https://github.com/vllm-project/vllm/pull/38684)


### Base Information

- **PR Number:** #38684
- **Author:** [benchislett](https://github.com/benchislett)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-01 20:34:50
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38684/files) (2):**
  - `vllm/model_executor/models/deepseek_mtp.py`
  - `vllm/model_executor/models/deepseek_v2.py`

### Summary

**What changed and why**  
The PR fuses the WK and Weights_Proj projections in the DSV3.2 Indexer into a single merged linear layer (`wk_weights_proj`). This replaces the previous separate `wk` and `weights_proj` layers to improve performance by reducing GEMM operations, as benchmark data shows speedups in both decode and TTFT scenarios compared to baseline and overlapping approaches.

**Technical impact**  
The changes consolidate two linear projections into one `MergedColumnParallelLinear` layer that outputs concatenated tensors for `wk` and `weights_proj`, which are then split in the forward pass. This reduces kernel launch overhead and improves GPU utilization. The fusion also simplifies the model architecture by removing one layer and adjusting weight loading logic to map the fused parameters correctly.

**Potential risks**  
The fusion assumes `wk` is not quantized with FP4, as noted in the comments; if future quantization configurations use FP4 for WK, performance could degrade. Additionally, the change introduces a new tensor-splitting operation that must maintain exact numerical equivalence to avoid model accuracy drift, though GSM8k tests show only minor variations.

**Key insights**  
This optimization provides a clear performance win, but developers should ensure the quantization config remains compatible and monitor accuracy metrics. The implementation is cleaner than overlapping projections and sets a precedent for fusing other linear layers in similar architectures. Consider adding a fallback mechanism if FP4 quantization is later supported for WK.

---

## 7. [Fix multiline-format string for python 3.10](https://github.com/vllm-project/vllm/pull/38739)


### Base Information

- **PR Number:** #38739
- **Author:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged By:** [hmellor](https://github.com/hmellor)
- **Merged time:** 2026-04-01 20:19:35
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38739/files) (1):**
  - `vllm/compilation/passes/ir/lowering_pass.py`

### Summary

**What changed and why**  
The change fixes a multiline f-string syntax that was invalid in Python 3.10. The original code attempted to split an f-string expression across multiple lines inside the curly braces, which Python 3.10's parser rejects. The fix separates the string concatenation from the f-string interpolation.

**Technical impact**  
This is a syntax-level fix that ensures compatibility with Python 3.10 while maintaining identical runtime behavior. The change affects how a unique identifier string is constructed for IR operations, but the final string output remains unchanged.

**Potential risks**  
Minimal risk since this is purely a syntax correction. However, developers should verify that the string concatenation doesn't introduce any subtle whitespace differences in the generated UUID strings. The change could potentially affect caching or lookup mechanisms if the UUID format changes unexpectedly.

**Key insights**  
Always test code across all supported Python versions, especially when using newer f-string features. The fix demonstrates proper handling of multiline f-strings by separating string construction from interpolation. Consider adding Python version checks in CI to catch similar syntax issues early.

---

## 8. [[BugFix] Fix precommit breakage due to conflicting in-flight merges](https://github.com/vllm-project/vllm/pull/38759)


### Base Information

- **PR Number:** #38759
- **Author:** [njhill](https://github.com/njhill)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 15:35:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38759/files) (1):**
  - `vllm/v1/executor/ray_executor_v2.py`

### Summary

**What changed and why**  
The change fixes a type annotation breakage in the Ray executor initialization. The `futures_queue` was incorrectly typed as `deque[tuple[FutureWrapper, Any]]` but is now corrected to `deque[FutureWrapper]`, resolving a conflict caused by overlapping PR merges.

**Technical impact**  
This correction ensures type consistency between the queue declaration and its usage throughout the executor. The queue now properly stores `FutureWrapper` objects instead of tuples, aligning with the expected data structure for managing asynchronous task futures.

**Potential risks**  
If any code was relying on the tuple structure (with the second `Any` element), this change could cause runtime errors. However, given this appears to be a correction of a recent merge conflict, the tuple typing was likely erroneous from the start.

**Key insights**  
Always verify type annotations after resolving merge conflicts, as subtle discrepancies can cause pre-commit failures. This fix demonstrates the importance of maintaining consistent type signatures across related PRs to prevent integration issues.

---

## 9. [Revert "[Bugfix] Fix Qwen3CoderToolParser anyOf/oneOf type resolution for nullable params (#37831)"](https://github.com/vllm-project/vllm/pull/38751)


### Base Information

- **PR Number:** #38751
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-04-01 15:34:28
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38751/files) (2):**
  - `tests/tool_parsers/test_qwen3coder_tool_parser.py`
  - `vllm/tool_parsers/qwen3coder_tool_parser.py`

### Summary

**What changed and why**  
This PR reverts commit 582340f, which introduced changes to handle `anyOf`/`oneOf` type resolution for nullable parameters in the Qwen3Coder tool parser. The revert was necessary because the original changes broke a test on the main branch, indicating a regression in functionality.

**Technical impact**  
The revert removes specialized logic for parsing JSON Schema `anyOf`/`oneOf` constructs and type-as-array definitions, which were added to support Pydantic v2's representation of optional fields. The code now falls back to simpler type resolution, treating `anyOf` schemas as objects and defaulting to string for unresolved types. This may affect type conversion accuracy for nullable parameters.

**Potential risks**  
- Loss of proper type conversion for optional fields (e.g., `Optional[int]` may not be parsed as integers)
- Inconsistent handling of `$ref` schemas and complex union types
- Possible breaking changes for users relying on precise type coercion in tool calls
- The underlying test failure that prompted the revert remains unaddressed

**Key insights**  
The revert highlights a need for more robust testing of edge cases in JSON Schema resolution. Developers should investigate the specific test failure before reattempting the fix, ensuring compatibility with both Pydantic v1 and v2 schemas. Consider adding integration tests that validate the full pipeline with real-world schemas.

---

## 10. [[Bugfix] Preserve original ImportError in gRPC server entrypoint](https://github.com/vllm-project/vllm/pull/38673)


### Base Information

- **PR Number:** #38673
- **Author:** [CatherineSue](https://github.com/CatherineSue)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-04-01 15:16:44
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38673/files) (1):**
  - `vllm/entrypoints/grpc_server.py`

### Summary

**What changed and why**  
The PR modifies a try-except block to preserve the original ImportError traceback when importing gRPC dependencies. Previously, `from None` suppressed the underlying error, which could mislead users into thinking the package was missing when it was actually installed but had internal import issues (e.g., version mismatches).

**Technical impact**  
This change improves debugging by chaining exceptions, making the root cause visible. The error message now distinguishes between missing packages and broken imports, guiding users more effectively. It affects only error reporting in the gRPC server entrypoint, with no functional impact on server operation.

**Potential risks**  
The updated error message is more verbose, which could clutter logs if not formatted cleanly. There is a minor risk that users might overlook the chained exception details if they only scan the final error line. Additionally, the change assumes the chained exception will always provide actionable insights, which may not hold for all import failures.

**Key insights**  
Always preserve original exceptions when wrapping errors to aid debugging. Consider logging the chained exception separately if output length is a concern. This pattern should be reviewed in similar import guards across the codebase to ensure consistent error handling.

---

## 11. [[Feat][Executor] Introduce RayExecutorV2](https://github.com/vllm-project/vllm/pull/36836)


### Base Information

- **PR Number:** #36836
- **Author:** [jeffreywang-anyscale](https://github.com/jeffreywang-anyscale)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-04-01 14:34:29
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36836/files) (14):**
  - `.buildkite/test_areas/distributed.yaml`
  - `tests/distributed/conftest.py`
  - `tests/distributed/test_mq_tcp_multinode.py`
  - `tests/distributed/test_ray_v2_executor.py`
  - `tests/distributed/test_ray_v2_executor_e2e.py`
  - `tests/test_ray_env_utils.py`
  - `tests/utils_/test_ray_utils.py`
  - `vllm/envs.py`
  - `vllm/v1/executor/abstract.py`
  - `vllm/v1/executor/ray_env_utils.py`
  - `vllm/v1/executor/ray_executor.py`
  - `vllm/v1/executor/ray_executor_v2.py`
  - `vllm/v1/executor/ray_utils.py`
  - `vllm/v1/worker/worker_base.py`

### Summary

**What changed and why**  
This PR introduces RayExecutorV2, a new Ray-based distributed executor that replaces Ray compiled graphs with a MessageQueue-based control plane. It reuses MultiprocExecutor's MQ-based RPC and NCCL data plane while spawning workers as Ray actors into placement group bundles. The changes include a feature flag (`VLLM_USE_RAY_V2_EXECUTOR_BACKEND`) to opt into the new executor, comprehensive testing infrastructure, and performance benchmarking showing comparable results to existing backends.

**Technical impact**  
The architecture shifts from Ray's compiled DAG approach to a more flexible MessageQueue system that automatically uses shared memory for intra-node communication and TCP fallback for cross-node workers. This provides better isolation, supports async scheduling by default, and maintains compatibility with existing placement group management. The executor inherits from MultiprocExecutor, reusing its proven control plane while leveraging Ray for resource management.

**Potential risks**  
The feature is behind a feature flag (default off), reducing immediate deployment risk. However, the complex GPU assignment flow where CUDA_VISIBLE_DEVICES is set after actor placement could cause initialization issues in edge cases. The TCP fallback for cross-node communication adds network dependency, and the extensive new code paths require thorough testing across different cluster configurations. The PR explicitly notes EEP (Elastic Engine Protocol) support is out of scope.

**Key insights**  
The new executor shows performance parity with existing backends in benchmarks, making it a viable replacement. Developers should note the automatic driver-node-first bundle sorting that ensures rank 0 co-location with the executor. The comprehensive test suite (unit, integration, multi-node) provides good coverage, but production deployments should validate across diverse hardware configurations before enabling the feature flag by default.

---

## 12. [[Refactor] Simplify FutureWrapper in MultiprocExecutor](https://github.com/vllm-project/vllm/pull/38644)


### Base Information

- **PR Number:** #38644
- **Author:** [yzong-rh](https://github.com/yzong-rh)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-04-01 14:28:26
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38644/files) (1):**
  - `vllm/v1/executor/multiproc_executor.py`

### Summary

**What changed and why**  
The refactor simplifies `FutureWrapper` by having it directly store the `get_response` callable and manage its own queue insertion. Previously, the caller had to manage `(future, callable)` tuples and a separate drain loop for blocking calls. Now, `collective_rpc` always creates a `FutureWrapper` and uses `future.result()` for blocking paths, eliminating duplicate aggregation logic.

**Technical impact**  
This change reduces code duplication and centralizes response-fetching logic within `FutureWrapper`. The architecture becomes cleaner as the future now encapsulates both its response retrieval method and queue management, making the executor's flow more straightforward and consistent between blocking and non-blocking operations.

**Potential risks**  
The removal of the explicit drain loop in `collective_rpc` for blocking calls relies on `future.result()`'s internal draining, which must correctly handle all pending futures. If `_wait_for_response` fails or the queue order is corrupted, it could lead to hangs or incorrect results. Additionally, the change assumes `get_response` is safe to call multiple times or is idempotent, which should be verified.

**Key insights**  
The refactor improves maintainability by reducing boilerplate and ensuring a single code path for response aggregation. Developers should ensure that `get_response` functions are side-effect free or idempotent, as they may be called during queue draining. The performance improvement shown in benchmarks suggests the simplification also reduces overhead, making this a positive change for both code clarity and efficiency.

---

## 13. [[Bugfix] Restrict TRTLLM attention to SM100, fixing GB300 (SM103) hang](https://github.com/vllm-project/vllm/pull/38730)


### Base Information

- **PR Number:** #38730
- **Author:** [stecasta](https://github.com/stecasta)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 12:08:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38730/files) (3):**
  - `docs/design/attention_backends.md`
  - `tools/pre_commit/generate_attention_backend_docs.py`
  - `vllm/utils/flashinfer.py`

### Summary

**What changed and why**  
The changes restrict the TRTLLM attention backend to only SM100 (compute capability 10.0) instead of the broader 10.x family. This fixes a regression where GB300 (SM103, CC 10.3) would hang during inference with large batch sizes due to a FlashInfer 0.6.7 compatibility issue. The fix ensures SM103 falls back to the default FlashInfer attention backend, which works correctly.

**Technical impact**  
This narrows the hardware compatibility of the TRTLLM attention backend, potentially reducing performance on newer SM10x GPUs until the underlying FlashInfer issue is resolved. The documentation and code generation scripts are updated to reflect the exact CC requirement, ensuring consistency between runtime behavior and documented support.

**Potential risks**  
If the FlashInfer fix is applied later, this change might unnecessarily restrict TRTLLM attention on compatible SM10x GPUs, leading to suboptimal performance. Additionally, any future GPUs with CC 10.x (e.g., 10.1, 10.2) would also be excluded, which may require revisiting the condition once broader validation is complete.

**Key insights**  
The fix is a targeted workaround for a specific hardware/software incompatibility. Developers should monitor the linked FlashInfer issue (#2939) for updates and consider reverting or adjusting the CC check once a fix is available. Ensure all documentation and automated tooling (like the doc generator) stay synchronized with code changes to avoid confusion.

---

## 14. [Feature/silu block quant fusion v1](https://github.com/vllm-project/vllm/pull/32996)


### Base Information

- **PR Number:** #32996
- **Author:** [Monishver11](https://github.com/Monishver11)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-04-01 11:50:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/32996/files) (11):**
  - `CMakeLists.txt`
  - `benchmarks/fused_kernels/silu_mul_block_quant_benchmark.py`
  - `csrc/ops.h`
  - `csrc/quantization/fused_kernels/fused_silu_mul_block_quant.cu`
  - `csrc/torch_bindings.cpp`
  - `docs/design/fusions.md`
  - `tests/compile/fusions_e2e/models.py`
  - `tests/compile/passes/test_silu_mul_quant_fusion.py`
  - `tests/kernels/core/test_fused_silu_mul_block_quant.py`
  - `vllm/_custom_ops.py`
  - `vllm/compilation/passes/fusion/act_quant_fusion.py`

### Summary

**What changed and why**  
This PR introduces a fused CUDA kernel for SiLU+Mul followed by groupwise FP8/INT8 quantization, along with pattern matching to automatically replace the unfused operations. The changes aim to improve performance for FP8-block quantized models by reducing memory bandwidth and kernel launch overhead.

**Technical impact**  
The implementation adds a new CUDA kernel that processes SiLU activation, element-wise multiplication, and per-group quantization in a single pass. It integrates with the existing compilation framework through pattern matching in `act_quant_fusion.py`, enabling automatic fusion when the `fuse_act_quant` pass is enabled. The kernel supports group sizes of 64 and 128, both row-major and transposed scale layouts, and works with FP16/BF16 inputs quantized to FP8 or INT8.

**Potential risks**  
The kernel currently requires hidden sizes divisible by group size (64/128), which may not hold for all model architectures. Scale upper bound (`scale_ub`) support is marked as not yet implemented in tests. There is a conditional skip when DeepGemm is supported, which could limit applicability on newer hardware. The fusion is CUDA-only and not active on ROCm platforms.

**Key insights**  
Performance benchmarks show significant speedups (~2.4x) over unfused implementations. The fusion maintains numerical accuracy within acceptable tolerances, as validated by lm_eval tests. Developers should ensure model dimensions align with supported group sizes and be aware of the DeepGemm compatibility constraint. The pattern matching automatically handles both scale layout orientations, simplifying integration.

---

## 15. [[Compile] Fix nvfp4 compile warning](https://github.com/vllm-project/vllm/pull/38573)


### Base Information

- **PR Number:** #38573
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-01 11:28:58
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38573/files) (1):**
  - `csrc/libtorch_stable/quantization/fp4/nvfp4_quant_entry.cu`

### Summary

**What changed and why**  
The change fixes a compiler warning on H200 systems by conditionally compiling the `nvfp4_quant_sm_supported()` function. The warning occurred because the function was declared but never referenced when certain NVFP4 feature flags were not defined. The fix wraps the entire function definition with a preprocessor guard that matches the same condition used for its declarations.

**Technical impact**  
This change ensures the code only compiles the `nvfp4_quant_sm_supported()` function when at least one of the NVFP4 SM architecture flags (`ENABLE_NVFP4_SM100` or `ENABLE_NVFP4_SM120`) is enabled. This maintains the existing conditional compilation logic while eliminating the unused function warning, resulting in cleaner build output without affecting runtime behavior.

**Potential risks**  
There's a low risk if the preprocessor condition doesn't perfectly align with where the function is actually called elsewhere in the codebase. If the function is referenced outside these guard conditions, it could lead to undefined reference errors. Additionally, the nested `#if` statements within the function could become harder to maintain if more SM versions are added.

**Key insights**  
The fix correctly addresses the symptom but developers should verify that all call sites for `nvfp4_quant_sm_supported()` are properly guarded by the same preprocessor conditions. Consider creating a shared macro or header definition for these architecture checks to ensure consistency across the codebase and simplify future maintenance when adding support for new SM versions.

---

## 16. [[Misc] Rename think_start_str/think_end_str to reasoning_start_str/reasoning_end_str](https://github.com/vllm-project/vllm/pull/38242)


### Base Information

- **PR Number:** #38242
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [njhill](https://github.com/njhill)
- **Merged time:** 2026-04-01 09:56:45
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38242/files) (5):**
  - `docs/features/reasoning_outputs.md`
  - `tests/v1/entrypoints/openai/test_thinking_token_budget.py`
  - `tests/v1/logits_processors/test_correctness.py`
  - `vllm/config/reasoning.py`
  - `vllm/v1/sample/logits_processor/builtin.py`

### Summary

**What changed and why**  
This PR renames configuration fields from `think_start_str`/`think_end_str` to `reasoning_start_str`/`reasoning_end_str` across the codebase and documentation. The change decouples the terminology from the specific `<think>` tag, allowing broader compatibility with models that use different reasoning delimiters (e.g., `<seed:think>` or `</think>\n<answer>\n`).

**Technical impact**  
The renaming affects the `ReasoningConfig` class, logits processors, tests, and documentation. All references to the old field names have been updated, including property names, private backing fields, and method calls. This is a non-breaking change for external users if they update their configuration accordingly, but it requires updates to any code that directly uses the old field names.

**Potential risks**  
If users or downstream systems rely on the old field names (e.g., in serialized configurations or custom integrations), they may encounter errors until updated. The change also introduces a risk of missing updates in less obvious places (e.g., external plugins or undocumented usage), though the PR appears comprehensive.

**Key insights**  
This refactor improves abstraction and flexibility for reasoning models. Developers should update any configuration files or API calls to use the new field names. The PR aligns with a trend toward generic "reasoning" terminology, which better accommodates diverse model implementations. Ensure all team members are aware of the naming change to avoid integration issues.

---

## 17. [[Kernel] Add MXFP8 to Marlin GEMM/MoE and refactor Mxfp8LinearOp](https://github.com/vllm-project/vllm/pull/34664)


### Base Information

- **PR Number:** #34664
- **Author:** [mgoin](https://github.com/mgoin)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 09:41:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34664/files) (15):**
  - `csrc/moe/marlin_moe_wna16/generate_kernels.py`
  - `csrc/moe/marlin_moe_wna16/marlin_template.h`
  - `csrc/moe/marlin_moe_wna16/ops.cu`
  - `csrc/quantization/marlin/generate_kernels.py`
  - `csrc/quantization/marlin/marlin.cu`
  - `csrc/quantization/marlin/marlin_template.h`
  - `tests/kernels/moe/test_moe.py`
  - `tests/models/quantization/test_mxfp8.py`
  - `vllm/model_executor/layers/fused_moe/fused_marlin_moe.py`
  - `vllm/model_executor/layers/fused_moe/oracle/fp8.py`
  - `vllm/model_executor/layers/fused_moe/oracle/mxfp8.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`
  - `vllm/model_executor/layers/quantization/mxfp8.py`
  - `vllm/model_executor/layers/quantization/utils/marlin_utils_fp8.py`
  - `vllm/model_executor/layers/quantization/utils/mxfp8_utils.py`

### Summary

**What changed and why**  
This PR adds MXFP8 support to Marlin GEMM and MoE kernels, combining FP8 weights with e8m0 microscaling block scales. It also refactors the `Mxfp8LinearOp` class to consolidate backend-specific logic, enabling automatic backend selection (Marlin for SM80+, FlashInfer CUTLASS for SM100+, or emulation fallback).

**Technical impact**  
The changes extend Marlin kernel configurations to handle MXFP8 quantization, introducing new kernel generation entries and modifying scale handling to support 8‑bit scales. Backend logic is unified in `Mxfp8LinearOp`, which now manages weight processing and dispatch based on device capability. This enables efficient MXFP8 inference on a wider range of hardware (SM80+).

**Potential risks**  
The refactored backend selection and weight processing could introduce regressions if device capability detection fails or if scale reordering logic is incorrect for certain shapes. The new Marlin kernel paths require thorough validation across different GPU architectures to ensure numerical accuracy and performance consistency.

**Key insights**  
Developers should note that MXFP8 now works on SM80+ GPUs via Marlin, not just Blackwell (SM100+). The `Mxfp8LinearOp` class now encapsulates all backend‑specific weight transformations, simplifying integration. Ensure that workspace, size_n, and size_k parameters are correctly passed when using the Marlin backend.

---

## 18. [[NIXL][BUG] Fix Triton heterogeneous TP](https://github.com/vllm-project/vllm/pull/37940)


### Base Information

- **PR Number:** #37940
- **Author:** [yzong-rh](https://github.com/yzong-rh)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-04-01 08:23:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37940/files) (5):**
  - `tests/v1/kv_connector/nixl_integration/config_sweep_accuracy_test.sh`
  - `tests/v1/kv_connector/unit/test_nixl_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl_connector.py`
  - `vllm/v1/attention/backends/triton_attn.py`
  - `vllm/v1/attention/ops/triton_reshape_and_cache_flash.py`

### Summary

**What changed and why**  
This PR fixes Triton attention backend issues with heterogeneous tensor parallelism (TP) and cross-layer KV cache sharing. The changes ensure Triton uses the correct KV cache layout (HND) for head-splitting in heterogeneous TP scenarios and properly enables cross-layer blocks for Triton similar to FlashInfer.

**Technical impact**  
Triton attention backend now correctly supports heterogeneous TP configurations by enforcing HND layout requirements and enabling cross-layer KV cache sharing. The KV cache stride calculations now respect the configured layout (NHD/HND), and validation logic prevents incompatible layout usage with heterogeneous TP. Test coverage is expanded for Gemma models and Triton backend with heterogeneous TP.

**Potential risks**  
The HND layout requirement for heterogeneous TP may break existing configurations using NHD layout. The stride order changes could affect performance or correctness if cache layout assumptions differ elsewhere. Cross-layer block enabling for Triton may introduce new edge cases in multi-GPU scenarios.

**Key insights**  
Developers must use HND layout when configuring heterogeneous TP with Triton backend. The fix aligns Triton's behavior with FlashInfer for cross-layer sharing. Test updates confirm Gemma model compatibility and heterogeneous TP support—ensure new Triton configurations follow the same layout constraints as FlashInfer.

---

## 19. [Add ibm-granite/granite-vision-3.3-2b to supported models documentation](https://github.com/vllm-project/vllm/pull/38714)


### Base Information

- **PR Number:** #38714
- **Author:** [jesus-talavera-ibm](https://github.com/jesus-talavera-ibm)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-04-01 08:22:26
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38714/files) (1):**
  - `docs/models/supported_models.md`

### Summary

**What changed and why**  
The PR adds `ibm-granite/granite-vision-3.3-2b` to the supported models documentation under the `LlavaNextForConditionalGeneration` backend. This change addresses a documentation gap where users searching for "granite vision" would find no mention, incorrectly assuming the model is unsupported, despite it being functional in vLLM and already registered in the test registry.

**Technical impact**  
This is a documentation-only change with no impact on code, architecture, or runtime behavior. It improves discoverability by aligning the public documentation with existing internal test registrations, ensuring users can verify support for the Granite Vision model.

**Potential risks**  
There are minimal risks as no code is modified. However, if the model's compatibility or backend mapping changes in the future, the documentation may become outdated unless proactively updated. The change also assumes the existing test registry entry (`tests/models/registry.py`) fully validates the model's functionality.

**Key insights**  
Maintaining synchronization between internal test registries and public documentation is critical for user experience. Developers should consider establishing a process to automatically or routinely update `supported_models.md` when new models are added to the test registry to prevent similar gaps. This change exemplifies a low-effort, high-value improvement to documentation accuracy.

---

## 20. [[Misc] Fix docstring typo: buildin -> builtin](https://github.com/vllm-project/vllm/pull/38722)


### Base Information

- **PR Number:** #38722
- **Author:** [crawfordxx](https://github.com/crawfordxx)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 07:39:47
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38722/files) (1):**
  - `vllm/entrypoints/openai/parser/harmony_utils.py`

### Summary

**What changed and why**  
Fixed a typo in a docstring where "buildin" was corrected to "builtin" in the `has_custom_tools()` function. This change improves documentation clarity and maintains consistency with standard terminology.

**Technical impact**  
No functional or technical impact—this is purely a documentation correction. The code logic, behavior, and all runtime characteristics remain unchanged.

**Potential risks**  
There are no risks associated with this change since it only affects a comment string and doesn't modify any executable code or configuration.

**Key insights**  
This is a straightforward typo fix that enhances code readability without introducing side effects. Developers should continue to follow the same patterns for documenting similar terminology elsewhere in the codebase.

---

## 21. [Fix shape comment in extract_hidden_states example](https://github.com/vllm-project/vllm/pull/38723)


### Base Information

- **PR Number:** #38723
- **Author:** [fynnsu](https://github.com/fynnsu)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-01 07:29:34
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38723/files) (1):**
  - `examples/offline_inference/extract_hidden_states.py`

### Summary

**What changed and why**  
The PR fixes an incorrect comment about tensor shape dimensions in an example script. The comment previously stated the hidden states tensor had shape `[num_hidden_layers, prompt len, hidden size]`, but it's actually `[prompt len, num_hidden_layers, hidden size]`. This correction ensures documentation accurately reflects the system's output.

**Technical impact**  
This change has no functional impact on code execution since it only modifies a comment. However, it improves documentation accuracy, helping developers correctly understand the tensor structure when working with extracted hidden states from the model.

**Potential risks**  
The main risk is minimal—if developers were relying on the incorrect comment for their implementations, they might have already written code with transposed dimensions. However, since this only affects documentation, the actual system behavior remains unchanged.

**Key insights**  
Always verify documentation against actual system outputs, especially for tensor shapes which are critical for downstream processing. While this change is minor, accurate comments prevent confusion and potential bugs in user code that references these examples.

---

## 22. [[MoE Refactor] Make SharedExperts class for use with DefaultMoERunner](https://github.com/vllm-project/vllm/pull/35153)


### Base Information

- **PR Number:** #35153
- **Author:** [bnellnm](https://github.com/bnellnm)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-01 06:44:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/35153/files) (34):**
  - `tests/kernels/moe/utils.py`
  - `tools/ep_kernels/install_python_libraries.sh`
  - `vllm/distributed/elastic_ep/elastic_execute.py`
  - `vllm/lora/layers/fused_moe.py`
  - `vllm/model_executor/layers/fused_moe/config.py`
  - `vllm/model_executor/layers/fused_moe/cutlass_moe.py`
  - `vllm/model_executor/layers/fused_moe/experts/trtllm_fp8_moe.py`
  - `vllm/model_executor/layers/fused_moe/fused_moe_method_base.py`
  - `vllm/model_executor/layers/fused_moe/fused_moe_modular_method.py`
  - `vllm/model_executor/layers/fused_moe/layer.py`
  - `vllm/model_executor/layers/fused_moe/modular_kernel.py`
  - `vllm/model_executor/layers/fused_moe/oracle/fp8.py`
  - `vllm/model_executor/layers/fused_moe/oracle/mxfp4.py`
  - `vllm/model_executor/layers/fused_moe/oracle/nvfp4.py`
  - `vllm/model_executor/layers/fused_moe/oracle/unquantized.py`
  - `vllm/model_executor/layers/fused_moe/prepare_finalize/deepep_ll.py`
  - `vllm/model_executor/layers/fused_moe/router/fused_moe_router.py`
  - `vllm/model_executor/layers/fused_moe/runner/default_moe_runner.py`
  - `vllm/model_executor/layers/fused_moe/runner/moe_runner.py`
  - `vllm/model_executor/layers/fused_moe/runner/shared_experts.py`
  - `vllm/model_executor/layers/fused_moe/shared_fused_moe.py`
  - `vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py`
  - `vllm/model_executor/layers/quantization/awq_marlin.py`
  - `vllm/model_executor/layers/quantization/bitsandbytes.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/compressed_tensors_moe.py`
  - `vllm/model_executor/layers/quantization/experts_int8.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/gguf.py`
  - `vllm/model_executor/layers/quantization/gptq_marlin.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`
  - `vllm/model_executor/layers/quantization/moe_wna16.py`
  - `vllm/model_executor/layers/quantization/mxfp4.py`
  - `vllm/model_executor/layers/quantization/quark/quark_moe.py`
  - `vllm/model_executor/models/transformers/moe.py`

### Summary

**What changed and why**  
This PR introduces a new `SharedExperts` class to centralize shared experts functionality, moving it out of `DefaultMoERunner` and modular kernels. The changes ensure shared experts are executed at the proper time and only once, simplifying coordination between the runner and kernels. This refactor is part of a larger MoE migration effort.

**Technical impact**  
The `SharedExperts` class now manages shared expert execution, including overlap strategies, stream synchronization, and output reduction. The `DefaultMoERunner` delegates to this class, and modular kernels conditionally integrate shared experts based on async support. Return types are unified to `torch.Tensor` (instead of optional tuples), simplifying call sites.

**Potential risks**  
- The `SharedExperts` class introduces new logic for determining execution order (`SharedExpertsOrder`), which may have edge cases in different parallel configurations (e.g., EPLB with non-default backends).  
- Changes to stream synchronization and tensor recording could affect performance or correctness on CUDA platforms if not properly validated.  
- The removal of tuple returns from many methods assumes all callers are updated; any missed updates could cause runtime errors.

**Key insights**  
- The refactor improves separation of concerns, making shared expert logic more maintainable and testable.  
- Developers should verify that all execution paths (especially for overlapping and DP chunking) are correctly handled in their target environments.  
- The `use_dp_chunking` property is now centralized in `FusedMoEParallelConfig`, reducing duplication.

---

## 23. [[Bugfix] Revert "Zero-init MLA attention output buffers to prevent NaN from CUDA graph padding"](https://github.com/vllm-project/vllm/pull/38359)


### Base Information

- **PR Number:** #38359
- **Author:** [elvircrn](https://github.com/elvircrn)
- **Merged By:** [tlrmchlsmth](https://github.com/tlrmchlsmth)
- **Merged time:** 2026-04-01 06:11:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38359/files) (2):**
  - `vllm/v1/attention/backends/mla/cutlass_mla.py`
  - `vllm/v1/attention/backends/mla/flashinfer_mla.py`

### Summary

**What changed and why**  
This PR reverts a previous optimization that pre-allocated zero-initialized output buffers to prevent NaN values from contaminating downstream operations in CUDA graph padding scenarios. The change restores the original `torch.empty` allocation (in CUTLASS MLA) and removes the `out=` workaround (in FlashInfer MLA), eliminating the overhead of maintaining and resizing pre-allocated buffers.

**Technical impact**  
The removal of pre-allocated buffers simplifies the code by eliminating stateful buffer management and FlashInfer-specific workarounds. Performance may slightly degrade due to the loss of buffer reuse across CUDA graph replays, but this is offset by avoiding unnecessary zero-initialization and reducing complexity. The change also resolves a shape mismatch issue in FlashInfer when `q_len > 1`.

**Potential risks**  
If CUDA graph padding introduces uninitialized memory in output buffers, downstream reductions could produce NaN values, potentially breaking model outputs. The test plan must rigorously validate that NaNs do not reappear. Additionally, performance regressions may occur in high-throughput decode scenarios due to increased memory allocation overhead.

**Key insights**  
This revert prioritizes correctness and code simplicity over micro-optimizations. Developers should ensure the test plan is comprehensive, particularly for edge cases with padded sequences. Consider upstream fixes to FlashInfer to properly support `out=` with 4D tensors, and monitor performance in production workloads to assess the impact of removing buffer reuse.

---

## 24. [[Bugfix] Fix Qwen3CoderToolParser anyOf/oneOf type resolution for nullable params](https://github.com/vllm-project/vllm/pull/37831)


### Base Information

- **PR Number:** #37831
- **Author:** [AAISSJ](https://github.com/AAISSJ)
- **Merged By:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged time:** 2026-04-01 05:22:29
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37831/files) (2):**
  - `tests/tool_parsers/test_qwen3coder_tool_parser.py`
  - `vllm/tool_parsers/qwen3coder_tool_parser.py`

### Summary

**What changed and why**  
This PR fixes incorrect type resolution for `anyOf`/`oneOf` schemas, type-as-array patterns, and `$ref` references in `Qwen3CoderToolParser._convert_param_value`. The previous fix (#36032) hardcoded `param_type = "object"` for all `anyOf` schemas, which broke nullable parameters with non-object types (e.g., `Optional[int]`). The new solution introduces two helper methods to cleanly resolve the effective type from parameter definitions.

**Technical impact**  
The changes replace ad‑hoc inline logic with a structured type‑resolution pipeline. The new `_resolve_param_type` method handles four schema patterns: direct `type` fields, `anyOf`/`oneOf` variants, `$ref` references, and a fallback to `"string"`. This ensures that nullable optional types (like `Optional[int]`) are correctly routed to their proper conversion branches (`int()`, `float()`, etc.) instead of being forced through `json.loads`.

**Potential risks**  
1. The resolution picks the **first non‑null type** from `anyOf`/`oneOf` variants; if a schema contains multiple non‑null types (e.g., `anyOf[string, integer, null]`), the choice of `"string"` may not match the actual runtime value.  
2. `$ref` schemas are uniformly treated as `"object"`, which assumes the referenced definition is always an object‑type—this could be incorrect for non‑object references.  
3. The fix is scoped only to `Qwen3CoderToolParser`; the XML parser (`Qwen3XMLToolParser`) still has the same issue and requires a follow‑up change.

**Key insights**  
- The helper `_first_non_null_type` elegantly handles both scalar types and type‑as‑array patterns, making the logic DRY and reusable.  
- Developers should note that the resolution order (first non‑null) is a pragmatic heuristic; for multi‑type unions, consider whether a more sophisticated dispatch is needed.  
- The comprehensive test suite added covers the critical edge cases and ensures no regression for existing functionality.

---

## 25. [[KVTransfer] Fix TpKVTopology.is_kv_replicated equality case](https://github.com/vllm-project/vllm/pull/38179)


### Base Information

- **PR Number:** #38179
- **Author:** [JianDan0212](https://github.com/JianDan0212)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-04-01 03:41:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38179/files) (1):**
  - `vllm/distributed/kv_transfer/kv_connector/utils.py`

### Summary

**What changed and why**  
The PR fixes a bug in `TpKVTopology.is_kv_replicated()` where the equality case (`tp_size == total_num_kv_heads`) incorrectly returned `True`. The condition has been updated from `tp_size // self.total_num_kv_heads >= 1` to `tp_size > self.total_num_kv_heads` to ensure that only when TP workers exceed KV heads is the KV cache considered replicated.

**Technical impact**  
This change corrects the classification of KV-cache layouts, ensuring that when TP size equals the number of KV heads, each TP rank owns a distinct KV head rather than a replicated layout. This affects distributed computation decisions, potentially altering how KV data is partitioned and transferred across TP workers.

**Potential risks**  
If downstream logic relies on the previous behavior, this fix could inadvertently affect performance or correctness in edge cases where `tp_size == total_num_kv_heads`. Additionally, the change might introduce subtle bugs in systems that assumed replication in equality scenarios, requiring validation across affected code paths.

**Key insights**  
Developers should verify that all uses of `is_kv_replicated()` align with the updated semantics, especially in KV transfer and cache management logic. The added comment clarifies the intent, but reviewing dependent modules for any implicit assumptions about equality cases is recommended to prevent regression.

---

## 26. [(security) Enforce frame limit in VideoMediaIO](https://github.com/vllm-project/vllm/pull/38636)


### Base Information

- **PR Number:** #38636
- **Author:** [jperezdealgaba](https://github.com/jperezdealgaba)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-01 03:23:46
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38636/files) (2):**
  - `tests/multimodal/media/test_video.py`
  - `vllm/multimodal/media/video.py`

### Summary

**What changed and why**  
The PR adds frame limit enforcement in `VideoMediaIO.load_base64()` to prevent memory exhaustion from excessive base64-encoded JPEG frames. The change truncates frames when `num_frames > 0`, raises an error for `num_frames == 0`, and allows unlimited frames when `num_frames == -1`. New tests verify the enforcement, edge cases, and error handling.

**Technical impact**  
This introduces a security boundary that mitigates denial-of-service (DoS) risks by limiting resource consumption during video frame decoding. The change affects all callers using `load_base64` with `video/jpeg` MIME type, ensuring they adhere to the configured frame limit. The architecture now explicitly handles invalid `num_frames` values, improving robustness.

**Potential risks**  
If `num_frames` is set too low for legitimate use cases, it may truncate valid video data unexpectedly. The split operation (`data.split(",", self.num_frames)`) assumes frames are comma-separated without embedded commas; malformed input could bypass truncation. The error for `num_frames == 0` is new and may break existing integrations that rely on zero as a valid input.

**Key insights**  
The fix is critical for security but should be paired with validation of the `num_frames` parameter at higher levels. Consider logging warnings when truncation occurs to aid debugging. Ensure all video MIME types (not just `video/jpeg`) are protected similarly, and review other media loading paths for analogous vulnerabilities.

---

## 27. [Add `verified` label to trigger `pre-commit`](https://github.com/vllm-project/vllm/pull/38708)


### Base Information

- **PR Number:** #38708
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-01 02:31:03
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38708/files) (1):**
  - `.github/workflows/pre-commit.yml`

### Summary

**What changed and why**  
The PR adds a new `verified` label that maintainers can apply to trigger the `pre-commit` workflow without running tests, whereas the existing `ready` label triggers both pre-commit and tests. This provides finer-grained control over CI execution for trusted contributors.

**Technical impact**  
The change modifies the GitHub Actions workflow to accept either `verified` or `ready` labels (or the existing merged PR count check) as a condition for running pre-commit. This introduces an additional label option without altering the underlying workflow structure or the pre-commit job itself.

**Potential risks**  
If maintainers confuse `verified` with `ready`, they might unintentionally skip test runs on PRs that require them. Additionally, the error message now includes both labels, which could be slightly more verbose but remains clear.

**Key insights**  
This is a low-risk change that enhances workflow flexibility. Teams should document the distinction between `verified` (pre-commit only) and `ready` (pre-commit + tests) to prevent misuse. Consider updating contributing guidelines to reflect the new label's purpose.

---

## 28. [[Perf] triton bilinear_pos_embed kernel for ViT](https://github.com/vllm-project/vllm/pull/37948)


### Base Information

- **PR Number:** #37948
- **Author:** [zhandaz](https://github.com/zhandaz)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-04-01 01:52:02
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37948/files) (3):**
  - `benchmarks/kernels/benchmark_vit_bilinear_pos_embed.py`
  - `tests/kernels/core/test_vit_bilinear_pos_embed.py`
  - `vllm/model_executor/models/qwen3_vl.py`

### Summary

**What changed and why**  
Added a fused Triton kernel (`triton_pos_embed_interpolate`) for bilinear position embedding interpolation in ViT models, replacing ~25 small eager-mode CUDA kernel launches with a single GPU kernel. This reduces CPU overhead and improves performance for Qwen3 VL variants. A pure-PyTorch fallback (`pos_embed_interpolate_native`) maintains compatibility when Triton is unavailable.

**Technical impact**  
The change optimizes the `fast_pos_embed_interpolate` method by consolidating multiple operations (linspace, meshgrid, indexing, etc.) into one kernel, which includes spatial-merge reordering. This reduces kernel launch overhead and improves throughput, especially for smaller grid sizes. The architecture remains backward-compatible via the fallback.

**Potential risks**  
Numerical differences may arise due to precomputed scaling in Triton vs. `torch.linspace` in the native path, though tolerances are defined. The Triton kernel requires divisibility of height and width by `spatial_merge_size`. If Triton is missing or fails, the fallback ensures correctness but may reintroduce performance overhead.

**Key insights**  
Significant speedups (up to 51x kernel-level, 28% faster encoder forward) demonstrate the value of kernel fusion. Developers should ensure Triton is available in deployment environments to maximize gains. The comprehensive testing suite validates accuracy across dtypes and grid shapes, providing confidence in the implementation.

---

## 29. [[Core] Simplify multimodal masking](https://github.com/vllm-project/vllm/pull/34246)


### Base Information

- **PR Number:** #34246
- **Author:** [lgeiger](https://github.com/lgeiger)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-04-01 01:18:22
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34246/files) (9):**
  - `tests/models/test_utils.py`
  - `vllm/model_executor/models/interfaces.py`
  - `vllm/model_executor/models/nano_nemotron_vl.py`
  - `vllm/model_executor/models/qwen2_5_omni_thinker.py`
  - `vllm/model_executor/models/qwen3_omni_moe_thinker.py`
  - `vllm/model_executor/models/qwen3_vl.py`
  - `vllm/model_executor/models/utils.py`
  - `vllm/v1/worker/gpu/mm/encoder_runner.py`
  - `vllm/v1/worker/gpu_model_runner.py`

### Summary

**What changed and why**  
This PR simplifies multimodal embedding merging by leveraging PyTorch 2.9.0+ improvements where `target[mask] = src` no longer triggers CUDA synchronization when `mask` is a CPU tensor. It replaces `masked_scatter_` with direct indexing, removes GPU mask transfers, and eliminates double-buffering for mask tensors.

**Technical impact**  
The changes streamline the multimodal embedding pipeline by reducing GPU memory usage (no mask buffers) and simplifying code paths. The `_merge_multimodal_embeddings` function now uses standard indexing, and mask tensors remain on the CPU, avoiding unnecessary data transfers while maintaining performance.

**Potential risks**  
The PR assumes PyTorch ≥2.9.0 behavior; older versions could reintroduce synchronization issues. Removing buffer swapping may affect concurrent operations if other parts of the code rely on asynchronous mask updates. The error message update from "masked scatter" to "index put" might obscure debugging context.

**Key insights**  
Developers should verify PyTorch version compatibility and ensure no hidden dependencies on GPU mask tensors. The simplification is beneficial but requires thorough testing across multimodal models to confirm no regressions in embedding placement or performance. The added test using `torch.cuda.set_sync_debug_mode("error")` is a proactive validation of the synchronization claim.

---

