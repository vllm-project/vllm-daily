# vLLM Merged PR Report

**Report Date:** 2026-04-15 PDT

**Total Merged PRs:** 36

---

## 1. [use spawn multiproc method on xpu](https://github.com/vllm-project/vllm/pull/39671)


### Base Information

- **PR Number:** #39671
- **Author:** [xinyu-intel](https://github.com/xinyu-intel)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-15 23:42:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39671/files) (1):**
  - `vllm/platforms/xpu.py`

### Summary

**What changed and why**  
Added automatic configuration of the `VLLM_WORKER_MULTIPROC_METHOD` environment variable to `"spawn"` for XPU platforms when it is not already set. This ensures compatibility with XPU hardware, which only supports the `spawn` multiprocessing method, particularly for data-parallel workloads.

**Technical impact**  
The change enforces a consistent multiprocessing method across XPU environments, preventing potential runtime errors or suboptimal performance. It integrates seamlessly by checking for existing environment variable settings, avoiding conflicts with user-defined configurations.

**Potential risks**  
If other parts of the codebase rely on alternative multiprocessing methods (e.g., `fork`), this override could introduce subtle issues in mixed-hardware deployments. Additionally, the change is limited to XPU contexts, so similar adjustments may be needed for other platforms with analogous constraints.

**Key insights**  
This is a proactive, hardware-specific optimization that enhances user experience by eliminating manual configuration. Developers should verify that `spawn` is compatible with all XPU use cases and consider documenting this behavior to inform users of the automatic environment variable setting.

---

## 2. [Add Jina Embeddings v5 model support (fixes #38633)](https://github.com/vllm-project/vllm/pull/39575)


### Base Information

- **PR Number:** #39575
- **Author:** [Roy214](https://github.com/Roy214)
- **Merged By:** [noooop](https://github.com/noooop)
- **Merged time:** 2026-04-15 23:37:11
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39575/files) (7):**
  - `docs/models/pooling_models/embed.md`
  - `tests/conftest.py`
  - `tests/models/language/pooling_mteb_test/mteb_embed_utils.py`
  - `tests/models/language/pooling_mteb_test/test_jina.py`
  - `tests/models/registry.py`
  - `vllm/model_executor/models/jina.py`
  - `vllm/model_executor/models/registry.py`

### Summary

**What changed and why**  
This PR adds support for the Jina Embeddings v5 model (`jinaai/jina-embeddings-v5-text-small`) to vLLM. The model uses Qwen3-0.6B-Base with four task-specific LoRA adapters (retrieval, text-matching, classification, clustering). The implementation merges the selected adapter into the base weights at load time to avoid runtime PEFT dependencies, and task selection is configured via `--hf-overrides '{"jina_task": "<task>"}'`.

**Technical impact**  
The changes introduce a new model class `JinaEmbeddingsV5Model` that extends `Qwen3ForCausalLM` and implements pooling support. Adapter merging occurs during weight loading, which simplifies inference but requires full weight reloading for task switching. The registry, documentation, and test utilities are updated to include the new model, and test adjustments ensure proper task and prompt handling for v5.

**Potential risks**  
If an unsupported task is specified, the system falls back to the default (`retrieval`) with a warning, which may silently affect performance. Adapter merging assumes the base model architecture matches Qwen3; any divergence could cause weight loading errors. The implementation does not support dynamic task switching at runtime—changing tasks requires reloading the model.

**Key insights**  
Developers must specify the task via `--hf-overrides` to leverage the correct LoRA adapter; otherwise, retrieval is used by default. The model is registered as a pooling model, so it won’t be wrapped by `as_embedding_model()`. Testing updates include a prompt prefix for v5 models, and the MTEB evaluation now uses a dedicated wrapper for Hugging Face models to ensure consistent encoding behavior.

---

## 3. [[Bugfix] add support for 'num_attention_groups' in ModelArchConfigConvertorBase for Step3p5](https://github.com/vllm-project/vllm/pull/39796)


### Base Information

- **PR Number:** #39796
- **Author:** [realliujiaxu](https://github.com/realliujiaxu)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-15 22:48:01
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39796/files) (3):**
  - `tests/config/base_model_arch_groundtruth.json`
  - `tests/config/test_model_arch_config.py`
  - `vllm/transformers_utils/model_arch_config_convertor.py`

### Summary

**What changed and why**  
Added support for the `num_attention_groups` configuration key in `ModelArchConfigConvertorBase` to handle the Step3p5 model architecture. This enables the converter to properly map this key to the total number of KV heads, ensuring compatibility with the Step-3.5-Flash model.

**Technical impact**  
The change extends the model configuration conversion logic to recognize `num_attention_groups` as an alias for KV heads, aligning Step3p5 with existing architectures. This allows the model to be correctly parsed and initialized within the vLLM framework without modifying core inference logic.

**Potential risks**  
If `num_attention_groups` is used inconsistently across other model types or conflicts with existing keys like `num_key_value_heads`, it could lead to incorrect head calculations. Additionally, the change assumes `num_attention_groups` always maps directly to KV heads, which may not hold for future architectures.

**Key insights**  
This is a minimal, backward-compatible extension that follows the established pattern for supporting new model configurations. Developers should verify that `num_attention_groups` is semantically equivalent to KV heads in the Step3p5 spec and consider adding validation if more complex mappings emerge.

---

## 4. [[CPU][IBM Z][Dockefile][Docs] Fix s390x builds for torch 2.11 and update docs for s390x](https://github.com/vllm-project/vllm/pull/39910)


### Base Information

- **PR Number:** #39910
- **Author:** [R3hankhan123](https://github.com/R3hankhan123)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 22:26:21
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39910/files) (5):**
  - `csrc/cpu/cpu_attn_impl.hpp`
  - `csrc/cpu/utils.hpp`
  - `docker/Dockerfile.s390x`
  - `docs/getting_started/installation/cpu.s390x.inc.md`
  - `requirements/common.txt`

### Summary

**What changed and why**  
This PR fixes s390x (IBM Z) builds for PyTorch 2.11 by addressing a silent crash caused by protobuf during server launch and an L2 cache detection failure during inference. The changes include: adding VXE ISA support in CPU attention metadata, implementing s390x-specific L2 cache size detection, updating the Dockerfile to use compatible dependency versions and workarounds, and updating documentation to reflect new requirements.

**Technical impact**  
The modifications enable vLLM to run correctly on s390x architecture by ensuring proper CPU feature detection and avoiding crashes from incompatible C++ extensions. The Dockerfile changes switch from building `outlines-core` to a patched `llguidance` version, pin specific library versions (e.g., Arrow 19.0.1, setuptools<70), and enforce pure Python protobuf to prevent crashes. The L2 cache detection now falls back to system queries on s390x when PyTorch capabilities lack the key.

**Potential risks**  
Using a forked/patched version of `llguidance` (`s390x-fix-v2` branch) may introduce maintenance overhead and compatibility issues if upstream changes. Removing protobuf C++ extensions could impact performance for serialization-heavy operations. The fallback L2 cache detection might yield suboptimal performance if system queries fail or return inaccurate sizes.

**Key insights**  
Developers should note that s390x support remains experimental and requires careful dependency management. The workaround for protobuf (pure Python implementation) is critical for stability but may need revisiting if performance becomes a concern. The documentation updates clarify new build prerequisites and datatype support (FP32, BF16, FP16), which is essential for users targeting this architecture.

---

## 5. [[perf][cpu] Accelerate BF16 GELU with LUT impl on Arm CPUs](https://github.com/vllm-project/vllm/pull/37469)


### Base Information

- **PR Number:** #37469
- **Author:** [fadara01](https://github.com/fadara01)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 22:26:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/37469/files) (9):**
  - `.buildkite/scripts/hardware_ci/run-cpu-test-arm.sh`
  - `cmake/cpu_extension.cmake`
  - `csrc/cpu/activation_lut_bf16.cpp`
  - `csrc/cpu/torch_bindings.cpp`
  - `csrc/ops.h`
  - `tests/kernels/core/test_cpu_activation.py`
  - `vllm/_custom_ops.py`
  - `vllm/model_executor/layers/activation.py`
  - `vllm/platforms/cpu.py`

### Summary

**What changed and why**  
This PR introduces a Look Up Table (LUT) based implementation for BF16 GELU on Arm CPUs to accelerate performance. The change addresses a performance bottleneck where GELU accounted for ~5% of quantized Whisper runtime. The LUT approach provides up to 8x speedup for GELU, improving overall Whisper performance by up to 5%.

**Technical impact**  
The implementation adds a new LUT-based kernel (`activation_lut_bf16.cpp`) that precomputes GELU values for all possible BF16 inputs (65,536 entries) and uses table lookups during inference. It integrates with the existing `GELU` custom op, automatically enabling the LUT path on Arm CPUs for contiguous BF16 tensors. Build configuration ensures the kernel is only compiled for Arm platforms with ASIMD support.

**Potential risks**  
The LUT initialization uses `std::call_once` with OpenMP parallelism, which may cause thread-safety issues if OpenMP threads are nested or if the initialization is called concurrently from multiple processes. The implementation assumes contiguous BF16 tensors; non-contiguous inputs will fall back to the slower native path, which could cause performance inconsistency. There is also a risk of precision loss due to BF16 rounding in the LUT, though tests validate correctness.

**Key insights**  
The LUT approach is highly effective for BF16 on Arm, but developers should ensure tensors are contiguous to benefit from the optimization. The fallback mechanism maintains correctness across platforms and data types. Consider extending the LUT pattern to other activations (e.g., SiLU) for further gains, and evaluate thread-safety of the initialization in production environments.

---

## 6. [[Bugfix] Reject empty tools array with HTTP 400](https://github.com/vllm-project/vllm/pull/39780)


### Base Information

- **PR Number:** #39780
- **Author:** [jigangz](https://github.com/jigangz)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-15 21:08:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39780/files) (4):**
  - `tests/tool_parsers/test_ernie45_moe_tool_parser.py`
  - `tests/tool_parsers/test_xlam_tool_parser.py`
  - `tests/tool_use/test_chat_completion_request_validations.py`
  - `vllm/entrypoints/openai/chat_completion/protocol.py`

### Summary

**What changed and why**  
The PR fixes a bug where an empty tools array (`tools: []`) was incorrectly accepted with HTTP 200. To align with OpenAI's API behavior (which returns HTTP 400 with error code `empty_array`), vLLM now rejects empty tools arrays. The root cause was that `data.get("tools")` returning `[]` (a falsy value in Python) bypassed validation logic, allowing the request to proceed.

**Technical impact**  
The `check_tool_usage()` validator now includes an early check to raise a `ValueError` for empty tools arrays, ensuring consistent error handling with OpenAI. This change removes previously unreachable fallback logic for `tool_choice="required"` with empty tools, simplifying the validation flow. Additionally, test files are updated to reflect the new rejection behavior and remove unnecessary empty tools arrays from test cases.

**Potential risks**  
If clients previously relied on silent acceptance of empty tools arrays, they may now encounter unexpected HTTP 400 errors, requiring updates to their request handling. The removal of the fallback block for `tool_choice="required"` with empty tools could affect edge cases, though this scenario is now caught earlier. Ensure all integration tests are updated to validate the new error response.

**Key insights**  
Always validate explicit empty arrays in API inputs to prevent unintended behavior, as falsy values like `[]` can bypass conditional checks. Aligning with upstream API specifications (e.g., OpenAI) improves interoperability but may introduce breaking changes for existing users. Review related validation logic for similar issues where empty lists or falsy values might slip through.

---

## 7. [[Mistral Grammar] Fix tool and reasoning parsing](https://github.com/vllm-project/vllm/pull/39217)


### Base Information

- **PR Number:** #39217
- **Author:** [juliendenize](https://github.com/juliendenize)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 21:05:04
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39217/files) (10):**
  - `tests/tool_parsers/test_mistral_tool_parser.py`
  - `tests/tool_use/mistral/test_mistral_tool_calls.py`
  - `tests/tool_use/mistral/utils.py`
  - `vllm/entrypoints/openai/chat_completion/protocol.py`
  - `vllm/entrypoints/openai/chat_completion/serving.py`
  - `vllm/entrypoints/openai/engine/serving.py`
  - `vllm/entrypoints/serve/render/serving.py`
  - `vllm/sampling_params.py`
  - `vllm/tokenizers/mistral.py`
  - `vllm/tool_parsers/mistral_tool_parser.py`

### Summary

**What changed and why**  
This PR fixes tool and reasoning parsing for Mistral models when using grammar-based tool-call enforcement. Previously, the serving layer would fall back to generic vLLM tool-call parsing paths after grammar injection, breaking tool-call extraction. The changes ensure the serving layer recognizes grammar-constrained Mistral output and routes it through `MistralToolParser` for correct parsing, while also handling `tool_choice="none"` cases to suppress special-token leakage.

**Technical impact**  
The modifications introduce a dedicated grammar-aware parsing path in the serving layer (`is_mistral_grammar_path`), enabling proper extraction of tool calls and reasoning content. The `MistralToolParser` now includes methods for combined reasoning/tool streaming and non-streaming tool-call construction. Additionally, tool parsing is now invoked even for `tool_choice="none"` when using grammar-capable tokenizers to prevent special-token leakage.

**Potential risks**  
- The new logic introduces branching based on `request._grammar_from_tool_parser`, which must be correctly set to avoid misrouting.  
- Changes to tokenizer adaptation and grammar validation could affect compatibility with non-Mistral models or older tokenizer versions.  
- The increased complexity in streaming logic may introduce edge cases in multi-turn or parallel tool-call scenarios.

**Key insights**  
- Developers should ensure `--tool-call-parser mistral` is used with compatible tokenizers (tekken/v11+) for grammar enforcement.  
- The fix centralizes Mistral-specific parsing in `MistralToolParser`, reducing fragmentation.  
- Extensive unit and E2E tests have been added, but integration testing with various Mistral model versions is recommended to validate behavior.

---

## 8. [[compile] Invoke split FX graph by codegen.](https://github.com/vllm-project/vllm/pull/38657)


### Base Information

- **PR Number:** #38657
- **Author:** [zhxchen17](https://github.com/zhxchen17)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 21:03:41
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38657/files) (3):**
  - `vllm/compilation/backends.py`
  - `vllm/compilation/caching.py`
  - `vllm/compilation/codegen.py`

### Summary

**What changed and why**  
This PR introduces a code generation layer to reduce inference loop runtime overhead. Instead of executing the FX graph directly, it generates optimized Python code that calls submodules sequentially, eliminating FX interpreter overhead and reducing CPython stack frame depth.

**Technical impact**  
The changes add a new `codegen.py` module that transforms the split FX graph into plain Python code, which is then compiled and cached. This reduces per-submodule call overhead from ~20µs to ~3µs. The serialization format now includes generated execution code and submodule names, affecting both AOT compilation and runtime paths.

**Potential risks**  
- The `exec()` call for code compilation introduces security considerations if code sources are untrusted.  
- Increased cache size due to storing generated code and submodule names.  
- Potential edge cases with complex node types not yet supported in code generation (currently only handles `call_module`, `getitem`, and `output`).  
- The liveness analysis for early `del` statements might incorrectly free values if user analysis has errors.

**Key insights**  
- This optimization provides significant performance gains by reducing Python-level dispatch overhead.  
- Developers should ensure all FX node types in stitching graphs are supported by `generate_execution_code()` to avoid runtime errors.  
- Consider extending the code generation to support more node types (e.g., built-in operators) for broader applicability.  
- The security implication of `exec()` is mitigated by generating code internally, but caution is needed if caching external artifacts.

---

## 9. [[ROCm][FEAT] Integrate aiter gemm w8a8 ptpc](https://github.com/vllm-project/vllm/pull/33773)


### Base Information

- **PR Number:** #33773
- **Author:** [vllmellm](https://github.com/vllmellm)
- **Merged By:** [tjtanaa](https://github.com/tjtanaa)
- **Merged time:** 2026-04-15 18:55:30
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/33773/files) (9):**
  - `tests/utils.py`
  - `vllm/_aiter_ops.py`
  - `vllm/model_executor/kernels/linear/__init__.py`
  - `vllm/model_executor/kernels/linear/scaled_mm/aiter.py`
  - `vllm/model_executor/layers/quantization/compressed_tensors/schemes/compressed_tensors_w8a8_fp8.py`
  - `vllm/model_executor/layers/quantization/fbgemm_fp8.py`
  - `vllm/model_executor/layers/quantization/fp8.py`
  - `vllm/model_executor/layers/quantization/modelopt.py`
  - `vllm/model_executor/layers/quantization/quark/schemes/quark_w8a8_fp8.py`

### Summary

**What changed and why**  
This PR integrates two new Aiter kernels (`AiterPreshuffledPerTokenFp8ScaledMMLinearKernel` and `AiterPerTokenFp8ScaledMMLinearKernel`) for FP8 inference on AMD ROCm platforms. The changes enable optimized weight-shuffling (bpreshuffle) and Composable Kernel (CK) fallback paths to improve throughput for per-token/per-channel (PTPC) quantization schemes, specifically targeting Large Language Models like Qwen3-8B and Qwen2.5-Coder-32B.

**Technical impact**  
The integration adds new kernel selection logic in the linear kernel registry, extending FP8 support on ROCm. It introduces weight preprocessing (shuffling/transposition) via `process_weights_after_loading` and adds configuration checks to ensure kernels are only used when tuned for specific shapes. The changes affect multiple quantization schemes (fbgemm_fp8, fp8, modelopt, quark) by ensuring weight post-processing is consistently applied.

**Potential risks**  
- Kernel selection depends on external CSV tuning files; missing configurations may fall back to slower paths or cause runtime errors.  
- Weight shuffling/transposition modifies layer parameters in-place, which could affect model serialization or compatibility with existing checkpoints.  
- The strict divisibility requirements (N, K % 16) and dtype constraints (bfloat16 output) may limit model support or cause silent fallbacks.

**Key insights**  
- Developers must set `VLLM_ROCM_USE_AITER=1` and ensure Aiter library installation for the kernels to activate.  
- Performance gains are contingent on kernel tuning; verify CSV configurations cover target model shapes.  
- The `process_weights_after_loading` call is now uniformly added across quantization schemes, ensuring weight preprocessing occurs reliably.

---

## 10. [[Model Runner V2][BugFix] fix num_sampled dtype for probabilistic rej…](https://github.com/vllm-project/vllm/pull/39951)


### Base Information

- **PR Number:** #39951
- **Author:** [TheEpicDolphin](https://github.com/TheEpicDolphin)
- **Merged By:** [WoosukKwon](https://github.com/WoosukKwon)
- **Merged time:** 2026-04-15 18:09:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39951/files) (1):**
  - `vllm/v1/worker/gpu/spec_decode/probabilistic_rejection_sampler_utils.py`

### Summary

**What changed and why**  
Changed the `num_sampled` tensor dtype from default (int64) to `torch.int32` in the probabilistic rejection sampler to match the expected return type of `RejectionSampler`. This fixes a Triton kernel compilation error in `_prepare_eagle_inputs_kernel` caused by dtype mismatch.

**Technical impact**  
Ensures consistency with the strict rejection sampler's return type, preventing Triton kernel assertion failures during speculative decoding. The change aligns the probabilistic sampler's output with downstream kernel expectations, maintaining compatibility across the speculative execution pipeline.

**Potential risks**  
If other parts of the codebase implicitly rely on `num_sampled` being int64, this could cause subtle type mismatches. Additionally, the fix assumes the error stems solely from this dtype inconsistency, but underlying kernel logic might have other assumptions about tensor shapes or memory layout.

**Key insights**  
Always verify dtype consistency when implementing similar samplers or kernels. Consider adding explicit dtype assertions in rejection sampler interfaces to catch mismatches early. Review related Triton kernels for other implicit dtype dependencies that could cause similar issues.

---

## 11. [[Quantization] - Layerwise reloading of Attention/KV quantized models](https://github.com/vllm-project/vllm/pull/38995)


### Base Information

- **PR Number:** #38995
- **Author:** [Josephasafg](https://github.com/Josephasafg)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 18:03:32
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38995/files) (4):**
  - `tests/model_executor/model_loader/test_reload.py`
  - `vllm/model_executor/model_loader/reload/__init__.py`
  - `vllm/model_executor/model_loader/reload/layerwise.py`
  - `vllm/model_executor/model_loader/weight_utils.py`

### Summary

**What changed and why**  
This PR implements layerwise reloading of attention scale weights (k_scale, v_scale, q_scale, prob_scale) for models with quantized KV caches. It fixes a limitation where attention layers couldn't reload their quantization scale parameters during weight reloading operations. Additionally, it corrects scalar weight counting in default_weight_loader to ensure CopyCounter tracks them accurately.

**Technical impact**  
The changes enable proper reloading of quantized attention models by deferring attention layer processing until after all linear layers are processed. This ensures scale weights are correctly loaded and processed while maintaining kernel tensor references for CUDA graph compatibility. The architecture now supports attention scale reloading while preserving existing quantization workflows.

**Potential risks**  
The implementation assumes attention tensor dtypes and shapes don't change during processing, which could break if future quantization methods alter these properties. There's also a risk that the deferred processing order might interact unexpectedly with other layer types or quantization schemes. The scalar weight fix could affect other weight loading scenarios beyond attention scales.

**Key insights**  
Attention layers now follow a two-phase loading process: first deferring, then processing with proper scale weight handling. Developers should ensure future quantization methods maintain consistent tensor shapes/dtypes. The scalar weight fix is subtle but important for accurate weight counting during reload operations. Testing with FP8-quantized models is now properly supported.

---

## 12. [Update to transformers v5](https://github.com/vllm-project/vllm/pull/30566)


### Base Information

- **PR Number:** #30566
- **Author:** [hmellor](https://github.com/hmellor)
- **Merged By:** [simon-mo](https://github.com/simon-mo)
- **Merged time:** 2026-04-15 16:29:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/30566/files) (41):**
  - `.buildkite/scripts/hardware_ci/run-cpu-test.sh`
  - `.buildkite/test_areas/models_basic.yaml`
  - `docker/Dockerfile`
  - `docker/Dockerfile.cpu`
  - `docker/Dockerfile.nightly_torch`
  - `docker/Dockerfile.rocm`
  - `docs/getting_started/installation/gpu.rocm.inc.md`
  - `requirements/common.txt`
  - `requirements/test/cuda.in`
  - `requirements/test/cuda.txt`
  - `requirements/test/nightly-torch.txt`
  - `requirements/test/rocm.in`
  - `requirements/test/rocm.txt`
  - `requirements/test/xpu.in`
  - `requirements/test/xpu.txt`
  - `tests/conftest.py`
  - `tests/lora/conftest.py`
  - `tests/lora/test_minicpmv_tp.py`
  - `tests/model_executor/test_weight_utils.py`
  - `tests/models/language/generation/test_common.py`
  - `tests/models/language/pooling_mteb_test/test_baai.py`
  - `tests/models/language/pooling_mteb_test/test_gte.py`
  - `tests/models/language/pooling_mteb_test/test_jina.py`
  - `tests/models/multimodal/generation/test_common.py`
  - `tests/models/multimodal/generation/test_nemotron_parse.py`
  - `tests/models/multimodal/generation/test_phi4siglip.py`
  - `tests/models/multimodal/generation/test_voxtral.py`
  - `tests/models/multimodal/generation/vlm_utils/core.py`
  - `tests/models/multimodal/pooling/test_colqwen3.py`
  - `tests/models/multimodal/pooling/test_intern_vit.py`
  - `tests/models/multimodal/pooling/test_jinavl_reranker.py`
  - `tests/models/multimodal/processing/test_musicflamingo.py`
  - `tests/models/registry.py`
  - `tests/models/utils.py`
  - `tests/reasoning/test_step3p5_reasoning_parser.py`
  - `tests/v1/e2e/spec_decode/test_spec_decode.py`
  - `vllm/model_executor/model_loader/gguf_loader.py`
  - `vllm/model_executor/models/gemma4_mm.py`
  - `vllm/model_executor/models/musicflamingo.py`
  - `vllm/model_executor/models/transformers/base.py`
  - `vllm/tokenizers/registry.py`

### Summary

**What changed and why**  
This PR updates the codebase to support Transformers v5.5.3, requiring updates to multiple dependencies (tokenizers, PEFT, accelerate, mamba, compressed-tensors) and CI environment variables. Several model architectures and tests are temporarily disabled due to incompatibilities with Transformers v5, such as changes to `_tied_weight_keys` and removed imports. The upgrade enables support for newer architectures released after Transformers v5.

**Technical impact**  
The upgrade introduces breaking changes in the Transformers API, affecting custom model code, tokenizers, processors, and configs. Dependency version constraints are tightened, and CI now includes a backward compatibility test suite for Transformers 4.57.5. The changes also modify multimodal input handling (e.g., Gemma4 video/audio processing) and GGUF loading logic to align with Transformers v5’s state dict structure.

**Potential risks**  
Temporarily disabled models and tests could hide regressions if not restored promptly. The extensive dependency updates increase the risk of version conflicts or unexpected behavior in edge cases. Transformers v5’s internal changes may cause subtle issues in custom model integrations, especially with trust_remote_code models. The removal of hf_transfer in favor of HF_XET_HIGH_PERFORMANCE may affect download reliability.

**Key insights**  
Developers must review the list of disabled models and prioritize upstream fixes or vLLM-side patches. The backward compatibility test is critical for ensuring no regression for users pinned to older Transformers versions. When working with multimodal models, ensure input padding and caching logic aligns with the updated field configurations. Monitor CI failures closely, as the upgrade may expose latent issues in model loading or tokenizer handling.

---

## 13. [[Model] Fix Gemma 4 token repetition by dynamic BOS injection for PT models](https://github.com/vllm-project/vllm/pull/39842)


### Base Information

- **PR Number:** #39842
- **Author:** [lucianommartins](https://github.com/lucianommartins)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-04-15 16:13:07
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39842/files) (1):**
  - `vllm/model_executor/models/gemma4_mm.py`

### Summary

**What changed and why**  
The PR modifies the `get_default_tok_params` method in the Gemma4 model to dynamically control BOS token injection. Previously, it always forced `add_special_tokens=False` to prevent duplicate BOS tokens in Instruction-Tuned (IT) models, but this broke Pre-Trained (PT) models lacking a chat template, causing token repetition. Now, it checks for a `chat_template` on the tokenizer and only disables special tokens for IT models, allowing PT models to receive the required BOS token.

**Technical impact**  
This change ensures Gemma4 PT models generate correctly by injecting a BOS token at position 0 for raw prompts, while preserving the existing behavior for IT models. It resolves a critical generation degradation (token repetition loops) and maintains compatibility across model variants without affecting other architectures.

**Potential risks**  
If the `chat_template` attribute detection is unreliable (e.g., missing or incorrectly set), it could lead to double-BOS issues in IT models or missing BOS in PT models. The fix assumes all PT models lack a chat template and all IT models have one—any deviation in model configurations might reintroduce errors.

**Key insights**  
Always validate tokenizer configurations dynamically rather than applying blanket rules across model types. Consider adding a unit test that verifies BOS token presence for PT models and absence of duplicates for IT models. This pattern could be useful for other model families with similar BOS handling requirements.

---

## 14. [Update registry for Nemotron-v3 VL Nano/Super](https://github.com/vllm-project/vllm/pull/39747)


### Base Information

- **PR Number:** #39747
- **Author:** [collinmccarthy](https://github.com/collinmccarthy)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-04-15 16:09:12
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39747/files) (3):**
  - `tests/models/registry.py`
  - `vllm/config/speculative.py`
  - `vllm/model_executor/models/registry.py`

### Summary

**What changed and why**  
This PR adds registry entries for two new model names (`NemotronH_Nano_Omni_Reasoning_V3` and `NemotronH_Super_Omni_Reasoning_V3`) that are aliases for the existing `NemotronH_Nano_VL_V2` model. It also wires in MTP/speculative decoding support specifically for the `NemotronH_Super_Omni_Reasoning_V3` variant by adding a conditional override in the speculative configuration handler.

**Technical impact**  
The changes extend the model registry to recognize the new model names, mapping them to the same underlying implementation (`nano_nemotron_vl`). The speculative decoding configuration is updated to properly handle the architecture name for the "Super" variant by promoting its `text_config` attribute, ensuring MTP detection logic works correctly for this specific alias.

**Potential risks**  
The alias approach means both new model names share identical configuration with `NemotronH_Nano_VL_V2`, which could cause confusion if the models actually have different capabilities or requirements. The conditional logic in `speculative.py` only targets `NemotronH_Super_Omni_Reasoning_V3`, leaving `NemotronH_Nano_Omni_Reasoning_V3` without explicit MTP support wiring, which may be intentional but should be verified.

**Key insights**  
Developers should note that these are registry aliases rather than new model implementations. The selective MTP support for only the "Super" variant suggests different intended use cases between the two new names. When testing these models, ensure the speculative decoding behavior matches expectations for the "Super" variant specifically.

---

## 15. [[CI Bug] fix flaky test test_fewer_blocks_with_hma[google/gemma-3-1b-it-512]](https://github.com/vllm-project/vllm/pull/39938)


### Base Information

- **PR Number:** #39938
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-15 14:20:06
- **Type:** `TRIVIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39938/files) (1):**
  - `tests/v1/kv_connector/unit/test_nixl_connector_hma.py`

### Summary

**What changed and why**  
The change reduces the `gpu_memory_utilization` parameter from 0.5 to 0.47 in a specific test (`test_fewer_blocks_with_hma`). This addresses a flaky test failure where the system reported insufficient free GPU memory (10.93 GiB available versus 11.02 GiB requested) during initialization.

**Technical impact**  
This adjustment lowers the memory threshold required by the test, allowing it to pass consistently in CI environments where available GPU memory may fluctuate. It does not affect production code or other tests, only the configuration of this single unit test.

**Potential risks**  
If the underlying memory calculation or system memory variability increases, even the reduced utilization (0.47) might still occasionally fail. The fix treats the symptom (memory threshold) rather than the root cause of memory variability in the CI environment.

**Key insights**  
While this is a minimal fix for test flakiness, consider investigating why free memory is inconsistent in CI. For robustness, tests could dynamically adapt to available memory or use mocked memory snapshots to eliminate environment dependencies.

---

## 16. [[FlashAttention] Don't overwrite `flash_attn_interface.py` when installing precompiled](https://github.com/vllm-project/vllm/pull/39932)


### Base Information

- **PR Number:** #39932
- **Author:** [MatthewBonanni](https://github.com/MatthewBonanni)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-15 13:43:16
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39932/files) (1):**
  - `setup.py`

### Summary

**What changed and why**  
The change modifies `setup.py` to skip overwriting `flash_attn_interface.py` (and `__init__.py`) when installing vLLM with precompiled binaries (`VLLM_USE_PRECOMPILED=1`). This ensures that locally modified versions of these source-controlled files are preserved during installation, aligning with existing exclusion logic in `vllm_flash_attn.cmake`.

**Technical impact**  
This update affects the package installation process by refining the file extraction logic from precompiled wheels. It prevents unintended overwrites of critical interface files, maintaining consistency between source-controlled code and precompiled binary deployments. The change is minimal and targeted, impacting only the installation flow when precompiled binaries are used.

**Potential risks**  
If the exclusion list is incomplete or mismatched with the CMake logic, it could lead to version skew between source files and precompiled components. Additionally, future changes to the file structure (e.g., new source-controlled files in `vllm_flash_attn`) might require updates to the exclusion set to avoid similar issues.

**Key insights**  
Developers should ensure that any new source-controlled files in the `vllm_flash_attn` directory are added to `flash_attn_files_to_skip` to maintain consistency. This change highlights the importance of synchronizing exclusion logic across build and installation scripts to prevent accidental file overwrites.

---

## 17. [[Bugfix][Perf] Indexer upcast WK to BF16 for fusion](https://github.com/vllm-project/vllm/pull/38928)


### Base Information

- **PR Number:** #38928
- **Author:** [benchislett](https://github.com/benchislett)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-04-15 13:39:33
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38928/files) (2):**
  - `vllm/model_executor/models/deepseek_mtp.py`
  - `vllm/model_executor/models/deepseek_v2.py`

### Summary

**What changed and why**  
The PR introduces an alternative fix to maintain fused WK+weights_proj operations for DeepSeek-V2 indexer layers when WK weights are stored in FP8 format. Instead of splitting the fused operation as in PR #38870, this solution upcasts FP8 WK weights to BF16 during weight loading, allowing the fusion to be preserved while handling FP8 checkpoints.

**Technical impact**  
This change modifies the weight loading logic to detect FP8 WK weights with separate scale tensors, dequantize them to BF16, and load them into the fused `wk_weights_proj` linear layer. The architecture now always uses fused WK+weights_proj projections (removing the conditional `is_fp4_ckpt` logic), improving performance while maintaining compatibility with FP8 checkpoints.

**Potential risks**  
The dequantization process assumes specific tensor naming conventions (`"indexer.wk."` in names) and FP8 format detection, which could fail with unconventional checkpoint formats. There's also a risk of memory overhead from maintaining the `_pending_wk_fp8` buffer during loading, though temporary. The testing note indicates the author's setup is broken, leaving some uncertainty about correctness.

**Key insights**  
This optimization demonstrates a clever trade-off: converting FP8 to BF16 at load time to preserve runtime fusion benefits, yielding measurable performance gains (11.90ms vs 12.74ms decode latency). Developers should verify the FP8 detection logic works across all expected checkpoint formats and consider adding validation for the dequantized weights' numerical accuracy.

---

## 18. [[Bug] Fix batch invariant test issue, bs=1 with `max_seq_num = 1`](https://github.com/vllm-project/vllm/pull/39320)


### Base Information

- **PR Number:** #39320
- **Author:** [yewentao256](https://github.com/yewentao256)
- **Merged By:** [yewentao256](https://github.com/yewentao256)
- **Merged time:** 2026-04-15 13:28:43
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39320/files) (1):**
  - `tests/v1/determinism/test_batch_invariance.py`

### Summary

**What changed and why**  
The test was simplified from using two separate LLM engines (one with batch size 1 and one with batch size N) to using a single engine configured for the larger batch limit (N). The purpose is to fix a batch invariance test issue where testing with two engines was outside the domain of batch invariance validation.

**Technical impact**  
This change makes the test more focused on true batch invariance by eliminating the variable of different engine instances. Now the comparison is between running a prompt alone versus running it in a mixed batch position within the same engine instance, which better isolates the batch size effect.

**Potential risks**  
The test no longer validates that different engine instances with different `max_num_seqs` configurations produce identical results, which might have been the original intent. There's also a risk that the test could pass even if there are subtle differences between engines with different batch size configurations.

**Key insights**  
The fix correctly focuses the test on batch invariance within a single engine instance. Developers should understand that batch invariance testing should control for engine instance variables. Consider whether separate tests are needed to validate consistency across different engine configurations.

---

## 19. [[Speculative Decoding] Add DFlash speculators config parsing](https://github.com/vllm-project/vllm/pull/38300)


### Base Information

- **PR Number:** #38300
- **Author:** [ZhanqiuHu](https://github.com/ZhanqiuHu)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-04-15 13:22:15
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38300/files) (4):**
  - `.buildkite/test_areas/spec_decode.yaml`
  - `tests/v1/spec_decode/test_speculators_dflash.py`
  - `vllm/model_executor/models/qwen3_dflash.py`
  - `vllm/transformers_utils/configs/speculators/algos.py`

### Summary

**What changed and why**  
This PR adds support for DFlash speculators configuration parsing, enabling automatic detection and user override of speculative decoding parameters. It updates the configuration parsing logic (`algos.py`), modifies Qwen3 DFlash weight loading to handle vocabulary mapping, and adds an end-to-end test to validate the auto-detection path.

**Technical impact**  
The changes extend the speculative decoding framework to support DFlash models by adding a new speculator type (`dflash`) with its configuration transformations. The Qwen3 DFlash model now properly initializes vocabulary mapping tensors when draft and target vocabularies differ, ensuring correct token ID translation during speculative decoding.

**Potential risks**  
If the `draft_vocab_size` and target vocabulary size mismatch without proper mapping initialization, token ID translation could fail silently. The test relies on an external model (`nm-testing/dflash-qwen3-8b-speculators`), which may become unavailable or change. The configuration parsing assumes required fields like `mask_token_id` and `aux_hidden_state_layer_ids` are present.

**Key insights**  
Developers should ensure DFlash speculator configs include all required fields, especially `mask_token_id`. The vocabulary mapping tensor is only created when vocab sizes differ, optimizing memory usage. The E2E test provides a valuable template for validating speculator auto-detection, but should be adapted for internal model testing.

---

## 20. [[Bugfix][NIXL] Fix `_logical_to_kernel_block_ids` conversion for non-mamba models](https://github.com/vllm-project/vllm/pull/39724)


### Base Information

- **PR Number:** #39724
- **Author:** [ZhanqiuHu](https://github.com/ZhanqiuHu)
- **Merged By:** [NickLucche](https://github.com/NickLucche)
- **Merged time:** 2026-04-15 13:08:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39724/files) (2):**
  - `tests/v1/kv_connector/unit/test_nixl_connector_hma.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/nixl/worker.py`

### Summary

**What changed and why**  
This PR fixes a regression where `_logical_to_kernel_block_ids` conversion was accidentally omitted for non-Mamba models (e.g., pure FA models) after refactoring for Mamba support. The bug caused assertion failures on Blackwell due to kernel block size mismatches. The fix restores the conversion in the non-Mamba code path.

**Technical impact**  
The change ensures consistent block ID conversion across all model types when kernel block size differs from logical block size. Non-Mamba models now correctly expand remote logical block IDs to kernel block IDs, maintaining compatibility with the FlashInfer kernel requirements and preventing runtime assertion failures.

**Potential risks**  
The fix could potentially affect performance for non-Mamba models due to additional block ID expansion operations, though this is minimal. There's a risk of similar omissions in other refactored code paths that handle block ID conversions. The test coverage focuses on specific parameter combinations but may not exhaust all edge cases.

**Key insights**  
This regression highlights the importance of maintaining parallel code paths during refactoring. Developers should ensure that both Mamba and non-Mamba code paths receive equivalent functionality updates. The added test provides good coverage for the specific bug but should be complemented with broader integration testing to catch similar issues.

---

## 21. [[CI] Only build release Docker images when NIGHTLY=1](https://github.com/vllm-project/vllm/pull/39882)


### Base Information

- **PR Number:** #39882
- **Author:** [khluu](https://github.com/khluu)
- **Merged By:** [khluu](https://github.com/khluu)
- **Merged time:** 2026-04-15 12:01:14
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39882/files) (1):**
  - `.buildkite/release-pipeline.yaml`

### Summary

**What changed and why**  
Added a manual approval step (`block`) before Docker image builds in the CI pipeline that only activates when `NIGHTLY=1` is not set. This prevents automatic image builds on non-nightly runs unless manually approved, while nightly builds continue unchanged.

**Technical impact**  
The pipeline now requires explicit human intervention to proceed with release Docker image builds for non-nightly commits. This reduces unnecessary resource usage and ensures image builds only occur when intended, without affecting wheel builds or nightly automation.

**Potential risks**  
If the `block` step is misconfigured or the condition `build.env("NIGHTLY") != "1"` fails, image builds could be incorrectly blocked or skipped. Dependency links (e.g., `allow_failure: true`) might cause silent failures if not carefully monitored.

**Key insights**  
This change optimizes CI costs by gating resource-intensive image builds. Ensure the `NIGHTLY` environment variable is consistently set in nightly jobs. Consider adding notifications or documentation to inform teams about the new manual step to avoid pipeline delays.

---

## 22. [[Mooncake] Fix mixed MLA+Eagle block-size validation](https://github.com/vllm-project/vllm/pull/39596)


### Base Information

- **PR Number:** #39596
- **Author:** [zhewenl](https://github.com/zhewenl)
- **Merged By:** [ywang96](https://github.com/ywang96)
- **Merged time:** 2026-04-15 11:36:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39596/files) (2):**
  - `tests/v1/kv_connector/unit/test_mooncake_connector.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/mooncake/mooncake_connector.py`

### Summary

**What changed and why**  
This PR fixes Mooncake registration failures for models using mixed MLA and Eagle attention backends. It removes an incorrect shape-based block-size assertion that assumed consistent tensor layouts across backends, and instead adopts a backend-driven block-size synchronization pattern similar to NIXL's approach.

**Technical impact**  
The changes ensure Mooncake correctly handles heterogeneous KV-cache layouts by querying active attention backends for the true physical block size. This aligns block-size validation with backend metadata, preventing false failures when MLA and Eagle tensors coexist in the same model.

**Potential risks**  
If `select_common_block_size` returns a block size larger than the configured `self.block_size`, the assertion `self.block_size > kernel_block_size` will fail. Additionally, the removal of shape validation could mask genuine tensor shape mismatches if backend metadata is incorrect.

**Key insights**  
Developers should note that block-size determination is now centralized via attention backend queries, reducing layout assumptions. The fix highlights the importance of backend-aware logic when supporting mixed attention mechanisms, and similar updates may be needed in other components interacting with heterogeneous KV caches.

---

## 23. [Bug/test eagle dp v2](https://github.com/vllm-project/vllm/pull/39838)


### Base Information

- **PR Number:** #39838
- **Author:** [Monishver11](https://github.com/Monishver11)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-04-15 10:48:13
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39838/files) (1):**
  - `.buildkite/test_areas/distributed.yaml`

### Summary

**What changed and why**  
The change removes the `test_eagle_dp.py` test from the Distributed Tests (2 GPUs) step in the CI pipeline. This is a temporary mitigation because the test has shown flaky behavior (intermittent failures) on H100 GPUs, which is disrupting the stability of the main branch.

**Technical impact**  
This modification only affects the CI/CD workflow configuration. It does not alter any application source code, libraries, or the test logic itself. The test file remains in the repository but will no longer be executed as part of this specific automated test group.

**Potential risks**  
The primary risk is that a genuine bug in the EAGLE DP functionality could go undetected until the test is re-enabled. There is also a risk that the underlying cause of the flakiness (suspected batch invariance issues) could manifest in production if not properly investigated and resolved.

**Key insights**  
This is a standard operational procedure for managing flaky tests to maintain CI reliability. The team should prioritize the promised investigation to diagnose the root cause—likely related to hardware-specific (H100) batch invariance—and implement a proper fix. The test should be restored to the pipeline once it is stable.

---

## 24. [[Test] Fix @create_new_process_for_each_test("fork") in interactive shell pipeline](https://github.com/vllm-project/vllm/pull/29130)


### Base Information

- **PR Number:** #29130
- **Author:** [markmc](https://github.com/markmc)
- **Merged By:** [robertgshaw2-redhat](https://github.com/robertgshaw2-redhat)
- **Merged time:** 2026-04-15 09:22:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/29130/files) (1):**
  - `tests/utils.py`

### Summary

**What changed and why**  
The fix moves the `os.setpgrp()` call from the parent process to the child process, ensuring the child runs in its own process group. This prevents SIGTERM signals from affecting unrelated processes like `tee` in interactive shell pipelines when job control is enabled.

**Technical impact**  
This change isolates the test process more effectively by creating a dedicated process group only for the child process. The parent process now uses the child's PID as the process group ID and handles cleanup more gracefully with proper error suppression for already-terminated processes.

**Potential risks**  
If the child process spawns its own subprocesses, they might not be properly contained within the new process group. The error suppression could mask legitimate issues if processes fail to terminate unexpectedly. There's also a theoretical race condition between process exit and the killpg call.

**Key insights**  
The solution addresses pipeline failures in interactive shells by better isolating test processes. Developers should be aware that this decorator creates process groups specifically to handle signal propagation issues. When debugging test failures in pipelines, consider process group isolation as a potential factor.

---

## 25. [[BugFix] KeyError on scope["method"] for realtime api websocket in AuthenticationMiddleware](https://github.com/vllm-project/vllm/pull/36934)


### Base Information

- **PR Number:** #36934
- **Author:** [daniebrill](https://github.com/daniebrill)
- **Merged By:** [russellb](https://github.com/russellb)
- **Merged time:** 2026-04-15 09:15:01
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36934/files) (1):**
  - `vllm/entrypoints/openai/server_utils.py`

### Summary

**What changed and why**  
The fix modifies the AuthenticationMiddleware to use `scope.get("method")` instead of direct dictionary access `scope["method"]`. This prevents a KeyError when handling WebSocket connections, as the "method" key only exists for HTTP requests, not WebSocket connections.

**Technical impact**  
This change ensures the middleware correctly handles both HTTP and WebSocket connection types without crashing. It maintains existing authentication behavior for HTTP requests while enabling proper WebSocket authentication flow, which is essential for the realtime API endpoint.

**Potential risks**  
The primary risk is that the condition `scope.get("method") == "OPTIONS"` will evaluate to `None == "OPTIONS"` for WebSocket connections, which is false. This could potentially bypass intended OPTIONS request handling, though OPTIONS is an HTTP-specific method not applicable to WebSockets.

**Key insights**  
Always use `.get()` for optional dictionary keys in ASGI scope objects, as different connection types have different required fields. This pattern should be reviewed in other middleware components to ensure similar issues don't exist elsewhere in the codebase.

---

## 26. [[KVConnector][LMCache] Propagate cache_salt through MP connector for per-user cache isolation](https://github.com/vllm-project/vllm/pull/39837)


### Base Information

- **PR Number:** #39837
- **Author:** [royyhuang](https://github.com/royyhuang)
- **Merged By:** [ApostaC](https://github.com/ApostaC)
- **Merged time:** 2026-04-15 09:10:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39837/files) (1):**
  - `vllm/distributed/kv_transfer/kv_connector/v1/lmcache_mp_connector.py`

### Summary

**What changed and why**  
This PR propagates the `cache_salt` field from vLLM's request object through the LMCache multi-process connector. The purpose is to enable per-user cache isolation in LMCache by threading this identifier through the storage and retrieval pipeline, allowing LMCache to apply per-user storage quotas.

**Technical impact**  
The changes extend the LMCacheMPRequestTracker and LMCacheMPRequestMetadata to store and forward the `cache_salt`. This salt is now passed to key LMCache adapter methods (`batched_submit_retrieve_requests`, `batched_submit_store_requests`, `maybe_submit_lookup_request`), enabling downstream cache systems to differentiate storage by user. The system maintains backward compatibility by defaulting to an empty string when the salt is not set.

**Potential risks**  
If the LMCache adapter methods are not updated to handle the new `cache_salts` parameter (as per the counterpart PR LMCache/LMCache#3029), runtime errors could occur. There is also a risk of inconsistent behavior if the salt propagation is incomplete in other parts of the system not covered by this PR.

**Key insights**  
This is a well-scoped change that cleanly extends existing structures. Developers must ensure the LMCache adapter is updated in lockstep. The default empty string preserves existing behavior, making this a safe, additive change. Integration testing with LMCache's per-user quota feature is the critical next step.

---

## 27. [[SpecDecode][Benchmark] Add SPEED-bench support to benchmarking CLI](https://github.com/vllm-project/vllm/pull/36029)


### Base Information

- **PR Number:** #36029
- **Author:** [talorabr](https://github.com/talorabr)
- **Merged By:** [mgoin](https://github.com/mgoin)
- **Merged time:** 2026-04-15 09:00:08
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36029/files) (2):**
  - `docs/benchmarking/cli.md`
  - `vllm/benchmarks/datasets/datasets.py`

### Summary

**What changed and why**  
Added support for the SPEED-Bench dataset to the vLLM benchmarking CLI, enabling evaluation of speculative decoding performance. The changes include a new dataset handler (`SpeedBench` class) that loads qualitative and throughput splits, plus documentation updates with usage examples and dataset preparation instructions.

**Technical impact**  
Extends the benchmarking framework to support a new dataset type with configurable subsets (qualitative/throughput splits) and category filtering. The implementation integrates seamlessly with existing dataset abstraction, reusing the `CustomDataset` base class and adding new CLI arguments (`--speed-bench-dataset-subset`, `--speed-bench-category`, `--speed-bench-output-len`).

**Potential risks**  
The dataset loader assumes a specific JSONL schema with a `messages` column; malformed files may cause runtime errors. The `prepare.py` script fetches external data, introducing dependency on third-party sources and network availability. License compliance is required as the dataset is governed by NVIDIA’s terms.

**Key insights**  
This addition enhances speculative decoding evaluation with a standardized dataset, but developers must ensure proper dataset download and license adherence. The implementation follows existing patterns, making it maintainable. Consider adding validation for JSONL structure and error handling for missing files in future iterations.

---

## 28. [[XPU] properly handle q_descale on XPU as quant query input not supported](https://github.com/vllm-project/vllm/pull/39676)


### Base Information

- **PR Number:** #39676
- **Author:** [yma11](https://github.com/yma11)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-15 06:52:59
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39676/files) (1):**
  - `vllm/v1/attention/backends/flash_attn.py`

### Summary

**What changed and why**  
The change modifies the `q_descale` parameter in the XPU encoder attention path to pass `None` when quantized query input is not supported. This ensures compatibility with XPU hardware that lacks support for quantized query inputs, aligning the encoder attention behavior with full attention.

**Technical impact**  
This conditional logic prevents the use of quantized query scaling factors (`layer._q_scale`) on XPU, while preserving quantized key and value scaling. It maintains functional parity with other attention paths and avoids potential runtime errors or incorrect computations when quantized queries are unsupported.

**Potential risks**  
If `self.supports_quant_query_input` is incorrectly configured, it could lead to inconsistent scaling between query and key/value tensors, causing accuracy degradation. Additionally, the change assumes that quantized key/value inputs remain supported on XPU, which should be validated to prevent silent failures.

**Key insights**  
Developers should verify that `supports_quant_query_input` is accurately set for all backends and hardware targets. Consider centralizing quantized input support checks to avoid similar conditional logic duplication. Testing should include mixed quantization scenarios to ensure scaling consistency.

---

## 29. [FIX: support language_model.backbone naming in NemotronH Nano VL quantization config](https://github.com/vllm-project/vllm/pull/39901)


### Base Information

- **PR Number:** #39901
- **Author:** [danielafrimi](https://github.com/danielafrimi)
- **Merged By:** [tomeras91](https://github.com/tomeras91)
- **Merged time:** 2026-04-15 06:49:49
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39901/files) (1):**
  - `vllm/model_executor/models/nano_nemotron_vl.py`

### Summary

**What changed and why**  
Added a `hf_to_vllm_mapper` to the `NemotronH_Nano_VL_V2` class to remap `language_model.backbone` to `language_model.model` in quantization config paths. This fixes a runtime failure where ModelOpt-generated quantization configs use the original Hugging Face naming convention, but vLLM internally renames these layers, causing lookup mismatches.

**Technical impact**  
This change ensures quantization configuration lookups succeed by aligning the layer naming between external quantization tools and vLLM's internal weight mapping. It extends the existing weight remapping logic used during model loading to also handle quantization metadata, maintaining consistency across the model initialization pipeline.

**Potential risks**  
If other parts of the quantization config or model structure use similar naming mismatches not covered by this mapper, they could still cause failures. There's also a risk that future changes to the internal weight mapping in `NemotronHForCausalLM` could desynchronize with this fix if not updated together.

**Key insights**  
This fix highlights the importance of maintaining naming consistency between external tooling and internal model representations. Developers should verify that all configuration paths affected by internal weight renaming are properly remapped, not just the primary weight loading paths. Consider adding similar mappers for other models that might have analogous quantization config mismatches.

---

## 30. [[BugFix][Graph] fix: handle empty sym_shape_indices in PiecewiseBackend.](https://github.com/vllm-project/vllm/pull/39395)


### Base Information

- **PR Number:** #39395
- **Author:** [chaunceyjiang](https://github.com/chaunceyjiang)
- **Merged By:** [ProExpertProg](https://github.com/ProExpertProg)
- **Merged time:** 2026-04-15 06:28:10
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39395/files) (2):**
  - `tests/compile/test_dynamic_shapes_compilation.py`
  - `vllm/compilation/piecewise_backend.py`

### Summary

**What changed and why**  
The fix addresses a bug in `PiecewiseBackend.__call__` where empty `sym_shape_indices` (when all inputs have static shapes) caused an index error. The change adds a conditional path to handle this case by selecting the single compiled `range_entry` instead of trying to index into the empty list. A corresponding test validates the fix for static-shape compilation scenarios.

**Technical impact**  
This ensures robust handling of static-shape compilation paths in the graph engine, preventing crashes when no symbolic dimensions are present. The system now correctly falls back to the only compiled range entry, maintaining compatibility with existing dynamic-shape logic while supporting fully static inputs.

**Potential risks**  
If multiple compiled range entries exist despite empty `sym_shape_indices`, the assertion will fail, indicating an inconsistency in range management. The test assumes specific model configurations (small `max_model_len` and `max_num_batched_tokens`) to trigger static compilation; changes to compilation heuristics could affect test reliability.

**Key insights**  
Always validate edge cases where collections like `sym_shape_indices` may be empty. The fix is minimal and focused, but developers should ensure the single-compiled-entry assumption holds across all compilation modes. Consider adding broader validation for range-entry consistency during backend initialization.

---

## 31. [[XPU][MXFP4] add mxfp4 quant op for XPU](https://github.com/vllm-project/vllm/pull/39857)


### Base Information

- **PR Number:** #39857
- **Author:** [zufangzhu](https://github.com/zufangzhu)
- **Merged By:** [jikunshang](https://github.com/jikunshang)
- **Merged time:** 2026-04-15 05:28:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39857/files) (2):**
  - `vllm/_xpu_ops.py`
  - `vllm/model_executor/layers/quantization/utils/mxfp4_utils.py`

### Summary

**What changed and why**  
Added MXFP4 (4-bit) quantization operations for XPU hardware, including both real implementation (`_xpu_mxfp4_quantize_impl`) and fake/fused version (`_xpu_mxfp4_quantize_fake`). The changes extend existing MXFP8 quantization support to enable more memory-efficient 4-bit quantization on Intel XPU devices.

**Technical impact**  
This introduces a new low-precision format (float4_e2m1fn_x2) with block-wise quantization (block size 32) for XPU acceleration. The implementation follows the same pattern as MXFP8 ops, integrating with the existing custom op registration system and providing a utility function in the quantization utils module.

**Potential risks**  
The fake implementation currently returns uninitialized tensors which could lead to undefined behavior if used incorrectly. Input validation assumes 2D contiguous tensors with last dimension divisible by 32 - violating these assumptions will cause assertion failures. The hardcoded block size (32) may not be optimal for all model architectures.

**Key insights**  
The implementation correctly reuses the established XPU op registration pattern, ensuring consistency. Developers should ensure proper tensor preparation before calling these functions and be aware that the fake implementation doesn't perform actual quantization. Consider making block size configurable for flexibility across different model types.

---

## 32. [[Metrics] Add request_id to FinishedRequestStats to enable correlation between metrics and requests](https://github.com/vllm-project/vllm/pull/39710)


### Base Information

- **PR Number:** #39710
- **Author:** [Csrayz](https://github.com/Csrayz)
- **Merged By:** [markmc](https://github.com/markmc)
- **Merged time:** 2026-04-15 04:24:17
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39710/files) (3):**
  - `tests/v1/metrics/test_stats.py`
  - `vllm/v1/engine/output_processor.py`
  - `vllm/v1/metrics/stats.py`

### Summary

**What changed and why**  
Added a `request_id` field to `FinishedRequestStats` to enable correlation between per-request performance metrics and the originating request. This change is a prerequisite for Prometheus exemplars and supports observability pipelines that don't rely on OpenTelemetry, allowing downstream consumers to trace metrics back to specific requests.

**Technical impact**  
The change threads the request ID from `OutputProcessor` through `IterationStats.update_from_finished_request()` into `FinishedRequestStats`. This enriches the stats logging plugin interface with request context, enabling correlation across traces, logs, and metrics without altering existing aggregation or metric collection logic.

**Potential risks**  
The `request_id` is optional (`str \| None`), which may lead to inconsistent data if callers omit it. There's also a risk of increased memory usage if many finished requests are retained, though this is minimal. The change assumes `req_state.external_req_id` is always populated; if not, correlation may fail silently.

**Key insights**  
This is a low-risk, high-value change for observability. Ensure all callers provide a valid `request_id` to avoid null values. Consider adding validation or logging for missing IDs in production. The implementation aligns with broader tracing efforts and should be leveraged by metrics plugins for enhanced debugging.

---

## 33. [[Quantization][Autoround][CPU] Add W4A16 Support](https://github.com/vllm-project/vllm/pull/38192)


### Base Information

- **PR Number:** #38192
- **Author:** [Zhenzhong1](https://github.com/Zhenzhong1)
- **Merged By:** [bigPYJ1151](https://github.com/bigPYJ1151)
- **Merged time:** 2026-04-15 03:38:31
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/38192/files) (2):**
  - `tests/quantization/test_cpu_wna16.py`
  - `vllm/model_executor/layers/quantization/inc.py`

### Summary

**What changed and why**  
Added CPU support for W4A16 (4-bit weight, 16-bit activation) quantization via Intel Neural Compressor (INC). The changes include a new `apply_cpu_w4a16_quant_layer` method and updated routing logic to handle CPU-based GPTQ-style quantization, enabling efficient inference on CPU platforms.

**Technical impact**  
This extends the existing quantization framework to support CPU execution for W4A16 models, aligning with the existing XPU implementation. The system now routes CPU GPTQ requests through a dedicated method that enforces symmetric 4-bit quantization constraints, while maintaining compatibility with the existing GPTQ quantization layer logic.

**Potential risks**  
The implementation currently only supports symmetric quantization on CPU, which may limit compatibility with asymmetric-quantized models. Error handling relies on `NotImplementedError` exceptions, which could lead to abrupt failures if unsupported configurations are encountered. The tight coupling between platform detection and quantization method selection increases complexity.

**Key insights**  
Developers should note that CPU W4A16 support is currently restricted to symmetric quantization. When adding new quantization formats, ensure proper platform-specific routing in `get_quant_method`. Consider extending the framework to support asymmetric quantization for broader model compatibility in future iterations.

---

## 34. [fix online fp8 for MiniCPM models](https://github.com/vllm-project/vllm/pull/39862)


### Base Information

- **PR Number:** #39862
- **Author:** [yma11](https://github.com/yma11)
- **Merged By:** [DarkLight1337](https://github.com/DarkLight1337)
- **Merged time:** 2026-04-15 02:09:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/39862/files) (1):**
  - `vllm/model_executor/models/minicpmv.py`

### Summary

**What changed and why**  
This PR fixes a meta tensor device placement issue in MiniCPM vision models when using FP8 quantization. The previous fix (#36751) was incomplete, causing `NotImplementedError` when moving meta tensors. The solution introduces a new `_ensure_resampler_device()` method that moves the resampler to the correct device after weights are loaded, while preserving FP8 dtype requirements.

**Technical impact**  
The changes simplify device/dtype handling by removing redundant device moves from multiple `init_resampler()` methods and centralizing device placement logic. This ensures consistent behavior across all MiniCPM model variants and prevents conflicts with FP8 quantization's dtype management.

**Potential risks**  
If `_ensure_resampler_device()` is called before weights are loaded, it could still trigger meta tensor errors. The flag-based approach (`_resampler_moved`) assumes single-device execution and may not handle multi-device scenarios. Removing explicit device moves could affect models where resampler initialization depends on device context.

**Key insights**  
Device movement should be deferred until after weight loading to avoid meta tensor issues. The solution correctly separates device placement from dtype management, which is critical for FP8 quantization. Consider adding validation to ensure `_ensure_resampler_device()` is only called post-weight-loading and documenting the execution flow for future maintenance.

---

## 35. [[kv_offload+HMA][3/N]: Remove block_size from KVEvents](https://github.com/vllm-project/vllm/pull/36644)


### Base Information

- **PR Number:** #36644
- **Author:** [orozery](https://github.com/orozery)
- **Merged By:** [orozery](https://github.com/orozery)
- **Merged time:** 2026-04-15 01:53:20
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/36644/files) (6):**
  - `tests/v1/kv_connector/unit/offloading_connector/test_scheduler.py`
  - `tests/v1/kv_offload/test_cpu_manager.py`
  - `vllm/distributed/kv_transfer/kv_connector/v1/offloading/scheduler.py`
  - `vllm/v1/kv_offload/abstract.py`
  - `vllm/v1/kv_offload/cpu/manager.py`
  - `vllm/v1/kv_offload/cpu/spec.py`

### Summary

**What changed and why**  
This PR removes the `block_size` field from `OffloadingEvent` dataclass and all related code. The change eliminates redundant block size reporting since GPU KV cache events already provide this information, and enables grouping blocks with variable sizes into single events.

**Technical impact**  
The `OffloadingEvent` interface is simplified by removing the `block_size` parameter. All event creation and consumption code now uses a default value of 0 for block size in downstream components. CPU offloading manager initialization no longer requires block size, making it more flexible for handling variable block sizes.

**Potential risks**  
Test assertions now check for `block_size == 0` instead of actual block sizes, which could mask issues if block size reporting is actually needed elsewhere. The change assumes GPU KV cache events always provide accurate block size information, which could cause problems if those events are missing or incorrect.

**Key insights**  
This is a cleanup that reduces redundancy and enables future variable block size support. Developers should verify that all consumers of `OffloadingEvent` properly handle the missing block size field. The default value of 0 in the scheduler indicates this field is now deprecated and should eventually be removed from the `BlockStored` event as well.

---

## 36. [[Bugfix] Fix tool_calls Iterable consumed when debug logging is enabled](https://github.com/vllm-project/vllm/pull/34844)


### Base Information

- **PR Number:** #34844
- **Author:** [wojciech-wais](https://github.com/wojciech-wais)
- **Merged By:** [vllm-bot](https://github.com/vllm-bot)
- **Merged time:** 2026-04-15 01:32:47
- **Type:** `SUBSTANTIAL`
- **[Changed Files](https://github.com/vllm-project/vllm/pull/34844/files) (3):**
  - `tests/entrypoints/openai/test_tool_calls_serialization.py`
  - `vllm/entrypoints/chat_utils.py`
  - `vllm/entrypoints/openai/chat_completion/protocol.py`

### Summary

**What changed and why**  
The PR fixes a bug where enabling debug logging (`VLLM_LOGGING_LEVEL=debug`) caused tool calling with Mistral models to fail. The issue occurred because Pydantic v2 wraps `Iterable` fields (like `tool_calls` in OpenAI's message types) in one-shot lazy iterators. When debug logging serialized the request via `model_dump_json()`, it consumed the iterator, leaving subsequent readers (like the Mistral tokenizer) with an empty `tool_calls` sequence.

**Technical impact**  
Two changes ensure `tool_calls` are always materialized as lists: (1) Updated vLLM's internal TypedDicts (`CustomChatCompletionMessageParam` and `ConversationMessage`) to type `tool_calls` as `list` instead of `Iterable`. (2) Added a Pydantic validator to `ChatCompletionRequest` that eagerly converts any non-list `tool_calls` to lists during both before and after validation phases. This prevents iterator exhaustion and ensures compatibility across all code paths.

**Potential risks**  
The validators run on every request, adding minor overhead for message processing. If external libraries pass extremely large `tool_calls` iterators, materializing them eagerly could increase memory usage. The fix assumes `tool_calls` should always be a list; any code relying on lazy evaluation or iterator-specific behavior may break.

**Key insights**  
Always materialize `Iterable` fields when they may be serialized or read multiple times. The dual validator approach (before/after) is necessary because Pydantic can re-wrap lists in `ValidatorIterator` during type validation. Developers should ensure that any similar `Iterable` fields in protocol classes are handled consistently to avoid similar bugs.

---

